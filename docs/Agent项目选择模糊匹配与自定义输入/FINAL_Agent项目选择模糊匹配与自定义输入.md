# Agent 项目选择模糊匹配与自定义输入 - 最终报告

## 交付结论

Agent 聊天中的项目追问已从“后端给几个就只能选几个”改为完整的可搜索选择器：默认合并推荐候选与当前用户项目，输入关键词后远程查询匹配项目，并始终提供“其他（自定义输入）”。

## 关键实现

- 推荐候选与项目 API 结果按项目 ID 去重，推荐项优先。
- 250ms 防抖调用 `/api/projects?keyword=...`，请求序号防止旧响应覆盖新结果。
- 有关键词时仅展示远程匹配结果和“其他”，避免无关初始推荐干扰搜索。
- 自定义数字按 `project_id` 提交，自定义名称按 `project_query` 提交并由后端继续模糊确认。
- Clarify 下拉弹层提升到 3100，修复被 Agent 抽屉 3000 层级遮挡而无法点击的问题。

## 质量与生产状态

- 前端 56 passed，后端 1041 passed。
- ESLint、Ruff、生产构建和 `git diff --check` 通过。
- 最终生产镜像：`sha256:8c952cc86c5561736df6f76cce7105a0c856b4c9d6e6f5ac2b211ee329ccfe78`。
- 容器运行正常，重启 0、OOM=false，HTTP/HTTPS 200。
- ClamAV 运行态异常已恢复，Docker 健康状态为 `healthy`，Backend 到 `clamav:3310` 的协议级 PING/PONG 验证通过。
- 真实聊天的项目首屏、模糊搜索、其他选项、自定义输入和后端回填均通过。

完成日期：2026-07-15。
