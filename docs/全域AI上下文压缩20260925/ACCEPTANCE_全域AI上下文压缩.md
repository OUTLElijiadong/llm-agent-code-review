# 全域 AI 上下文压缩：验收记录

| 入口 | 原有缺口 | 候选实现与核验 | 生产状态 |
|---|---|---|---|
| 旧聊天 `/ai/chat` | 固定字符摘要可能漏掉中段历史、分类失败静默回退 | 按窗口预算和来源分片压缩；校验来源 ID、原文引文、时间顺序，当前消息保留原文；关联回归 182 项通过 | 待发布实测 |
| 公开 `/v1/responses` | `previous_response_id` 直接回放完整历史，无压缩门槛 | 模型只接收来源压缩投影，持久账本保留全量；二层原始 ID、最后输入、429/5xx/超时、长度截断重试均有回归；记录额外调用用量 | 待发布实测；此开放接口按 Bearer 密钥隔离 |
| 协同审查 | 画像、Skill、经验过长时分片审查直接失败 | 源码、启用规则和输出契约保留原文；其余上下文分片压缩并逐层核来源，输出预算变化时重新计算 | 待发布实测 |
| 安全哨兵 | 数据流仅取前 30 项，高危仅复核前 12 条 | `full` 模式下容量内全列表分批并报告完成单元；空路径拒绝，完整已存证据进入逐批高危复核；单项目及跨项目超保留容量、模型请求预算不足时显式失败。`triage/static_full` 的 `top_n` 范围仍按原契约标注 | 待发布实测 |
| 全链审计 | 高危证据超窗；多个验证目标可只返回部分索引而称完成 | 长证据按来源压缩，结果逐目标校验；推理确认与沙箱实际复现分开；简报预览显式标总数/省略 | 待发布实测 |
| 基类预算 | `char/2` 对中文和密集 JSON 低估输入 | 完整消息 JSON 的 UTF-8 字节上界，按真实输出上限加协议余量；中文、密集 ASCII 与正常输入边界回归 | 待发布实测 |
| 圆桌手机表格 | 390px 宽主持长表格竖排 | 表格局部横向滚动，前端 121 文件、1343 项测试、lint、生产构建通过 | 待发布后真实点击截图 |

2026-09-25 最终候选代码后端全量回归 **4803 通过、5 跳过**（`/tmp/prism-v413-backend-final-pytest.log`），新增聊天/安全哨兵定向回归 129 通过，Ruff 与 `git diff --check` 通过；前端在最后一次样式修改后 121 文件、1343 项测试、lint、生产构建通过。生产镜像/迁移/备份/健康、三账号真实点击和真实模型调用状态将在发布后逐项补齐。来源标记只证明片段被处理，不构成语义绝对无损的数学证明；完整性还需关键事实断言、真实调用和失败态核对。
