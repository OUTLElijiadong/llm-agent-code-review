# 全域 AI 上下文压缩：验收记录

| 入口 | 原有缺口 | 候选实现与核验 | 生产状态 |
|---|---|---|---|
| 旧聊天 `/ai/chat` | 固定字符摘要可能漏掉中段历史、分类失败静默回退 | 按窗口预算和来源分片压缩；校验来源 ID、原文引文、时间顺序，当前消息保留原文；关联回归 182 项通过 | 代码已发布；长历史真实模型路径待单独验收 |
| 公开 `/v1/responses` | `previous_response_id` 直接回放完整历史，无压缩门槛 | 模型只接收来源压缩投影，持久账本保留全量；二层原始 ID、最后输入、429/5xx/超时、长度截断重试均有回归；记录额外调用用量 | 已用正式生产镜像和真实圆桌历史触发压缩并取得完整回答；调用者主动限制输出时仍可能返回 `incomplete`，不视为完整成功；此开放接口按 Bearer 密钥隔离 |
| 协同审查 | 画像、Skill、经验过长时分片审查直接失败 | 源码、启用规则和输出契约保留原文；其余上下文分片压缩并逐层核来源，输出预算变化时重新计算 | 代码已发布；真实长上下文审查待单独验收 |
| 安全哨兵 | 数据流仅取前 30 项，高危仅复核前 12 条 | `full` 模式下容量内全列表分批并报告完成单元；空路径拒绝，完整已存证据进入逐批高危复核；单项目及跨项目超保留容量、模型请求预算不足时显式失败。`triage/static_full` 的 `top_n` 范围仍按原契约标注 | 代码已发布；真实 `full` 模式待单独验收 |
| 全链审计 | 高危证据超窗；多个验证目标可只返回部分索引而称完成 | 长证据按来源压缩，结果逐目标校验；推理确认与沙箱实际复现分开；简报预览显式标总数/省略 | 代码已发布；真实长证据审计待单独验收 |
| 基类预算 | `char/2` 对中文和密集 JSON 低估输入 | 完整消息 JSON 的 UTF-8 字节上界，按真实输出上限加协议余量；中文、密集 ASCII 与正常输入边界回归 | 代码已发布；生产边界案例待单独验收 |
| 圆桌手机表格 | 390px 宽主持长表格竖排 | 表格局部横向滚动，前端 121 文件、1343 项测试、lint、生产构建通过 | Safari 390×844 真实重开已显示正常换行、13/13 进度与到期禁言；横向手势位移仍未取得证据 |
| 论坛发帖助手 | 长草稿、画像和知识片段无入窗预算，可能整体直送 | 完整消息 UTF-8 字节上界；超窗逐来源分片，核对 ID/指纹/原文引文；标题与草稿结尾保留；失败不生成正式建议 | v4.0.15 已发布且定向回归通过；生产长草稿真实模型待验 |
| 渗透验证探测背景 | 分片摘要只检查非空，遗漏来源和状态也可被接受 | 每个来源路径、片号、短原文引文和事实逐项核验；摘要仍超预算则显式停止 | v4.0.15 已发布且定向回归通过；真实长证据阶段待验，其他渗透阶段继续排查 |
| Agent 团队依赖 | 私有模型输入误用公开事件 500 字/20 项预览，再把截后哈希标成完整 | 私有结果完整脱敏，临时成员按模型窗口分片压缩；显式部分覆盖阻断；确定性汇总从完整私有结果计数，第 201 条不再遗漏 | v4.0.15 已发布且定向回归通过；生产真实团队长依赖待验 |
| 上游支付状态 | 压缩途中上游 402 被误报为 502 网络不可用 | 返回 402 `upstream_payment_required`，不把已生成的部分摘要送入正式模型 | v4.0.15 已发布且定向回归通过；生产重试须待上游恢复 |
| 渗透其余阶段 | 情报来源字符裁切、固定字段与片段拼接超预算、探测假设固定保留前 6 条仍称完成 | 六类提示词总量受限，超长来源分片或来源压缩；模型缺少必要列表、超推演线候选均明确失败并保留候选 | 下一版候选定向 54 项及全量回归通过；真实模型待验 |
| 已发布 Agent 的只读 Skill | 整包工具 JSON 超窗只能拒绝，无法继续工作 | 按 JSON 来源分片压缩，校验来源 ID、指纹、顺序和尾部引文；失败明确拒绝 | 下一版候选定向 34 项及全量回归通过；真实模型待验 |
| 正式审查聚合完成口径 | 聚合器隔离无效发现后仍广播协同完成 | 无效、遗失声明或聚合器内部兜底均使任务覆盖失败，保留有效发现供人工复核 | v4.0.16 候选关联 43 项及全量回归通过；生产待验 |
| 沙箱报告方法论知识 | 三条检索命中拼接后 `[:2000]`，尾段未送到报告 Agent | 短来源保留原文；长来源约 4000 字分片，逐片校验 ID、SHA-256、首中尾引文，单次报告最多 12 次压缩调用；失败走确定性报告 | v4.0.16 候选定向 24 项及全量回归通过；真实模型待验 |

v4.0.13 于 2026-09-24 20:14:17 UTC 发布提交 `bca596b9b6bb00dc315746b2755dafe2a59478de`；发布前数据库备份独立恢复 103 张表、迁移 058，`ops-check.sh` 于 20:16:06 UTC 报 `ok`，版本/镜像/默认环境一致，`/healthz` 与 `/readyz` 均返回 4.0.13。同账号普通用户在 Safari 390×844 重新打开 #188 圆桌，版本、13/13 进度、16 条有序记录、到期禁言可见；审查员跨账号刷新深链显示归属拒绝。长表格不再逐字竖排，截图 `/tmp/prism-v413-mobile-roundtable-table-390x844.jpg`；Safari 响应式预览的横向手势没有形成可确认的位移证据，不能把手势验收写为通过。

v4.0.13 的公开 Responses 压缩用 #188 真实发言在生产容器隔离进程触发后，两次上游输出达到上限，明确返回 `context_compaction_failed`，未发送半截摘要。v4.0.14 发布前候选代码增大有界重试预算并在多来源输出仍超限时递归拆批；同一真实数据、同一生产 API 的隔离进程复跑返回 HTTP 200、`completed`，9 个输入项中压缩 5 项，投影 4107 token，一次压缩操作含 3 次上游尝试。正式发布后的独立复跑结果见下段。

v4.0.14 于 2026-09-24 20:37:01 UTC 发布提交 `bcec640729e9833032cefe57865e3d24a7da2cad`。发布前备份 `code_review_20260924T203113Z_bcec640729e9.sql.gz` 的校验与独立容器恢复通过（103 表、迁移 058）；20:37:49 UTC 的 `ops-check.sh` 为 `ok`，前后端镜像和默认环境与发布账本一致，HTTPS/健康正常。Safari 真正刷新后显示 v4.0.14：管理员上一轮用户消息和小菱回复按序恢复；普通账号在 390×844 重开 #188，进度 13/13、发言与到期禁言可见，截图 `/tmp/prism-v414-roundtable-390x844.jpg`；审查员打开普通账号圆桌深链被拒绝，且能进入操作审计页。正式生产镜像的隔离进程读取 #188 的 16 条真实发言、选取 8 条并触发一次压缩操作，HTTP 200、`completed`，答案正确指向最初目标和最后追问，未写入会话。第二组取 6 条发言时，主动设置 1024 和 4096 的最终输出上限均得到上游 `incomplete`；这是调用者输出预算边界，不能把有正文的 4096 结果称为完整回答。

第二组取消显式输出上限再试时，压缩途中上游返回 HTTP 402，未进入最终回答阶段；不能把它算作默认预算成功。v4.0.14 当时把这个上游状态误报为 502 `upstream_unavailable`。v4.0.15 修正为保留 402 `upstream_payment_required`，对无法继续的压缩明确失败，不使用部分摘要；修正已发布，尚未在上游恢复后复测真实模型。

v4.0.13 发布前后端全量回归 **4803 通过、5 跳过**（`/tmp/prism-v413-backend-final-pytest.log`），聊天/安全哨兵定向 129 通过，前端 121 文件、1343 项测试、lint、生产构建通过。v4.0.14 新补丁后后端全量 **4804 通过、5 跳过**（`/tmp/prism-v414-backend-final-pytest.log`），Responses 定向 35 通过，Ruff 和差异格式检查通过。来源标记只证明片段被处理，不构成语义绝对无损的数学证明；完整性还需关键事实断言、真实调用和失败态核对。

v4.0.15 合并四处新增路径与支付状态修正后，后端全量 **4818 通过、5 跳过**（`/tmp/prism-v415-backend-final2-pytest.log`），Ruff 与 `git diff --check` 通过；前端未改，沿用上述前端完整回归，正式发布时重新完成生产构建。该数字只证明本地回归，新增论坛、渗透和团队长上下文路径尚无生产真实模型成功证据。

v4.0.15 于 2026-09-24 21:24:16 UTC 发布提交 `497535852441dedfa3120cea3f6a6d974cbd5639`。发布前备份 `code_review_20260924T211837Z_497535852441.sql.gz` 经独立容器恢复验证，103 张表、迁移 `058_roundtable_sessions`；21:24:51 UTC 的 `ops-check.sh` 为 `ok`，发布账本、默认环境、前后端镜像 SHA 一致，五个关键服务 healthy，HTTPS 与公开健康检查正常。Safari 审查员账号真实刷新 `/audit` 显示 v4.0.15，并能查看审计记录；其打开普通账号圆桌深链仍被拒绝。普通账号重新登录后，在 v4.0.15 恢复有序的小菱历史消息，打开 #188 圆桌可见 13/13 进度、已保存的多位 Agent 发言与结束状态，输入区提示追问窗口已结束。手机截图分别为 `/tmp/prism-v415-reviewer-audit-closed-390x844.jpg`、`/tmp/prism-v415-ordinary-roundtable-390x844.jpg`。这些 UI/发布证据不等于新增长上下文路径的真实模型成功证据。

v4.0.16 候选修复 T13–T16 后，后端全量回归 **4847 通过、5 跳过**（`/tmp/prism-v416-backend-final2-pytest.log`），Ruff 和差异格式检查通过。正式审查红测先复现无效聚合声明被丢弃但任务仍显示成功，以及内部兜底被误标完整；修复后任务标记覆盖不完整并保留有效发现。渗透依赖清单回归还覆盖嵌套路径与环境标记；Skill 和沙箱知识分别覆盖伪来源与中段遗漏、单轮调用上限。该候选尚未发布，真实模型调用受上游 HTTP 402 状态阻挡。
