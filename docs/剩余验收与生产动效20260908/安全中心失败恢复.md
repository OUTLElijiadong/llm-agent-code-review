# 安全中心失败恢复

用户要求所有失败可回退并反馈。完整阅读 SecurityCenter 与既有 withFeedback 后，发现概览 catch 静默归空，规则清单失败显示零且无重试。真实组件7条红测复现，修复后7条通过。

复用既有错误归一与失权清缓存判断；两读请求独立重试并禁止并发重复；错误常驻含请求编号和下一步，暂时错误保留旧结果并标记未更新，401/403/404清除对应旧结果。清单未取得时计数为横线，真正空清单才为0并显示空态。卸载后忽略迟到请求。OWASP静态知识保持可读。未注入生产故障。

独立复核及最终构建尚待完成。
