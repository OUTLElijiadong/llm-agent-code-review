# 用户功能导航与接口接入 - 审批清单

- [x] 完整性：覆盖六个用户入口、路由守卫、主菜单、全局搜索和后端契约。
- [x] 一致性：管理员超级用户语义与后端 `require_admin`/admin 数据范围一致。
- [x] 可控性：不修改数据库和后端业务逻辑，只修复前端导航权限与测试契约。
- [x] 可测性：角色判定、路由守卫、API 路由和生产点击均可独立验证。
- [x] 安全性：非管理员仍不能进入 `/admin/*`，协议相对路径仍拒绝。

结论：允许进入 Automate。
