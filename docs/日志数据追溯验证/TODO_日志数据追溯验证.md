# 日志数据追溯验证待办

## 当前无阻塞

本轮要求的真实网页查看、真实点击、日志监控、API/数据库交叉核对均已完成。

## 后续可选优化

- 给 AI 调用日志增加按 `task_id`、`file_id`、`user_id` 的页面筛选输入框。
- 给系统审计页增加日志详情弹窗，展示完整对象上下文。
- 将 AgentEventBus 的实时 `trace_id` 事件持久化到数据库，形成事件级链路追踪。
- 处理前端构建中的既有 Sass 弃用和 Monaco 大 chunk 警告。
