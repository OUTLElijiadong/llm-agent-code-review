# 真实 Agent 发布与生产闭环待办

当前生产版本为 v4.0.5（`c7a8078eaa95b4b85fd0996a734cb2d5e17d7608`）。已完成事项仅限下列普通账号团队 `#69` 的验收；其余项目仍待验证。

1. [x] v4.0.5 生产团队 `#69` 普通账号真实 UI 总结果 3/3；已发布 Agent 与临时 Agent 并行。成员、独立事件和用量已复核，共有 4 次 worker 模型调用。
2. [ ] 修复并复验聊天持久化：刷新后团队和聊天页面仍可打开，但最初用户提问缺失；checkpoint 保留，`/session` 仅显示较新的后台 run。v4.0.6 修复尚未发布；发布后现场确认首问、完整顺序、checkpoint 和 `/session` 数据均恢复。
3. [ ] 性能待办：后台聊天记录 10 次调用、373778 tokens；分析并处理该调用量/令牌量问题。此指标与团队 worker 的 4 次模型调用分开统计。
4. [ ] v4.0.6 尚未发布。独立代码审查发现两处会话历史重复边界，均已先红后绿修复。最终后端全量测试 4551 passed、5 skipped、4 warnings，耗时 114.37s；Ruff 全量 `backend/app` 与 `tests` 通过。前端 1310 通过，Playwright 37 通过、9 跳过，另有本地布局 Playwright 23/23 通过；lint/build 通过。发布后核对生产版本与 SHA，再对源快照、租约、关键词字面量、移动报表打印和聊天恢复做生产回归。
5. [ ] 重复运行团队以检查稳定性，并在生产聊天中验证 `list_code_files.keyword`；补核聊天顺序、汇总结论和团队终态。
6. [ ] 管理员、审查员、普通账号逐项做正向权限、跨账号拒绝（含会话和团队直接对象访问）、聊天历史隔离、刷新与异常退出恢复、正常路径意外 404，以及桌面和 320/390px 手机视口界面验收。复现过的手机问题、报告语言/操作区/表格布局和后台错误提示须按当前生产版重测；每项记录账号、页面、动作、结果与证据。保留真正有用的 Agent 和业务记录，不创建演示项目。
7. Safari 先前定位到后台心跳、收件箱和会话读取在短时断网时反复弹 `Network Error`。在当前生产页面重复断网与恢复，确认后台降噪/退避、主动操作错误反馈、消息持久化及顺序。
8. 生产磁盘使用率最近已知为 87%，超过 85% 告警阈值；`ops-check.sh` 当时为 `degraded`。按保留策略评估旧镜像与备份后再清理；本轮未删除数据。需复核当前磁盘状态。
9. 旧账号口令曾出现在已跟踪的 E2E 源码中。完成验收后由账号持有人轮换三个账号的共用口令；历史 Git 对象不会因新提交自动删除。
