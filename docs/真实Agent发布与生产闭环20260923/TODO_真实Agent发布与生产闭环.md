# 真实 Agent 发布与生产闭环待办

当前生产版本为 v4.0.6，发行代码运行 SHA 为 `c86a7483aec160b2aa55c8e3195a3ad26d813387`。后续 docs-only 提交不改变该运行 SHA；发布后真实 UI 验收仍未完成。

1. [x] v4.0.5 生产团队 `#69` 普通账号真实 UI 总结果 3/3；已发布 Agent 与临时 Agent 并行。成员、独立事件和用量已复核，共有 4 次 worker 模型调用。
2. [ ] 复验聊天持久化 UI：此前 Safari 刷新后首问缺失且 `/session` 只显示较新的后台 run；19:06:53 UTC 服务端只读 session 组装随后返回 #69 的 4 个 run、11 条公开消息，首条为 238 字 root #852 提问，顺序与 checkpoint 可见消息一致且无重复，最新 #855 completed（9 events、6 mesh events）。这证明服务端组装数据存在，不证明浏览器可见渲染；v4.0.6 已发布，现场仍需复验首问、顺序、checkpoint 与 `/session` 一致性。
3. [ ] 性能待办：后台聊天记录 10 次调用、373778 tokens；分析并处理该调用量/令牌量问题。此指标与团队 worker 的 4 次模型调用分开统计。
4. [x] v4.0.6 已于 2026-09-23 18:59:29 UTC 发布，发行代码 SHA `c86a7483aec160b2aa55c8e3195a3ad26d813387`；前后端同 SHA，备份隔离恢复 101 表、Alembic head `057`，HTTPS/health/ready 通过。`ops-check` 无阻断但磁盘 88%、状态 `degraded`。截至核验时后端与前端发布后日志筛查的 404/5xx 计数均为 0，仅代表该时窗；19:06:53 UTC 容器日志无 404/5xx/ERROR/Traceback。发布前独立审查发现的两处会话历史重复边界均先红后绿修复。发布前后端测试 4551 passed、5 skipped、4 warnings（114.37s），Ruff 全量 `backend/app` 与 `tests` 通过；前端 1310，Playwright 37 通过、9 跳过，另有本地布局 Playwright 23/23；lint/build 通过。
5. [ ] 重复运行团队以检查稳定性，并在生产聊天中验证 `list_code_files.keyword`；补核聊天顺序、汇总结论和团队终态。
6. [ ] Mac 锁屏且 IAB `getTab` 超时，本轮未完成 v4.0.6 发布后的真实生产点击、手机布局及聊天首问 UI 复验。虽然服务端只读 `get_agent_response_session(user)` 已返回完整首问和有序消息，仍须恢复可用浏览器并确认实际可见渲染。逐项做管理员、审查员、普通账号正向权限、跨账号拒绝（含会话和团队直接对象访问）、聊天历史隔离、刷新与异常退出恢复、正常路径意外 404，以及桌面和 320/390px 手机视口验收。复现过的手机问题、报告语言/操作区/表格布局和后台错误提示须按当前生产版重测；每项记录账号、页面、动作、结果与证据。保留真正有用的 Agent 和业务记录，不创建演示项目。
7. Safari 先前定位到后台心跳、收件箱和会话读取在短时断网时反复弹 `Network Error`。在当前生产页面重复断网与恢复，确认后台降噪/退避、主动操作错误反馈、消息持久化及顺序。
8. 生产磁盘使用率本次为 88%，超过 85% 告警阈值；`ops-check.sh` 无阻断但状态为 `degraded`。按保留策略评估旧镜像与备份后再清理；本轮未删除数据。需复核当前磁盘状态。
9. 旧账号口令曾出现在已跟踪的 E2E 源码中。完成验收后由账号持有人轮换三个账号的共用口令；历史 Git 对象不会因新提交自动删除。
