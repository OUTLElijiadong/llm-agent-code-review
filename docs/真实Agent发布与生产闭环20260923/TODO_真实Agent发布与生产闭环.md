# 真实 Agent 发布与生产闭环待办

1. 发布 v4.0.4 候选补丁后，重复调用真实已发布 Agent，记录版本、模型用量、证据与问题/空问题结论；再由小菱创建含已有 Agent 与新 Agent 的团队，核对成员独立运行事件、总结和聊天记录顺序，并展示生产截图。
2. 管理员、审查员、普通账号逐项做正向权限、跨账号拒绝、会话隔离、刷新/异常退出恢复、正常路径意外 404，以及桌面和手机端视口界面验收；复现过的缺陷须重复测试排除偶发。保留真正有用的 Agent 与业务记录，不创建演示项目。
3. 旧账号口令曾出现在已跟踪的 E2E 源码中。完成验收后由账号持有人轮换三个账号的共用口令；历史 Git 对象不会因新提交自动删除。
4. 生产磁盘使用率 86% 已超 85% 告警阈值。清理脚本的 dry-run 已审阅；旧镜像与 52 GB 备份需按保留策略另行处理，未执行删除。
5. 刷新完成会话时偶见 `Network Error` 提示，但同一时窗服务端未发现 4xx/5xx。需定位失败请求、治理背景轮询的提示与重试；此前现象不能当作已修复。
