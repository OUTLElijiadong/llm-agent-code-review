# 真实 Agent 发布与生产闭环阶段结果

观察日期：2026-09-24。生产已发布 v4.0.6，发行代码运行 SHA 为 `c86a7483aec160b2aa55c8e3195a3ad26d813387`。备份隔离恢复、迁移、前后端同 SHA、HTTPS、health/ready 检查通过；`ops-check` 无阻断但磁盘 88%、状态 `degraded`。日志抽查截至核验时后端与前端的 404/5xx 计数均为 0。19:06:53 UTC 的服务端只读 session 组装复核返回团队 `#69` 首条提问及有序消息；但此前 Safari 刷新后的首问缺失仍未通过发布后浏览器渲染复验。Mac 锁屏且 IAB `getTab` 超时，生产真实点击和手机验收未完成。

## 已交付与核实

- 审查员在生产工坊创建有真实职责的“鉴权与账号隔离审查员”v1，完成静态校验并提交审批单 #239。管理员经真实页面审阅，取得用户确认后批准并发布。独立只读复核显示审批单 #239 为 `approved`、版本 #6 为 `published`、Agent #4 已启用且指向版本 #6、发布记录 #7 为 `published`。该 Agent 无额外能力、无 Skill；每代码分片约新增一次模型调用属于发布前估计，不是实际用量。
- v4.0.5 生产团队 `#69` 由普通账号通过真实 UI 完成 3/3 成功，已发布 Agent 与临时 Agent 并行运行；成员、独立事件和用量已复核，共 4 次 worker 模型调用。结论仅覆盖该账号与该团队。
- 此前 Safari 刷新后团队与聊天页面可打开，但首条用户提问未恢复显示；checkpoint 仍在，`/session` 仅显示较新的后台 run。随后 19:06:53 UTC 容器内只读调用 `get_agent_response_session(user)` 返回 #69 的 4 个 run、11 条公开消息，首条为 238 字 root #852 提问，顺序与 checkpoint 可见消息一致且无重复；最新 #855 completed，含 9 个 events、6 个 mesh events。该服务端结果不证明 Safari 已正确渲染。
- 同一 `user` surface 对管理员 #73、审查员 #78 返回空，管理员 `admin` surface 也为空；该时段容器日志无 404/5xx/ERROR/Traceback，仅作为时段内服务端证据。后台聊天记录到 10 次调用、373778 tokens，另列为性能待办，不与 worker 调用混算。
- v4.0.4 生产任务 #181 对真实项目 15 的 `SecurityConfig.java` #783 完成审查：1/1 文件，评分 64/100，形成 10 条问题（2 高、6 中、2 低），报告可打开。这证明已发布 Agent 能参与这一次正式审查，不证明所有 Agent 或并行团队均成功。
- v4.0.4 小菱团队 #67 因已发布成员未携带 `input.code` 而取消。小菱补源码创建的团队 #68 中，已发布成员任务 #210 完成并独立评分 62；临时成员 #211 连续输出截断，汇总成员 #212 因依赖失败被阻断，团队终态为失败。小菱向用户说明了部分结果和失败原因；聊天中 `list_code_files.keyword` 也曾被工具契约拒绝。这些是已复现的真实生产缺陷。
- v4.0.5 修复团队可用当前账号有权访问的 `project_id`、`file_id` 作为代码输入，并在运行时复核文件归属、状态、内容版本及权限；临时成员遇输出截断仅有界重试一次；文件列表工具支持 `keyword`；报告明确区分项目主语言与实际审查语言，并调整 320/390px 封面、操作按钮及表格滚动。既有审批、会话恢复和手机端修复也随版本发布。团队 `#69` 已完成列明的普通账号并行/UI/刷新复验；其他工具调用细节及报告语言、布局仍须按后续验收矩阵单独核对。
- v4.0.5 本地后端全量 4527 通过、5 跳过，Ruff 通过；前端 1310 项单测、ESLint、vue-tsc、Vite 构建通过；浏览器回归 35 通过、9 项需隔离登录后端的用例跳过。320/390px 报告布局回归重复三轮通过。这些结果不替代生产真实点击。
- v4.0.6 发布前候选修复源快照、租约、关键词字面量、移动端报表打印及刷新后最初用户提问缺失。独立代码审查发现两处会话历史重复边界，均先由失败回归复现、修复后通过（先红后绿）。候选后端全量测试 4551 passed、5 skipped、4 warnings，耗时 114.37s；Ruff 全量 `backend/app` 与 `tests` 通过。前端 1310 通过，Playwright 37 通过、9 跳过，另有本地布局 Playwright 23/23 通过；lint/build 通过。此处均为发布前候选验证。
- 发布脚本于 2026-09-23 18:59:29 UTC 部署 v4.0.6，发行代码 SHA `c86a7483aec160b2aa55c8e3195a3ad26d813387`，前后端同 SHA；备份隔离恢复为 101 张表，Alembic head `057`，HTTPS/health/ready 通过。`ops-check` 无阻断，但磁盘 88%、状态 `degraded`。发布后日志筛查中的后端与前端 404/5xx 计数截至核验时均为 0，不代表长期无错误。
- Mac 锁屏且 IAB `getTab` 超时，导致 v4.0.6 发布后真实点击、手机布局与聊天首问恢复 UI 复验未完成。后续 docs-only 提交仅更新验收文档，不改变上述生产发行代码运行 SHA。
- 已将旧 E2E 文件中的真实账号口令明文默认值从新版本移除，并增加本机隔离和 `release=dev` 门禁。旧 Git 历史可能保留此前口令；账号持有人仍需在验收后轮换。

## 待完成的现场验收

团队 `#69` 的成员、事件和用量独立复核已完成；此前 Safari 可见 UI 刷新后首问缺失，但 19:06:53 UTC 服务端 session 组装返回了完整 11 条公开消息与首问，顺序和 checkpoint 可见消息一致且无重复。因此后端持久化/组装有只读证据，浏览器渲染是否恢复仍未验收。Mac 锁屏和 IAB 超时是本次未完成现场点击的原因，不能将其写成通过。管理员 #73、审查员 #78 的同一 `user` surface 及管理员 `admin` surface 返回空；仍需浏览器账号隔离验收。后台聊天的 10 次调用/373778 tokens 需单独做性能分析。还须完成管理员、审查员的团队正向验收、三账号权限与跨账号对象检查、浏览器进程异常退出恢复、桌面及手机页面逐项点击、意外 404、网络中断恢复，以及报告语言和布局复验。发布前最终后端全量测试 4551 passed、5 skipped、4 warnings（114.37s），Ruff 全量 `backend/app` 与 `tests` 通过；本地布局 Playwright 23/23 通过。该时段服务端日志未见404/5xx/ERROR/Traceback，但不构成长时间保证。当前发行代码 SHA 为 `c86a7483aec160b2aa55c8e3195a3ad26d813387`；文档后续 docs-only 提交不会改变运行 SHA。已记录的 v4.0.4 失败不能因代码修复、本地测试或不同团队的通过而改判通过。步骤见 [[TODO_真实Agent发布与生产闭环]]，逐项证据见 [[ACCEPTANCE_真实Agent发布与生产闭环]]。

## 证据边界

本地截图和自动化证明所测布局与代码行为，不等于生产真实点击；公网 HTTP 200 只证明服务入口可达，不证明登录后交互和权限正确。Agent 工坊“执行测试”为结构校验，不等于有效模型结果。v4.0.4 的任务 #181 是一次正式审查成功，团队 #68 明确失败；v4.0.5 团队 #69 证明普通账号这一团队总结果 3/3 且成员/事件/用量已复核。19:06:53 UTC 服务端 session 组装结果含首问且顺序无重复，但 Safari 可见 UI 尚未复验，不能据服务端 payload 推断浏览器显示通过。v4.0.6 已部署；该项现场 UI 验收仍未完成。
