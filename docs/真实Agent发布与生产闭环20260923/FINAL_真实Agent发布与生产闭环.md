# 真实 Agent 发布与生产闭环阶段结果

观察日期：2026-09-23。当前状态：生产平台 v4.0.3 与永久 Agent 已发布；真实模型调用在生产复现失败，v4.0.4 候选修复已完成本地全量测试，待发布后重试。团队闭环尚未完成，不能称为全流程验收通过。

## 已交付

- 审查员在生产工坊创建有真实职责的“鉴权与账号隔离审查员”v1，完成静态校验并提交审批单 #239。管理员通过真实页面审阅发布包；用户当次确认后在生产页面批准并发布。独立只读复核显示审批单 #239 为 `approved`、版本 #6 为 `published`、Agent #4 已启用且指向版本 #6、发布记录 #7 为 `published`，四者对应同一发布。发布包为首次发布、无额外能力、0 Skill；预计每代码分片新增一次模型调用，实际用量仍待真实运行验证。
- 代码修复覆盖审批单类型/版本/所有者绑定、管理员修订路径、已发布 Agent 修订期间旧版组队、发布新版后旧团队固定快照、归档会话轮询与恢复竞态、管理员发布页签导航，以及侧栏/卡片/操作列等响应式错位。
- 隔离候选 `e2bd4cc8bcd9994fb1b681c438aefd54f0cdefe5` 的后端全量 4514 通过、5 跳过；前端单测 1302 通过，ESLint、类型检查和构建通过；本地布局浏览器 23 通过。9 项需登录隔离后端的旧 E2E 未运行，不能冒充生产验收。
- 生产部署脚本完成两次备份验证、前后端同 SHA 镜像、迁移头核对和 HTTPS 冒烟。公网 `/healthz` 与 `/readyz` 均为 v4.0.3 / `e2bd4cc8bcd9994fb1b681c438aefd54f0cdefe5`。发布后巡检无阻断项，磁盘 86% 属告警。
- 旧 E2E 文件中的真实账号口令明文默认值已从新版本删除，并增加本机隔离标记及后端 `release=dev` 检查。旧 Git 历史可能保留此前口令；账号持有人应在本轮验收后轮换。
- 真实审查员会话定位到新 Agent 后，模型调用连续遇到输出截断和规则字典属性访问异常，均未形成有效审查结论。v4.0.4 候选让规则格式同时接受 ORM 与字典，只在输出截断时按配置上限提高预算重试一次；后端全量 4517 通过、5 跳过、覆盖率 80%。这仅证明本地修复，仍需生产实调复验。
- 普通账号对真实项目 15 的 `SecurityConfig.java` #783 完成只读定位；审查员失败会话与普通账号成功会话在刷新后各自按序保留。普通账号越权管理员治理页得到明确 403，返回原代码页成功。尚未完成管理员交叉隔离、团队并行与正式审查。
- Safari 390×844 真实手机视口暴露发起审查、代码文件列表和历史任务详情的裁切；并验证问题详情展开、查看任务跳转、小菱浮窗开关。v4.0.4 候选已修复侧栏、任务列表、发起审查、历史任务详情和代码文件列表；前端 1309 项单测、32 项本地浏览器测试通过，9 项登录型用例因没有隔离后端而跳过。针对手机文件列表及审查表单的 9 项用例另重复三轮、27/27 通过；总数缩减时单分页组件只发起一次页码自动纠正请求。生产复验仍待发布。
- Safari 控制台把反复的 `Network Error` 定位到后台心跳、收件箱和会话读取请求在短时断网时失败；v4.0.4 候选已增加后台传输错误静默及轮询退避，并保留主动操作的错误反馈。本地回归已通过；生产尚未复验，不能记作已消除。

## 尚未验收

Agent 发布成功只证明审批与目录持久化。发布后的真实模型结果、小菱并行团队独立成员事件、三账号会话隔离与异常退出、生产页面逐项错位和意外 404 尚未完成现场核验。具体顺序与判定见 [[TODO_真实Agent发布与生产闭环]] 和 [[ACCEPTANCE_真实Agent发布与生产闭环]]。

## 证据边界

本地截图和自动化证明所测布局与代码行为，不等于生产真实点击。生产 HTTP 200 只证明 SPA 路由入口可达，不证明权限与页面交互。工坊“执行测试”为结构校验，不等于真实模型有效性。真实团队须以独立成员事件、模型用量和持久会话为准。
