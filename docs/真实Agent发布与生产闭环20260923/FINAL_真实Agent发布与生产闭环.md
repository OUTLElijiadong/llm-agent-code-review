# 真实 Agent 发布与生产闭环阶段结果

观察日期：2026-09-24。当前生产为 v4.0.5，运行提交 `c7a8078eaa95b4b85fd0996a734cb2d5e17d7608`。普通账号真实 UI 团队 `#69` 总结果 3/3 成功，已发布 Agent 与临时 Agent 并行；成员、事件和用量已独立复核，共记录 4 次 worker 模型调用。刷新后团队与聊天页面可打开，但最初用户提问缺失；checkpoint 仍在，`/session` 仅显示较新的后台 run，故聊天持久化未完全通过。v4.0.6 正在修复该问题，尚未发布。以上不构成全部角色或全流程验收通过。

## 已交付与核实

- 审查员在生产工坊创建有真实职责的“鉴权与账号隔离审查员”v1，完成静态校验并提交审批单 #239。管理员经真实页面审阅，取得用户确认后批准并发布。独立只读复核显示审批单 #239 为 `approved`、版本 #6 为 `published`、Agent #4 已启用且指向版本 #6、发布记录 #7 为 `published`。该 Agent 无额外能力、无 Skill；每代码分片约新增一次模型调用属于发布前估计，不是实际用量。
- v4.0.5 生产团队 `#69` 由普通账号通过真实 UI 完成 3/3 成功，已发布 Agent 与临时 Agent 并行运行；成员、独立事件和用量已复核，共 4 次 worker 模型调用。结论仅覆盖该账号与该团队。
- 刷新后团队与聊天页面仍可打开，但首条用户提问未恢复显示；checkpoint 仍在，`/session` 仅显示较新的后台 run。后台聊天记录到 10 次调用、373778 tokens，另列为性能待办，不与 worker 调用混算。
- v4.0.4 生产任务 #181 对真实项目 15 的 `SecurityConfig.java` #783 完成审查：1/1 文件，评分 64/100，形成 10 条问题（2 高、6 中、2 低），报告可打开。这证明已发布 Agent 能参与这一次正式审查，不证明所有 Agent 或并行团队均成功。
- v4.0.4 小菱团队 #67 因已发布成员未携带 `input.code` 而取消。小菱补源码创建的团队 #68 中，已发布成员任务 #210 完成并独立评分 62；临时成员 #211 连续输出截断，汇总成员 #212 因依赖失败被阻断，团队终态为失败。小菱向用户说明了部分结果和失败原因；聊天中 `list_code_files.keyword` 也曾被工具契约拒绝。这些是已复现的真实生产缺陷。
- v4.0.5 修复团队可用当前账号有权访问的 `project_id`、`file_id` 作为代码输入，并在运行时复核文件归属、状态、内容版本及权限；临时成员遇输出截断仅有界重试一次；文件列表工具支持 `keyword`；报告明确区分项目主语言与实际审查语言，并调整 320/390px 封面、操作按钮及表格滚动。既有审批、会话恢复和手机端修复也随版本发布。团队 `#69` 已完成列明的普通账号并行/UI/刷新复验；其他工具调用细节及报告语言、布局仍须按后续验收矩阵单独核对。
- v4.0.5 本地后端全量 4527 通过、5 跳过，Ruff 通过；前端 1310 项单测、ESLint、vue-tsc、Vite 构建通过；浏览器回归 35 通过、9 项需隔离登录后端的用例跳过。320/390px 报告布局回归重复三轮通过。这些结果不替代生产真实点击。
- v4.0.6 候选修复源快照、租约、关键词字面量、移动端报表打印及刷新后最初用户提问缺失。独立代码审查发现两处会话历史重复边界，均先由失败回归复现、修复后通过（先红后绿）。最终后端全量测试 4551 passed、5 skipped、4 warnings，耗时 114.37s；Ruff 全量 `backend/app` 与 `tests` 通过。前端 1310 通过，Playwright 37 通过、9 跳过，另有本地布局 Playwright 23/23 通过；lint/build 通过。v4.0.6 尚未发布，这些结果只代表候选验证。
- 发布脚本于 2026-09-23 16:03:05 UTC 完成 v4.0.5 前后端同 SHA 部署，迁移头 057；发布前独立备份恢复验证为 101 张表。公网 `/healthz`、`/readyz` 均返回 v4.0.5 和完整运行提交。发布后 `ops-check.sh` 无阻断，容器、账本、备份及 HTTPS 检查正常；磁盘使用率 87% 超过 85% 告警阈值，状态仍为 `degraded`，未执行清理。
- 已将旧 E2E 文件中的真实账号口令明文默认值从新版本移除，并增加本机隔离和 `release=dev` 门禁。旧 Git 历史可能保留此前口令；账号持有人仍需在验收后轮换。

## 待完成的现场验收

团队 `#69` 的成员、事件和用量独立复核已完成，但刷新后聊天首问缺失，完整持久化未通过；v4.0.6 修复尚未发布，发布后须现场复验首问、checkpoint 与 `/session` 恢复。后台聊天的 10 次调用/373778 tokens 需单独做性能分析。还须完成管理员、审查员的团队正向验收，三账号正向及拒绝权限、跨账号会话直接对象访问、浏览器进程异常退出恢复、桌面及手机页面逐项点击、意外 404、网络中断恢复，以及报告语言和布局复验。独立审查发现的两处会话历史重复边界已先红后绿修复；最终后端全量测试 4551 passed、5 skipped、4 warnings（114.37s），Ruff 全量 `backend/app` 与 `tests` 通过；本地布局 Playwright 23/23 通过。v4.0.6 尚未发布。已记录的 v4.0.4 失败不能因代码修复、本地测试或不同团队的通过而改判通过。步骤见 [[TODO_真实Agent发布与生产闭环]]，逐项证据见 [[ACCEPTANCE_真实Agent发布与生产闭环]]。

## 证据边界

本地截图和自动化证明所测布局与代码行为，不等于生产真实点击；公网 HTTP 200 只证明服务入口可达，不证明登录后交互和权限正确。Agent 工坊“执行测试”为结构校验，不等于有效模型结果。v4.0.4 的任务 #181 是一次正式审查成功，团队 #68 明确失败；v4.0.5 团队 #69 证明普通账号这一团队总结果 3/3 且成员/事件/用量已复核，但刷新后初始用户问题缺失，因此聊天恢复不算完整通过。v4.0.6 候选及其中的 session 修复尚未发布。
