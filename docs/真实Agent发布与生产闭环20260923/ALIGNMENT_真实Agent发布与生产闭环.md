# 真实 Agent 发布与生产闭环对齐

## 原始需求

用户要求审查员基于真实生产代码创建有用的子 Agent，管理员审批并成功发布，再通过真实点击完成小菱调用、团队审查、聊天记录、账号隔离、异常恢复和相关功能验收。真实业务结果可以保留；不得把合成样本伪装成真实业务数据。

## 当前生产与边界

- 2026-09-23 现场 `/healthz` 返回 v4.0.2，运行提交 `7e7559acf2916cc8ae7380e33bb2519a0addf12c`；页面也显示 v4.0.2。
- 审查员账号当前只可见一个明确标注为合成样本的项目。本轮不以它充当真实源码；从上述运行提交提取真实的审批接口及服务层源码作为审查对象，保留来源提交和哈希。
- 生产登录只使用用户给定的管理员、审查员、普通账号；报告不记密码、令牌、完整私人聊天或无关第三方内容。
- 审查员创建的永久 Agent 首版不申请外联、Shell、写入或额外 Skill；发布后会加入正式审查，每个代码分片约增加一次模型调用。
- 用户已授权真实创建和生产验收。界面操作工具仍要求在全站 Agent 发布这一权限扩展的实际点击前再次确认；审批单准备、读取和其他独立工作可继续。

## 已核实实现约束

- 审查员入口为 `/agents?section=studio`，管理员发布审批入口为 `/admin/governance?section=releases`。
- 工坊“执行测试”仅为结构与权限静态校验，真实模型效果必须在发布后另行调用核验。
- `agent_delegate` Skill 只是提示词上下文，真实并行团队必须以 `create_agent_team` 成员与事件轨迹为证据。
- 正常路由的意外 404 为缺陷；跨账号团队详情的防枚举 404 须单列为预期拒绝。

## 待实测问题

发布后真实源码审查质量、三账号对话隔离、全流程页面按钮、异常退出与 404、生产界面对齐，以及发现缺陷的修复和重新发布状态，均以现场证据结论为准。
