# 待办

- 修复或升级 bWAPP 样本中的 PHP lint 问题后，重新运行 `sbx_eab9935fc88d47cd800a51e6` 同类白盒任务，才能取得样本层面的 `passed=true`；当前平台已完成执行并正确导出失败报告。
- 全仓既有 ESLint 6 项错误仍待单独治理，位置见验收记录；不影响本次构建和公网验收。
- 公网项目归档若被恶意文件策略拒绝，继续按现有隔离、审计和人工复核流程处理，不得绕过校验。
