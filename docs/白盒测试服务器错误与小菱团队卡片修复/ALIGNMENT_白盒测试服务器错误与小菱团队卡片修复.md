# 对齐说明

## 原始需求

1. 修复 `www.lijiadong.cn` 启动白盒测试时的服务器错误。
2. 小菱调用子 Agent 团队时立即展示团队卡片，而不是在最终结论后统一补充。
3. 关闭悬浮窗后重开，或新建对话后再打开历史对话，团队卡片不能消失。

## 已确认事实

- 公网已复现：`POST /api/sandboxes` 返回 500。
- 生产项目 `#157` 的活动源码为真实 `bWAPP-master.tar.gz`，不是 ZIP。
- 根因是反编译规划对所有源码归档无条件调用 `zipfile.ZipFile`，触发 `BadZipFile`，异常未转换为业务错误。
- 前端团队卡片当前在消息列表外统一渲染，且本地会话快照未保存团队关联。

## 范围与边界

- 复用现有安全归档读取器，支持源码 ZIP/TAR/GZ 系列并继续识别内嵌 APK/AAB/DEX/JAR。
- 不放宽恶意文件隔离策略，不执行未经验证的反编译命令。
- 将团队 ID 绑定到调用时间线消息，并持久化到现有会话快照；不改变 Agent Team 后端契约。
- 完成本地回归、前端构建和公网部署后的白盒创建及卡片恢复验收。

## 疑问澄清

无未决业务问题。生产恶意文件策略仍由现有项目归档校验链裁决；本次只修复归档格式误判和前端生命周期丢失。
