# BOUNDARY_FORM_agent-governance-platform

> 用途：请填写本任务的关键边界。  
> 填写方式：把每项的「你的选择」改成 A/B/C，或在对话里回复编号。  
> 推荐默认组合：`B B B A B B B B B B B B A A B`

## 1. 高风险系统操作边界

你的选择：`A`

- `A` 仅权限变更、删除数据、生产配置变更算高风险。
- `B` 权限变更、删除数据、生产配置、业务代码修改、prompt/skill/策略修改、shell、外部 API、批量知识入库都算高风险。推荐
- `C` 所有影响其他用户、其他 Agent 或生产数据的动作都算高风险。

补充说明：

```text
哪些动作必须人工审批：

哪些动作可以自动审批：

```

## 2. 自我进化生效边界

你的选择：`B`

- `A` 全部立即生效，仅保留审计和回滚。
- `B` 知识和个人记忆可立即生效；prompt/skill/策略自动生成新版本，灰度生效并可回滚。推荐
- `C` 所有进化先进入沙箱试运行，通过评估后自动生效。

补充说明：

```text
允许自动修改的内容：

必须进入审批或灰度的内容：

```

## 3. Agent 修改业务代码边界

你的选择：`A`

- `A` 默认允许直接修改业务代码。
- `B` 允许生成补丁和 PR/变更提案，真正写入业务代码需策略允许或管理员确认。推荐
- `C` 禁止 Agent 修改业务代码，只能给建议。

补充说明：

```text
是否允许直接写入仓库文件：

是否允许创建提交/PR：

```

## 4. shell / 本地命令执行边界

你的选择：`A`

- `A` 默认经过策略引擎；读命令自动放行，写命令/危险命令阻断或升级。推荐
- `B` 默认允许执行，执行后审计。
- `C` 默认禁止，只有管理员手动授权。

补充说明：

```text
允许自动执行的命令类型：

必须阻断或审批的命令类型：

```

## 5. 策略引擎失败兜底边界

你的选择：`B`

- `A` 执行优先，记录策略失败。
- `B` 阻断优先，进入审批或告警。推荐
- `C` 根据动作风险：低风险执行，高风险阻断。

补充说明：

```text
策略服务不可用时默认行为：

策略规则冲突时默认行为：

```

## 6. 每日抓取来源边界

你的选择：`B`

- `A` 仅抓项目代码、项目文档、现有 docs。
- `B` 抓项目代码、项目文档、官方文档、指定 URL、GitHub issue/PR；外部来源由管理员配置白名单。推荐
- `C` Agent 可自行发现和扩展抓取来源。

补充说明：

```text
固定官方文档来源：

指定 URL 白名单：

GitHub 仓库/issue/PR 来源：

是否允许抓社区博客/论坛：

```

## 7. 抓取内容入库边界

你的选择：`B`

- `A` 全部自动入库。
- `B` 低风险自动入库，高风险/低置信/外部未知来源进入审批。推荐
- `C` 全部先进入暂存库，自动蒸馏但不进入正式知识库。

补充说明：

```text
低风险内容定义：

高风险内容定义：

低置信阈值：

```

## 8. Agent 知识库隔离边界

你的选择：`B`

- `A` 完全隔离，Agent 不能读取其他 Agent 知识库。
- `B` 默认隔离；允许通过策略引擎授予只读共享知识域。推荐
- `C` 所有 Agent 可共享全局知识库。

补充说明：

```text
允许共享的知识域：

禁止共享的知识域：

```

## 9. 奖励/惩罚影响边界

你的选择：`B`

- `A` 只记录分数，不影响行为。
- `B` 影响调度优先级、预算、自动审批阈值、是否降权，但不直接封禁。推荐
- `C` 可自动封禁工具、停用 Agent、回滚版本。

补充说明：

```text
奖励会提升什么：

惩罚会降低什么：

是否允许自动停用 Agent：

```

## 10. 告警通知边界

你的选择：`B`

- `A` 只在管理后台显示。
- `B` 管理后台 + 系统审计 + 可扩展 Webhook 配置。推荐
- `C` 管理后台 + 邮件 + Webhook + 短信/企业微信等外部通知。

补充说明：

```text
本期必须支持的通知方式：

后续预留的通知方式：

```

## 11. 知识库表设计边界

你的选择：`C`

- `A` 复用现有 `knowledge_doc/knowledge_chunk`，增加 `owner_type/owner_id`。
- `B` 新增 Agent 专用知识库表，避免破坏个人知识库。推荐
- `C` 两者并存，并增加统一检索视图。

补充说明：

```text
是否允许迁移现有个人知识库表：

是否要求用户知识库和 Agent 知识库完全分表：

```

## 12. 调度实现边界

你的选择：`C`

- `A` 项目内轻量调度表 + 后端启动时周期扫描。
- `B` 先做任务表和手动触发，定时由系统 cron / Docker cron 配置。推荐
- `C` 引入 Celery/Redis 或 APScheduler。

补充说明：

```text
是否允许新增 Redis/Celery：

每日任务执行时间：

失败重试次数：

```

## 13. OpenClaw / Hermes 外部依赖边界

你的选择：`A`

- `A` 本期只做项目内概念抽象，不接外部依赖。推荐
- `B` 预留接口，后续提供链接后再接。
- `C` 本期必须接入真实项目，需要提供仓库或文档。

补充说明：

```text
真实 OpenClaw 链接/路径：本期不需要，仅使用概念

真实 Hermes 链接/路径：本期不需要，仅使用概念

```

## 14. 管理端入口与用户端隔离方式

你的选择：`A`

- `A` 同一个前端应用内做独立 AdminLayout：管理员登录后进入 `/admin/overview`，左侧只显示管理后台菜单；普通用户仍走 `/dashboard` 和用户端菜单。推荐
- `B` 共用现有 AppLayout 和 `/dashboard`：管理员仍能看到普通业务菜单，只是多显示管理菜单。
- `C` 完全独立管理应用：新增单独 admin 前端入口/构建/部署，例如 `/admin-app` 或独立子域，和用户端运行时隔离。
- `D` 严格后台模式：管理员账号只能访问 `/admin/**`，不能访问项目、代码、报告、论坛等普通用户页面。

补充说明：

```text
管理员登录后默认页面：

管理员是否还能访问普通用户页面：

```

## 15. 本轮实现节奏边界

你的选择：`C`

- `A` 先完成完整 6A 文档，确认后再实现。
- `B` 完成 6A 文档 + 第一阶段可运行 MVP。推荐
- `C` 直接冲 L4 全量实现。

补充说明：

```text
本轮必须实现：

可以放到后续实现：

```

## 16. API Key 和外部凭证边界

你的填写：

```text
是否已有外部 API Key：

是否允许 Agent 使用用户个人 API 配置：

是否需要为每个 Agent 配独立凭证：

所有凭证是否统一放入 .env / 管理端加密配置：

```

## 17. 沙箱隔离边界

你的填写：

```text
是否要求每个 Agent 独立进程：

是否要求工具执行沙箱：

是否要求网络访问白名单：

是否要求文件访问白名单：

```

## 18. 验收优先级边界

你的填写：

```text
最高优先级验收项 1：

最高优先级验收项 2：

最高优先级验收项 3：

可以延后的验收项：

```
