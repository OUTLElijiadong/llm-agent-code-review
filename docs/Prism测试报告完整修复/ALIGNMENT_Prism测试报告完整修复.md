# Prism 测试报告完整修复：对齐文档

## 原始需求

以 `/Users/li/Documents/Prism测试交付-20260826/` 为证据输入，完整修复已经复现并确认的问题，覆盖代码、自动化测试、生产部署和真实页面点击验收。

后续增量需求纳入同一验收范围：保留 `claude/elated-darwin-18d0f5` 的有效前后端改动；无权限功能不显示；用户端顶栏、卡片、数字和路由动效统一优化；小菱导航以可见虚拟鼠标点击真实页面入口后跳转；后台自动模型任务不得在缺省配置下消耗额度。

## 基线

- 目标生产主机：`81.70.251.90`，代码目录 `/opt/code-review`，公网入口 `https://www.lijiadong.cn`。
- 本轮本地分支：`codex/prism-test-report-fixes-20260826`；发布 SHA 以最终验收记录为准。
- Claude worktree：`claude/elated-darwin-18d0f5`；只迁移有效功能提交，不覆盖当前代码和配置。
- 数据库迁移目标：`043_remote_import_lifecycle`，发布前备份，前向升级，不在生产自动 downgrade。
- 外部修复方案：`/Users/li/Documents/Prism测试交付-20260826/04-修复方案.md`。

## 问题定性

| 类别 | 项目 | 决定 |
| --- | --- | --- |
| 必须修复 | N 路重复发现、命令注入漏报、CVSS 丢失/失真、行号偏移、评分口径、安全头继承、登录限流、远程导入阻塞、HTML 预览假成功、修复方案不定位、空邮箱兼容、Radio 弃用 | 修改代码并补自动化测试 |
| 保持业务语义 | 干净 `.sh/.php` 上传 | 仍作为源码审查；补非执行、恶意文件隔离测试 |
| 已正确 | 修改密码空表单、项目视图切换 | 不新增重复提示；只补回归与无障碍状态 |

## 边界

1. 不删除或改写任务 163 的历史审计结果。
2. 不把相同 CWE 粗暴视为同一问题；根因聚类必须参考文件、调用点和证据。
3. 不把缺失 CVSS 存成 `0.0`，不强制同 CWE 同分。
4. 不因扩展名默认隔离可审查脚本源码。
5. 数据库迁移只做向后兼容的新增字段或新增表。
6. 不修改真实生产用户的密码、角色或状态；验收专用临时账号可在授权范围内创建，并在验收后通过软删除清理。
7. 无权限入口采用失败关闭：未知路由、模型编造路由、外部地址和守卫拒绝的路由均不渲染。
8. 动效不得改变业务数据、组件稳定尺寸或键盘/触控可用性，并尊重系统“减少动态效果”设置。

## 已消除的歧义

- “0 分与必须处理矛盾”不是公式缺陷；真实缺陷是重复扣分、明细不可见、多端阈值不一致。
- “缺安全响应头”只对首页和静态资源成立；API 已有四个安全头。
- “数据库是否损坏”结论为否；重复数据源于写库前结果管线未做 N 路归并。
- 用户已明确要求完整修复，允许按已核验修复方案实施、测试、部署并做非破坏性线上验收。

## 成本与自动化口径

- 管理小菱仍可由管理员主动发起全自动运维编排、只读核验、审批续跑和子 Agent 监督。
- JARVIS 定时巡逻只采集证据并产生告警；不主动把简报送入模型。
- 周期 Skill 与事件触发默认关闭，避免登录、定时任务或审查事件隐式消耗用户额度；启用必须显式配置，并经过日预算闸门。
- “自动化”指已授权任务的状态推进、心跳、重试、恢复和结果回传，不等于后台无限制调用模型。
