# Prism 测试报告完整修复：验收记录

## 当前状态

本地代码与自动化门禁已通过；生产部署和三角色真实点击在本轮发布后记录。本文只记录已取得的证据，不用计划替代结果。

## 变更边界与合并记录

- [x] 已保留 Claude worktree `claude/elated-darwin-18d0f5` 的有效提交：思考城市、团队事件增量接口、子城市生命周期；未合并其中标记为生产基线覆盖的 `b1cfdd9`，避免覆盖本地修复。
- [x] 已合并测试交付报告修复、Agent 团队监督、圆桌结果管线、成本保护和远程导入生命周期。
- [x] 远程导入取消使用独立只读租约会话校验，取消发生在普通文件或审计归档入库后均不留下半成品。
- [x] 周期 Skill 和事件触发默认关闭；管理员显式开启时仍受 Agent 日预算闸门保护。JARVIS 巡逻和健康检查默认只采集确定性证据，不隐式调用模型。

## 自动化修复验收

| 范围 | 结果 | 证据 |
| --- | --- | --- |
| N 路去重、位置归一化、圆桌共用持久化入口 | 通过 | issue merger、圆桌回归测试 |
| CWE-78 确定性检测 | 通过 | AST/静态规则与安全 argv 回归测试 |
| CVSS 与评分口径统一 | 通过 | 向量校验、归并后评分、报告一致性测试 |
| 安全响应头和登录限流 | 通过 | API/配置/限流/网关头回归测试 |
| 脚本源码审查与恶意内容隔离 | 通过 | 沙箱、恶意扫描和源码边界测试 |
| 远程导入持久化异步闭环 | 通过 | 生命周期、任务、API、迁移和跨会话取消测试 |
| HTML 预览和修复方案定位 | 通过 | 报告页面组件及接口回归测试 |
| 邮箱、密码、视图切换和 Radio 回归 | 通过 | 前端表单、路由和组件测试 |
| Agent 团队/圆桌纠正与可追问 | 通过 | 团队服务、Responses、圆桌和前端恢复测试 |
| 后台模型成本保护 | 通过 | 默认关闭、预算并发闸门、JARVIS 不投递测试 |
| 小菱 Pro / 子 Agent Flash 模型口径 | 通过 | 生产配置只读核验、运行时分层测试、首屏标签与 SSE 真实模型覆盖测试 |
| 无权限导航隐藏与未知路由失败关闭 | 通过 | 侧栏、顶栏、Agent 链接及共享路由守卫测试 |
| 小菱旧消息已知路由安全恢复 | 通过 | 导航语义、静态路由、代码围栏、显式协议优先及未知路径回归测试 |
| 小菱虚拟鼠标真实点击与单次跳转 | 通过 | 真实 `data-route` 元素点击、离屏滚入视区、面板上层显示、回调兜底和防双跳测试 |
| 毛玻璃、数字滚动、入场和路由过渡 | 通过 | 既有计数器复用、组件测试、类型检查及生产构建 |

## 本地质量门禁

- [x] 后端全量 pytest：`2032 passed`，`1 warning`（既存测试类收集警告，无失败），总覆盖率 `75%`。
- [x] 后端 Ruff：`All checks passed`。
- [x] 前端 Vitest：`51 files passed, 316 tests passed`。
- [x] 前端 ESLint：通过。
- [x] 前端 `vue-tsc` 与生产构建：通过，Vite 转换 `3668 modules`。
- [x] 远程导入定向回归：后端 `28 passed`，前端 `16 passed`；成本/调度/事件回归 `40 passed`。
- [x] `git diff --check`：通过。

## 生产验收记录

| 检查项 | 状态 | 证据 |
| --- | --- | --- |
| 生产分支、提交和镜像一致 | 待本轮发布 | 发布后填写完整 SHA |
| 数据库备份、迁移 043、健康检查 | 待本轮发布 | 发布日志和 Alembic 版本 |
| 管理员小菱：新对话、不自动消费额度、团队监督 | 待真实点击 | 浏览器截图/网络与控制台记录 |
| 审查员：角色入口、项目隔离、审查流程 | 待真实点击 | 浏览器操作记录 |
| 普通用户：项目/代码/报告/小菱流程 | 待真实点击 | 浏览器操作记录 |
| 圆桌讨论、子 Agent 追问与修正 | 待真实点击 | 会话、团队和圆桌事件记录 |
| GitHub 导入取消/完成闭环 | 待真实点击 | 任务状态及无半成品核验 |
| 浏览器控制台无新增 error/warn | 待真实点击 | 三角色控制台采样 |

## 结论规则

只有生产提交、迁移、健康检查和三角色真实点击全部完成后，才将“待本轮发布”改为“通过”。任何线上失败必须保留失败证据并先修复、再重跑对应门禁。
