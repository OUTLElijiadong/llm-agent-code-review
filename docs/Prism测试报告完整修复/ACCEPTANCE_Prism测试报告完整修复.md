# Prism 测试报告完整修复：验收记录

## 当前状态

本地代码、自动化门禁、生产部署和三角色关键路径真实点击均已完成。本文只记录已取得的证据，不用计划替代结果。

## 变更边界与合并记录

- [x] 已保留 Claude worktree `claude/elated-darwin-18d0f5` 的有效提交：思考城市、团队事件增量接口、子城市生命周期；未合并其中标记为生产基线覆盖的 `b1cfdd9`，避免覆盖本地修复。
- [x] 已合并测试交付报告修复、Agent 团队监督、圆桌结果管线、成本保护和远程导入生命周期。
- [x] 远程导入取消使用独立只读租约会话校验，取消发生在普通文件或审计归档入库后均不留下半成品。
- [x] 周期 Skill 和事件触发默认关闭；管理员显式开启时仍受 Agent 日预算闸门保护。JARVIS 巡逻和健康检查默认只采集确定性证据，不隐式调用模型。

## 自动化修复验收

| 范围 | 结果 | 证据 |
| --- | --- | --- |
| N 路去重、位置归一化、圆桌共用持久化入口 | 通过 | issue merger、圆桌回归测试 |
| CWE-78 确定性检测 | 通过 | AST/静态规则与安全 argv 回归测试 |
| CVSS 与评分口径统一 | 通过 | 向量校验、归并后评分、报告一致性测试 |
| 安全响应头和登录限流 | 通过 | API/配置/限流/网关头回归测试 |
| 脚本源码审查与恶意内容隔离 | 通过 | 沙箱、恶意扫描和源码边界测试 |
| 远程导入持久化异步闭环 | 通过 | 生命周期、任务、API、迁移和跨会话取消测试 |
| HTML 预览和修复方案定位 | 通过 | 报告页面组件及接口回归测试 |
| 邮箱、密码、视图切换和 Radio 回归 | 通过 | 前端表单、路由和组件测试 |
| Agent 团队/圆桌纠正与可追问 | 通过 | 团队服务、Responses、圆桌和前端恢复测试 |
| 后台模型成本保护 | 通过 | 默认关闭、预算并发闸门、JARVIS 不投递测试 |
| 小菱 Pro / 子 Agent Flash 模型口径 | 通过 | 生产配置只读核验、运行时分层测试、首屏标签与 SSE 真实模型覆盖测试 |
| 无权限导航隐藏与未知路由失败关闭 | 通过 | 侧栏、顶栏、Agent 链接及共享路由守卫测试 |
| 无权限业务操作隐藏与执行前二次守卫 | 通过 | 项目、审查、问题、代码、安全、报告和仪表盘粒度权限回归测试 |
| 小菱旧消息已知路由安全恢复 | 通过 | 导航语义、静态路由、代码围栏、显式协议优先及未知路径回归测试 |
| 小菱虚拟鼠标真实点击与单次跳转 | 通过 | 真实 `data-route` 元素点击、离屏滚入视区、面板上层显示、回调兜底和防双跳测试 |
| 管理小菱不遮挡表格操作 | 通过 | 1280x800 组件回归、拖拽位置持久化、移动端布局和生产表格真实点击 |
| 毛玻璃、数字滚动、入场和路由过渡 | 通过 | 既有计数器复用、组件测试、类型检查及生产构建 |

## 本地质量门禁

- [x] 后端全量 pytest：`2039 passed`，`1 warning`（既存 `TestReviewReporterAgent` 自定义 `__init__` 收集警告，无失败）。
- [x] 后端 Ruff：`All checks passed`。
- [x] 前端 Vitest：`61 files passed, 370 tests passed`。
- [x] 前端 ESLint：通过。
- [x] 前端 `vue-tsc` 与生产构建：通过，Vite 转换 `3669 modules`。
- [x] 远程导入定向回归：后端 `28 passed`，前端 `16 passed`；成本/调度/事件回归 `40 passed`。
- [x] `git diff --check`：通过。

## 生产验收记录

| 检查项 | 状态 | 证据 |
| --- | --- | --- |
| 生产运行代码和镜像一致 | 通过 | 实际运行的后端与前端镜像均为功能提交 `eccab68a24c9e280d6d4bc3111f800f6f2e69ecd` |
| 服务器最终验收文档同步 | 通过 | 服务器源码在运行镜像验收后仅快进本文件所在的纯文档提交，未改变容器运行代码 |
| 数据库备份、迁移 043、健康检查 | 通过 | 隔离恢复验证为 85 张表；Alembic 为 `043_remote_import_lifecycle`；MySQL、Redis、ClamAV、后端、前端均健康 |
| 管理员小菱：新对话和零成本页面操作 | 通过 | 新建后标签为“新对话”；虚拟鼠标从用户管理跳转系统操作审计；`AiCallLog` 前后均为 `9496` |
| 管理员表格操作不被小菱遮挡 | 通过 | 1280px 宽生产页面保持小菱展开，真实点击“设置角色”成功弹窗，取消后无数据变更 |
| 审查员：角色入口、权限隔离和小菱导航 | 通过 | 临时审查员登录；无管理员菜单；小菱以彩框和虚拟鼠标跳转 `/reviews`；直达 `/admin/users` 进入 `/403` |
| 普通用户：导航与权限失败关闭 | 通过 | 普通用户首页真实数据正常；无管理员导航、无“无权限”噪声；直达管理页进入 `/403` |
| 临时审查员清理 | 通过 | 管理端确认软删除；数据库用户 `id=100` 为 `status=-1` 且令牌版本递增，历史数据保留 |
| 圆桌讨论、子 Agent 追问与修正 | 自动化通过，未重复付费实跑 | 团队服务、Responses、圆桌、恢复和前端交互均包含在全量测试；为避免再次消耗用户额度，本轮发布后不触发付费长链路 |
| GitHub 导入取消/完成闭环 | 自动化通过，未重复外网导入 | 生命周期、租约、幂等、跨会话取消、失败无半成品和迁移回归通过；本轮未新建线上项目 |
| 浏览器控制台无新增应用错误 | 通过 | 管理员、审查员、普通用户生产页面采样均为 0 条应用 error |

## 结论规则

生产提交、迁移、健康检查和三角色关键路径真实点击均已完成。付费模型长链路与外网仓库导入在本轮明确采用自动化证据，不冒充生产实跑；后续任何线上失败仍须保留失败证据并先修复、再重跑对应门禁。
