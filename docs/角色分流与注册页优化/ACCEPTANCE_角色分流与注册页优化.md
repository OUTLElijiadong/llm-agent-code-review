# 角色分流与注册页优化验收记录

## 完成情况

- [x] 新增 `frontend/src/utils/roleHome.ts`，集中维护角色默认首页。
- [x] 登录成功后按角色跳转：管理员 `/admin/users`，审查员 `/reviews`，普通用户 `/projects`。
- [x] 根路径 `/` 和已登录访问 `/login`、`/register` 时按角色分流。
- [x] 侧栏菜单按 `admin/reviewer/user` 角色裁剪。
- [x] 全局搜索入口按角色过滤。
- [x] 注册页重做为 Prism 品牌风格，和登录页保持一致。

## 验证

```bash
cd frontend
npm run build
```

结果：通过，包含 `vue-tsc` 类型检查与 Vite 生产构建。

## 可视检查

- 桌面端 `http://127.0.0.1:5173/register`：左右分栏、品牌区、5 个输入框和提交按钮均正常渲染。
- 移动端 `390x844`：品牌区自动收起，注册表单单栏显示，页面无横向溢出。

## 风险

- 本次只做前端默认入口和菜单分流，后端仍按既有权限模型处理接口访问。
