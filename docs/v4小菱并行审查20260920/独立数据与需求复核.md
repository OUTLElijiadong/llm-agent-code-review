# 独立数据与需求复核

复核日期：2026-09-20。复核者为本轮前端代理，对其他代理的后端数据、生产基线与知识记录另行核对。本文不把自己的前端实现算作独立复核，也不将本地/API 桩测试算作生产验收。

## 已核验的结论

1. **生产基线为 3.9.14。** `生产基线.json` 和 `生产基线补充.json` 的公网 health/ready 均为 200，版本 3.9.14、release `ccc9284af7d9ddf71d47e022a667a68eef4fbe18`；后端/前端镜像与该 release 一致。补充文件明确 SSH 目标、工作目录和命令，迁移 current=head=`056_older_run_titles`，ops-check 退出码 0。复核的是已落盘原始输出，未重新 SSH 访问服务器。
2. **报告撤权、隔离、快照和旧 Worker 保护与修复证据一致。** 初始 before/after 中成员撤销后的报告/导出从 200 变为 404、列表变为空；历史文件名恢复为冻结的 source.py/python；旧 Worker 未覆盖新 Worker 的 coverage。原隔离场景先删除成员，存在混杂因素；本次在内存 SQLite 恢复成员并验证报告重新 200 后，再隔离项目，报告/导出再次 404，独立确认隔离本身有效。详见 `证据/独立复核复跑.json` 和隔离复跑脚本。
3. **后端全量已结束。** `/tmp/prism-v4-backend-full.log` 结尾是 4311 passed、5 skipped、4 warnings；独立解析 XML 得到 4316 总计、0 failures、0 errors、5 skipped，两者一致。5 项跳过均因未提供明确创建的隔离 Redis 容器，不能写成所有测试无跳过。此前日志运行中的 70%/86% 等进度没有被计为完成。
4. **知识库偏好与用户原话/截图相符。** 中文文字和标签优先底边视觉对齐，详情/查看任务独立操作列，共同参照坐标；英文大小写排版区分，以及侧栏切换按钮完整可见。页面明确区分工程方案与个人偏好，将“仅运行现有 Agent”限定为本次业务范围，没有扩展为人格判断或永久技术偏好。最终知识库 validate/postflight 仍由主任务收口。

## 摘要数量与独立发现

- 合成正式任务 101 条问题，实际返回 100 条，`finding_count_total=101`、`findings_truncated=true`。合成审计 250 条，依赖摘要保留 200 条、`omitted_count=50`。
- 两节点最终保留 300 条，`bounded_finding_tasks` 同时标记 formal 与 audit。最新汇总正文已明确“2 个节点的问题明细有截断，请查看原任务/报告中的完整结果”，文档也写明 100/200 上限和数量仅针对返回条目去重。可以认定已披露范围；不能把 300 描述成全部 351 条的完整问题总数。
- 独立复跑发现仅有 file_name 时，one.py 与 two.py 相同行号/标题的两条问题被合为 1 条。主代理已修复文件名回退，完全缺少位置的条目也不再互相去重。本代理再次执行同一合成输入，现保留 2 条；最新相关测试独立运行 **9 passed，退出码 0**，其中包含两个文件及两个未知位置应保留 4 条的测试。修正前结果保留于 `独立复核复跑-修正前.json`。

## 未完成边界

生产基线证据的公网/迁移/ops-check缺项已由补充文件闭环。当前未发现上述限定复核范围内仍未解决的计数或来源冲突。

**仍不得宣称 v4 已上线。** 本机首次真实模型运行 `20260920T120914Z/验收结果.json` 为 passed=false、真实 HTTP 尝试 0；它只是保留的失败证据。服务器隔离环境的后续真实模型审查、审计和并行调用验收尚待负责代理完成后另行复核，生产发布后的健康、版本、业务与浏览器结果也尚待主任务补证。

源码、证据 SHA256 与复核时间保存在 `证据/独立复核记录.json`。本报告不承诺全代码库不存在其他逻辑问题。
