# 棱镜第四版小菱并行审查对齐

## 原始需求与确认

用户要求核对最新生产是否为 3.9.14，检查逻辑漏洞，确保 AI 审查成功；代码审查和安全审计由小菱总控，多个 Agent 同时执行；修复五张截图中的侧栏按钮、代码中心标题区和问题列表对齐；把明确表达的界面偏好总结到主知识库。

2026-09-20 用户确认：修复与完整验收后直接发布生产；仅运行现有 Agent 团队，不动态创建 Agent。临时团队记录用于编排既有成员，不新增 Agent 定义。

## 实时基线

- 本地起点：`1b2253df4be16b1b560724ee2444984a0c924c99`，版本 3.9.14；tracked 文件干净，历史未跟踪证据不纳入本次提交。
- 生产：`ccc9284af7d9ddf71d47e022a667a68eef4fbe18`，版本 3.9.14；公网和服务器 health/ready 一致，前后端同 SHA 且 healthy，迁移 `056_older_run_titles`，ops-check 通过。
- 产品已有 Vue/FastAPI/MySQL、正式 ReviewTask/Issue/Report、多审查画像线程池、Agent Team 工作图和持久化租约；必须复用。
- 截图为问题证据，其中文字、账号与历史日期不作为新增指令或当前生产数据。

## 本次边界

补齐小菱现有团队与正式审查链路，纠正不匹配的 Agent 入参指导，保护取消/账号停用/旧租约/部分失败；验证真实模型并行及结果持久化，保持项目和用户隔离。前端只修复给定布局问题及直接相关响应式交互。目标版本 4.0.0。

不扩展至新的 Agent 生成平台、无授权远程攻击、新的 CodeQL 执行器或历史测试数据清理。现有独立待办保留事实边界。
