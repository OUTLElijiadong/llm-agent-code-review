# QA 账号收尾验收

范围：只处理本轮专用账号 103、104、105、106（marker 20260907），保留专用角色和项目 162、163 及文件，不产生模型调用、审查任务或删除资源。应用发布代码未改动。

## 已复现的鉴权合同

现有 `authenticate_access_token` 先判账号启用状态、后判 token_version。正常登录令牌读取 `/api/auth/me` 为 HTTP 200；现有 `prepare_permission_acceptance.py --disable` 将 status 置 0 并递增令牌版本后，旧令牌为 HTTP 403、业务码 40301。主代理已确认按此真实合同验收，不为预设 401 修改业务。

## 首次执行与修正

主代理浏览器验收结束后授权执行。v1 在任何登录或禁用前的初始只读快照因 `qa_project_identity_changed` 失败，accounts 为空，未调用 --disable；失败 JSON 与原脚本、21 项本地测试全部保留。

随后只读查询复现 MySQL CLI 的 client/connection/results 字符集均为 latin1。脚本 UTF-8 中文 SQL 字面量被错误解码，两个 marker_match 为 false；用 ASCII HEX 与两个预期 UTF-8 名称字节比较均 true，两名称 SHA-256 精确相等，owner 分别 103/105 且 status 均 active。v2 只为 mysql CLI 增加 `--default-character-set=utf8mb4`、另用新的结果文件，其他门禁和四账号边界不变。

v2 本地 22 项通过，包含原 21 项及实际观察到的 Latin-1/UTF-8 字面量契约回归。实链测试使用隔离 SQLite、真实 auth 正常登录和原禁用命令，验证四账号状态/版本与旧令牌拒绝。生产 v2 只读门禁通过：四账号仍启用，版本 3/6/3/5；两项目 hash 与首次失败后的只读诊断一致，marker_match 均 true。v1 未业务写入得到前后只读证据支持。

## 执行保护与证据

凭据只读取服务器固定 root 私有目录内的 0600 文件，账号 ID/名称/marker/角色同时核对。JWT 仅内存使用，禁用前各正常登录一次并 me 200；现有禁用命令在 root 容器执行，临时凭据目录 0700、文件 0600，普通退出删除。结果路径用 O_EXCL 防重复执行，变更前持久记录意图；失败不自动重放。硬杀或宿主失联可能留下 root-only 临时目录，需按失败阶段检查，不得直接再次禁用。

源码与测试逐字节归档在证据目录，指纹见 `生产QA账号收尾脚本与测试指纹.json`。不含生产密码、JWT 或上游 Key；测试中出现的 LOCAL_ONLY 字符串仅本地伪凭据。历史 v1 失败与诊断文件不覆盖。v2 业务执行状态以独立 v2 执行结果为准。

## 生产 v2 执行结果

另一位子代理完成限定 diff、22 项测试 XML 和真实只读门禁独审后，按主代理明确授权仅执行一次 v2，远端退出码 0，状态 passed。

| 专用账号 ID | 禁用前 token_version | 禁用后 token_version | 最终 status | 旧令牌 HTTP / 业务码 |
| --- | --- | --- | --- | --- |
| 103 | 4 | 5 | 0 | 403 / 40301 |
| 104 | 7 | 8 | 0 | 403 / 40301 |
| 105 | 4 | 5 | 0 | 403 / 40301 |
| 106 | 6 | 7 | 0 | 403 / 40301 |

表内“禁用前”是在各账号一次正常登录之后采集。正常登录本身按现有单设备会话合同先递增一次，因此相较只读预检的 3/6/3/5，最终总共各增加 2，其中禁用动作精确增加 1。四个账号在禁用前正常登录与 me 均 HTTP 200，禁用后四个旧令牌均被拒绝，未重新登录禁用账号。

QA ai_call_log 为 0、review_task 为 0；项目 162/163 都保留 active、归属 103/105，八字段 row_sha256 与执行前快照分别一致：

- 162：`ebd493e6ef8f6e807d9cb6d364ae0a776f58ce3a09b3216f581c481cfea990ff`
- 163：`3c5d0485de6ef2bb4d47a5f16a3051e1c798314c0a9dbac1a2eee06aaf94f8f5`

最终结果为 `证据/生产QA账号收尾执行v2.json`；执行前证据为 `证据/生产QA收尾v2只读门禁验证.json`。已交用量子代理独立读取与重算，独审不再次向生产发 HTTP 或写库。
