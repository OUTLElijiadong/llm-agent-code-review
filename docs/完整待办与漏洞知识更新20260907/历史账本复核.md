# 历史任务与执行账本：只读复核

观察时间UTC 2026-09-07；使用MySQL REPEATABLE READ一致性只读事务，事务末ROLLBACK。共核验12项逻辑关联，所有owner和成员team一致性、工具request绑定、参数及终态结果JSON完整性均有独立统计。

## 指定历史任务

|任务/团队|当前状态|事件证据|结论|
|---|---|---|---|
|task11 / team5|queued / failed|58领取；62运行后重排队；63同团队task12失败；65父团队失败|父团队失败后子任务未收尾，历史异常仍保留；1次尝试、无活跃租约，不能称正在运行|
|task35 / team12|waiting_dependency / failed|164 task33三次尝试后dead_letter；165 task34 blocked；166父团队失败；task35无领取事件|依赖链未收尾，0次尝试、从未启动；不能改成已执行成功|

实际终态集合以服务 _TERMINAL_TASK 为准：completed、failed、blocked、cancelled、dead_letter、expired。全量父子状态查询发现的非终态异常恰为上述2条。初版诊断条件遗漏blocked/dead_letter，返回过多候选；已复現、修正并另存复核证据，未将那些终态记录误报为故障，初始证据保留。

## 孤儿工具账本

agent_tool_execution共2576条，其中7条找不到对应agent_response_run，另11项关联缺失均为0。7条本身均success，参数/结果JSON有效，request_id与run_id+call_id的hash相符；这仅证明账本内部一致，不能证明父运行删除原因或重新构造父任务。

|工具账本ID|工具|账本状态|关联审批数|结论|
|---|---|---|---|---|
|125|list_projects|success|0|保留账本，父运行来源缺失，不补造关联|
|126|admin_list_users|success|0|保留账本，父运行来源缺失，不补造关联|
|127|admin_list_roles|success|0|保留账本，父运行来源缺失，不补造关联|
|128|admin_list_users|success|0|保留账本，父运行来源缺失，不补造关联|
|129|admin_describe_capabilities|success|0|保留账本，父运行来源缺失，不补造关联|
|2580|admin_execute_capability|success|1|保留账本，父运行来源缺失，不补造关联|
|2581|admin_execute_capability|success|1|保留账本，父运行来源缺失，不补造关联|

DatabaseCheckpointStore.delete 当前代码允许删除运行检查点而保留执行账本，这是可能形成此类记录的代码路径，**不是已经证明这些历史记录由该路径删除**。旧日志没有精确外键不按临近时间归入根任务。

两条有副作用的孤儿工具2580/2581均为beta_codes.generate，分别关联已批准的critical审批231/232。工具结果中的邀请码ID与现存邀请码表ID23、24、25和26、27逐个相符（数量3+2），创建者一致。原数据库状态仍active，但expiry均为2026-09-01，按应用effective_status当前已过期，不能称仍可使用。未取回明文邀请码、未重发、未撤销或删除记录。详见孤儿能力审批补证.json、孤儿能力实际结果关联.json。

## 结果未决工具

|工具账本ID|工具|父运行ID/状态|当前结论|
|---|---|---|---|
|472|audit_security_for_project|124 / failed|executing，仅证明曾写执行占位，结果未确认；禁止自动重试副作用|
|634|run_full_project_validation|179 / failed|executing，仅证明曾写执行占位，结果未确认；禁止自动重试副作用|
|2286|run_full_project_validation|579 / failed|executing，仅证明曾写执行占位，结果未确认；禁止自动重试副作用|

已对上述3条保留创建/更新时间和结果hash，未把父运行failed反写成工具failed或success。运维账本属于另一执行域，聚合状态仅作为快照，不把缺少Responses父记录直接视为运维孤儿。

完整原始证据：[修正后的只读快照](证据/历史任务与孤儿账本复核.json)、[采集器](证据/collect_historical_ledger.py)。发布后将按相同ID和字段指纹再次对账，独立子代理复算后签收。本待办是“复核并留证”，不是“把历史异常修数消除”。
