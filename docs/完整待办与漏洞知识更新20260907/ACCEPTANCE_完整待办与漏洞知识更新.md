# 完整待办与漏洞知识更新：验收记录

阶段：原待办3.8.6本地全量通过；用户追加五项已完成，最终整合与独立复核通过，统一生产发布尚未执行。只有已执行且有原始证据的项目才能写通过。

|项目|当前状态|
|---|---|
|需求及生产基线|已核实3.8.5/f1ab668，bundle发布获确认|
|权限与按钮矩阵|本地权限专项1067项通过；生产夹具92个HTTP通过；新版538格负向矩阵及8手工鉴权待发布后执行；610源码交互不代表逐按钮通过|
|用量外键与恢复继承|7表42外键、独立事务、每请求记账与恢复继承完成；最终真实MySQL通过|
|历史任务/孤儿账本|只读核验及独立复核完成；部署后须对照原快照复核状态|
|项目161|全部8字段和schema两时点一致，已独立复核|
|OWASP/CWE/CVE|官方数据升级及独立重算通过；生产版本待发布|
|动效|选型答复待回|
|独立复核/生产发布|本地全量3897后端/962前端通过；独立数据核验通过；新增五项最终整合中，尚未制作本轮bundle|

整合首轮：前端936项通过，ESLint与生产构建通过。后端首轮暴露13项失败，已按实际契约分组复现修复，不以更改生产安全门禁解决测试失败。

调度器4项旧权限测试没有启动应用lifespan，却默认要求已启动调度器；独立重跑相同失败。测试显式关闭后台调度设置后验证权限业务，并新增“启用但运行时缺失必须503且回滚配置”实测，28项全部通过。生产调度代码未修改。原始证据为`证据/调度器独立复现.xml`和`证据/调度器兼容回归.xml`。

生产验收前备份`code_review_20260907T064755Z_f1ab6684e86c.sql.gz`，404342825字节；独立cr_testdb恢复89张表、迁移047通过。账号创建首次因非root容器不能写/app/storage而在任何数据库写入之前失败；改用容器私有临时路径并复制0600凭据到服务器/root专用目录，凭据不进入仓库或报告。

最终整合：后端3897项、前端962项，0失败/错误/跳过，ESLint和生产构建通过。914个源码与数据文件在测试结束后指纹未改变。原始XML、覆盖率和摘要见证据目录。生产浏览器发现的仪表盘无权限入口、项目reviewer上传/成员管理/编辑/恢复以及扫描入口已修；必须在新版真实页面复验后才能记生产通过。

追加修复最终整合：后端3922、前端994全过，前端Lint与构建通过。两个大数据页面在并发构建时触发5秒超时，32项独立复跑以及低并发994全量均过，未修改测试限时或应用行为。发布只读取证17项为后续独立工具附加测试。最终HTTP计划跨主机路径兼容修复及独立核验另记工具证据，不冒充已执行生产矩阵。
