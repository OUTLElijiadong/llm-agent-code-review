# 完整待办与漏洞知识更新：验收记录

阶段：3.8.6 已通过完整 Git bundle 发布生产，运行提交为 `0a14b5359a446ab7b447e10c061b1ebbe6d15e29`；追加五项代码修复、最终整合及独立数据复核通过。全站逐控件验收和动效确认仍有未完成项，详见最终台账及 TODO；不以本地测试替代真实点击。

|项目|当前状态|
|---|---|
|需求及生产基线|已核实3.8.5/f1ab668，bundle发布获确认|
|权限与按钮矩阵|本地权限专项1067项通过；发布前夹具92个HTTP通过；新版550个真实请求含12正常认证/权限查询和538负向格（299个401、239个403），另8手工鉴权401；重启23请求通过。610源码交互不代表逐按钮通过|
|用量外键与恢复继承|7表42外键、独立事务、每请求记账与恢复继承完成；最终真实MySQL通过|
|历史任务/孤儿账本|发布后与原快照逐目标复核及独审通过，指定状态/时间/内容hash未改；全局ops_execution.success另有+240，不称全系统账本数量不变|
|项目161|全部8字段和schema前后及发布后一致，已独立复核；与审查任务161分别取证|
|OWASP/CWE/CVE|已发布OWASP2025、CWE4.20/944条有效Weakness及41条精选PHP生态CVE；10类别和20/27规则响应逐字段一致，非全量CVE/NVD数据库|
|动效|选型答复待回|
|独立复核/生产发布|最终应用全量3922后端/994前端通过，Lint/typecheck/构建通过；另17/16验收工具专项通过。完整bundle266403299字节，生产前后端及048迁移核验通过，未push远端|

整合首轮：前端936项通过，ESLint与生产构建通过。后端首轮暴露13项失败，已按实际契约分组复现修复，不以更改生产安全门禁解决测试失败。

调度器4项旧权限测试没有启动应用lifespan，却默认要求已启动调度器；独立重跑相同失败。测试显式关闭后台调度设置后验证权限业务，并新增“启用但运行时缺失必须503且回滚配置”实测，28项全部通过。生产调度代码未修改。原始证据为`证据/调度器独立复现.xml`和`证据/调度器兼容回归.xml`。

生产验收前备份`code_review_20260907T064755Z_f1ab6684e86c.sql.gz`，404342825字节；独立cr_testdb恢复89张表、迁移047通过。账号创建首次因非root容器不能写/app/storage而在任何数据库写入之前失败；改用容器私有临时路径并复制0600凭据到服务器/root专用目录，凭据不进入仓库或报告。

追加需求前整合基线：后端3897项、前端962项，0失败/错误/跳过，ESLint和生产构建通过。914个源码与数据文件在该轮测试结束后指纹未改变。此计数保留为历史基线，最终以3922/994为准。新版普通成员真实页面已验证项目reviewer上传/成员管理/编辑/恢复入口隐藏，查看代码/历史版本可正常使用；具体逐控件状态见新版UI台账。

追加修复最终整合：后端3922、前端994全过，前端Lint与构建通过。两个大数据页面在并发构建时触发5秒超时，32项独立复跑以及低并发994全量均过，未修改测试限时或应用行为。发布只读取证17项、HTTP工具16项为后续工具专项，未冒称应用全量重复执行。最终生产HTTP计划为发布候选版本，已实际执行并保存脱敏响应证据。

发布完成时间为2026-09-07 09:01:57 UTC（北京时间17:01:57）。正式脚本完成预检、完整备份、独立容器恢复89张表、迁移047→048、前后端重建及HTTPS/health/ready验收。SSH会话中断后读取服务器日志及运行态确认发布成功，没有重复发布。当前269个本版前端资源逐SHA一致，服务目录共3709个文件（包含历史哈希资源）。最终证据见 `证据/新版生产数据独立复核.md`。

浏览器最终3050格：12所列静态场景完成、11部分场景、12状态观察、2405未测、610管理员阻塞，原台账不覆盖。成员104与无权限106完成浏览器正常退出后，四个QA账号统一禁用，旧JWT均403/40301，禁用版本各+1，模型/审查记录均0，项目162/163完整8字段保持；见`证据/生产QA账号收尾执行v2.json`。收尾脚本先因CLI latin1解码中文名在写入前被门禁拦截，复现并修正utf8mb4后22项及独审通过，v2明确授权一次执行成功，v1失败记录保留。
