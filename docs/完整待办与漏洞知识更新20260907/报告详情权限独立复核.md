# 报告详情权限独立复核

独立完整阅读报告详情补丁、真实接口契约和相关测试，没有发现本次补丁的阻断问题。原 19 项 shallowMount 回归与新增 5 项真实 Element Plus、Pinia 挂载测试共 24 项通过；XML 为 `证据/报告详情权限独立复跑.xml`，冻结指纹为 `证据/报告详情权限独立复核.json`。

格式生成和下载同时满足页面 report:view、已成功加载报告、对应格式权限；领域报告还与仅 JSON 的真实能力求交。入口、下拉菜单、重试和直接 handler 都校验。查询参数 generate 等待加载完成，再校验格式和权限；加载失败收口。额外读取问题列表先校验 issue:view。HTML 预览接口实际只要求 report:view，补丁保留可读账号预览和打印已读报告的能力。

新增的 5 项测试实际点击页面按钮、Element Plus 下拉命令并观察请求；覆盖仅查看账号预览、仅 JSON 导出、真实 Pinia 撤权后隐藏入口且重试拒绝，以及 sandbox/pentest 加载前默认关闭、加载后仅 JSON 的能力交集。沙箱页面明确安全风险未评定，不由执行评分推断低风险。

网络、文件 URL 和下载锚点为测试替身；5 项是真实组件挂载交互，原 19 项使用 shallowMount，均不等于生产点击或实际文件制品验证。初轮测试框架因 Vue Router mock 不完整而初始化失败的 XML 已保留，不能当作产品缺陷红测。该测试文件已纳入主任务最终 994 项前端全量。
