# 连接测试与向量调用独立复核

复核者：用量归因子代理；被复核实现：安全知识库子代理新增的 ping 与远端 embedding 记账。复核仅本地读源码并独立运行测试，没有向真实供应商发送请求。

独立重跑三个文件，34 项通过，0 失败、0 错误、0 跳过：`test_embedding_usage_recording.py`、`test_api_config_service.py`、`test_llm_endpoint_service.py`。原始 XML/日志和文件摘要为本目录 `ping-embedding-independent*`。

已核验：

- ping 先验证并固定公网目标，每次实际 POST 在 finally 独立提交用量；GET 模型列表和校验拒绝均不造账。日志失败位于重试捕获之外，真实登录路由传 db/user，不保存 API 配置。
- ping 的 HTTP 200/choices 列表判定与原连接接口一致，没有在记账补丁中另改连接协议。
- embedding 每 32 个输入实际请求一次；33 条输入产生 2 HTTP 和 2 账本。先读取失败响应中已报告 usage，再继续原有哈希降级。
- 空输入、未启用远端、本地哈希与 pin 拒绝不造账；未知用户保存 NULL。个人知识检索/入库、Agent 知识检索、能力/发布 Agent 搜索、人工知识入库/抓取、超管重嵌均传受信用户；全局计划抓取没有可证明发起用户，维持 NULL。
- 新 embedding 日志不存原文、向量、URL、密钥或上游错误原文；仅持久化模型名、异常类别、真实用量与来源。
- 额外复核 Agent 知识新增、个人与 Agent 检索、能力排序共 4 HTTP/4 日志，root/run/tool/user 精确；上游缺 usage 保持三个 NULL，模型名中的密钥回显被脱敏。
- UsageAccountingError 在 embedding 与重嵌两层均独立向外抛出，测试证明不进入哈希兜底后的再次远端收费；真正上游失败的重嵌重试则每 HTTP 各记一笔。

结论：本轮两个新增入口的用量归因补丁通过独立复核；未发现新的归因或重复计费问题。历史状态、组织授权及真实生产验收仍按主任务证据，不以这里的 MockTransport 测试替代。
