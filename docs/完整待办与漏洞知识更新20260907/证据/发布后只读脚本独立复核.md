# 发布后只读脚本独立复核

已按当前源码核对真实接口与字段契约，无阻断问题；未执行脚本、未读取凭据。预计 23 次请求：4 次专用账号登录与 19 次 GET。固定 103–106 的账号、用户名、标记及专用角色均在任何 HTTP 前核对。真实接口为 `/api/code-files/1892`，文件字段为 `file_name/content/project_id`；安全清单的 `catalog_metadata` 及十分类六个映射字段与本地快照一致。

登录会更新该专用账号的会话版本、最后登录时间/IP，并写登录审计，可能使其旧 JWT 失效。其余 GET 只读业务数据，不调用模型。HTTPS 校验、禁重定向、0600 非符号链接凭据文件和独占输出沿用已核验 Runner；日志不记录密码、JWT 或文件正文。公开安全清单写入证据不涉及项目正文。

`--expected-release` 证明本地 HEAD，不能独立证明服务器部署 SHA；需要结合发布记录验证。文件指纹见同名 JSON。
