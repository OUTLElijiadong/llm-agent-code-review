# 新版生产数据独立复核

对现有证据逐项重新计算，并通过一次只读 SSH 补采公开前端资源哈希和本轮备份白名单元数据；没有生产业务写入、HTTPS 请求或测试重跑。已通过：

- 3.8.6 / `0a14b5359a446ab7b447e10c061b1ebbe6d15e29` 的两个健康容器、镜像及发布版本一致；048 的 7 表 × 6 字段 = 42 外键逐项与迁移源码一致，5 个运行源文件指纹与当前提交一致。
- 550 次 HTTPS 请求精确对应 12 次登录/自身角色权限读取，以及最终计划的 299 次匿名 401、239 次无权限 403。另 8 次手动匿名补充均为 401；不是把未执行计划当成已请求。
- 持久性与安全清单 23 次请求、32 个断言通过；真实 Schema 序列化后的 10 分类、20 秘密规则、27 静态规则全部逐字段一致。
- 同 SQL 的历史复核基线中，task11/team5、task35/team12、2576 条工具账本、7 条孤儿和 3 条 executing 记录的目标状态、关联、时间与内容指纹不变。全局 `ops_execution.success` 从 42660 增至 42900，增加 240；原因未独立核实，不能宣称全系统账本数量完全不变。最早基线使用更宽任务查询，不能把其额外返回行误判为后来删除。
- 项目 161 的完整 8 字段及整行指纹与原备份、发布前基线一致；此次没有再次读取备份或执行回填。审查任务 161 是另一实体：原总数 16、success、原更新时间和报告 SHA 保留，新解析仍为 4 条、4 条未分级，helper/采集器/SQL 指纹均核验。
- 截至账号证据采集时，103–106 有 2 个专用项目和文件 1892/1893，模型账本与审查任务均为 0。该初始快照中账号仍启用；后续收尾证据已独立核实四账号全部 status=0、禁用版本各增加1、四个旧令牌请求均为403/40301，模型/审查任务仍0，专用项目162/163八字段整行hash保持。v1初始门禁失败和v2成功分别保留，未把失败当作完成。

前端逐文件映射已于 09:26:53–09:27:01 UTC 补采：镜像预期 269 个路径和实际服务中的对应 269 个路径 SHA 全部相同，独立重算 manifest 为 `a9ac32a5400e48aa7b23f2674671fe6a7e4acf0dc8dee9cc86c061d4ac508ea5`，与原运行态证据一致。容器、镜像、启动时间和发布账本在两轮证据中相同。服务目录总计 3709 文件，包含其他旧资源；本地 dist 未用作生产基线。

本轮发布账本关联的备份为 `code_review_20260907T085623Z_0a14b5359a44.sql.gz`，大小 **404,458,323 字节**，SHA256 为 `1fadbcde8e2a3cb2e77d220f5cb8ee053abcd8392e718d03af8ba82b4c1772f6`。实际压缩文件、`.meta` 与 `.sha256` 三方一致；白名单元数据记录创建于 08:56:23 UTC、原因 pre_deploy、提交 0a14、迁移前 `047_review_input_snapshot`、89 张表。此次没有读取 SQL 正文或重新执行备份、恢复、迁移。

正常登录会更新专用账号会话与审计；这些副作用已经授权，不能表述为 HTTPS 验收全程零写。HTTP 矩阵和静态数据证据也不能替代全部按钮实际点击。完整证据指纹和各项结果见同名 JSON。

最终UI台账另已独立核实：610源码控件 × 5角色共3050格，12格完成所列静态场景、11格部分实例或事件、12格状态观察、2405格未测、610格管理员阻塞。JSON与CSV各导出单元格一致；91条观察不等于91个唯一按钮。详见 `生产UI最终台账独立复核.json`。
