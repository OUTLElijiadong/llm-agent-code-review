# 发布后数据复核流程独立审查

审查任务 161 采集器新增可选 `--output`，默认仍为原来的 `审查任务161真实报告解析核验.json`。发布后应传独立文件名，例如：

```sh
python3 docs/完整待办与漏洞知识更新20260907/证据/collect_review161_summary_readonly.py --output docs/完整待办与漏洞知识更新20260907/证据/审查任务161发布后只读核验.json
```

只读 SQL、事务、来源校验和 helper 指纹绑定不变；没有运行生产采集。旧采集器源码已按原 SHA 保存为 `collect_review161_summary_readonly_before_output.py`，确保发布前证据仍可复核。脚本只新增参数和输出路径，变更指纹见 `审查任务161采集器输出参数变更.json`。

发布后对照应独立确认：报告原文 SHA、数据库原总数 16、success 状态、原 update_time 未变；新解析结果仍为 4 个报告条目、4 个未分级。发布前后的查询 SHA 和 helper SHA 应相同；采集器增加参数后其 SHA 合理变化，不能误报为数据变化。

该采集器把本地 helper 发往生产只读进程执行，证明实际数据库报告的解析结果和历史未改，不能单独证明容器已加载新版代码。发布流程应另核对完整 Git bundle/发布 SHA、容器内 helper SHA，并以真实生产 API 或浏览器验证 4 条显示。专用账号 103–106 的持久性与 403/404、安全目录复核使用已审查的 HTTPS 脚本；这些账号没有自动获得历史任务 161 的权限。

2026-09-07 08:22 UTC 重新逐文件计算：MySQL 迁移证据的 15 个执行前 SHA、15 个执行后 SHA、9 个已加载运行模块 SHA，以及迁移采集器 SHA，全部与当前源码一致。没有重复运行 MySQL。原证据的 42 FK、历史 NULL/孤儿状态保留、删除 SET NULL、回退再升级、独立用量事务及临时库清理仍绑定当前关键实现。该证据没有生产连接或真实模型请求；7 token 属合成事务验证。逐项结果见 `发布前MySQL证据源码复核.json`。
