# 项目161：完整前后字段证据

发布后补记：完整8字段和schema已在3.8.6发布后再次比较，整行SHA继续相同；见`证据/项目161发布后完整字段复核.json`与`证据/新版生产数据独立复核.md`。本文与审查任务161是不同实体。

本次已补齐隔离前备份与生产当前记录的全部八个字段比较。比较点为UTC 2026-09-05 20:16:10命名备份和UTC 2026-09-07 06:29:14只读事务快照；不推断两时点之间不存在短暂变化。

旧阻塞已复现：MySQL JSON_OBJECT将DATETIME(0)序列化成带六位零小数的字符串，原解析器仅接受秒级字符串。先核实information_schema两日期列DATETIME_PRECISION=0，再用DATE_FORMAT输出完整秒值。该操作没有截断有效精度、改变时区或修改数据库值。

备份为原始402726075字节文件，SHA256仍为7bb99942cc0e571008743fc66b0d1cdd88fcf13640579b32fb493c0679d7dc6f；单次打开、流式解析project表并对完整压缩文件重新哈希。备份和当前project均100行，目标均唯一；8列类型、顺序、nullable、字符集、主键一致。

|字段|前后比较|
|---|---|
|`id`|一致|
|`user_id`|一致|
|`project_name`|一致|
|`description`|一致|
|`language`|一致|
|`status`|一致|
|`create_time`|一致|
|`update_time`|一致|

整行规范化SHA256：`9e49ca15c51357ed0fc9c71703c8371ab9b67593e493b8385b4e753826131194`。逐字段双份hash和采集器hash见[原始证据](证据/项目161完整字段比较.json)。仅回传hash，不回传项目名称、说明或源码；未恢复数据库、未执行备份SQL、未修改项目161或历史状态。

待独立子代理复核采集器、日期边界和原始JSON后签收。
