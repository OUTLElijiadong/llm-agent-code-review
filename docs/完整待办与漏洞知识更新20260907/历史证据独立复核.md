# 历史证据独立复核

复核子代理独立读取脱敏 JSON、两版账本 SQL 采集器、项目 161 比较器 v1/v2，运行 `证据/independent_recheck.py`。7 组检查通过，结果及输入摘要保存在 `证据/独立复算结果.json`。本次独立复核没有连接生产数据库、打开生产备份或修改历史状态。

项目 161 的证据覆盖 id、user_id、project_name、description、language、status、create_time、update_time 共 8 个唯一字段；逐字段和整行的前后摘要均相等。归档备份大小 402726075 字节、预期 SHA、单次读取稳定性与比较器 SHA 均匹配。原值留在生产内存，本复核核对摘要、源程序和控制流，未冒称重新计算原始业务字段哈希。

v1 曾因当前行 JSON 带小数秒形式触发严格 DATETIME 门控而阻塞，未证明字段不同。v2 仅对 datetime 字段使用 DATE_FORMAT，并先核验真实完整列集、类型、空值约束、字符集和 DATETIME 精度等于 0。独立合成反例证明精度 6 会阻断，NULL 与文本 NULL 不等，字段实际变化会被检出；没有用截断时间掩盖真实高精度差异。取消“当前总项目数必须仍为 100”的门禁与仅比较 161 的目标相符，不影响唯一目标行、schema 和前后字段验证。

两次历史只读快照的状态计数、关联缺失计数、完整性聚合、6 个指定团队子任务、23 条事件、7 个工具孤儿及 3 个未决工具内容完全一致。采集器 SHA 与实际生成 SQL 的 SHA 重新计算匹配；SQL 只有 SET SESSION、START TRANSACTION READ ONLY、SELECT、ROLLBACK，前后事务标记均为 read_only=1。

| 独立重算项 | 结果 |
| --- | --- |
| 根 Responses 运行 | 778 |
| 工具执行 | 2576 = 3 executing + 344 failed + 2229 success |
| 团队/子任务/成员 | 63 / 197 / 189 |
| Mesh 消息/运维执行 | 850 / 85840；不把累计失败数当作本轮故障数 |
| 非终态子任务挂在终态团队 | task11/team5：queued/failed；task35/team12：waiting_dependency/failed |
| 缺失根运行的工具行 | 125、126、127、128、129、2580、2581，共 7；均保留原 success 与回执摘要 |
| 未决 executing 工具 | 472、634、2286；其根运行均 failed，结果摘要为空 |

首次宽筛选返回 29 行，包含合法终态 blocked/dead_letter。独立按当前终态集合重算后，只有 task11、task35 两个异常，与修正 SQL 的结果相等；从 29 变为 2 是查询口径纠正，不是数据库修复。两个指定任务的状态、更新时间、租约/尝试、结果与错误摘要均跨两次快照保持。

两个 admin_execute_capability 孤儿额外有 critical/approved 审批，动作来源为 beta_codes.generate。追加重新读取实际结果关联证据：2580 返回的 3 个对象 23/24/25、2581 返回的 2 个对象 26/27 均存在，返回 ID 与数据库 ID 相等、创建者匹配，去重后共 5 个，数量与各自回执一致。这些事实能够排除“仅因父运行缺失就认定未审批或输出全不存在”，不能据此重建已缺失的父运行、补造用量或认定根运行成功；也不把 current_status=active 等同于过期日期仍有效。

本报告支持“完成历史复核且采集器未写业务状态”；它不声称全生产历史从未被任何其他操作修改，也不把已有孤儿/未决状态改为成功。

另按主任务要求独立读取本地 MySQL 迁移采集器、结果 JSON、048 migration、Alembic env 和数据库配置。纯 AST/JSON 脚本 `证据/independent_mysql_recheck.py` 重算 42 条外键为 7 表 × 6 字段；外键名、目标表及 SET NULL 与迁移完全相等，见 `证据/独立MySQL迁移复核.json`。采集器限定本机 127.0.0.1:3307 的随机专用库，并将同一 DB_HOST/PORT/NAME 明确传给 Alembic；未复制生产业务记录。048 升级前后原字段比较、归因列全 NULL、executing 孤儿保持以及降级重升均有断言。

该迁移结果中的“历史保持”针对本地合成的 7 张来源表和工具表，不是生产全库逐行比较。42 条约束结构全部核验，真实无效外键拒绝、父删除 SET NULL 且日志保留只在 ai_call_log.root_agent_run_id 上作代表性执行验证。本次独立复核没有重新连接任何数据库。

集中模型账本补充证据更新后，再次完整读取新增脚本段、JSON 与 ai_usage_context 实现并刷新指纹：业务 Session 先 flush 合成用户 987655，直接调用 record_usage_attempt 记录 0/7/7 tokens，再回滚业务；另一个 Session 查到用量及准确根关联，查不到该用户，证明独立审计事务没有顺带提交业务数据。采集器没有调用模型 HTTP；7 tokens 是明确的合成输入，只用于真实 MySQL 事务验证，不能称为真实模型消耗。


最终冻结版本再验证：MySQL 临时库 prism_acceptance_048_7bea65b5e022，于 2026-09-07 07:41:52–07:42:14 UTC 完成并删除。新增 15 个关键源码/建库文件 before/after 指纹、9 个实际加载用量及归因模型模块路径与指纹，全部等于当前冻结源码，测试期间无漂移。42 外键、原字段保持、孤儿状态保持、回退重升与独立审计事务回滚断言继续通过。独立 AST/JSON 核对和另一个未编写采集器的归因代理复核均通过；没有为了刷新指纹把旧运行伪称当前实测。
