# 生产权限执行器独立复核

2026-09-07，安全知识子代理独立完整阅读 `backend/scripts/verify_permission_acceptance_https.py`（561 行）、对应测试（137 行），复核 get_current_user、require_admin/super_admin、require_permission 和报告导出权限依赖。没有修改该脚本。

独立运行 `tests/test_production_permission_acceptance_runner.py`：6 passed（7.40 秒）。真实隔离 SQLite 登录、534 条前置拒绝请求和两个项目/文件/普通成员闭环通过，ReviewTask 和 AiCallLog 数量均为 0。

结论：没有阻止既定执行的问题。凭据文件要求本机所有的 0600 普通文件并拒绝符号链接；HTTPS 验证默认 CA、禁止重定向和环境代理；先核对专用账号 ID/用户名/role/status、专用角色及完整权限集合；日志仅请求元信息、响应 hash 和受控资源 ID，不写 payload、密码、JWT 或响应正文；发送前持久化请求意图，网络失败不自动重放 POST。

负向矩阵只发送经源码依赖白名单证明会在端点之前抛出 401/403 的请求；未知依赖/公开或无前置门禁路由保留 blocked。fixture 变更仅涉及两个标记项目、标记源码版本、重命名和 reviewer 成员关系，无模型或审查任务提交。

执行证据边界：plan 验证的是 source-root 指定源码的哈希，远端 HTTPS 服务是否正在运行该源码需发布验收确认 Git/镜像/文件版本；不能仅凭本地 plan 宣称远端匹配。日志中的 blocked 路由没有完成真实 403 验收，不把它们计为 passed。

## 匿名手工鉴权补充执行器独立复核

安全子代理完整阅读 `backend/scripts/verify_manual_auth_https.py`、`backend/tests/test_manual_auth_anonymous_contract.py`，并逐查实际入口：SSE 缺 token 在事件总线实例之前拒绝；沙箱预览空 token 在 JWT 解码阶段抛 401，先于任何 `db.get` 与 worker 请求；Responses 四类操作均先执行 `_bearer_identity`，缺凭据先于载荷处理、存储或上游。8 条固定请求涵盖 SSE 1 条、沙箱 GET/HEAD/POST 3 条、Responses POST/GET/DELETE/input_items 4 条；路径使用负 ID，无账号凭据与模型输入。

复用已审阅 HTTPS Runner 的 TLS 校验、禁重定向与禁环境代理，空 token map，不登录、不建立 CookieJar；执行开关默认为关闭。固定案例与来源集合纳入计划哈希，结果文件权限 0600，不保存响应原文。独立复跑 `pytest tests/test_manual_auth_anonymous_contract.py -q --no-cov`：9 项通过；forbidden sentinel 验证 8 次匿名 401 均未调用 DB/事件总线/worker/上游/响应存储；额外一项阻断路径漂移。未发现阻止现有固定 `--execute` 计划的问题。本段证明本地实际入口与执行计划；生产 HTTPS 状态由主代理的执行证据单独记录。
