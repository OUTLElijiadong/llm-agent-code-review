# 项目权限与项目161取证独立复核

安全数据子代理于 2026-09-07 独立复核，结论：未发现阻止当前实现发布的问题。只读代码、已有证据并运行本地测试，没有改生产权限或项目字段。

## 项目与文件权限

完整审阅成员路由和既有服务：GET 增加 `project:view`，POST/PUT/DELETE 增加 `project:member:manage`；路由依赖与原 `require_project_access` 求交，没有替换 owner/admin/reviewer 资源判定。跨项目无访问仍由服务拒绝，reviewer 不会因单有动作权限获得写权限。

ProjectDetail 的上传、审计包、修复副本删除、成员增删与角色更新使用服务端 `can_update/can_delete` 和动作权限，确认后再次检查撤权。CodeEditor、VersionHistory 默认只读，按实际文件 project_id 读取项目授权后才启用编辑/恢复；按钮、Ctrl+S 与直接 handler 均校验，授权读取失败继续只读。CodeFileList 读取与下载分别检查动作权限。SecurityScanModal 的手工和自动扫描，包括旧范围请求结束后的续发，都经过 `canScan`。

合法 reviewer 仍可凭 `security:scan` 扫描可见项目；没有 `security:view` 不预取落库扫描结果；AI 修复手册保留 `project:view` 下的现有入口。独立真实挂载 ProjectResourcePermission 17 项和 SecurityScanModal 22 项全部通过，共 39 项。源码及本地测试不冒充公网点击验收。

## 项目161字段证据

完整阅读 `证据/collect_project161_post_release.py` 及原 `compare_project161_backup_readonly.py` 的规范化算法，独立比对 `项目161完整字段比较.json`、`项目161发布前再次核对.json`：基线文件 SHA-256 一致，8 个字段哈希与整行哈希全部一致。

Collector 固定读取 `project.id=161`，先要求预期 40 位 release 与 Git HEAD 一致；SQL 使用 REPEATABLE READ + READ ONLY 一致性快照，SELECT schema/count/目标行后 ROLLBACK。没有数据写入、备份恢复或状态改写；目标行数和 `@@transaction_read_only` 均须为 1。结构漂移、字段改变、查询失败或 release 不符均失败退出。

规范化保留字段顺序、名称、SQL 类型与 NULL。bigint 校验有符号 64 位范围后转十进制字符串；varchar 保留 UTF-8 原文并验证 utf8mb4 与长度；DATETIME 先验证精度为 0，再按秒固定格式处理。逐字段哈希输入为 `[名称, SQL类型, 值]` 的紧凑 UTF-8 JSON，整行哈希使用完整 8 字段数组，与基线一致。列名/类型/顺序、主键、nullable、字符集与时间精度变化均阻断，新增列不会被忽略。

输出只有哈希、计数与版本，项目原文仅在进程内存中比较。Collector 参数应使用原始完整比较基线（status 为“通过”）；发布前再次核对 JSON 是执行结果（status 为 passed），不能代替基线。发布后仍需传新 release 再执行，尚未执行的发布后结果不能提前标为通过。

## 仪表盘HTML导出补充独审

独立阅读 Dashboard 下载 diff 与实际模板：仅 `report:export:html` 允许 HTML 下载，摘要/图表要求完整可信数据，loading/error 或权限不符不触发下载。动态数据插入文本节点前统一转义 `&<>`，文件名使用受控时间范围与日期。Blob 链接附着 DOM、设置 download 后点击，finally 移除；失败立即回收 URL，成功延迟回收。提示明确为“已请求下载”，没有声称浏览器已写盘。

独立复跑 Dashboard、登录、注册和 HTTP 四文件共 83 项通过，含 Blob 实际解码、统计范围/数值、恶意标题转义、失败清理和 HTML/PDF 权限区分。原 CUA window.open 被工具安全策略拒绝的动作不作为成功下载证据；本次未尝试绕过工具策略。

功能导航补充：AppHeader 此次将原“全局搜索”改为“功能导航”，输入/标题/按钮 accessible name 一致，空结果明确引导项目名称到项目管理搜索；没有声称实现项目内容检索。现有路由/助手功能目录及权限过滤没有改写，Cmd/Ctrl+K 和 Escape 行为保留。独立 AppHeader 四项通过。
