# v3.9.4 完整复核与角色授权收敛总结

## 最终判定

v3.9.4 部署真实、版本闭环，但不是 v3.9.2 问题的完整修复版。仍可复现五角色、渗透自签、小菱重登移出视口、无主旧会话缓存迁入当前账号以及安全中心/审查规则重复。

## 已发布的 v3.9.5 修复

- 只保留 `user/reviewer/admin` 三个可分配角色，`super_admin` 唯一且不可分配；`auditor` 权限和用户收敛到 `reviewer`。
- 角色迁移保留已停用的历史关联，同时确保每个账号恰好一个与 `user.role` 一致的有效核心角色；新增/移除关联均入迁移账本，支持精确降级，线上和 MySQL 离线语义对齐。
- 申请人提交 `pending_authorization`，另一名评审员/管理员原子批准；批准人不能取消或删除他人委托。
- 小菱历史与浮窗状态按账号隔离，admin/user 会话面显式区分，重登自动恢复等待真实面板尺寸后定位。

## 质量结果

- 后端：4140 passed / 5 skipped / 654 warnings（既有依赖弃用/测试告警，无失败）。
- 前端：106 个测试文件、1186 项全部通过；ESLint、vue-tsc、production build 通过。
- 候选变更 Ruff 定向通过；全仓仍有 154 个旧静态规范问题，不属于本轮新增。
- 生产证据截图已脱敏标注并嵌入知识库。

## 生产发布与反向验收

- 生产版本为 `3.9.5`，不可变提交为 `2b46ff8160571d787e77dec3e101bcd66179cc5f`；前后端运行同一 SHA，公网和回环 `/healthz`、`/readyz` 均返回同版同 SHA。
- Alembic 已到 `053_role_pentest_authorization (head)`。活动角色只有 user/reviewer/admin/super_admin；48 个用户的核心角色关联最小值和最大值均为 1，与 `user.role` 不一致数为 0。
- Reviewer 具备自定义 Agent 的创建、本人资产更新、测试、提交和调用权限；`pentest:authorize` 只授予 reviewer/admin/super_admin。
- 真实公网闭环中，申请人提交成功、自批 403；Reviewer 可见授权队列，在生产页面点击批准后申请人读回 `authorized` 且批准人为另一账号；Reviewer 取消或删除他人委托均为 403。两条有界验收委托随后由申请人取消、删除并确认 404。
- 同账号小菱在真实浏览器退出、重新登录后仍保持打开且完整位于 1440×900 视口；7 条历史标题重登前后数量、标题与顺序一致。
- 最新发布前备份为 `code_review_20260917T141840Z_2b46ff816057.sql.gz`，412,327,302 字节；SHA-256 为 `b4533dfc9d17a732817f2c3471ade17ce935b34baa9597de9afd3d980139c703`，校验和和 gzip 结构通过，发布门禁的隔离恢复验证通过。

## 最终证据边界

本轮范围内的版本、角色收敛、Reviewer Agent 能力、独立渗透授权、小菱重登可见性与同账号历史恢复已经生产闭环。安全中心/规则页合并、全输入注入矩阵、DeepSeek 付费全项目回归以及存量测试/演示账号治理尚未完成，继续列在 TODO；因此结论是“v3.9.4 缺陷已由 v3.9.5 修复并发布”，不是“全部项目问题均已清零”。
