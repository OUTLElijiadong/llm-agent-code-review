# v3.9.4 完整复核与角色授权收敛待办

## 发布门禁

- [x] 将候选修复固化为 v3.9.5 不可变提交 `2b46ff8160571d787e77dec3e101bcd66179cc5f`，排除用户自有 `.claude/launch.json` 与未确认 `backend/uv.lock`。
- [x] 生产 MySQL 备份并在独立库完成恢复校验；最新发布备份、字节数与 SHA-256 已记入验收。
- [x] 执行 Alembic 053，核对 `user.role`、有效 `user_role`、角色状态、`pentest:authorize` 权限和迁移备份表。
- [x] 同一不可变 SHA 构建并切换前后端；VERSION、运行环境、health/ready 和数据库 head 一致。

## 生产功能验收

- [x] 普通用户提交授权申请；本人批准 403；另一评审员在生产页面批准成功；并发重复批准由回归用例锁定为失败。
- [x] 评审员能查看并批准，但取消/删除他人委托为 403；批准人和状态由申请人读回确认。为避免启动付费主动扫描，本轮不点击“启动测试”。
- [x] 角色页只展示三个可分配角色和唯一最高管理员；非核心角色全部停用，旧 ID 更新/分权/数据域绕过由 API 回归锁定为 403。
- [x] 同账号退出重登后小菱浮窗完整位于视口内；7 条历史标题重登前后完全一致；跨账号与 user/admin 作用域隔离由服务端所有权和前端回归用例覆盖。

## 非本次发布阻断，继续置顶

- [ ] 安全中心与审查规则单页合并。
- [ ] CodeQL + OWASP 规则执行基座，CNVD/CNNVD 做情报和中文映射。
- [ ] 全输入点注入/特殊字符矩阵。
- [ ] DeepSeek 有预算的全项目真实回归，以覆盖账本而非只看上下文上限。
- [ ] 测试/演示账号归属确认后进行可追溯软删除或归档。
