# v3.9.4 完整复核与角色授权收敛待办

## 发布门禁

- [ ] 将候选修复固化为 v3.9.5 不可变提交，排除用户自有 `.claude/launch.json` 与未确认 `backend/uv.lock`。
- [ ] 生产 MySQL 备份并在独立库完成恢复校验；记录备份路径、校验值和恢复结果。
- [ ] 执行 Alembic 053，核对 `user.role`、有效 `user_role`、角色状态、`pentest:authorize` 权限和迁移备份表。
- [ ] 同一不可变 SHA 构建并切换前后端；核对 VERSION、镜像标签、health/ready 和数据库 head。

## 生产功能验收

- [ ] 普通用户提交授权申请；本人批准 403；另一评审员批准成功；第二人重复批准失败。
- [ ] 评审员能查看并批准，但取消/删除他人委托为 403；申请人获批后能启动。
- [ ] 角色页只展示三个可分配角色和唯一最高管理员；审计员/自定义角色的旧 ID 调用更新、分权和数据域接口必须 403。
- [ ] 同账号退出重登后小菱浮窗在视口内，历史可精确找回；不同账号和 user/admin 会话面互不可见。

## 非本次发布阻断，继续置顶

- [ ] 安全中心与审查规则单页合并。
- [ ] CodeQL + OWASP 规则执行基座，CNVD/CNNVD 做情报和中文映射。
- [ ] 全输入点注入/特殊字符矩阵。
- [ ] DeepSeek 有预算的全项目真实回归，以覆盖账本而非只看上下文上限。
- [ ] 测试/演示账号归属确认后进行可追溯软删除或归档。
