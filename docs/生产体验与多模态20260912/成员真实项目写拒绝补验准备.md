# 成员真实项目写拒绝补验准备

核验应用提交：`7a2368f20453606ed09691ee0002d07df4629ec3`。本次只增加文档下独立执行器和离线自检，不修改应用、不纳入此次应用发布提交，也尚未执行生产请求。

## 固定范围

脚本：`证据/既有QA成员原描述写拒绝.py`。复用最终容器 `scripts/verify_permission_acceptance_https.py` 的 Runner；正常成功路径严格 10 个 HTTPS 请求：

1. QA107 owner_a 登录、读取自己的角色和权限，共 3 请求。
2. QA108 member_a 同样 3 请求；两账号必须系统 role=user、唯一 QA 角色、权限集合严格等于 20 项且含 project:update。
3. owner GET `/api/projects/164`，必须既有标记项目 `QA权限验收-20260908-a`、active、owner可更新；完整响应含 description、update_time 和嵌套字段仅保留内存，日志只记整体／逐字段 SHA256。
4. owner GET `/api/projects/164/members`，必须恰为 107 owner、108 reviewer。
5. member PUT `/api/projects/164`，唯一请求体 `{"description":原始字符串}`；字符串须真实存在且不超过 500 字，不能自动替换 null、截断或猜写内容。预期严格 403。
6. owner 再 GET 项目全字段比较。即使上一步意外 200、422 或传输错误也只执行这一读取用于采集后态；**不重试写请求**，不修复或覆写变化。

请求白名单同时约束顺序、账号、方法、路径、预期码与载荷；不接受 token override。意外 200 即使全部字段相同也判安全失败；update_time 或其他字段变化输出 changed_fields 与前后摘要，不输出原文，不用 SQL 恢复。登录与项目拒绝审计属于预期系统记录，不宣称数据库零写入。

## 门禁和执行

默认不提供 `--execute` 时不读凭据、不构建 HTTP 客户端。先比对绑定的 Runner、项目 API／schema／service、成员 API／service、来源版本 service 源文件 SHA256，再导入已验证 Runner，随后校验完整最终路由计划及其源码摘要。任一不同先停止，不能自动更新摘要继续。

由主代理将文档执行器复制到最终后端容器的受控临时目录。计划应为最终容器重新生成并核验的 0600 计划；凭据使用已授权既有 QA 清单，不能新建账号或改密码。示意命令中的文件名由主代理替换为实际文件：

```bash
python /tmp/qa-member-original-description.py --source-root /app --plan /tmp/final-plan.json
python /tmp/qa-member-original-description.py --source-root /app --plan /tmp/final-plan.json --credentials /tmp/existing-qa-credentials.json --output /tmp/member-original-description-result.json --execute
```

只允许生产基址 `https://lijiadong.cn`，TLS 校验、禁代理和禁重定向均沿用 Runner。输出路径必须不存在，由 Runner 以独占方式创建 0600 文件。过程每个请求先保存意图和随机请求编号，响应只记状态／字节数／摘要。不要在输出或知识库粘贴凭据、token 或完整项目内容。

## 离线验证

`证据/既有QA成员原描述写拒绝离线自检.py` 用真实 Runner 与内存 HTTP 替身，**8 项通过**：严格 403／10 请求、意外 200 即使无变化也失败、200且时间戳变化保留差异、无效原描述提前停、缺 project:update 提前停、非白名单 DELETE 发网前拒绝、源码摘要改动阻断、默认 dry-run 不读凭据不发请求。所有日志验证 0600 且不含密码／token／响应或描述原文；结果见 `证据/成员原描述写拒绝-离线自检.log`。

这份证据只证明执行器合同与失败保护，真实成员写 403 要以主代理发布后执行日志为准。未覆盖代码保存、版本写入、删除或历史数据修正。

## 临时执行交接

9月13日已删除工具未使用的 sys 导入，离线自检改为直接 patch sys.argv；8 项测试及两文件 Ruff 再次通过。模型代理独立重跑 8 项通过，未发现执行合同阻断缺陷。

本地临时上传包装脚本：`/tmp/prism-0913-member-write-qa.sh`；默认仅输出准备状态、无网络。主代理结束其他 QA 会话并审阅后，显式 `bash /tmp/prism-0913-member-write-qa.sh --execute` 才上传并执行。脚本固定执行器 SHA256 `d60061e3c001de94a2ddb1db3d29deb83257e0983af7aecda99cfadba035c4bd`，本机／服务器临时副本／容器副本分别核对，绑定应用 Git HEAD 和 APP_RELEASE 为上列提交，生成最终计划并 dry-run 后才复制原 root 0600 凭据。退出保全服务器独立结果目录，删除本次凭据与执行器副本；不修改原凭据或应用文件。

包装脚本及提取出的远程段均经 bash -n 与 ShellCheck 通过。本子仅运行默认无网络路径，未运行 --execute；生产结论仍待主代理执行日志。
