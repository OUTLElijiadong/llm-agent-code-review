# 最终生产只读取证结论

本轮最终应用提交 `7a2368f20453606ed09691ee0002d07df4629ec3`，版本 **3.9.1**。实际取证时间为 UTC 2026-09-12 16:18:47 至 16:19:26（北京时间/台北时间2026-09-13 00:18:47至00:19:26）。只通过SSH读取Git、Docker、数据库一致快照和安装元数据，未登录QA、未发送业务HTTP、未修改业务数据。

- 发布首尾核验均通过：HEAD、current.env、两容器镜像ID/启动身份、APP_RELEASE、051迁移与绑定源码一致，两容器健康。镜像本版274个前端文件与服务目录逐文件摘要全相等；服务目录共2893文件，包含保留旧资产，不把它误写为仅274文件。
- 历史账本发布前后逐字段完全一致：task11/team5仍queued/failed，task35/team12仍waiting_dependency/failed；2条历史task、6条team-task、23条事件、7条孤儿工具账本、3条未决执行均未变化，也无新增记录。保留历史真实状态，不将不一致修饰为已正常完成。
- 项目161完整8字段的本轮前后SHA与整行SHA均相等；整行 `9e49ca15c51357ed0fc9c71703c8371ab9b67593e493b8385b4e753826131194`。未重读或恢复旧备份。
- 42个数据库外键的结构与实际非NULL引用分别通过：目标存在、来源/目标owner明确且相同，无异常。review_task的root/agent/tool字段各有1个非NULL引用；ai_call_log的root/agent字段各64个、tool字段14个。这里计的是各字段引用数，不能相加解释成调用次数。其余历史NULL保持未知，没有猜补。本轮没有发布前的实际42引用快照，不能声称该项前后未变，也不能仅据结构与同owner证明所有真实调用链。
- 运行容器Python3.11.15、cryptography50.0.1、Pillow12.3.0、FastAPI0.139.0、Starlette1.3.1。`pip check`退出0，提示No broken requirements found。另有pip缓存目录不可写而禁用缓存的提示；没有依赖冲突。

`最终发布前后比对.json`保存比较结果和7个输入文件SHA。除自动比较器外，另用直接JSON等值与SHA重算再次核对历史五组、项目8字段和输入清单，一致。多模态图片输入/文本输出和实际任务调用的专门证据由另一子代理采集，本组不冒称已覆盖。
