# 最终权限与普通用户工作台独立复核

复核版本：`7a2368f20453606ed09691ee0002d07df4629ec3`。本次复核不新增生产 HTTP 请求，不登录 QA 或浏览器；工作台使用生产只读事务重算。

## 权限证据

四份生产执行日志共 **704 次请求、87 项内容或身份断言**。逐条核对请求顺序、账号、方法、路径、预期与实际状态、响应摘要、请求标识及断言，均通过。

| 执行组 | 请求数 | 200 | 401 | 403 | 404 | 断言数 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 完整 HTTP 权限矩阵及四账号前置 | 566 | 12 | 311 | 243 | 0 | 11 |
| 既有资源读取隔离及前置 | 91 | 54 | 0 | 19 | 18 | 57 |
| 手动鉴权补测、普通用户工坊及前置 | 37 | 6 | 8 | 23 | 0 | 6 |
| 项目成员有效原描述写拒绝及前置 | 10 | 9 | 0 | 1 | 0 | 13 |
| 合计 | 704 | 81 | 319 | 286 | 18 | 87 |

共享原始计划完整包含 325 个 HTTP 方法路径，按执行器序列化规则重算计划摘要一致；54 个源文件指纹直接与最终 Git 提交逐个一致。成员执行器另核七个服务源指纹。成员 108 具有测试角色的 `project:update` 权限，但在项目 164 仅为项目审查成员；提交有效的原描述体得到真实 403，之后 owner 再读的 18 个字段摘要及完整响应摘要与前态一致。

704 是含登录及角色前置的请求数，不能当作 704 个不同按钮。原矩阵 96 个阻断格保留分类，公开接口、本人资源和 WebSocket 不能统一预期 403；没有用 422 或随机对象 404 冒充权限通过。这里的 18 个 404 均来自既有资源的跨账号读取拒绝合同。

日志有意不保存响应正文，因此本地复核验证了执行期记录的 87 项断言及对应冻结实现，没有声称从脱敏哈希重新恢复并解析原始正文。发送端 QA 请求标识与响应标识不同，原因是 Nginx 固定设置 `X-Request-Id $request_id`；双方标识均留存。

逐条结果与输入文件 SHA 见 `证据/最终生产权限694/704逐条独立复核.json`。成员独立计划的原始文件哈希核验进度以该 JSON 的 `member.original_plan_hash_recomputed` 为准。

## 普通用户 5 工作台

UTC 2026-09-12 16:27:26 在生产开启只读一致性快照，首尾只读标志均为 1，最后回滚关闭。仅查询用户 5 可见项目范围，未导出代码、报告或其他业务正文。

- 项目 11，活动代码文件 630，活动源码归档中的文件 7973。
- 成功审查任务 27；代码审查有效评分样本 11，分数和 543，平均 `543 / 11 = 49.3636…`，界面应显示 **49.4**。旧的混合任务均分 64.1 包含非代码审查类型，已不用于此指标。
- 累计问题 648，其中严重 145。分布为严重 145、高 214、中 173、低 87、未分级 29，合计 648。
- 近 30 天问题 24：严重 0、高 1、中 2、低 0、未分级 21，合计 24。窗口起点以取证时刻计算，为 UTC 2026-08-13 16:27:26。

直接 SQL 与实际工作台服务聚合结果一致，问题聚合另独立调用底层条目解析器重算。累计 648 来自已存问题 616 和报告条目 32；窗口内 24 全部来自报告条目。无严重度的 21 条应保留“未分级”，不能显示成四档全零，也不能抹掉。

问题聚合扫描的候选任务数 39 与成功任务数 27 使用不同条件：前者是可见范围非删除任务集合，不应标成 39 个窗口内已完成任务。证据见 `证据/用户5生产工作台只读重算.json`。这部分是数据核验，真实图表布局和交互由主代理另行操作验收。
