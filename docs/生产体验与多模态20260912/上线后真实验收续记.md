# 上线后真实验收续记

应用提交：7a2368f20453606ed09691ee0002d07df4629ec3。生产于 2026-09-13 00:01:50（台北）发布完成，完整 bundle 76,693,927 字节，SHA256 cebe9a4545af2b0ca08696c74875e38d03625e650c6d162d9ff2421b7cb12b4d。未推送 Git 远端。首轮资源同步失败及自动回滚记录保留，第二轮应用/数据库/静态资源均通过门禁。

## 已真实验证

- 管理员内测码页面 23 条：正常点下一页变成第二页 3 条，不再打开悬浮助手；1280×900截图中分页与助手分离。
- 自进化缺评估证据提案：列表待重新评估、审批禁用；详情保留原历史闸门状态并明确其证据不足。未执行评估或审批、未改历史提案状态。
- 管理员模型注册表实际点击从接口同步；正式 deepseek-flash 视觉能力为真，V4 Pro 为假。将图片角色从实验别名改为正式 deepseek-flash，保存分配并刷新确认持久化。逐个子 Agent 分配展开可见 15 个入口，未虚构逐个模型调用已验收。
- 旧全局覆盖凭据不可解，运行回退系统默认的提示属实；独立只读报告已记录，未改密钥或强行启用旧覆盖。
- 管理员正常退出进入登录页 v3.9.1，再登录普通账号；未出现旧版退出缺 token 的提示。
- 普通用户平均代码评分 49.4、11 份有效代码审查；独立重算 543/11，旧混合口径 64.1 不再显示。其他数量见独立工作台复核报告。
- 严重度折叠→刷新→展开后，完整环图显示，0/1/2/0/21 合计24；1280×900目视无挤压。断网模拟下摘要、后台、趋势、严重度等出现分区错误/重试，恢复网络单独重试严重度成功，再全部刷新恢复5/5。网络模拟已解除。断网同时仍出现多个通用 Network Error toast，未将这一细节冒称最佳体验。
- 头像：内置兔兔保存；合成蓝圆上传1308字节；同名文件替换红方746字节后页面与顶部同步更新；伪造PNG失败后常驻提示保留原头像，数据库仍为红方；重新选有效同名图片成功；恢复默认后avatar=null、资产0，与测试前相同。未修改密码。
- 小菱真实图片请求已识别蓝色圆形；同会话下一纯文字回答验收完成。资产、模型及根任务FK由独立代理只读取证。取消合成响应页面明确停止，终态另行SQL核验。
- 生产权限704请求：566矩阵、91既有QA读取隔离、37补充、10成员有效原描述写入拒绝全部通过。成员真实PUT返回403，18字段指纹一致。请求日志与计划在证据/最终生产权限694；目录名保留最初批次名，合计以704为准。

## 新发现与待完成

- 图片回复结束后服务端历史同步丢缩略图变成[图片]，历史消息时间显示UTC并被下一轮同步时间全部覆盖；已复现并交独立子代理补红测修复，尚未把这一点记完成。需要修复后再次完整bundle发布并复验。
- 本轮用浏览器下载事件等待导出统计报告超时；不能把已有点击/toast说成本轮已拿到下载文件。代码有Blob下载、异常反馈与延迟回收，仍需浏览器下载能力或真实文件取证。
- 审查者系统角色的正常登录会话尚缺。项目成员reviewer不等同于系统reviewer，不扩权绕过。
- 其余页面、窄屏、图片回放、偏好持久化、用户注册引导、危险写按钮的未测边界仍按矩阵结算，不把页面打开算全部按钮通过。

本记录不包含登录凭据、模型密钥、私人图片或业务正文；截图已在真实浏览器中目视核验，尚无本地截图文件交付。
