# 最终发布与模型配置独立复核

## 模型配置只读诊断

2026-09-13 00:23:47（UTC+8）使用生产 Backend 内的数据库连接，以明确只读一致快照读取旧配置，在进程内调用不写库的密钥解密函数，最后回滚。没有调用 `get_llm_config`、`get_llm_config_public` 或 `resolve_api_config`：它们可能机会式重加密或停用旧配置并提交，不适合此项只读诊断。没有输出密钥、密文、环境变量，也没有发起模型请求。

确认旧全局覆盖配置为 `gpt-5.5`，已停用，存在密文但当前独立密钥环和历史 JWT 派生项均无法解密。当前独立密钥环有 1 项。故页面“凭据不可用，运行时已回退系统默认”有实据；单凭现存数据不能区分历史加密 secret 丢失/替换与密文损坏，也不能归因为本轮密码库升级。

系统默认端点是官方 DeepSeek，默认模型 `deepseek-v4-flash`、编排默认 `deepseek-v4-pro`，凭据非空。过去 24 小时有 7 条 `deepseek-v4-pro` 成功日志，最新为 2026-09-11 19:24:07 UTC。这只证明历史调用成功，不能证明当前这把密钥以及 Flash/视觉模型可用，更不能把日志精确归到某个密钥。当前可用性由根任务真实调用另行验证。

安全处置选择：

- 可以继续使用已配置系统默认，保留旧覆盖失效提示；当前不需修改数据库。
- 若恢复旧全局覆盖，管理员需使用该服务的有效原始 API key 在管理界面重新保存，由现有逻辑用当前密钥加密；不能只改 active 或模型名来修复密文。
- 若持有历史加密 secret，可按既有密钥环轮换机制恢复兼容解密，再由现有流程重加密。历史 secret 必须经既有安全配置通道处理，不能写入文档或聊天；没有旧 secret 或原 API key 时不能凭空恢复。

本子任务未执行任何处置，诊断脚本和脱敏结果在证据目录。

## 最终发布证据独立重算

纯本地核验 `最终发布只读.xrfyq93b`，未再次登录或连接生产来核这一组文件：

- 7 项输入 SHA256 重算均匹配；历史任务 2、同队任务 6、事件 23、孤儿执行 7、未收敛执行 3，各列表与本轮发布前逐字段完全相等。
- 项目 161 的 8 字段指纹和整行 SHA256 与本轮发布前相等；这些是脱敏指纹比较，没有重建或改写原始字段。
- 发布首尾除采集时间外全结构相同，两个容器均绑定 `7a2368f20453606ed09691ee0002d07df4629ec3` / 3.9.1 且 healthy。
- 42 项数据库外键独立核对唯一字段、目标 id、SET NULL；5 个运行源码 SHA256 直接与该 Git 提交对象逐一重算相同。
- 42 项实际引用的非空引用均无缺目标/未知 owner/owner 冲突，每表总行数与指纹摘要行数相符。**没有发布前 42 项实际引用快照，不能声称这些引用前后相等。**
- 本版预期资源 274 个，资源卷连同保留的旧版文件共 2893 个，两者口径不同。采集报告断言本版全部一致，但 JSON 没有逐文件原始清单，本子不能另重算整个资源 manifest。
- 运行环境记录 Python 3.11.15、cryptography 50.0.1、Pillow 12.3.0、FastAPI 0.139.0、Starlette 1.3.1，pip check 返回 0。pip 的缓存目录权限提示未造成检查失败，也不代表漏洞审计。

## 已有测试证据完整性

直接核验最终“补丁”日志：前端 102 文件 / 1160 测试通过；后端 4112 通过、5 跳过、3 warnings，覆盖率汇总 79%；完整前端构建与 ESLint 通过。5 个跳过的 Redis 用例另有真实断网 Redis 容器 5/5 通过、network none、最终 remaining_containers=0 的证据。不能把它改述为同一次全量 4117 无跳过。

发布脚本全量日志有 19 项发布绑定、13 项故障/回滚及最终 PASS；独立资源入口测试证明顺序复制、保留窗、重启和回滚兼容。早期 `backend-complete-final.log` 曾收集失败，但随后 release/补丁完整日志已通过，不应引用早期文件名误判最终状态。

这些证据支持已测范围，不等同每个生产写按钮、附件恢复、模型真实调用和所有权限组合均已验收。根任务仍须把真实界面及接口测试中未执行/受限项目如实保留，并完成主知识库回填。
