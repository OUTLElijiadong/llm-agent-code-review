# 最终发布只读取证准备

## 当前状态

仅完成源码审查、补充采集器和离线验证，未执行发布后生产采集。等待主执行者确认最终完整Git bundle绑定提交的前后端均健康；不得在中间版本或部署切换期间取证。

6 项离线自检通过，Ruff 通过。使用本轮真实发布前脱敏证据验证比较器，并注入 SQL 失败、历史状态变化、项目字段变化、外键集合缺项和 owner 不一致，均不能冒充通过。离线通过不能替代生产结果。

## 采集器与契约

1. `证据/collect_release_readonly.py`：校验指定提交与生产 HEAD、版本、current.env、两容器真实镜像 ID、启动时间、健康状态、051 迁移、42 个实际 FK DDL；核后端选定文件与提交摘要、前端镜像内全部文件与实际服务目录摘要。自身头尾防止混合两次发布。采集器只读 Docker/Git/SQL，无 HTTP。
2. `docs/完整待办与漏洞知识更新20260907/证据/collect_historical_ledger.py`：复用原 SQL，一致快照覆盖 task11/team5、task35/team12、历史事件、孤儿及执行中工具账本，内容只输出摘要。**其 MySQL 错误分支仍可能进程 exit 0**，因此不能只看 SSH 成功；本轮比较器强制无 `error`、首尾只读事务标记和查询摘要匹配。
3. `docs/失败恢复与复测核验20260908/证据/collect_project161_fingerprint.py`：复用严格的完整 8 列结构、时间规范化和摘要算法，不读旧备份、不改项目。内部 `equals_before_release` 指嵌入的旧备份基线，不能直接称本轮前后相等；本轮比较器额外与 `项目161发布前字段证据.json` 比较完整字段集合、8 摘要及整行摘要。
4. `证据/collect_attribution_readonly.py`：新增只读一致快照，精确生成 7 表 × 6 字段的 42 项实际引用统计，包含总行数、非 NULL 数、目标缺失、来源 owner 未知、目标 owner 未知、owner 不一致。`sandbox_environment` 使用真实 `owner_id`；`agent_team_task` 没有 user_id，通过目标任务的 team.user_id 求 owner。其余对应 user_id。任一异常返回失败；保留每张来源表 ID/owner/六字段的稳定整体摘要，不输出会话或源码。历史 NULL 单独留作未知，绝不猜填。
5. `证据/compare_release_readonly.py`：本地读取全部脱敏文件，校验头尾发布身份完全稳定、历史基线每行逐字段未变、项目161本轮8字段未变、实际42项归因通过。历史集合新出现的记录单列 `new_observed_ids`，不假定线上统计总数静止；基线记录被删或字段变化均失败。

现有发布前文件没有实际42项引用逐项快照，所以只能证明发布后真实结构与引用契约，不能补造发布前实际引用相等。DDL 的42项完整性和真实调用链归因仍是不同证据；owner相同与引用存在也不能单独证明链路语义。

多模态资产、视觉任务输入输出与相关真实用量由个性化子代理单独准备采集，避免重复。本文件不冒称已经核验多模态生产留档。

## 主执行者确认健康后运行

从本轮隔离工作树根执行。脚本通过 stdin 送入服务器 Python，不落远程脚本、不创建业务数据；MySQL 在只读事务内执行，结尾回滚。两次发布采集包围三个业务采集，最终比较要求容器启动身份、镜像、源码、前端文件和发布账本全部保持一致。

```bash
set -euo pipefail
umask 077
qa_final_sha=${PRISM_ACCEPTANCE_RELEASE:?请显式传入已核验最终发布的完整SHA}
qa_evidence=docs/生产体验与多模态20260912/证据
qa_final_dir=$(mktemp -d "$qa_evidence/最终发布只读.XXXXXX")
ssh -o BatchMode=yes -o ConnectTimeout=10 root@81.70.251.90 "python3 - --expected-release $qa_final_sha" < "$qa_evidence/collect_release_readonly.py" > "$qa_final_dir/发布身份开始.json"
ssh -o BatchMode=yes -o ConnectTimeout=10 root@81.70.251.90 'python3 -' < docs/完整待办与漏洞知识更新20260907/证据/collect_historical_ledger.py > "$qa_final_dir/历史账本发布后.json"
ssh -o BatchMode=yes -o ConnectTimeout=10 root@81.70.251.90 "python3 - --expected-release $qa_final_sha" < docs/失败恢复与复测核验20260908/证据/collect_project161_fingerprint.py > "$qa_final_dir/项目161发布后.json"
ssh -o BatchMode=yes -o ConnectTimeout=10 root@81.70.251.90 "python3 - --expected-release $qa_final_sha" < "$qa_evidence/collect_attribution_readonly.py" > "$qa_final_dir/42项实际归因引用.json"
ssh -o BatchMode=yes -o ConnectTimeout=10 root@81.70.251.90 "python3 - --expected-release $qa_final_sha" < "$qa_evidence/collect_release_readonly.py" > "$qa_final_dir/发布身份结束.json"
python3 "$qa_evidence/compare_release_readonly.py" --before-dir "$qa_evidence" --after-dir "$qa_final_dir" --expected-release "$qa_final_sha" --output "$qa_final_dir/最终发布前后比对.json"
```

遇到任何失败停止并保留本次目录；不能把失败结果覆写为下一次通过。若最后比较指出历史新记录出现，应据 `new_observed_ids` 另审当前活动情况；不自动修复状态。不进行生产登录、HTTP 权限矩阵、数据恢复或旧备份重读。
