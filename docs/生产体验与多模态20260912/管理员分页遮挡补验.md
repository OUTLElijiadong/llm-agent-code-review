# 管理员分页遮挡补验

## 真实复现

2026年9月12日，管理员正常登录生产，在1280×900视口进入内测码管理，列表共23条。正常点击“下一页”后打开贾维斯副驾，列表仍在第一页。普通用户布局的避让修复没有覆盖独立的管理员布局。

## 修复与验证

管理员主滚动区增加100像素加设备安全区的底部空间，920及480像素断点保留该空间。副驾位置、拖拽与业务数据不变。新增3项回归先失败，修复后两套布局及内测码相关9项测试通过；类型检查、ESLint通过，生产构建通过（15.25秒）。

该补丁晚于62fe4f3发布启动发现，必须另行提交完整Git bundle并发布，不能仅凭本地通过宣称生产已修。上线后需再次正常点击分页，并核验窄屏底部控件。
