# 历史生产证据独立复核

复核日期：2026年9月12日。复核对象为主代理采集的生产脱敏证据；子代理独立重算文件、阅读采集程序、核查本地Git对象，没有再次连接生产数据库，也没有执行业务写入。

## 已核验事实

- 生产源码为 `bc207b273441e2f01f988122d28e2f2acb17f4c5`，当时运行镜像为 `c0e4e2ab270273808171da5518e77c9742d64eb2`。本地Git对象证明源码领先一个提交，差异仅为 `deploy/deploy.sh`、`deploy/lib/common.sh`、`deploy/tests/test_scripts.sh`。本轮代码尚未发布，不能把源码修复等同于生产已修复。
- 历史账本证据的查询摘要与9月7日既有采集程序生成的完整SQL一致。程序开启只读一致性快照，事务头尾均记录只读为1，最后回滚。`SET SESSION` 是连接参数设置，不是业务状态改写。
- 重算总数：运行789，工具执行2643，团队63，团队任务197，团队成员189，消息850，运维执行98139。工具总数与完整性统计一致，7个缺失运行关联与7条孤儿明细一致，3个执行中标记与未完成明细一致。
- task11/team5、task35/team12 两条历史任务、所在团队6条任务、23条事件、7条孤儿工具、3条未完成工具，与9月8日发布后证据逐字段完全相同，包括状态、时间、内容摘要、依赖和租约信息。
- 项目161的8个字段摘要、整行摘要与采集器内嵌的旧备份核验基线一致；旧基线文件摘要也独立重算匹配。字段集合无缺失或重复，目标行数为1，完整表结构已由采集器严格验证。当前 project 表104行是全表记录数，不是项目161的问题数。未重新读取备份，更没有恢复旧备份。

## 口径与完成边界

- 7条孤儿工具仍是缺失父运行的历史记录。`success`、合法JSON及摘要只能证明记录自身内容与格式，不能证明父运行存在或所有业务动作正确完成。3条 `executing` 的父运行均已失败，不能作为当前真实运行进程统计。
- 当前证据支持“历史对象未变化并已复核”，不支持“历史孤儿已修复”。按用户要求保留状态，不自动补造关联或改写历史。
- 项目161文件名为发布前证据，比较基线来自旧备份；本轮发布后仍应重新采集8字段并对比，才构成本轮前后闭环。
- 这些文件不覆盖全部模型调用与根任务精确归因、不覆盖新一轮账号/全部按钮/403矩阵、漏洞参考最新日期，也不证明全部页面真实点击通过。这些事项必须使用各自的生产与测试证据验收。
- 生产基线中的健康与就绪状态来自主代理当时采集；独立复核没有重新发送健康请求。关于凭据未修改的描述是执行范围记录，不能由这两份只读业务JSON单独证明。

## 可重复验证

运行 `python3 docs/生产体验与多模态20260912/证据/独立复核生产证据.py`，输出 `证据/生产证据独立复核摘要.json`。脚本只读仓库文件和Git对象，校验失败即停止；输入文件摘要记录于输出，防止把后续变更当成此次已核验对象。

## 管理员首发断网恢复补测

另已完成 `AdminCopilot.vue` 的首发创建确认前断网恢复：在内存保存原始消息与图片，重试先核对服务器会话；服务器已有新运行则恢复该运行，无新运行才重发原请求；核对失败保留重试并明确反馈。旧运行不会误认成此次提交，重复点击核对只发一个请求，会话切换与卸载后丢弃迟到结果。35条组件测试通过，涉及两文件的ESLint通过。测试证明组件行为，生产实际网络中断场景仍由主任务验收。
