# 权限合同与详情页独立复核

日期：2026-09-12。基线：生产 3.9.0 / bc207b273441e2f01f988122d28e2f2acb17f4c5 的隔离工作树。
本文件为子代理独立复核，未执行生产写入、Git 提交或浏览器界面验收。

## 全量合同失败复现与修复

主日志 `/tmp/prism-0912-backend-contracts.log`：4 失败、1621 通过。独立复跑确认：

1. 路由固定计数停留在 3.8.7 的 313 条 / 41 个源文件，当前实际 325 条 / 42 个源文件。不是 FastAPI lazy router 漏发现；现有 `iter_route_contexts` 正确识别完整有效上下文，保留隐藏端点、继承依赖及 WebSocket 未验收记录。门禁显式升级到本轮基线，新增模型注册、分配、个人画像路径断言。
2. 新增 `require_studio_role` 未纳入已复核依赖，导致 15 条工坊接口未发送负向请求。已逐层读取其角色判断、RBAC 只读查询；将该 guard 纳入白名单，同时绑定其委托服务 `agent_studio_service.py` 的源码哈希。所有工坊路由匿名和无权限矩阵均明确纳入，缺源码仍被严格拒绝。当前匿名可验 311 条，无权限可验 243 条。
3. `source_sha256` 在主并行全测中前后不一致，独立复跑不再出现，符合期间其它代理机械格式化源码导致的快照变化。保留严格前后哈希校验，未加重试、跳过或忽略不一致；发布计划应在源码冻结后生成。
4. 审查输入生命周期两例测试将 API 解析模拟为 `None`，却未隔离新增模型选择层，触发模拟对象不符合 ApiConfig。仅补齐测试边界 mock，保留快照不可变、版本缺失回滚、任务/输入账本原子性的全部断言；运行代码不接受非法 None 配置。

验证：`test_permission_route_contexts.py` 20 项、`test_production_permission_acceptance_runner.py` 7 项全部通过；后者对隔离 SQLite 使用真实 FastAPI 登录、普通成员/跨组织 CRUD 及匿名和无权限 HTTP 请求，保留请求总量 > 600、模型调用账本零增量、凭据不泄漏等约束。加独立模型入口 20 项及审查输入生命周期 13 项，组合 60 项全部通过。详见证据目录。

## 详情页独立源码复核

- ProjectDetail 新卡片将有效代码库文件与归档成员数分开，字段分别来自 `active_file_count` 和 `archive_file_count`，没有相加或把归档成员当已入库文件。
- 新增任务跳转按钮检查 `review:view`，现有上传、下载、成员管理及审计权限入口保留。项目属性折叠使用原生 details，支持键盘开关。
- 发现并已回报主代理：最近任务查询包含 sandbox_test / pentest，但原 RecentTask 未传 review_type，新卡片把所有成功任务分数称为“代码评分”。应传任务类型并显示对应评分标签，不能把测试结果混为代码质量。
- 发现并已回报主代理：项目属性 dl 使用固定 220px 最小列宽，窄屏内层可能溢出；应与外层卡片一样使用 min(100%, 220px)。
- ReviewTaskDetail 本轮分数圆圈改纵向排列、禁止收缩，能避免数值与 /100 挤在一行；现有 900px 下三栏改单栏保留。
- 发现并已回报主代理：ReviewTaskDetail 原仅区分 sandbox_test，pentest 仍标“代码质量”并使用优秀/良好代码风险描述，需明确渗透测试评分。
- 沙箱纯文本报告使用报告条目及未分级口径，问题工作台仅在结构化问题存在时显示；未发现本轮重新引入“报告16但四档全0”的裸文本报告矛盾。
- 请求失败处理保留 generation / sequence 丢弃旧响应；403/404 清空对应快照，普通网络失败保留已有结果和重试提示。上述为源码核验，最终每页布局、真实数据和权限点击需由主代理在生产浏览器验收。


## 最终正式环境空参数问题修复

正式 Python 3.11 / FastAPI 0.139.0 的全量测试暴露 `test_complete_permission_matrix.py` 仍直接遍历 app.routes 并过滤 APIRoute，实际只找到 3 个直接注册的健康/指标路由，其认证/guard集合为空，4个参数化测试被 pytest 记为空参数skip。此前“该文件完整矩阵已执行”的推断不能沿用。

新增完整清单断言先红测复现 `3 != 325`。随后仅修改此测试文件，复用已有生产权限执行器的 `iter_api_route_contexts`，保留继承依赖和有效prefix、纳入工坊role guard，unwrap包装端点以识别真实应用源码。收集阶段明确断言325条，防止未来缩水变成skip；独立断言325唯一路径、42来源、311认证、243guard、15工坊。

正式锁环境整文件 **1010 passed、0 skipped（3.91秒）**，包括311×2匿名/停用身份请求、243无权限请求、38普通管理员对唯一超管接口拒绝，以及既有真实JWT项目隔离/CRUD/回退断言。Ruff通过。未修改生产逻辑，不启动lifespan/模型调用。

其余全量套件中5个依赖隔离Redis容器的skip不在此次修复范围，仍应明确记录为未执行，不能计为通过。最终全量总数以主任务重新运行结果为准。
