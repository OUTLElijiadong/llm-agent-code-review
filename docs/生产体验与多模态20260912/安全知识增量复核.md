# 安全知识增量复核

2026年9月12日重新读取官方来源，发现一条既有CVE记录更新和一条新增维护者公告，已通过既有离线生成流程更新参考集。保留41条CVE统计，另列1条尚无CVE编号的公告，不将精选PHP资料称为全量NVD数据库。

## 官方来源与变化

- [OWASP官方入口](https://owasp.org/Top10/) 当前重定向至2025最终版。旧下载URL出现308，新域直接程序下载返回403；改从[OWASP官方仓库](https://github.com/OWASP/Top10/tree/master/2025/docs/en)读取十个分类的原始Markdown，逐类映射40、16、6、32、37、39、36、14、5、24项，与现有分类全部一致，合计249个标识。没有用抓取失败冒充未变化。
- [MITRE当前版本页](https://cwe.mitre.org/data/index.html) 仍为4.20、944个有效弱点。重新下载的完整XML ZIP与9月7日原始包字节完全相同。
- [PHP变更日志](https://www.php.net/ChangeLog-8.php) 与9月7日原始文件字节完全相同。两份既有GitHub维护者公告虽然页面外框变化，但公告正文逐字一致。
- 重新下载39个既有官方CVE JSON（38个活动参考和1个已移除的Flatpak纠错证据）；38个文件原始摘要相同，唯一变化为 [CVE-2026-48019](https://cveawg.mitre.org/api/cve/CVE-2026-48019)：9月8日新增CISA ADP，CNA漏洞描述与影响版本保持不变。更新记录时间、来源摘要，并保留新增ADP原始结构。
- [Laravel 9月10日新公告](https://github.com/laravel/framework/security/advisories/GHSA-jh5r-qr3c-85q8) 描述调试页面的条件性DOM XSS。已读取维护者官方JSON，保留两个主版本分支范围及修补版本、发布时间、来源摘要。官方明确没有CVE编号，因此单独列为补充公告。该记录不能证明用户项目已经命中或可被利用。
- CVE-2026-48041、CVE-2026-12184、CVE-2026-9672 的CVE JSON仍返回404；对应既有官方维护者公告/PHP变更日志重核成功，保留原来源类型，未伪造CNA来源。

## 更新范围与日期

`build_security_catalog.py` 继续离线构建分类JSON、前端快照、结构化漏洞参考及模型实际读取的Markdown。此次增加独立的 `cve_verified_at` 和 `supplementary_advisory_count`，分类原始快照日期仍为9月7日，漏洞参考重核日期为9月12日。安全页面既有范围说明会显示41条CVE、1条无CVE编号公告及真实重核日期。未改历史报告、业务状态或知识数据库。

## 验证与复现

新增回归先复现缺少独立核验日期字段：1失败、7通过。完成后后端安全专测9项通过；前端OWASP语义和安全页面9项通过；生成器及测试Ruff检查通过。另用 `证据/安全来源独立重算.py` 离线重算原始包、十类映射、CVE差异、维护者正文及新增公告字段，全部通过。

本轮官方原始下载保存于 `证据/安全官方原始快照20260912.zip`，大小2,436,698字节，摘要及逐文件长度见证据目录。包中保留实际失败记录、官方GitHub原始Markdown和来源URL，不把旧HTML伪装成新下载。

如需重建，解包9月7日分类快照至临时目录，以本轮包中的 `verified_advisories.json` 覆盖对应文件，再运行：

```text
python3 backend/scripts/build_security_catalog.py --source-dir /绝对路径/合并快照目录 --verified-at 2026-09-07
```

生成器从参考文件读取9月12日独立日期，分类沿用已逐项复核一致的旧快照。生产发布、模型进程缓存重启和真实页面新范围说明验收由主任务统一执行，本子任务未部署。
