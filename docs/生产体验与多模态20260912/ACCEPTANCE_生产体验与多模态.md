# 生产体验与多模态验收

## 发布前状态

本轮版本计划为 3.9.1，当前尚未发布。源码基线 bc207b273441e2f01f988122d28e2f2acb17f4c5，原生产应用镜像 c0e4e2ab270273808171da5518e77c9742d64eb2（3.9.0）。两者仅发布脚本提交差异。

## 已复现并修复

- 工作台未导入 getRunning，旧测试通过但真实后台进度没有请求；独立失败提示、刷新与权限过滤补齐。
- 深度分析折叠、刷新、展开后趋势和严重度画布缩为 100×220；补尺寸观察、重绘和文字数据兜底。
- 平均评分布局及任务评分圆球对齐；审查列表 processed_files 前后端契约补齐。
- 移出项目后统计缓存短暂泄露旧可见范围；实时可见范围参与查询与缓存键。
- 代码库文件和整包归档成员混用名称；分项展示并保留历史数量与状态。项目详情卡片和折叠属性，审查卡片可直接查看。
- 头像真实解码、大小限制、重复上传缓存与跨账号隔离，失败保留旧值。
- 偏好回填和保存原子性、低频引导、学习关闭和私有本地知识记录；工坊全部接口严格身份门禁与部分成功恢复。
- 模型能力、角色及逐个子 Agent 配置消费，图片续跑恢复与当轮结束后默认模型回归。
- 普通端仅图片可发送；附件读取逐张反馈并隔离切换会话；管理员首发断流先核验服务端运行再重试，避免重复执行。
- Docker 构建恢复 vue-tsc 类型门禁，原脚本仅构建使类型错误进入生产。

## 当前验证

- 前端最终全量：101 文件、1149 测试通过；完整构建含 vue-tsc、全量 ESLint 通过。
- 后端正式 Python 3.11 锁最终全量：4103 通过、5 个环境依赖跳过（179.12秒）；这5个Redis测试已另在本地断网专用容器实际全部通过（6.48秒），容器已清理。
- 补测发现旧权限枚举在新版FastAPI为空，修复后1010项权限/隔离整文件实际通过，无跳过。收集阶段强制325路由/42来源/311认证/243门禁，禁止空参数冒领。
- 后端 app/tests Ruff、差异检查通过；发布脚本完整合同测试通过，覆盖切换前失败、切换后回滚和回滚失败反馈。
- 前端全部npm依赖、后端生产锁漏洞审计为0。后端开发锁仍有black/pytest/pip相关7条来源记录、4个唯一公告，不能称全依赖零漏洞。
- 安全来源独审：61文件清单一致，944有效弱点、249映射，41 CVE与1无编号公告分开，4个生成产物逐字节一致。
- 图片上下文压缩独立红例转绿；无法容纳图片时明确预算错误，不静默丢图。
- 新增真实UI发现：任务分页被小菱入口覆盖，已改主内容底部安全间距；正常退出时旧Mesh异步请求继续，已加代次隔离及旧凭据迟到401保护。相关红绿测试、类型检查通过。
- 管理员Agent页由拥挤长表格改卡片与折叠细节，七个真实分类中文映射和四项指标独审通过。其他管理模式未擅自改数据。

## 真实生产界面发布前核验

通过正常管理员与普通用户登录，未改凭据。1280×900 页面截图已在任务中逐页检查，未声称截图文件已落盘。

|页面或控件|实测结果|
|---|---|
|管理员概览、用户管理、发布审批|实际数据加载、卡片无明显重叠；未执行审批或用户修改|
|大模型配置、拉取模型|真实拉取返回2模型；发现最新Flash被旧逻辑标为非视觉，修复待发布|
|策略中心|可加载，仍有表格配置，不把现状声称全站卡片化|
|普通工作台|真实复现图表缩窄、后台进度缺失；评分与文件口径问题已修待发布|
|项目搜索|输入皮卡丘并提交后返回1项；点击卡片进入项目23|
|项目23详情、审查页签|显示真实288文件和关联任务；旧表格改为卡片待发布|
|审查任务列表|39条真实任务，成功/失败评分区分，可进入任务161|
|任务161|已处理1/1，未分级4、报告条目4；缺严重度为横线，不是零；评分圆球偏上已修|
|报告161|任务链接进入正文成功；未改历史报告|
|问题追踪、展开详情|616条结构化问题，展开/收起显示真实说明；与报告条目不是同一口径|
|审查规则|116规则可加载，搜索入口可见；仍保留规则表格|
|安全中心、A10详情与关闭|OWASP2025已在线，CWE4.20/944弱点、41条参考CVE；弹窗说明和关闭正常|
|Agent办公室及MetaGPT页签|可见5目录，17明确为内置可适配，切换成功|

## 历史证据

本轮只读SQL一致快照，无业务写入。项目161发布前8字段与旧基线一致。历史任务11/35、6条同队任务、23事件和7条孤儿执行仍原样保留。发布后还需再采字段证据，当前不能称本轮前后闭环。

## 尚待完成的门禁

剩余页面按钮点击、生产备份与 bundle 发布、发布后关键功能/多模态实调/字段证据、主知识库 validate 与 postflight。全部控件矩阵必须逐项标记；未点击的写操作不能因只读页面通过而标为通过。

## 补充页面与卡片改造

代码中心、沙箱历史、论坛、维修申请、意见反馈、个人知识库、个人中心均已在普通账号真实加载。维修新建弹窗可取消；偏好入口弹出完整四组选项，可稍后关闭。代码中心补读取失败重试；维修与反馈改为展开卡片，提交、回复、处理失败保留草稿并在弹窗内反馈。个人中心原本机无消费者默认审查偏好已移除。未执行的论坛发布、后台任务启动、删除、账号改密等不计为验收通过。

## 发布前补充真实点击

普通账号功能导航搜索“皮卡丘”显示正确的项目管理引导，关闭正常；导出报告显示下载提示（本轮未取得本地下载文件，不凭提示声明文件验证）。不存在任务162返回“审查任务不存在或当前账号无权访问”，重试后仍显示明确错误且可返回列表。列表第二页本身可加载，但普通点击被悬浮入口遮挡，已修待生产再验。

管理员Agent管理35条实际加载；审批中心加载后存在历史已处理与待处理项，初次加载的空态不作为最终结果；工具权限、任务调度、监控告警、奖惩页面实际数据与截图已检查。未点击批准、授权、关闭历史告警或运行计划任务。
