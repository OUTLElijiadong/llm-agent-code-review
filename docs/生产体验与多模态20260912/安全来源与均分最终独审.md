# 安全来源与均分最终独审

复核日期：2026-09-12。复核者：模型子代理，独立于安全目录与工作台实现代理。结论：本次限定范围内未发现剩余阻断缺陷。未修改应用代码、历史报告或生产数据库，只写本独审结论与测试日志。

## 安全来源与生成产物

- 对本轮 ZIP 内全部 61 个文件独立检查字节数、SHA256、文件名集合与清单一一对应，无漏项和多项。完整包 2,436,698 字节，SHA256：`876e71544e2c9d9ddf75bcade9f0d0bb492bbd3b6b0eb4d6fe4ac68cf9ef71a0`。
- 不依赖报告中的硬编码数字，重新解析本轮 MITRE XML：969 个 Weakness 节点，其中排除 Deprecated 后 944；版本 4.20。再次访问 [MITRE 官方当前版本页](https://cwe.mitre.org/data/index.html)，页面同样显示 4.20 与 944。
- 从十份本轮 OWASP 官方 Markdown 的“List of Mapped CWEs”列表重新提取标识，逐分类比对应用 JSON：40、16、6、32、37、39、36、14、5、24。合计且去重后均为 249；按 MITRE XML 类型为 246 个 Weakness 和 3 个 Category。没有把 249 当作有效弱点总数。
- 参考记录 41 条，CVE ID 唯一且格式正确；另有 1 条 `GHSA-jh5r-qr3c-85q8`，`cve_id=null`，没有混入 CVE 计数。41 条由 38 份活动 CVE JSON及 3 份官方维护者/PHP来源记录组成；原始 ZIP 的 39 份 CVE JSON另含一份此前剔除的 Flatpak 纠错证据，没有重新纳入参考集。
- 实时重新请求 [CVE-2026-48019 官方 JSON](https://cveawg.mitre.org/api/cve/CVE-2026-48019) 和 [Laravel 公告官方 API](https://api.github.com/repos/laravel/framework/security-advisories/GHSA-jh5r-qr3c-85q8)，两者与本轮保存文件的 JSON及原始 SHA256 完全一致。公告仍无 CVE 编号，影响/修复版本两个分支结构原样保留。
- 重新比较 9 月 7 日与本轮包：仅 CVE-2026-48019 JSON变化；CNA 内容相同，新增 CISA ADP及更新时间。两份 GitHub公告外框字节变化但正文相同，参考记录有意保留旧原始整页哈希；其旧包仍可验证，未把旧哈希误称新下载摘要。
- 将旧分类 HTML快照及本轮漏洞参考合并到临时目录，把生成器 ROOT 指向临时输出，重新生成四个产物：后端分类 JSON、后端漏洞参考 JSON、模型知识 Markdown、前端分类 JSON。四份均与当前应用文件逐字节一致。应用目录未被生成器写入。
- 分类快照日期仍为 9 月 7 日、漏洞参考独立核验日期为 9 月 12 日，来源语义一致；范围文字明确为 PHP 生态精选参考集，不声称全量 CVE/NVD 或项目已命中。

## 平均代码评分

读取实际 SQL、schema、前端显示与 HTML导出逻辑后，确认：

1. 仅成功任务、当前用户可见且未删除项目、`quick/standard/security/performance/full` 五种代码审查类型、非空且 0–100 的评分计入 AVG及同一条 SQL 的样本数。该白名单与 ReviewStartIn 允许的审查类型一致，排除 sandbox_test、pentest 和未知历史类型。
2. `review_count` 仍表示全部成功执行数量，`code_review_count` 专门表示有效代码评分样本数，前端说明“份有效代码审查·不含测试”；两个字段没有混用。
3. 无样本时保持兼容 API `avg_score=0, code_review_count=0`，界面显示 `—/100` 和“暂无代码评分样本”，不绘制风险仪表；真实零分 `avg_score=0, code_review_count>0` 显示 `0.0/100`，不被当成未知。缺失样本数字段亦不冒充有效评分。
4. 实现仅调整查询过滤与显示，不更新任何历史任务 score、status、review_type。合法零分保留、非法 >100 分排除；历史数据仍可原样复核。

独立重跑后端 `test_dashboard_live_progress.py` 8 项全部通过，包含跨项目可见范围、文件口径、非法/未知类型排除和真实零分；前端专选“真实全零、真实零分、代码评分样本口径”4项全部通过（其余57项未在本次定向选择内，未声称重跑）。日志见 `证据/均分显示独立复测4项.log`。

该结论确认来源、生成产物及隔离测试完整性，不替代主任务后续生产版本核对和真实页面验收。
