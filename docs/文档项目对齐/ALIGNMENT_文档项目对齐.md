# ALIGNMENT_文档项目对齐

## 1. 原始需求

用户要求「把所有文档和整个项目对齐」。

## 2. 项目上下文

- 当前项目为 FastAPI + Vue3 + DeepSeek 的智能代码审查平台。
- 顶层管理文档为 `说明文档.md`;核心文档位于 `docs/01-11` 和各专项 6A 目录。
- 当前代码事实以工作区为准:
  - HTTP API:87 条
  - WebSocket:1 条(`/api/ws/discuss/{session_id}`)
  - Agent:14 个
  - 业务表:14 张
  - 后端测试:115 passed,覆盖率 39%
  - 前端构建:`npm run build` 通过

## 3. 边界确认

本次只做文档对齐,不修改业务代码、不回滚已有未提交代码改动。历史进度记录保留当时结果,但在需要的位置补充当前复核口径。

## 4. 已识别不一致

| 类别 | 不一致 | 决策 |
| --- | --- | --- |
| 数量口径 | README/设计文档仍写旧表数、服务数、前端模块数 | 以当前目录和运行时注册结果更新 |
| 上传限制 | 多处写 200KB 与扩展名白名单 | 改为当前默认 20MB,扩展名不限但阻止系统/构建目录 |
| API 文档 | 安全审计、自进化、文件夹上传、取消/删除任务未完整体现 | 补正式接口说明 |
| 部署文档 | 写了不存在的 `deploy/nginx.conf` 和 `docker-compose.dev.yml` | 改为 `frontend/nginx.conf` 与复用 mysql 服务 |
| 测试文档 | 旧结果 112 passed/38% 与当前不一致 | 补当前 115 passed/39% |

## 5. 疑问澄清

无必须中断确认的问题。当前实现和工作区事实作为权威来源。
