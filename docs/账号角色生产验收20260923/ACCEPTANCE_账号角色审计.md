# 账号角色与审计权限验收记录

## 本地检查

- 后端：4489 passed、5 skipped；Ruff、compileall、单一 Alembic head 通过。
- 前端：116 个测试文件、1286 项通过；lint 与生产 build 通过。
- 部署契约与失败恢复脚本：19 项绑定测试和部署故障恢复测试通过。
- 生产恢复验证：隔离恢复 100 张表，备份校验通过，备份对应旧 revision 056。

## 生产版本

- 产品版本：4.0.2
- 后端/前端 SHA：`7e7559acf2916cc8ae7380e33bb2519a0addf12c`
- Alembic current/head：`057_reviewer_audit_menu_route`
- 健康/就绪：HTTP 200；公开 OpenAPI：HTTP 404；HTTP 重定向至 HTTPS：308。
- 5 个关键容器健康；新发布备份 gzip 与 SHA 校验正常，年龄 0 小时。

## 账号验收矩阵

| 账号类型 | 登录/身份 | 唯一有效角色 | 审计权限/菜单/API | 审查任务 | 团队隔离 |
|---|---|---|---|---|---|
| 管理员 | 通过 | `super_admin` | 审计、用户管理、RBAC API 均 200 | 200 | 本账号列表仅含自有团队 |
| 审查员（审计员合并后） | 通过 | `reviewer` | 有 `audit:view`，菜单 `/audit` 可见，审计 API 200 | 200 | 本账号列表仅含自有团队 |
| 普通用户 | 通过 | `user` | 无 `/audit` 菜单，审计/用户管理/RBAC API 均 403 | 200 | 本账号列表仅含自有团队 |

- reviewer 和普通用户跨账号读取权限均为 403。
- 三个团队列表两两不相交；对三组归属的团队详情执行六次跨账号请求，均返回 404。
- 生产数据库只读复核：auditor 状态为 disabled、auditor 角色分配数为 0、目标审查员 reviewer 有效分配数为 1。
- 生产 API 登录仅生成正常登录审计记录，没有改账号或业务数据。
- 独立 Agent 只读复核确认了生产版本/SHA、镜像标签、唯一迁移 head、HTTP 检查及数据库角色事实，并核对账号矩阵的六个有向跨账号团队访问组合；它没有重新登录或复现 API 矩阵。

## 稳定性状态

发布完成后 15 分 35 秒巡检：发布账本、五个关键服务、备份校验、迁移与 HTTPS 均通过；`backend`、`frontend` 在观察窗内重启数为 0，六个容器 OOM 均为 false。MySQL 的累计重启数为 1，但容器启动时间为 2026-09-01，早于本次观察窗。磁盘使用率 86%，高于 85% 告警阈值但低于 95% 临界阈值，巡检判为 `degraded / can_continue=true`，没有阻断项。本次未清理生产备份或镜像。

浏览器真实点击和视觉验收不由本 API 矩阵代替，本次没有执行浏览器人工点击验收。
