# 账号角色与审计权限生产验收总结

## 结果

生产已从 v4.0.1 更新到 **v4.0.2**，运行 SHA 为 `7e7559acf2916cc8ae7380e33bb2519a0addf12c`，数据库唯一 head/current 为 `057_reviewer_audit_menu_route`。

本次修复让审计 API 按 `audit:view` 授权，使合并后的 reviewer 能访问审计；把 reviewer 的审计菜单移到独立 `/audit` 路由；保留管理员能力，并由后端拒绝普通用户请求。迁移保留原菜单路径供精确回滚。

三账号均成功登录，分别只有一个有效角色：`super_admin`、`reviewer`、`user`。审查员拥有 `audit:view`，`/audit` 菜单可见且审计 API 通过；普通用户的审计、用户管理和 RBAC 管理接口均被 403 拒绝。生产数据库只读核验显示旧 `auditor` 角色停用且无账号分配。

三账号的 Agent 团队列表两两不相交，对其他账号团队详情的六次跨账号请求均返回 404。非管理员读取其他账号角色权限返回 403。完整结果见 [ACCEPTANCE_账号角色审计.md](ACCEPTANCE_账号角色审计.md)。

## 验证与发布

后端 4489 项通过、5 项跳过；前端 1286 项通过，lint 与生产构建通过；部署脚本绑定/故障恢复测试通过。发布完成后 15 分 35 秒复查，后端和前端没有重启，容器 OOM 为 false，健康/就绪均为 HTTP 200；备份校验通过，HTTP→HTTPS 为 308，OpenAPI 返回 404。

独立 Agent 只读复核了生产 SHA、版本、容器标签、迁移、健康状态和角色数据库事实，并核对矩阵计数。它没有再次登录账号，所以账号 API 成功项以本轮直接运行结果为证。

## 尚未验收

本次没有进行浏览器逐按钮点击或视觉回归。生产磁盘使用率 86%，巡检为 `degraded / can_continue=true`，低于 95% 临界值；未自动清理任何备份或镜像，待维护人员审阅 dry-run 后决定后续治理。
