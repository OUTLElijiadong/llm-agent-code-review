# TODO_腾讯云服务器部署

## 待办

- `80` 端口已可公网访问；`8000` 当前公网超时，如需直连后端调试，需要在腾讯云安全组放行 `8000`，否则保持关闭更安全。
- 如需正式生产访问，后续补充域名解析与 HTTPS 证书。
- 如需长期运维，后续补充数据库备份计划和容器日志持久化。
- 服务器已新增 `/swapfile-code-review` 作为 2GB 持久化 swap；如升级服务器规格，可重新评估是否保留。
