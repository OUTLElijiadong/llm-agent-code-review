# 小菱与全栈优化：阶段验收

最终续接验收以《本批续接修复验收》为准：目标补丁后端2820项、前端620项通过，独立打回缺口已补测修正；下文587与58等均为此前阶段数，不与最终数相加。新候选尚未发布，生产真实登录和业务验收仍阻塞。

最终独立原6探针复跑全部通过，620/2820及源文件指纹已由另一子代理重新核对。UTC2026-09-06 00:35:42的知识库validate/postflight为432文件、0错误/0警告，闭环回执已生成；这不等于生产业务矩阵全部通过。

## 已完成

| 范围 | 验收证据 | 状态与边界 |
| --- | --- | --- |
| Git 澄清与旧修复 | 既有分支、两个本地提交、验证过的 Git bundle | 已完成；不是没有 Git，不需要新建仓库；未推送远端 |
| 旧修复生产发布 | 3.8.4 / 343616d9，89 表备份恢复，047、镜像与环境一致、公网与登录页 | 已完成；不等于登录后全部业务通过 |
| 知识库对齐 | ready 收据、主页/治理/路由/地图/项目线索 | 已完成本阶段定向读取；历史记录未作为当前生产事实 |
| 架构审视 | 72 条来源记录、12 项结论、5 项原函数隔离复现、2 项确定性核对 | 已完成定向评审；长文档和全仓逐行精读未完成，见覆盖清单 |
| 前端审视 | 224 个非测试文件结构扫描、重点状态与动效组件读取 | 已完成有边界的评审；不是逐页浏览器全按钮验收 |
| 指定动效库 | 固定 SHA、独立工作副本、46 个调用、244 项 HTTP 核对、9 项库测试 | 已准备；未把原库或制作依赖加入 Prism |
| 动效浏览器 | 图库实际显示、流程分类筛选、变体切换、真实媒体播放 | 已执行；未代用户加入选择列表，未覆盖用户剪贴板，未宣称每段全片逐帧验收 |
| 发布独立复核 | 47/47 Git 证据引用、2782/575 计数、生产 13/13 限定检查 | 已通过；完整范围和未测项见独立报告 |
| 测试来源补证 | 四项目逐项身份、说明/路径、1079 字节源码指纹及时间关联 | 已补齐24/25/26/27的来源；早期三项阻塞记录保留，不倒改原报告 |
| 知识库元数据 | UTC 2026-09-05 20:10:48，431 文件，0 错误/0 警告 | 本次已运行，不沿用历史校验 |

## 继续执行的本地门禁

- 成员仪表盘：删除写死的v3.4/1M宣传和非实时接口的“实时·流式”标签；统计、维度、严重度、频率、评分分别反馈读取状态。失败不再伪装为0或空列表，空维度不再无限“汇总中”，过期区间响应不覆盖当前数据，单区可重试，未完整读取时禁导出；真实零分与没有完成审查分别显示。
- 隔离回归：新文件12项在旧实现全部失败；首次测试收集因缺少子组件隔离而失败，首次绿灯运行有2项日期文本断言不精确，修正测试夹具后12项及6项既有权限测试通过。原失败日志保留，不把测试环境错误计为生产缺陷。
- 本地前端全量587项通过，TypeScript、目标文件ESLint和生产构建通过。没有把这批新增页面改动计入已运行的3.8.4，也没有用本地用例替代真实登录后浏览器验收。
- 团队R05：继续修复已复现的父失败/子未终态组合，保持既有fail-fast；独立分支、依赖、重试、运行中兄弟与迟到结果为门禁，代码审查和最终测试结果另补。历史11/35暂未修改。

## 当前执行与后续门禁

- 契约生成规范化：本地已完成。17项测试在旧实现为16失败/1通过，修复后17通过；Ruff和无写入--check通过，主线程再次check并确认产物前后SHA不变。两份生成资料均为32份静态职责契约，不宣称32个真实运行Agent。该工程工具补丁未进入运行中的3.8.4。
- 四项目可逆隔离：新备份89表/047恢复验证通过后，24/25/26/27返回committed。仅status改为quarantined，关联数据未删除；独立后验另存，不能以回执替代数据库重查。
- 生产父子终态：只读JOIN确认task11/team5为queued/failed、task35/team12为waiting_dependency/failed；未将它们改为completed，未触发模型或重试。本批正在处理其代码路径，未把开发中补丁写作已发布。
- 新架构/SOP/预算/动效：属于后续实施方案，不在本轮记录为已实现。

## 仍阻塞或未测试

- 生产浏览器没有管理员/普通成员/无权限账号的已登录会话，角色正向、全按钮、组织隔离和真实业务闭环未完成。
- 真实模型、逐工具审批、SOP 长链路、沙箱取消确认、多进程恢复等未执行生产故障注入。
- 没有逐条证明论坛、知识内容、Agent 记忆和所有历史数据的现实来源；89 表计数不等于这项证明。
- 还未按用户选定调用码制作新动效。图库中的示例是参考素材，不是 Prism 业务数据或本次新设计成品。
- 历史全仓 Ruff 问题与旧 ShellCheck 警告不在本次新规范化脚本范围内，不宣称整个仓库零警告。

## 总体验收语义

本阶段交付包含已发布修复、已准备预览、定向源码/数据审视和可实施方案；未作“所有代码无 Bug、全部数据真实、所有业务已通过”的无限范围签署。阶段完成和产品全量验收分别记录。

## 隔离后独立复核

UTC 2026-09-05 20:48:40的独立报告为9项通过、1项阻塞。四目标的实际状态、时间戳、源码1079字节及固定SHA、项目字段覆盖、已知关联与六张核心表行数保持通过；当前3.8.4/release一致，两条父子异常再次确认。本地17项和32份静态契约单独重计。项目161不在操作目标中且仍active；其完整变更前字段快照不足，不能把“未列入操作目标”写成“独立证明所有字段均未改变”。原报告保留，不为了凑全通过恢复生产数据库。
