# 本批续接修复验收

## 版本与发布边界

本批基于已发布的`343616d9d40c1e44e17c7da465c7173c418debb5`继续修复，属于本地待发布候选。UTC2026-09-06 00:16:55至00:16:57再次确认：生产healthz/readyz仍为3.8.4及该完整SHA，服务器Git干净，匿名auth/me返回401。本批新代码没有覆盖生产，不能拿前一次发布结果给新补丁签收。

真实生产浏览器仍停留登录页。管理员、普通成员、无权限账号的页面/按钮/隔离，以及真实模型、工具审批、团队和SOP长链路没有完成本批生产验收。因此候选先保留并提交，待真实账号验收和发布门槛满足后再沿原流程备份、验证恢复、按精确SHA发布；不绕过认证，不用测试替身伪装真实业务。

## 一、成员仪表盘

- 移除写死的v3.4、1M及“实时·流式”宣传；页面实际展示最近已完成记录，不将时间排序的评分称为TOP排名。
- 摘要和四个图表各自显示读取中、成功或失败，进度由实际返回项数计算。成功分区无需等待其他接口；失败分区可单独重试，不制造零值、空列表或重复轮询。
- 首载/摘要失败不显示伪造统计卡；没有成功审查时均分为破折号且不判风险；真实零分保留0.0，并复用全项目评分风险口径。
- 旧日期响应、旧摘要响应和卸载后结算不能覆盖当前状态；刷新失败隐藏过期图，保留其他成功分区及原缓存供重试，不把旧值当新结果。
- 风险接口实际四类全零响应显示空态，饼图同时设置零和保护，不呈现四个等分的虚假占比。
- 四个图表在赋值前检查数组、成员、必需标签/标识、非负安全整数计数、0至100有限评分及真实日历日期/时间戳；摘要逐个校验最近任务。异常载荷受控报错，不静默改成0或跳过坏记录后称完整成功。
- 所有当前数据有效才允许导出；无权限、读取中或任一区失败时，按钮与直接调用两层均拒绝。报告区分累计概览与所选区间，不把近30/90天统称周报。
- 保留报告窗口的noopener/noreferrer。返回null不再误判为弹窗被拦截并立即回收URL；正常路径只反馈“已请求打开”，抛错反馈失败并释放临时URL。

### 测试与独立打回

初期12项回归全部红灯；首次收集缺少子组件隔离、首次绿灯的两处日期断言不精确，都作为测试过程问题保留，不计为生产缺陷。基础修复后587项全量通过，补导出后589项全量通过，但这不是最后验收。

独立子代理随后用6个隔离探针发现3处尚未收口的旧缺陷：真实四类全零饼图、非法图表成员、非法最近任务成员。主线程将它们和覆盖缺口写入正式测试，45项测试在修复前为24失败/21通过并有1次渲染错误；修正后45项全部通过，另6项既有跨页权限回归通过。6项跨页用例只有1项原来针对Dashboard，不把它们全部计为该页面专属用例。

最终前端全量为**78文件、620项通过，0失败/0跳过**；TypeScript、目标文件ESLint及生产构建通过。前后源文件SHA一致。证据使用`continuation-frontend-reviewed`前缀，不再使用587/589作为最终封版计数。

## 二、团队任务终态

- 维持既有fail-fast：终态失败分支出现后，在同事务把剩余queued/waiting_dependency变为有原因的blocked，并记录task.blocked事件；不把未执行任务写成completed。
- 运行中的兄弟任务保留租约，停止领取后续工作，待真实结果返回或原资源清理路径完成再归约团队终态；没有宣称外部资源已经停止。
- 领取和完成使用刷新后的加锁读取，统一相关恢复路径父→子锁序；失败或已完成父团队拒绝迟到结果，旧会话/旧租约不能复活旧尝试。
- 显式重试识别因失败根节点阻断的独立分支；尚有自动重试额度的正常失败不提前终结整个团队，不新增continue_independent政策。
- 主线程补齐子代理指出的“缺少复核节点但仍有running任务”遗漏：仍在执行时先等待收尾，没有在途任务则立即失败，防止提前终态使原结果和清理路径失效。

子代理服务定向测试58项通过。主线程新增3项复核节点异常用例，先2失败/1通过，修复后连同服务、两个dispatcher与API共107项通过。最终后端**2820项通过，0失败/0跳过，1条既有PytestCollectionWarning**；目标Python文件Ruff与生成契约check通过。最终使用`continuation-backend-reviewed-20260906`原始记录，源码前后SHA一致。

并发证据限SQLite双会话交错与CAS反例，不冒称真实MySQL多进程故障注入。历史task11/team5和task35/team12没有自动改库；其原排队/依赖等待状态已留证，受控数据修复仍需精确快照、预览和审计，不用改成成功掩盖异常。

## 三、生成契约规范化

职责契约从真实源目录生成，去掉硬编码旧数量，补齐既有operations与sandbox_deployer资料；两份生成文件为32份静态职责契约，不是32个真实运行Agent。生成器使用标准库隔离载入，并提供只读`--check`，资料缺失或漂移返回失败而不写文件。17项红绿回归纳入后端全量，独立重计与主线程check均已执行。

## 四、真实业务验收矩阵

| 环境与身份 | 当前结果 | 没有验证的范围 |
| --- | --- | --- |
| 生产匿名 | health/ready、版本、匿名401、本次之前的登录必填反馈通过 | 不等于任何登录后权限或业务成功 |
| 生产管理员 | 阻塞：没有已登录会话 | 新仪表盘、所有页面/按钮、真实Agent及导出窗口 |
| 生产普通成员 | 阻塞：没有已登录会话 | 正向能力、项目隔离、跨组织拒绝、刷新持久化 |
| 生产无权限账号 | 阻塞：没有已登录会话 | 页面隐藏及真实403；不能用管理员代测 |
| 本地组件/API/服务测试 | 2820后端、620前端通过 | Mock、SQLite不等于真实模型或生产数据验收 |
| 数据隔离后验 | 四目标、六表计数和字段边界按独立报告通过 | 项目161全字段前后比较仍有证据缺口 |

## 五、仍未实施的优化

执行所有权和跨进程恢复fencing、未知工具结果核账、停止确认、执行与需求验收分离、版本冻结的持久SOP、根任务调用/用量关联、授权预算和调度增量补槽，仍按设计分批实施。本批没有引入新框架、模型、收费预算、数据库迁移或运行时动效库。

动效图库仍为独立参考预览，用户未选择调用码，不把示例视频或原库源码复制进业务页面。全仓逐行审计、所有历史数据现实来源、全角色全按钮与真实模型故障矩阵尚未完成，不作相反签署。

## 六、证据状态

各阶段原始失败、通过和独立打回报告分别保存，不能把最后通过回写到早期记录。证据目录的SHA256SUMS涵盖归档文件；生产3.8.4证据与本批本地候选严格分列。

最终独立复验已完成：原探针代码不改，6/6通过；45+6定向、620前端、107团队定向及2820后端全量被重新解析核对，XML/JSON计数一致。前端2文件、后端4文件均与测试前后和当前源码指纹匹配。三项被打回的Dashboard缺口关闭，旧报告与原红色结果保留。定向子集不重复累加为更多测试。报告见“证据/independent-continuation-final-review.md”。

知识库于UTC2026-09-06 00:35:42完成validate及postflight：432文件、0错误/0警告；闭环回执为`/Users/li/Documents/Obsidian/myself/03_索引/审计/任务闭环/任务闭环-20260906-003542.md`。知识工具的“通过”不替代本报告的业务矩阵；生产登录、历史数据证明及新候选发布仍按各自阻塞/未执行状态保留。
