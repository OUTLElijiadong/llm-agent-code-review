# 项目161备份基线独立补证

## 结论：阻塞

本文件为新增补证，不覆盖原隔离后验报告。尝试比较2026-09-05 20:16:10 UTC命名备份中的161与本次生产快照；最终字段比较未完成，不宣称证明两个时点之间未发生过临时变化。

- 具体阻塞：`datetime_format_or_precision`。本次不重新打开或重扫备份。

- 归因更正：JSON的`parser`对象已证明备份解析、主键检查和161基线提取完成。此前把这些项目写为“尚未完成”是报告归因错误，现予纠正。
- 尚未完成：当前161行规范化、备份与当前161逐字段相等比较、逐字段及整行规范化SHA输出。未猜测、截断或转换原值，不能据此断言161业务数据不一致；最终字段对账缺口继续保留。

## 备份与解析边界

- SHA256：`7bb99942cc0e571008743fc66b0d1cdd88fcf13640579b32fb493c0679d7dc6f`；字节：402726075；期望哈希匹配：True。
- 压缩文件仅打开一次，解析project区段；无关表不做字段或计数解析，到project结束后仅顺序消耗剩余压缩字节用于同一次SHA核验。未恢复或创建数据库，未执行dump SQL。

- **备份直接证据**：`parser.project_rows=100`、`parser.target_rows=1`、`parser.primary_keys_unique=true`；备份DDL完整8列、列序、类型、nullable已记录，161行已在服务器内存提取并规范化。上述100/1是备份解析结果，不是当前库计数。
- **当前库证据边界**：JSON没有持久化`current`、`field_comparison`或`row_fingerprint`对象，不能把备份聚合当作当前库聚合或最终比较结果。
- **控制流归因（非新增采集）**：本地采集器SHA256与JSON的`collector_sha256`一致。`/tmp/prism-optimization-20260906/compare_project161_backup_readonly.py:236`仅在全部备份行检查完成后写入`parser`；随后`current_project`在当前schema、nullable、字符集、精度和行数门控后，于`:260`规范化当前161行触发格式阻塞。由此更正为当前行规范化受阻，而非备份解析受阻；不猜测未保存的具体日期格式。
- 字符串按已确认utf8mb4及MySQL反斜杠规则解析；支持引号加倍、NULL、整数字面量，未支持语法直接阻塞。

## 保留边界

- 生产release账本仍匹配343616d9：True。
- 不参与本地R05修改，不重查89表，不取回dump，不输出源码、项目说明、环境或凭据，不登录或调用真实模型。
- 既有阶段报告保持：True；本次两份补证文件均0600。
- 本次归因校正只使用既有本地JSON和SHA一致的采集器，未再次SSH或读取备份，未改旧阶段报告。
- 规范化保留字符、空白及NULL语义；整数按精确十进制；DATETIME(0)验证后原样使用，不做时区或Unicode转换。
