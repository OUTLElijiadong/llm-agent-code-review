# 生产发布与数据独立只读复核

## 结论：通过（限定本次只读发布、结构、聚合与测试来源核验）

- 发布提交：`343616d9d40c1e44e17c7da465c7173c418debb5`；版本：`3.8.4`。
- 生产数据事务（UTC）：`2026-09-05 19:52:07.375710` 至 `2026-09-05 19:52:10.539339`。
- 四项目补证事务（UTC）：`2026-09-05 20:04:11.021271` 至 `2026-09-05 20:04:11.077268`。
- 核验范围内无失败项。旧计数日志采集身份仍有证据缺口；登录业务全量验收未测。四项目来源已逐项补齐，不沿用旧报告的整批推断。

## 一、四项目逐项来源判断

| 项目ID | ID/owner/精确名/说明 | 单文件及相对路径 | 完整源码指纹 | 时间与任务文件关联 | 判断 |
|---|---|---|---|---|---|
| 24 | 全匹配 | 1文件，固定路径匹配 | 1079字节，SHA256匹配 | 名称epoch时差1秒；项目至文件/测试任务整数秒时差0；精确关联匹配 | **可证明** |
| 25 | 全匹配 | 1文件，固定路径匹配 | 1079字节，SHA256匹配 | 名称epoch时差1秒；项目至文件/测试任务整数秒时差0；精确关联匹配 | **可证明** |
| 27 | 全匹配 | 1文件，固定路径匹配 | 1079字节，SHA256匹配 | 名称epoch时差1秒；项目至文件/测试任务整数秒时差0；精确关联匹配 | **可证明** |
| 26 | 全匹配 | 1文件，固定路径匹配 | 1079字节，SHA256匹配 | 名称epoch时差1秒；项目至文件/测试任务整数秒时差0；精确关联匹配 | **可证明** |

- 四项目的服务器内源码SHA256均为：`5058a415e5bf92ea54204f5e691f3941f3d761d0a280c4b5cbb071dc8ea9f4e9`。
- 对照基准为精确创建脚本内嵌的固定请求内容，通过AST读取而不执行脚本；当前release与历史ae712da提交的该内容逐字节相同。旁边fixtures文件与实际请求内容不同，没有用它替代正确基准。
- 相对路径依据历史创建服务对缺省file_path使用file_name的规则，项目说明使用脚本固定值的哈希精确比较；没有取回源码、项目说明或这些数据库字段原文。
- 时间核验使用UTC-naive及完整名称中的精确epoch，并与既有独立历史快照创建时间逐项相等；没有把前缀匹配当唯一依据。项目至文件、至测试任务的TIMESTAMPDIFF(SECOND)均为0，只表示非负间隔不足1秒，不表示原始时间值相同。
- 项目26另有历史验收明确记载项目—文件1450—任务53，服务器精确关联计数为1。24/25/27判为固定测试数据来源可证明，不声称找到其原始HTTP会话或操作者身份。
- 补证快照中四项目均active、尚未quarantined；7条审查任务为6 success、1 failed，无其他状态。此为快照事实，不代替执行瞬间的可逆隔离门禁。**本代理未执行apply，项目161不在逐项范围。**

## 二、版本、实际镜像与迁移

| 检查 | 独立结果 |
|---|---|
| 公网healthz / readyz | 均HTTP200；ok / ready；均3.8.4且release完整SHA一致 |
| 服务器Git与发布账本 | 完整SHA一致；账本发布时间UTC 2026-09-05 19:36:21 |
| Backend实际imageID | `sha256:ea9eea7997be8ab879b6ddba16bd97a80efd69c9d936dc68b524ee9aedde6cdc`；与账本及release tag对象一致 |
| Frontend实际imageID | `sha256:47f434723751b5e0e9704cd8b212e47d9185a6e483675b9173b9d4e6547c582b`；与账本及release tag对象一致 |
| 前端实际版本产物 | buildInfo只有版本3.8.4；服务路径与镜像原产物字节相同；index亦相同 |
| 迁移头 | 唯一`047_review_input_snapshot`，与账本一致 |
| 四新增列 | version_no:int、content_sha256:varchar(64)、file_snapshot:json、coverage:json；均nullable=YES |

前端JS没有release SHA字面量，两个镜像OCI版本标签为空；不把这些缺席项冒充版本证据。这里用实际imageID—release tag—账本绑定提交，用实际buildInfo字节核验版本。

## 三、数据库聚合与完整性

在生产库身份匹配、REPEATABLE-READ、read_only=1的单个一致性快照内，对89张InnoDB基础表逐表COUNT(*)，不是information_schema估算。分组结果与同事务表总数一致。

| 表/对象 | 总数 |
|---|---:|
| `user` | 39 |
| `project` | 100 |
| `code_file` | 1771 |
| `code_version` | 1773 |
| `review_task` | 146 |
| `review_task_file` | 2185 |
| `review_issue` | 1201 |
| `review_report` | 72 |
| `ai_call_log` | 9532 |
| `agent_team` | 63 |
| `agent_team_task` | 197 |
| `agent_job_run` | 54800 |

- 17项核心逻辑关联、1项数据库实际外键：全部孤儿数0；任务文件和问题文件跨项目关联均0；活动文件当前版本缺失0。
- 项目为29 active、71 deleted，包含四个尚未隔离的测试项目；文件/任务/问题表总数未按有效业务项目、登录用户权限或删除状态过滤。
- 1201是标准问题明细数，不等于全部审查领域发现；9532是AI日志行数，不证明实际模型调用/计費完整性。
- 历史2185条任务文件的三列快照字段均NULL，146条任务coverage均NULL，符合不虚构历史输入的迁移边界；新任务写入行为未在本次执行。

## 四、发布前累计计数口径

| 表 | 原日志 | 独立快照 | 差值 |
|---|---:|---:|---:|
| `review_task` | 146 | 146 | +0 |
| `agent_team` | 63 | 63 | +0 |
| `agent_team_task` | 197 | 197 | +0 |
| `agent_job_run` | 54742 | 54800 | +58 |

审查任务、团队、子任务与调度执行记录不是同一种实体，不能相加当独立业务任务；调度增长58（成功57、失败1）只作时点差异记录，不作原因归因。原日志本身缺SSH/SQL/事务时间证据，该缺口保留；本次新快照则有独立连接身份与查询摘要，不反向伪造原日志来源。

## 五、发布证据与fresh备份

- 本地后端JUnit重新计数2782通过、0失败/错误/跳过；前端575断言、77文件通过。
- 两份SHA256SUMS共47条，逐一读取指定release Git blob，路径和原始字节哈希47/47一致；测试XML/JSON也与release中的版本相同。日志尾部空白原样保留。
- fresh备份：`/opt/code-review/backups/code_review_20260905T193203Z_343616d9d40c.sql.gz`。
- 独立读取字节数：**402690439**；SHA256：`f3dfe91bacb6d0953d8119273fe2f3c127362822923488876f1eff677c25813c`。
- 两值与主线程后续提供值完全一致。89表隔离恢复是主线程完成的验收，本代理未复跑恢复。

## 六、完整通过/失败/阻塞边界

| 核验门 | 判定 |
|---|---|
| 本地测试及47条发布证据闭环 | 通过 |
| Git账本及公网版本一致 | 通过 |
| 实际镜像ID与账本及release标签一致 | 通过 |
| 后端运行版本及前端构建版本一致 | 通过 |
| 补证期间账本与运行镜像稳定 | 通过 |
| 047版本及四列类型nullable一致 | 通过 |
| 89表精确计数及分状态对账 | 通过 |
| 一致性只读事务身份 | 通过 |
| 17逻辑及1实际外键无孤儿 | 通过 |
| 跨项目关联及当前版本无缺失 | 通过 |
| 四项目固定测试来源逐项可证明 | 通过 |
| fresh备份哈希字节双侧一致 | 通过 |
| 采集无未恢复错误 | 通过 |
| 原pre-deploy累计日志采集身份 | 阻塞：原始证据未包含连接/SQL/事务时间 |
| 登录后权限正向、全按钮、真实模型和全量业务真实性 | 未测：不属于本次只读证据 |
| 隔离apply | 本代理未执行；不把来源结论当执行成功 |

## 七、操作与证据边界

- 当前可证明的是四项目与固定AC2测试样例的来源关联；24/25/27没有找到各自原始HTTP创建回执，不重建具体操作者或某一次脚本进程。此缺口不冒充已经补出的证据。
- 本地precheck保留其生成时的三项目阻塞，作为阶段记录不回写。后续完整内容指纹、路径、说明、精确身份和时间关联已逐项补证，以本报告来源矩阵为后续结论。
- pre-deploy-task-counts-and-disk.log本身仍缺采集身份、SQL和事务时间；新的独立生产快照有明确SSH目标及只读事务证据，但不反向伪造旧日志的来源链。
- 四表处于不同层级，不能相加当独立业务任务数。agent_job_run为累计调度执行记录，不是人工审查、独立用户任务或AI调用次数；没有时间窗，不是今日数量。
- 调度累计由54742变为54800，其中success增加57、failed增加1；仅说明两份快照数值差异，未将差异归因于本次发布、特定任务或数据损坏。
- 生产数据快照固定于所列UTC时间。四项目来源补证为稍后的独立只读快照；本报告不把不同时间查询合并声称一个事务。
- 前端buildInfo产物仅包含版本3.8.4，没有release SHA字面量；源码仅声明APP_RELEASE_SHA而未作为页面消费者使用。因此前端release绑定使用账本、实际imageID和tag对象，未伪称前端JS独立自报SHA。两个镜像未提供OCI版本标签，该项不冒充已验证证据。
- 047四列允许NULL符合迁移设计；2185条既有任务文件记录的三列均NULL，146条既有任务coverage均NULL。没有伪造历史快照；本次未新建任务，未验证新任务快照写入功能。
- 全部89表COUNT与核心孤儿检查不等于逐条业务真实性、全角色全按钮、登录正向能力、真实扫描/模型用量或全部业务链路验收。无认证浏览器，相关业务功能未测。
- 只读取既有备份字节及哈希，没有解压取回数据行，也没有运行恢复；主线程89表隔离恢复成功属于主线程证据。本代理没有重新运行ops-check。
- 本代理不执行隔离apply、不创建或登录账号、不生成JWT或密码、不调用真实模型、不修改生产或主仓库；不运行E2E创建脚本。项目161不在逐项来源核验或隔离范围。

首轮SSH采集曾因账本相对基目录判断错误在读取数据库前退出；通过只读存在性复现和deploy.sh确认后改用deploy/.releases/current.env，最终采集无未恢复错误。没有修改生产文件。

对应JSON包含完整89表聚合、各状态求和、全部关联检查、逐项目白名单布尔值/哈希/时间差与输入证据哈希；文件权限0600。
