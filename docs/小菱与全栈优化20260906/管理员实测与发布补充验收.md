# 管理员实测与发布补充验收

## 本轮范围与事实边界

用户提供管理员登录并明确继续、修复后发布。沿用现有执行共识，覆盖本次实际复现的管理页面显示、仪表盘数值闭环、运维失败反馈及此前已测试但未发布的团队终态修复。保留 MySQL、FastAPI、Vue 与原部署体系，不新增模型、预算策略、运行时动效库或数据库迁移，不回填历史任务为成功。

生产观察使用真实管理员 UI、只读数据库查询、宿主运维账本与真实 Agent 工具回执。测试目录的接口夹具仅用于隔离回归，不进入生产业务。未逐条证明所有历史内容的现实来源，不能把数据库中存在等同于来源已经证实。

## 已复现并修复

| 问题 | 原因 | 修复与验证 |
| --- | --- | --- |
| 六个治理页面直接显示表达式 | Vue 表格插槽误写单大括号 | 改为插值；真实 Element Plus 表格单元格、未知值回退与不可信文本回归 |
| Agent 刷新缺少完整反馈 | 首次加载、刷新失败、空数据未充分区分 | 显示加载状态、防重复操作、失败保留上次数据，显式重试与空态分别处理 |
| 问题总数与图表不相等 | 只绘制固定八维，未归类记录参与总数但未入图 | 未知类型合并为“未归类”，图、图例、总数使用同一聚合；实测数据分解 295 = 60 + 235 |
| 运维失败只有笼统错误 | 服务错误摘要没有保留实际阻断项，副驾驶丢弃失败数据 | 统一保留已知阻断项中文名称和标识，审计与回复一致；未知项不编造、不透出，失败状态与禁止继续不变 |
| 运维文档与脚本不一致 | README 仍推荐前后端拆分部署 | 文档改为仅允许同一 SHA 全量发布，与既有脚本一致 |

宿主账本在 UTC 2026-09-06 02:21:02.940348 记录过 `https` 实际失败，`can_continue=false`、命令退出码 1。邻近时刻成功不抵消该失败；本次不宣称已经证明更深层网络或证书根因，也不通过放宽 HTTPS 门禁掩盖它。

## 本地门禁

- 后端全量：2828 通过，0 失败、0 跳过；1 条既有测试类收集警告。
- 前端全量：79 个测试文件、648 项通过，0 失败、0 跳过。
- 定向：治理与 Dashboard 73 项，运维与副驾驶 37 项；属于全量子集，不重复累加。
- 本轮 Python 文件 Ruff 通过；本轮 Vue/TypeScript 文件 ESLint、全前端类型检查通过。
- 生产构建、部署脚本回归、独立复核与发布证据在本轮完成后补录。未完成项目不得记为通过。

原始测试记录：`/tmp/prism-readonly-20260905-tests/admin-final-backend-full-20260906.xml`、`/tmp/prism-admin-acceptance-20260906/final-validation/frontend-full.xml` 及各自日志。

## 生产真实验收

- 管理员成功登录；已有 26 次页面观察覆盖 24 个唯一后台路径，不等于所有按钮与全部权限分支通过。
- 模型连接测试通过，返回实际模型与耗时；已保存凭据不可用时的系统默认回退警告保持如实显示，未改写模型设置。
- 真实管理员 Agent 运维运行：8 步，7 成功、1 失败；动作属于只读查询，未执行变更、扫描或审批。会话与审计正常落库不等于数据库物理零写入。
- 35 条 Agent 治理配置不等于 35 个运行实例；32 份静态职责契约不等于 32 个进程。
- 审查入口选择真实项目后展示有效非空文件数；指定文件模式为空时明确提示并禁用开始按钮，未发起付费审查或制造测试项目。
- UTC 2026-09-06 15:11:38 发布前巡检通过：生产仍为 3.8.4 / `343616d9d40c1e44e17c7da465c7173c418debb5`，047 迁移、容器、备份、HTTPS 与默认发布绑定均通过。

## 发布约束与待补证

仅发布干净源码的精确 Git SHA。发布脚本必须新建一致性备份并验证隔离恢复，前后端同一版本构建与切换，同源 HTTPS、健康检查及 assets 验证失败则触发应用回滚；不自动恢复或降级数据库。

普通成员/无权限账号、跨组织正反权限、所有按钮、长 SOP 故障恢复、真实多进程故障注入尚未完成。本轮未实现根任务用量外键归因、持久 SOP 和新调度架构；历史 task11/team5、task35/team12 未改库。项目 161 完整前后字段证据不足，保持未证明状态。

## 最终发布结果

待精确提交、部署及公网浏览器复验后补录。本段为空缺不代表发布成功。
