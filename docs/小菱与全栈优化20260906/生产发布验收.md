# 生产发布验收：3.8.4

## 发布结论

已完成旧修复的本地 Git 提交、精确版本传输和真实生产部署。发布成功不等于全部账号、全部业务和真实模型闭环均已验收；未执行项保留为阻塞，不用匿名健康接口替代登录后测试。

| 项目 | 实际证据 |
| --- | --- |
| 目标 | 既有生产服务器 `/opt/code-review`，公网 `lijiadong.cn` |
| 本地既有分支 | `codex/production-resilience-20260901` |
| 业务代码修复提交 | `83e4e9eee94556bb4d98b0aac5321ca476bd6b60` |
| 生产发布提交（含完整证据） | `343616d9d40c1e44e17c7da465c7173c418debb5` |
| 版本 | `3.8.4` |
| 传输方式 | Git bundle，未创建新分支、未向 Git 远端推送 |
| 传输包大小 | `4542987` 字节 |
| 传输包摘要 | `dc2120eec64e675f65a9b21d6c87414fea8b7dc7f730b7241bc7fea607d64a95` |
| 发布完成时间 | UTC `2026-09-05 19:36:21`，对应 UTC+08 `2026-09-06 03:36:21` |
| 后置巡检时间 | UTC `2026-09-05 19:37:32` |
| 后置巡检 | 顶层 `status=ok`，无阻断项；不是只检查命令退出码 |
| 生产 Git 状态 | 精确发布提交，工作树干净 |
| 数据库迁移 | `047_review_input_snapshot`，当前版本与唯一 head 相同 |
| 公网 | `healthz=ok`、`readyz=ready`，均返回 `3.8.4` 和同一发布 SHA |

未上传用户根目录 `.coverage 2`，未将旧只读审计中的数据库副本加入 Git。新动效库、React/Remotion 及未通过验证的架构修改未进入本次发布。

## 备份与回退

发布前新备份：服务器 `/opt/code-review/backups/code_review_20260905T193203Z_343616d9d40c.sql.gz`。

- 实际大小：`402690439` 字节。
- SHA256：`f3dfe91bacb6d0953d8119273fe2f3c127362822923488876f1eff677c25813c`。
- UTC `19:34:24`，独立 `cr_testdb` 恢复验证通过，89 表、备份时迁移 `046_finding_aggregation`；没有拿“压缩包可读”冒充恢复验证。
- 原应用回滚版本 `4b1711adf0e79a95eed35be3ab17605fe10869cf` 的前后端镜像保留；本次没有在真实业务库执行 downgrade/restore 演练。
- 047 为新增可空快照/覆盖字段，没有补造历史快照；回滚应用与降级数据库是两件事，不能因应用退回而删除新增历史证据。

生产 `.env` 在发布前后各备份一次，只校准 `APP_RELEASE`、`APP_VERSION`、`BACKEND_RELEASE`、`FRONTEND_RELEASE`，逐字验证其他内容不变并保持原文件权限。备份留在服务器限制访问目录，没有导出凭据。发布脚本自身只告警默认配置漂移，因此部署结束后另行完成校准并重跑巡检；最终状态不是带漂移的中间状态。

只清理超过 48 小时的构建缓存，Docker 报告释放 `18.01GB`；没有删除业务卷、数据库、运行容器或回滚镜像。两张原版本镜像的 ID 清理前后相同。磁盘使用率从发布前约 84% 降至清理后 73%，构建发布后 74%。

## 已执行验证

| 层级 | 内容 | 结果 |
| --- | --- | --- |
| 本地后端 | 2782 项测试 | 通过；不是生产模型调用测试 |
| 本地前端 | 77 文件、575 项测试 | 通过 |
| 发布链 | 19 项反例红绿回归及完整 Shell 回归 | 通过 |
| 类型与编译 | TypeScript、前端 ESLint/构建、运行时 Ruff、Python 编译、OpenAPI 基线兼容 | 通过；历史脚本/警告例外见旧验收报告 |
| 真实生产部署 | 备份恢复、迁移、服务、资产同步、镜像及默认环境一致性、同源 HTTPS | 通过 |
| 匿名生产 API | 未认证访问 auth/me、projects、reports | 均返回 401，没有绕过认证 |
| 生产浏览器 | 新版登录页、真实构建版本、删除虚假在线/速度宣传、必填输入反馈 | 通过 |
| 响应式 | 1280 宽页面、390 截图、320 DOM 宽度 | 均无所测页面横向溢出；只覆盖登录页，不扩展为全站手机验收 |
| 真实角色矩阵 | 管理员、普通成员、无权限账号的业务页面/按钮/隔离 | 阻塞：未提供已登录会话 |
| 真实模型闭环 | 工具调用、审批、团队执行、SOP 完整业务链 | 未执行；不能以 Mock 或 401 宣称通过 |

## 数据审视边界

发布前只读查询中，审查任务共 146 条（成功 111、失败 20、取消 5、删除 10），没有运行中的审查任务；Agent 团队均为终态，但有 1 条排队子任务和 1 条依赖等待子任务。它们需要结合归约/恢复代码继续核验，不因本次服务健康而自动视为已修复。

两种 utf8mb4 排序规则依然存在；临时跨表 UNION 的 1271 错误通过分表 SELECT 完成统计复核。它证明数据库规范存在漂移，但不是已经复现生产业务接口的同类故障，更不能直接对全库批量改排序规则。

本报告不证明论坛、Agent 记忆、知识内容和全部历史记录的现实来源；明确可证实的 E2E 项目与来源不明项目分别处理。精确隔离与独立复核结果在后续小节补记，不把 dry-run 写成实际修改。

## 原始记录

- `/tmp/prism-release-20260906/production-deploy.log`
- `/tmp/prism-release-20260906/production-preflight.log`
- `/tmp/prism-release-20260906/production-postflight.log`
- `/tmp/prism-release-20260906/post-deploy-ops.json`
- `/tmp/prism-release-20260906/pre-deploy-task-counts-and-disk.log`
- `/tmp/prism-release-20260906/public-3.8.4/http-checks.tsv`
- `/tmp/prism-release-20260906/production-login-3.8.4.png`
- `/tmp/prism-release-20260906/production-login-390.png`
- `/tmp/prism-release-20260906/production-mobile-320.json`

归档副本和独立复核在最终交付前追加，不覆盖原始记录。

## 测试项目可逆隔离与新增诊断

四项目的来源最初并非逐项充分：独立前置复核只直接证明26，随后对24/25/27补齐精确身份、固定说明/路径、时间及任务关联。四个文件均1079字节，服务器内源码SHA256均与真实创建脚本固定请求内容匹配：`5058a415e5bf92ea54204f5e691f3941f3d761d0a280c4b5cbb071dc8ea9f4e9`。没有按名称前缀直接决定隔离，没有取回数据库源码正文。

再次生成隔离前新备份 `/opt/code-review/backups/code_review_20260905T201610Z_343616d9d40c.sql.gz`，实际 `402726075` 字节、SHA256 `7bb99942cc0e571008743fc66b0d1cdd88fcf13640579b32fb493c0679d7dc6f`。UTC 20:18:33 独立恢复89表及047通过，随后原有可逆工具在单事务内精确处理24/25/26/27，返回 `committed`，只改 status，保留时间戳及关联数据，不处理161。

- 操作标识：`874c7f1be92b4c338c93d232389c1b2e`。
- 服务器恢复清单：`/opt/code-review/backups/release-20260906/quarantine-24-27/operation-20260905T201834Z.json`。
- 清单SHA256：`d36ca7e949035d7310e563a96fd1919519d5a45fb95f2175d8abb4a21133b59a`；同目录 `.result.json` 为完成回执，`.audit.jsonl` 为阶段审计。
- 回退应通过原脚本 restore、原清单与独立保管的摘要，重新核对快照和精确目标；不直接对全库执行恢复，不覆盖后续修改。
- UTC20:25:14，主线程再次执行生产巡检，顶层及release项均ok，047/服务/公网通过。

UTC20:18:26 的新增只读 JOIN（REPEATABLE-READ、read_only=1）确认：子任务11属于已 failed 的团队5，但仍 queued；子任务35属于已 failed 的团队12，但仍 waiting_dependency。对应事件类型也已只读聚合。这是确实存在的状态不一致，未将历史任务改成功，未触发模型或重试；归入下一批父子终态修复。它不证明全部历史成因已经查明，也不能因本次发布成功而忽略。

## 隔离后独立复核结果

UTC2026-09-05 20:48:40，另一子代理完成有界后验：manifest与committed回执及新备份摘要匹配；24/25/26/27的当前status为quarantined，所有项目字段除status外与清单before一致，create_time/update_time、1079字节固定源码指纹和已知关联保持。六表前后计数分别为project100、code_file1771、code_version1773、review_task146、review_issue1201、review_report72。该结论证明这些已检字段和行数，不把行数相等扩大成所有关联行内容逐字节未变。

10项有界检查中9项通过；项目161全字段不变的证明仍阻塞，因为缺少完整变更前快照。它不在清单或committed回执目标中，当前仍active且历史已知身份相符，但不能据此补写其说明、语言、更新时间均已独立证明未变。没有为补齐证明再次恢复或创建数据库，原始报告另存证据目录。

此后本地仪表盘和团队终态补丁属于下一批，不能引用本报告的3.8.4生产结果为新补丁背书。
