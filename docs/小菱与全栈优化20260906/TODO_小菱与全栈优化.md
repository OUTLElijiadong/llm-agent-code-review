# 小菱与全栈优化：下一步

## 用户需要提供

1. 在已打开的真实生产页面登录管理员账号，完成后告知“已登录”；不要把密码发到报告或代码里。随后分别使用普通成员和无权限账号完成角色矩阵，不以管理员代测全部权限。
2. 在已打开的独立动效图库选择喜欢的效果，发送精确调用码；选择前不会将示例视频、原库源码或未授权素材放进 Prism。

## 建议执行顺序

当前本地候选已完成仪表盘、团队终态和契约生成修复，后端2820/前端620通过；生产仍3.8.4。先完成真实登录后的本批验收，再按精确候选提交发布，不能跳过门禁。其余优化仍按下列顺序，不把所有权/SOP/预算列为已实现。

1. 先修父子终态、执行所有权、未知工具结果核账和停止确认；保留已完成部分，不将未知结果改为成功。
2. 再定义执行完成与需求验收的区别，冻结 SOP/嵌套版本，将需要持久化的工作流映射到现有 DAG。
3. 补根任务调用关联和可信用量，再谈统一预算；沿现有线程池实现增量补槽，不承诺没有测量的提速/省费比例。
4. 统一小菱/管理员/团队视图的状态反馈，先解决成员 Dashboard 未知与零的区别，再按选定位置加入轻量动效。
5. 用隔离环境测试每项异常和恢复；通过后分批发布，每批单独保留迁移、回退及真实生产验收。

## 不应直接执行

- 不按名称模糊清库；不处理来源未确认的项目161。
- 不自动改变旧 SOP 为真实副作用执行，不默认允许失败分支之外的任务继续，也不替用户设置新的资金/Token 上限。
- 不按看到的父子计数直接 UPDATE 历史状态；先看逐条关系和事件，再给修复预览。
- 不把本轮定向审视称为完整逐行审计，不用历史测试数或隔离模型替身给生产业务签收。

生产发布和本次规范化的最终状态以验收文档收尾小节为准，早期独立报告保留其采集时点，不倒改证据。
