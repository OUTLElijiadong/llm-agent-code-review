# 小菱临时团队与账号隔离：交付结果

## 生产状态

功能版本 **4.0.1** 于 **2026-09-23 09:25:19（UTC+8）** 发布，完整提交为 `20a2ff6bc79b11c8c36d867d87218ffbd96f0321`。前后端以同一提交构建和发布，公网 `/healthz` 与 `/readyz` 分别返回正常和就绪，版本与提交一致。GitHub 分支已推送，`v4.0.1` 标签指向该功能提交。后续验收文档提交不代表运行镜像变化。

最初核对的生产版本确为 3.9.14，随后上一轮已发布 4.0.0；本次是在该基础上完成用户后续确认的临时成员、私聊隔离与形象需求。不能把最初版本当作当前状态。

发布前生成新备份并在独立数据库中恢复 **100 张表**，迁移版本为 `056_older_run_titles`。旧应用镜像保留，回滚不自动降级或恢复生产数据库。首页及五个入口静态资源均返回 200，留存字节数和哈希。

独立生产只读核验 **27/27 通过**；60 项生产运行依赖与锁匹配，AnyIO 为 4.14.2，后台消息及团队消费者均实际启动。备份大小为 **416038987 字节**，压缩和哈希校验通过，恢复库已清理。运维汇总为非阻断降级：磁盘使用率 **85%**、允许继续、阻断项为零，其他检查正常。未擅自清理旧镜像或备份。

## 已交付内容

- 小菱可复用已有成员、生成独立职责的临时成员，并组成混合团队；无依赖工作节点并行，已有报告成员等待全部依赖后汇总。
- 临时定义、校验和、执行租约与结果保留审计；仅当前账号、当前团队任务有效，终态拒绝复用，不加入永久 Agent 或版本库。
- 临时成员进行有界只读专项分析。正式全项目代码审查、安全审计和业务写操作继续由主小菱的完整内置工作流及审批链执行，保留原账号权限。
- 聊天、草稿、事件、附件、团队、追问、私人知识、审批和关联日志按账号隔离；防止管理员旁路、关键词检索侧漏、换号迟到响应与旧图片缓存回流。
- 小菱重绘为圆润菱形精灵，提供五种状态和减少动画支持。上一轮侧栏折叠、中文底边和独立操作列修复继续包含在当前版本中。
- 修复临时分析重复汇总、无效模型证据仍计成功、运行期间撤权未重新取数，以及安全审计成员绕过统一权限包装的路径。
- AnyIO 升至 4.14.2，修补三项已知依赖漏洞，生产及开发锁只调整该包版本和官方哈希。

## 验收证据

| 项目 | 结果与边界 |
| --- | --- |
| 后端精确依赖全集 | 93 项锁定版本匹配；4487 通过、0 失败、5 跳过，共 4492 项 |
| Redis 专项 | 上述五项另有隔离 5/5 通过证据，相关业务与测试哈希未变；不把全量跳过改写成通过 |
| 前端清洁安装全集 | 1277/1277 通过，116 个测试文件；lint、类型检查与 4.0.1 构建通过 |
| 真实模型混合团队 | 隔离环境 19/19 检查通过；两个独立 HTTP 请求重叠 5.760290 秒，3+2 条发现精确汇总为 5 条 |
| 临时成员持久化 | 独立模型实验前后永久 Agent、版本、正式任务及报告新增量均为零 |
| 依赖审计 | 63 个后端审计记录及前端生产依赖未发现已知漏洞；不代表软件不存在其他漏洞 |
| 规范与编译 | 本轮变更及全部后端业务源码 Ruff 通过，Python 编译和唯一迁移头通过 |
| 公开提交 | 97 个功能提交文件经独立复核，无原始证据、截图、私有配置或真实密钥混入 |

旧开发虚拟环境存在依赖漂移，已另建严格按哈希锁安装的环境完成全量，未用旧环境结果替代。完整后端目录另有 154 条既有辅助脚本规范问题，与基线逐项相同，零新增；未纳入本次无关批量修改。原始失败、成功、模型和生产证据保留在本地证据目录，不提交至公开仓库。

## 仍需完成的生产登录补验

本轮浏览器控制通道持续超时，历史验收账号均已停用。未擅自启用账号或绕过认证。已向用户请求恢复生产登录会话或提供现有效验收账号的凭据文件位置。

因此，**当前版本登录后的自然语言建队入口及浏览器交互补验尚未完成**。已有本地界面/隔离真实模型证据和本次生产健康、资源检查不能替代该项。正常登录、合成文本自然语言建队、三任务终态、会话回投及生产只读对账脚本已准备，可从此处继续，不需要重复发布已验证的功能提交。

## 偏好知识库

用户明确确认的中文底边对齐、独立操作列、圆润菱形形象、复用或临时生成成员、生命周期和私聊隔离已记入主库。早期“仅运行已有成员”约束已明确标为被后续要求替代，避免检索时误用。工程选择与用户个人偏好分开记录；知识库最终校验和闭环收据另附。

最终知识库校验检查 362 份文件，0 错误、0 警告；闭环收据为 `任务闭环-20260923-013411.md`。收据中的治理检查通过不代表尚缺会话的生产业务补验通过。
