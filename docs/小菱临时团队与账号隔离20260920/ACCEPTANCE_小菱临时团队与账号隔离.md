# 小菱临时团队与账号隔离：验收记录

## 已完成

- 已有、用户自定义及按任务临时生成的成员可混合编组；无依赖节点并发，最终汇总依赖全部工作节点。
- 临时定义冻结、完整性校验、同账号同团队有效租约和终态拒绝复用；模型开始和返回前检查当前资料权限。
- 新私聊笔记进入个人知识库，旧聊天知识、审批、原文日志、附件、事件、追问及团队逐层账号隔离；不开放管理员旁路。
- 本人业务权限、已有审批链和正式审查审计工作流继续可用；安全审计复用 security:scan 权限包装。
- 菱形精灵图标重绘、减少动画支持；换号异步响应失效、图片缓存失效与布局验证已完成。
- 审批与团队接口定向加既有回归 50 项通过；其他模块详细红绿证据见各子报告。
- 前端在清洁锁定依赖下 1277 项全部通过，116 个测试文件；lint、类型检查和 4.0.1 构建通过，源码指纹一致。
- 隔离环境真实模型混合团队 19/19 检查通过：两次 HTTP 重叠 5.760290 秒，3 条源码发现和 2 条一般发现准确汇总为 5 条，永久 Agent 和版本新增量为零。
- 修补 AnyIO 至 4.14.2，生产及开发锁哈希经独立校验；前后端生产依赖审计未发现已知漏洞。

## 当前验收门禁

后端以两份哈希锁安装的独立环境完整验证：4487 项通过、5 项跳过、0 失败。93 项依赖版本精确匹配，compileall 通过，Alembic 唯一 head 为 `056_older_run_titles`。Redis 跳过项目另有隔离补验，详见后端最终复核报告。

精确提交生产发布及上线后复核仍进行中。不得将本阶段结果表述为生产已发布。失败首轮和环境原因均保留在本地证据中，私有原始证据不进入公开仓库。
