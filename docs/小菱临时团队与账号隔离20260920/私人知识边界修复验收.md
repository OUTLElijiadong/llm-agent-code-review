# 私人知识边界修复验收

日期：2026-09-20。阶段：本地修复和相关服务回归；无生产数据库操作。

## 精确复现

两账号与超级管理员的真实隔离 SQLite 数据：同一个 manager Agent 下建立本人聊天笔记、找不到原运行的孤儿笔记、平台共享手册。对统一召回、管理列表、激活入口、新聊天笔记持久化及同call重试进行14项断言。旧版13失败、1共享/本人正常激活通过。首轮新工具测试漏传mcp_provider的夹具错误已修正重跑，保留的有效红测是全部业务断言失败结果。

## 修复

新 save_knowledge_note 继续走现有写审批，调用已有 knowledge_service.add_document，写入当前 user_id 的个人知识库；返回 owner_type=user，审计对象改为 knowledge_doc。来源含 run_id+call_id；超过个人库64字符字段时用稳定哈希压缩，replace_existing=True仅替换同账号同调用的活动笔记，不把同轮不同笔记互相覆盖。

旧 AgentKnowledgeDoc 的 response_run: 前缀是私人来源，SQL先按 AgentResponseRun.user_id 过滤，再排序和limit。历史已存在的格式 response_run:<run_id> 必须精确匹配；孤儿/畸形引用默认隐藏。管理员没有豁免。列表、统一召回、激活均复用同一 SQL 条件；未知调用者只能读共享来源。新增保留前缀来源时拒绝伪造别人的运行引用。非私人平台共享来源保持原行为。

前端个人知识库新增“小菱笔记”中文来源；同路由账号或token切换清空文档、统计、检索词/结果和表单。迟到请求不能覆盖新账号；旧删除确认会关闭且不会以新账号发起删除。AI日志、系统审计、策略决策与工具调用日志对 content_redacted 明示“原文按账号隔离”。

## 验证与边界

- 新知识边界14/14通过；知识笔记本工具、治理服务、治理API完整相关组合59/59通过，3项为现有依赖弃用警告。
- 系统审计界面2红→2绿；知识页2红→2绿，另补删除弹窗关闭1红→绿；策略/工具真实Element Plus表格2红→绿，与相关UI组合31项通过。
- 最终前端全量/lint/build统一记入[前端验收清单](证据/前端/前端验收清单.json)，以最终文件为准。
- 发现的旧 knowledge.activate 审批标题/原文旁路已交主控修复。其独占 approval_service，本文不把尚未独立核验的审批改动宣称通过。
- 平台全部知识重嵌入是既有唯一超级管理员系统操作；本补丁不修改其执行语义，也不把管理员统计可见等同于私人原文可见。

未引入新数据表，未把历史数据删除或迁移。历史私人 Agent 笔记由原账号通过既有 Agent 知识读侧继续使用；本次之后新增笔记直接进入个人知识库，用户可按既有个人知识入口查看和删除。
