# 生产版本与备份独立复核

观察时间：二〇二六年九月二十三日 01:26—01:28 UTC。发布功能提交：`20a2ff6bc79b11c8c36d867d87218ffbd96f0321`。版本：`4.0.1`。复核者独立执行只读命令和 HTTP 检查，没有写入生产业务、执行迁移、再次恢复数据库、登录停用账号或清理历史镜像。

## 已核实结果

生产版本与健康探针共 27 项检查全部通过。仓库提交、版本文件、发布账本、后端实际 `APP_VERSION`/`APP_RELEASE` 均指向上述功能版本。前后端运行中的不可变镜像 ID 与本次发布记录和精确 SHA 标签一致，两容器均健康；没有未完成发布记录或维护锁。

公网 `/healthz` 和 `/readyz` 均返回 200，并分别返回 `ok` 和 `ready`，版本与提交一致。容器内及公网转发的 API 文档入口均为 404。Alembic 唯一头、发布账本及实际数据库版本一致，均为 `056_older_run_titles`。

运行镜像内 60 个生产依赖逐项与生产锁相同，锁文件 SHA-256 与本地发布候选一致；AnyIO 实际安装版本为 4.14.2。这里的 60 项是生产依赖，不能与本地完整开发测试环境的 93 项混淆。

后端运行命令显式使用 `--lifespan on`。当前容器日志中，01:23:04.920 UTC 记录 Mesh 消费器启动，01:23:04.921 UTC 记录团队消费者启动，随后记录应用启动完成。没有团队消费者被配置禁用或生命周期不受支持的提示。该项证明启动链真实执行；具体生产团队的模型并行和业务结果另由生产团队验收证明。

## 备份与回滚

发布前备份为 `code_review_20260923T011858Z_20a2ff6bc79b.sql.gz`，大小 **416038987 字节**。重新读取文件计算 SHA-256，与校验侧文件及备份元数据三者一致，gzip 完整性检查通过。

独立重算部署日志中恢复表数为 **100**，与备份元数据 **100** 相等，恢复后的迁移版本同为 `056_older_run_titles`。执行恢复的验证容器与生产数据库容器 ID 不同；再次只读查询验证库，`prism_verify_%` 临时数据库数量为零。部署日志两个本地副本逐字一致。此次仅验证已有恢复结果，没有额外恢复数据库。

上一版本发布记录保留为 `4.0.0` / `e153e938cee413298220a1aa30f838aac36cd015`，上一版前后端镜像仍存在，且镜像 ID 与上一发布账本一致。

## 运维状态与证据边界

只读运维巡检为 `degraded`，`can_continue=true`，阻断项为空。原因是磁盘使用率 **85%** 达到预警阈值；不能表述为所有运维项均正常。本轮没有删除备份、历史镜像或业务数据。

首轮只读采集因独立验证容器未设置 Docker Health 字段而遇到模板取值错误，原失败证据保留。修改采集脚本以安全读取可选字段后，第二轮 27 项全部通过。此错误属于验收脚本兼容问题，并非应用故障，不掩盖失败记录。

原始证据位于本地 `证据/20260923生产发布/`：`独立生产版本健康核验第二轮.json`、`独立后台生命周期核验.json`、`独立生产发布与备份重算.json` 和发布日志。版本与备份验收不代替前端操作、账号切换或真实模型任务验收；本报告没有据此宣称全平台无缺陷。
