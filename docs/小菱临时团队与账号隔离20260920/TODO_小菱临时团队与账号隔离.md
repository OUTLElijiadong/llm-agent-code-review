# 小菱临时团队与账号隔离：待办

## 待补验

生产已发布 4.0.1。剩余登录后的自然语言建队、会话回投和浏览器交互补验，需要恢复本任务的生产登录会话，或提供现有有效且有聊天权限的验收账号凭据文件路径。旧 QA 已停用，不擅自启用、加权或签发替代登录令牌。用户先前的直接发布授权与功能偏好仍有效，不重复确认。

执行现成的正常登录 HTTP 补验脚本后，再用生产只读脚本重算团队、模型账本、跨账号拒绝和永久库前后数量；浏览器恢复后补新图标、换号及团队卡片交互。不能用 HTTP 结果替代视觉复测。无需在源码没有变化时重复部署。

## 已知非阻断事项

- 生产磁盘使用率 85%，运维汇总为降级但允许继续、阻断项为零。保留当前及回滚镜像和本次已验证备份，没有擅自进行系统清理。
- 六个旧辅助/种子脚本的 154 条规范诊断与基线完全相同，零新增；本轮业务源码和变更文件检查通过。

## 已完成

精确依赖全量、前端全量和构建、临时团队真实模型并行、私聊隔离定向回归、图标组件验证、生产备份与 100 表恢复、精确 SHA 发布、27 项独立生产技术核验、公开分支与版本标签推送。知识库偏好已归档并修正失效约束，最终闭环收据见总结报告。
