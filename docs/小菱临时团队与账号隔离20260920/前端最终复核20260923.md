# 九月二十三日前端最终复核

最终源码通过 1277 项前端测试，116 个测试文件，0 失败、0 跳过；ESLint、vue-tsc 与注入版本 4.0.1 的 Vite 构建均退出 0。复核期间未修改前端业务源码、测试断言、超时阈值或依赖锁文件。此次只完成本地前端门禁，生产发布和真实浏览器验收由主控另行记录。

## 旧失败与本次证据

前次 1277 项全量中存在 2 项失败，保留原始 JSON：管理小菱断网恢复的最后一次调用断言，以及 501 个真实复选框交互测试超时。前次定向复验为 76 项通过。本次以两个 worker 限制进程资源，两次全量均为 1277 项全部通过；清洁依赖后的这两项耗时分别约 24 毫秒、786 毫秒。

旧失败本轮未复现。受控负载实验尚未证明并发是唯一根因，因此不把“并发负载相关”推断写成确定结论。未通过增大超时、删除用例或放松断言取得通过。

## 构建环境修复

第一次构建因 `node_modules/@types` 出现 13 个空目录副本（名称带 ` 3` 后缀），触发 TypeScript `TS2688`。副本目录元信息和失败日志均保留。执行已有锁文件的 `npm ci` 后，异常副本为零；清洁依赖下重新完成全量测试、lint 与构建。`package.json`、`package-lock.json` 和 TypeScript 配置均未修改，也未将本地副本问题误报为应用源码缺陷。

## 源码和数据复核

- `frontend/src` 全部文件及本次列明的 6 个配置文件共 364 个 SHA-256，在全量开始和最终结束时完全一致。
- 验收清单保留早期 1271 项、后来 1277 项中 2 项失败以及最终 1277 项全绿的不同时间结果，当前门禁以最终文件为准。
- 主知识库偏好页已独立只读检查：中文底边参照、独立固定操作区、圆润可爱且保留菱形、既有及临时成员混编、当前账号与任务范围、保留审计且不永久入库、聊天按账号隔离均有明确记录。后续确认明确覆盖早先“仅已有成员”，工程方案与用户偏好已分开。
- 九月二十日的本地 Vue 截图沿用；九月二十三日没有重新运行浏览器，也没有切换生产账号或访问生产数据库。

完整命令、退出码、原始日志与哈希见 [前端验收清单](证据/前端/前端验收清单.json)。本次未执行提交、推送或部署。
