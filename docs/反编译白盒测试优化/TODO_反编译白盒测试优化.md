# TODO：反编译白盒测试优化

| 待办 | 当前原因 | 完成条件 |
| --- | --- | --- |
| 公网认证业务闭环 | 当前会话没有可用登录凭据 | 使用现有非临时测试账号完成登录、项目创建/选择、APK 上传、白盒任务完成和组织隔离检查 |
| 四格式公网下载验收 | 依赖上一项产生的真实 sandbox report | 从公网 API/UI 下载 JSON、HTML、PDF、Word；分别解析并核对同一组输入清单 SHA、原始制品 SHA、输出 SHA |
| 前端 lint 基线 | 既有 6 个无关 `no-unused` 错误 | 独立清理这些文件后再把 lint 设为零错误门禁 |

## 生产操作顺序

1. 提供或指定现有测试账号，不在文档或仓库中保存密码。
2. 在公网创建一次真实 Android 审查任务，记录 task/report ID 和同一组证据 SHA。
3. 下载并解析四种格式，补充按页面/按钮的通过、失败、阻塞矩阵。
4. 失败时只做应用层回滚；数据库恢复使用显式确认口令和已验证安全备份。
