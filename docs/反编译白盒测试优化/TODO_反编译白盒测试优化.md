# TODO：反编译白盒测试优化

| 待办 | 当前原因 | 完成条件 |
| --- | --- | --- |
| 公网 APK/GitHub 白盒输入闭环 | 本次认证验收使用已有项目 13 的 1 个 Python 文件；未在公网重新上传 APK 或导入 GitHub 源码 | 在明确测试制品和账号范围后，从公网完成一次 APK/GitHub 输入、反编译、白盒门禁、任务报告，并把输入清单 SHA、原始制品 SHA、输出 SHA 与任务 ID 绑定 |
| 公网下载字节级验收 | 公网页面六个生成/导出按钮点击无前端错误，但浏览器工具不暴露 Blob 下载文件路径 | 在可获取下载文件的浏览器或 API 客户端中保存 JSON、HTML、PDF、Word，分别解析并核对同一组输入清单 SHA、原始制品 SHA、输出 SHA |
| 前端 lint 基线 | 既有 6 个无关 `no-unused` 错误 | 独立清理这些文件后再把 lint 设为零错误门禁 |

## 生产操作顺序

1. 指定可公开测试的 APK/GitHub 制品和账号范围，不在文档或仓库中保存密码。
2. 在公网创建一次真实 Android 审查任务，记录 task/report ID 和同一组证据 SHA。
3. 使用能取得下载文件字节的客户端下载并解析四种格式，补充按页面/按钮的通过、失败、阻塞矩阵。
4. 失败时只做应用层回滚；数据库恢复使用显式确认口令和已验证安全备份。
