# TODO：态势感知实时准确刷新

## 待办

- [ ] 需要在后端真实运行且数据库可用时，登录后访问 `/agents` 做一次端到端浏览器复验。

## 复验指引

1. 启动后端和数据库：`./dev.sh` 或按项目部署文档启动 MySQL + Backend + Frontend。
2. 登录系统后进入「Agent 中心」。
3. 发起一次 Agent 调用或圆桌讨论，观察对应 Agent 是否立即前置并切换为思考中/工作中。
4. 等待调用完成，确认该 Agent 回到空闲，态势感知的工作中数量同步归零。
