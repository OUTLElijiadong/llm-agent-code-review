# 全 AI 上下文压缩与完整性：待办

1. v4.0.8 `83334a0` 已发布；v4.0.9 角色知识脚本修复发布后，真实点击复验管理员顶栏小菱与独立贾维斯、审查员操作审计说明及三账号聊天隔离；已发布 Agent 与临时团队仍需本版新样本复核。
2. 监控知识库接近 200 万字的单篇入库：目前嵌入接口按 32 片分批调用，但写库前仍在内存累计全部向量。若实际资源或耗时超限，拆为带检查点的异步入库；不要以分批调用宣称整篇资源已有界。
3. 沙箱单次超过 32 片、单个文本超过 400 KB 等输入当前明确失败；如真实生产项目需要更大范围，应设计可恢复的多批账本后再提高上限。
4. 定向角色知识播种曾留下无切片文档 #52；修复上线后仅重播种两份指定手册，并核对新文档切片数大于零、每个目标仅保留一份有效文档，空文档已清理。后续可加失败后的空文档自动清理。管理员治理与运维继续使用独立贾维斯入口。
