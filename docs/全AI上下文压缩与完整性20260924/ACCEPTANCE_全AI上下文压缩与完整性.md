# 全 AI 上下文压缩与完整性：验收记录

观察日期：2026-09-24。当前均为候选源码及只读生产观察，尚未据此宣布发布通过。

| 入口或问题 | 当前证据 | 状态 |
|---|---|---|
| 生产基线 | 公网 `/healthz` 与 `/readyz` 返回 v4.0.6、release `c86a7483aec160b2aa55c8e3195a3ad26d813387` | 只读已核验 |
| 小菱 Responses | 候选运行时分块语义压缩、原始来源标记覆盖、最多 32 次压缩调用、长度耗尽增预算重试、半截工具不执行；相关 71 项测试和 Ruff 通过；终态半截文本隐藏定向 4 项通过 | 候选，待独立/全量/真实模型复核 |
| 小菱聊天长历史 | 服务端按账号、入口、会话恢复完整原始检查点（工具调用、结果、旧图片）；浏览器只提交本轮消息，恢复接口最近 100 条且可逐页加载旧消息；后端 89 项、前端 114 项定向测试和构建通过 | 候选，待全量/真实浏览器复核 |
| 旧聊天 Agent | 长历史改来源有序压缩，取消最后 3 条/10 条与首 200/500 字静默截取；相关 79 项及 Ruff 通过 | 候选，待独立/全量复核 |
| 共用 Agent 基类 | 超窗口改为 `input_exceeds_context`、零 HTTP、显式要求业务分片；安全哨兵已把此类失败纳入自适应二分；相关 79 项及哨兵 72 项通过 | 仅防静默丢证据，不能代替业务压缩 |
| 管理员副驾驶 | 当前账号完整历史入库读取，历史超 24K 字时逐源分片压缩，最近四条保留原文；压缩来源不全明确失败，结构重试不再丢历史/截当前问题。18 项定向测试通过 | 候选，待全量/真实模型复核 |
| 报告 Agent | 多层证据前缀裁剪及最多 15 问题的 length 假成功已修；大证据按来源分片压缩并校验覆盖与引文，失败明确标记。19 项定向测试、Ruff 通过 | 候选，待独立/全量/真实模型复核 |
| 已发布 Agent 技能 | Skill/委派/只读工具静默裁剪已去除；已发布单次审查新增源码分片、哈希和覆盖账本。26 项定向测试通过 | 候选，间接调用与失败边界待复核 |
| 正式审查与安全审计 | `review_service.py` 已有代码分片与输出长度重试，现补自定义画像完整输入预算门禁和重试预算门禁，6 项回归通过；`security_sentinel_agent.py` 有递归拆分和覆盖标记 | 候选，待端到端复核 |
| 沙箱测试生成、部署、语法修复 | ZIP 文件与依赖按原文分片并核对来源哈希/覆盖；语法修复重建完整原文件，修复元组误写；53 项定向测试通过。超出 32 分片、单文件 400 KB 等显式拒绝动态分析 | 候选，待全量/真实调用复核 |
| 渗透与全链审计 | recon/threat/detect/exploit 分片和全发现索引；失败或超容量不标成功，全链安全哨兵使用 full；48 项定向测试通过 | 候选，待全量/真实调用复核 |
| 项目/运维/规则蒸馏 | 项目文件清单全量来源分片，运维长事实分片压缩；蒸馏失败不再使进化整轮伪成功；相关入口 96 项与独立 59 项定向测试通过 | 候选，待全量复核 |
| 临时团队 Agent | 显式 1–5 文件及其依赖完整读取；超 60K 按来源哈希分片压缩，未指定文件而项目超过 5 文件时明确阻止；压缩、撤权、来源变化或超过 32 片不返回完成。定向 69 项通过 | 候选，待生产真实调用 |
| 个性化知识与文件 Agent | 知识正文不再只取 20K；嵌入按 32 片调用且核对数量；画像召回、文件 Agent 不再只取前 300/3000 字。文件 Agent 定向 27 项通过 | 候选；200 万字入库仍会累计全部向量，资源风险待优化 |
| 证据汇总 | 审查说明、证据、修复建议与代码去除静默前缀裁剪；MySQL TEXT 超 65535 UTF-8 字节明确失败。定向 52 项通过 | 候选，待生产真实审查 |
| 前端完整测试与构建 | 121 个测试文件、1334 项通过；ESLint 和 `vue-tsc + vite build` 均退出 0 | 本地已通过 |
| 本地浏览器布局 | Playwright 4 个布局文件共 37 项通过，覆盖 320/390/768/1024/1440 像素及只读/可处理权限；接口桩不等于生产真实账号验收 | 本地已通过 |
| 独立 Agent 复核、后端全量测试 | 独立复核发现并修复圆桌重启续答与报告阶段消息边界；后端全量 4713 项通过、5 项跳过，Ruff 全通过；Alembic 单 head `058_roundtable_sessions` | 本地已通过 |
| 真实模型与生产验收 | 尚未发布本候选，旧生产仍为 v4.0.6 | 待执行 |

“来源覆盖”可证明每个来源参与压缩，不能数学证明模型对每条语义都理解正确。生产验收用多样本、约束回问、证据锚点和失败门禁共同检验。
