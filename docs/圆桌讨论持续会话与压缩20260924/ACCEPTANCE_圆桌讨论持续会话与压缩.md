# 圆桌讨论持续会话与压缩：验收记录

观察日期：2026-09-24。下表只记录已取得的证据，候选实现与生产验收分开。

| 检查项 | 当前证据 | 状态 |
|---|---|---|
| 生产失败定位 | 圆桌 `disc_eb41e3ba9f`、报告任务 `#182`：10/10 发言成功，主持总结成功；结构化问题抽取模型调用提示 2969 token、完成 4096 token，返回 `finish_reason=length`，任务失败 | 已复现 |
| 手机头部布局 | 用户提供 Safari 390×844 截图；源码确认头部单行布局被固定按钮挤窄 | 已复现 |
| 结束边界 | 生产日志在失败后出现无回调注册的用户输入；源码先广播再调用回调，存在消息被记录却未被处理的竞态 | 已复现风险 |
| 跨页重开 | 面板只挂 Agent 中心，续会链接缺少前端要求的参会者参数 | 已定位 |
| 进度与压缩 | 当前仅有轮次提示、抽取默认输出 4096 token，长内容有固定字符截取 | 已定位 |
| 五分钟规则 | 用户确认结束后固定五分钟追问；后台独立回答、超时禁言、已收问题跨截止完成，进程重启自动续答；总结/抽取/报告阶段新消息先入账、报告封存后按顺序回复 | 候选通过，生产待验收 |
| 候选回归 | 2026-09-24：圆桌后端定向 33 项、面板定向 12 项；后端全量 4713 项通过、5 项跳过；前端全量 1334 项通过，构建、ESLint、Ruff 均通过 | 本地通过 |
| 生产重复复测 | v4.0.7 已部署；普通账号真实创建 `disc_13ecdb6fd0`，10/10 专家发言、13/13 进度单位、报告 #184 完成。新发现五分钟期间前端仍停 reporting 且输入禁用；v4.0.8 `83334a0` 已发布修复，需再开新圆桌复验 | 已复现新问题，生产复测待完成 |
| v4.0.8 追问修复 | WebSocket `done` 不提前断开；REST 对账同步进度、报告任务与截止时间；后台重开重放终态。前端定向 18/18、后端定向 19/19、全量 1337/4722 通过 | 已发布，真实追问待复测 |
| v4.0.9 生产实跑 | 普通账号真实点击小菱并发起项目 14、文件 710 的五专家圆桌 `disc_6f1e48b7f3`，报告任务 #185 保存了主持总结和 19 条结构化问题；第 2 轮可靠性专家输出在默认 4096 token 截断，专家发言仅 9/10，覆盖标为 `partial`、任务失败，手机端只显示笼统“失败” | 已复现，不能算完整报告 |
| v4.0.10 候选修复 | 专家发言保留同一完整输入、按 8192/16384/32768 token 有界重试并分别记账；仍截断则保留部分完成语义，圆桌面板与全局入口显示“部分完成”。后端全量 4726 通过、5 跳过；前端 1339 通过，构建、ESLint、Ruff 通过 | 本地通过，生产待复测 |
| v4.0.10 发布 | 2026-09-24 16:03:26 UTC 发布 SHA `5c26cc1f8eb06766b08a6b5df5f7c3b11ea08fbc`，前后端镜像健康、HTTPS 和迁移 058 一致；普通账号手机端重复发起同文件圆桌，进行中 | 已发布；业务结果待验 |
| 旧部分报告重开 | #185 旧会话进度仍持久为 `failed`，仅新 done 帧会写 `partial`；v4.0.11 在同账号、同报告 `coverage.stage=partial` 时只读映射为“部分完成”，不修改原始账本，也不开放失败后的追问。圆桌相关定向 91 项通过 | 本地通过，生产待验 |
| v4.0.10 同文件复跑 | 普通账号在 390×844 Safari 中经小菱核实并批准项目 14、文件 710 的五专家圆桌 `disc_632fa3e0e7`；报告 #186 独立 DB 核对 `success`、10/10 专家发言、0 失败、19 条问题、主持和抽取成功。手机端显示 13/13 总进度与五分钟倒计时，到期后输入禁用。第一次小菱请求曾出现 `Load failed`，点击重试后正常启动；尚未定位初次客户端失败根因 | 审查成功；窗口内提交追问未测 |
