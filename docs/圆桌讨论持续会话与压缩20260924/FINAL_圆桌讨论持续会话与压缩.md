# 圆桌讨论持续会话与压缩：交付说明

圆桌会话、发言和进度持久化并按账号隔离；桌面和手机可从全局入口重新打开后台讨论。长源码和发言按来源分片压缩，结构化问题抽取支持分批与长度重试；无法完整抽取时显示失败，而非生成貌似完整的报告。

用户确认的时间规则已实现：正常报告封存后固定五分钟继续追问，截止后禁言，不因追问续时。总结、抽取和写报告期间发送的消息先保存在原始账本，报告成功后由主持人按序回答。进程重启后自动扫描未答但已接受的问题；旧问题跨过五分钟仍需答复。

v4.0.7 真实生产验收中，普通账号小菱发起了圆桌 `disc_13ecdb6fd0`，五位专家完成 13/13 步并生成报告 #184；桌面和手机 390×844 视口均能从全局入口关闭后重开。该圆桌绑定项目 14 的 `agent_service.py`，主题未自动进入专家发言。验收发现报告生成后浮窗仍显示“正在生成报告 13/13”，五分钟倒计时内输入一直禁用；到期后禁言正确。这一缺陷不能用 13/13 进度或报告已生成来掩盖。

v4.0.8 `83334a0` 已上线，修复 WebSocket 过早断开及 REST 状态对账缺失，补后台重开终态重放；后端圆桌定向 19 项、前端定向 18 项通过。全量后端 4722 通过、5 跳过；前端 1337 通过，构建、Ruff 与 ESLint 通过。生产五分钟内追问必须在新圆桌完成后重新验证，旧会话期限不会延长。

v4.0.9 真实点击再次发起圆桌 `disc_6f1e48b7f3`，报告 #185 已保存总结与 19 条问题，但第二轮可靠性专家发言返回 `finish_reason=length`，覆盖只有 9/10；因此任务按不完整处理，不能据已保存的问题宣称审查成功。v4.0.10 候选修复将专家发言从默认 4096 token 改为有界递增重试，并让界面准确显示部分完成。生产部署和重复实跑的结果以验收记录为准。

v4.0.10 已于 2026-09-24 16:03:26 UTC 发布。独立核验指出旧 #185 圆桌的持久进度仍为 `failed`，因此只修复新 done 帧并不能纠正旧会话展示。v4.0.11 增加只读兼容：当同账号、同报告的任务状态和覆盖账本确认为部分完成时，REST 列表、详情及 WebSocket 重连统一显示“部分完成”；原始账本保留用于审计。

v4.0.10 上普通账号真实重跑相同文件，`disc_632fa3e0e7` 的报告 #186 为成功，10/10 专家发言、0 失败、19 条问题，已通过数据库覆盖记录核实。手机端进度与五分钟倒计时正常显示，到期后禁言真实可见；窗口内提交追问并收到回复仍需下一轮验证。首次小菱请求曾显示 `Load failed`，点击重试后成功，应单独保留为一次瞬时故障证据。
