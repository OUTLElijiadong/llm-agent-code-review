# 圆桌讨论持续会话与压缩：待办

1. v4.0.12 已在同一真实文件 `test_auth.py` 复跑：无原始 JSON 外壳，报告后追问一次模型调用、19 秒回复，待答进度与到期禁言均有手机截图。仍需用另一份真实文件重复模型截断与异常 JSON 边界；手机长表格当前挤成竖排，横向滚动修复需发布后截图复验。
2. 用不同账号验证会话目录、历史页和 WebSocket 归属拒绝；验证异常断开、服务重启后的已接收追问续答。普通账号 #187 已确认刷新重开、顺序回复与截止禁言；不能替代跨账号负向验收。
3. 若生产模型仍因上下文或输出上限失败，保存来源覆盖、模型 `finish_reason`、任务状态及具体未完成批次；不得用旧报告或本地桩成功代替真实复测。
4. 五分钟到期后的界面和服务端已禁言；可进一步在到期时主动关闭闲置 WebSocket，避免长期开页持续心跳。
5. 小菱第一次请求出现过瞬时 `Load failed`，重试成功但根因未定位；保留网络/客户端错误证据，重复出现时专项复现。
