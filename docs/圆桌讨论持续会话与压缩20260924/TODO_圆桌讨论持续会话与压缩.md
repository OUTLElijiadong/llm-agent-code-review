# 圆桌讨论持续会话与压缩：待办

1. v4.0.7 已用生产项目真实启动圆桌并核对 13/13 发言、后台重开和报告 #184；v4.0.8 `83334a0` 已发布，须新开圆桌，分别在运行中和结束后的五分钟内追问，核对主持人回复、报告状态与到期禁言。
2. 用不同账号验证会话目录、历史页和 WebSocket 归属拒绝；验证页面刷新、异常断开、服务重启后的已接收追问续答与截止后禁言。
3. 若生产模型仍因上下文或输出上限失败，保存来源覆盖、模型 `finish_reason`、任务状态及具体未完成批次；不得用旧报告或本地桩成功代替真实复测。
4. 五分钟到期后的界面和服务端已禁言；可进一步在到期时主动关闭闲置 WebSocket，避免长期开页持续心跳。
5. v4.0.9 的报告 #185 为 9/10 发言覆盖的部分产物；v4.0.10 部署后对同一真实文件重复发起圆桌，核对全部专家发言、报告完整性、五分钟追问与到期禁言，并增加至少一个不同文件样本，防止只靠一次调用判断偶发性。
