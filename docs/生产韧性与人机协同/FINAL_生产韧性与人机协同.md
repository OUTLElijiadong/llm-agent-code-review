# 生产韧性与人机协同：最终报告

## 交付结果

本次从对齐时的生产 `3.7.0` 更新至 `3.7.4`。系统已经从“单项异常可能被当成整体失败”调整为明确的正常、降级、阻断三态，并把原因、请求编号、是否可重试、下一步动作和人工确认入口贯通到运维、API、Agent 对话、生产调度和 FlyTrap 外部集成。

生产当前运行提交为 `c1fc546de597d6258ca29945dd824afb9744265d`，公网入口为 `https://lijiadong.cn`。

## 已完成能力

1. 运维巡检区分 `ok / degraded / error`，容量告警在临界值前允许继续，但关键依赖仍严格阻断。
2. API 错误统一返回安全消息、请求编号、重试属性和下一步动作，不泄露生产内部堆栈。
3. Agent 流式失败保留部分结果与时间线；取消、重试、Clarify 过期、审批和团队操作都有可见、可恢复的人机路径。
4. 运维执行器增加动作白名单、请求幂等和审计台账；安全巡检允许单来源降级，拒绝把全部来源失败伪装成成功。
5. 服务启动自动收敛跨进程孤儿运行，保留人工重跑决定，不自动重放未知副作用。
6. 调度器支持秒级 interval，生产 `sandbox_heartbeat` 已按 30 秒连续自然运行。
7. 发布脚本具备发布前备份、隔离恢复、健康/就绪、迁移、HTTPS 冒烟和应用版本回退记录。
8. FlyTrap 巡检区分本地采集与上游同步：运维噪声不计为攻击，上游超时保留本地队列和统计、创建管理员告警并允许下一轮继续，恢复成功后自动解除告警。

## 质量结论

- 后端 2113 项与前端 384 项测试全部通过。
- 生产备份、隔离恢复、容器、迁移、公网、结构化报错、浏览器交互和自然调度均为实测证据。
- 生产当前没有阻塞检查；磁盘 88% 被正确保留为需要人工处理的降级项。
- 本次没有数据库结构迁移，没有放宽认证、权限、审批或恶意代码拦截。
- 外部 FlyTrap 上报链路到 `42.194.238.178:8443` 仍超时；手动验收 run `38841` 和独立复核 run `38850` 均将其标为可重试降级并保留管理员告警 `68`，本地持久队列、其余安全来源和 Prism 主业务继续工作。

## 回退边界

应用可回退至提交 `8d97e91ea0de6ec9a58cbcb7ad1e1a0c6b452cdc`。回退脚本只切换应用镜像，不自动降级数据库；本次 Alembic 未变化，因此应用回退与当前数据库头兼容。数据库恢复仅在明确的数据事故和人工确认后使用已验证备份执行。

## 交付文档

- `ALIGNMENT_生产韧性与人机协同.md`
- `CONSENSUS_生产韧性与人机协同.md`
- `DESIGN_生产韧性与人机协同.md`
- `TASK_生产韧性与人机协同.md`
- `APPROVAL_生产韧性与人机协同.md`
- `ACCEPTANCE_生产韧性与人机协同.md`
- `TODO_生产韧性与人机协同.md`
