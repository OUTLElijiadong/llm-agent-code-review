# 生产韧性与人机协同：待办

以下事项不阻塞当前生产，但需要人的业务或运维决定，自动化没有越权处理。

## P1：磁盘容量（已处置，持续监控）

- 受控清理已完成并保留当前/上一版镜像、数据库卷、证书、备份和发布账本；此前清理曾释放旧镜像空间，发布后再次复核时根分区为 85%（约 29 GB 可用）。
- 本轮 `cleanup.sh --apply` 成功执行，新增回收为 0B（共享层和近期缓存仍受保护）；`ops-check` 仅提示 `disk_cleanup_review`，`can_continue=true` 且无阻断检查。再次达到告警线时先 dry-run 审核，不能直接删除受保护资源。

## P1：复核已收敛的自动任务

- 现状：启动时回收了 12 条旧 `running` 记录，其中健康结果仍标记 `db_backup` 需要人工复核。
- 影响：记录已经结束，不会继续占用调度；未知副作用没有被自动重放。
- 人工决定：在管理端检查对应运行记录和备份时间，仅在确认幂等性后重新运行确有必要的任务。

## P1：FlyTrap 退役（已完成）

- `flytrap-agent.service`、`flytrap-sync.service` 均已 `inactive/disabled`，证书续签 timer 同样停用；Prism 与运维开关均为 `false`。
- 退役状态不再访问上游、不产生重试或降级告警；历史 unit、日志、队列和退役备份只读保留，可按恢复文档回滚。

## P2：Clarify 状态持久化

- 现状：动态追问状态仍是进程内存存储；进程重启后旧 `clarify_id` 会失效。
- 已有回退：界面保留原因并支持“重新提问”，原问题预填后由人确认，所以不会完全堵死。
- 后续方案：若计划多副本或要求重启无感，复用现有 Redis 保存带 TTL、用户绑定和一次性消费语义的追问状态，并补跨进程并发测试。

## P2：数据库威胁信号覆盖范围

- 现状：MySQL `general_log` 未开启；安全巡检会明确报告该来源限制，同时继续完成其余来源，不把“未采集”伪装成“没有威胁”。
- 人工决定：只有在评估性能、磁盘和隐私成本后，才决定是否短期开启或改接低开销审计来源。

## 当前无缺失配置

生产发布、回退、备份、隔离备份校验、运维巡检、运维执行器、公网证书和通用 AI 默认 Key 均已配置并处于可用状态。当前真正的长期事项只有 Clarify 多副本持久化、数据库审计信号覆盖和异地备份/容量治理，不影响主业务继续运行。
