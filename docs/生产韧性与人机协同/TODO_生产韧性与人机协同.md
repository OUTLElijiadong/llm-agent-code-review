# 生产韧性与人机协同：待办

以下事项不阻塞当前生产，但需要人的业务或运维决定，自动化没有越权处理。

## P1：审阅磁盘清理候选

- 现状：根分区使用率 88%，告警阈值 85%，临界阈值 95%。
- 影响：当前可继续；继续增长到 95% 时会变成阻断检查。
- 建议操作：先在服务器执行 `cd /opt/code-review/deploy && ./cleanup.sh`。该命令默认只做 dry-run，不删除数据库卷、证书、备份、当前/上一版本状态。
- 人工决定：核对候选后，才决定是否执行 `./cleanup.sh --apply` 或扩容。不要在未核对保护清单时直接清理。

## P1：复核已收敛的自动任务

- 现状：启动时回收了 12 条旧 `running` 记录，其中健康结果仍标记 `db_backup` 需要人工复核。
- 影响：记录已经结束，不会继续占用调度；未知副作用没有被自动重放。
- 人工决定：在管理端检查对应运行记录和备份时间，仅在确认幂等性后重新运行确有必要的任务。

## P1：FlyTrap 上报链路超时

- 现状：验收期间 FlyTrap 到 `42.194.238.178:8443` 的 gRPC 连接超时，日志显示单事件经过多次重试后仍失败，心跳进入退避。
- 当前回退：FlyTrap 持久队列保留事件并继续重试；安全巡检 run `38841` 及独立复核 run `38850` 均将该动作标为 `degraded`、`retryable`、`can_continue=true`，其余来源和 Prism 主业务继续工作；管理员集成告警 `68` 保持 open。
- 人工决定：检查目标端口、网络策略和接收端服务状态；恢复后确认持久队列积压被消化和同步成功日志出现。下一轮健康巡检会自动关闭同一指纹告警，无需手工伪造成功状态。

## P2：Clarify 状态持久化

- 现状：动态追问状态仍是进程内存存储；进程重启后旧 `clarify_id` 会失效。
- 已有回退：界面保留原因并支持“重新提问”，原问题预填后由人确认，所以不会完全堵死。
- 后续方案：若计划多副本或要求重启无感，复用现有 Redis 保存带 TTL、用户绑定和一次性消费语义的追问状态，并补跨进程并发测试。

## P2：数据库威胁信号覆盖范围

- 现状：MySQL `general_log` 未开启；安全巡检会明确报告该来源限制，同时继续完成其余来源，不把“未采集”伪装成“没有威胁”。
- 人工决定：只有在评估性能、磁盘和隐私成本后，才决定是否短期开启或改接低开销审计来源。

## 当前无缺失配置

生产发布、回退、备份、备份校验、运维巡检、运维执行器和公网证书均已配置并处于可用状态。当前不需要补 API key、数据库迁移或服务账号配置；仍需处理的是 FlyTrap 外部网络/接收端连通性和磁盘容量，不属于应用配置缺失。
