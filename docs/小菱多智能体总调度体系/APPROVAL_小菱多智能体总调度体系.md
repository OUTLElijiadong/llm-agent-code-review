# APPROVAL - 小菱多智能体总调度体系

> 阶段：Approve
> 日期：2026-08-10
> 状态：已批准进入 Automate

## 1. 人工授权记录

用户明确选择“直接完成设计、开发、测试、服务器部署和真实验收”，授权使用已提供的服务器 IP 和 SSH 私钥自行检查，并确认：

- 按建议纳管代码、数据库和运行态中的全部 Agent。
- 小菱中可查看子 Agent 相互对话，默认折叠。
- 通信范围为同一账户下的不同对话。
- 使用项目现有 `.env` 模型配置。
- 全部 Agent 共享知识，采用较高粒度。
- 任务名使用“小菱多智能体总调度体系”。

## 2. 审批解释

本批准允许本任务的代码、迁移、测试、备份、生产发布和验收；不等于取消产品运行时的高风险操作审批。小菱最高权限通过现有 RBAC、固定工具、审批中心、审计账本和受控 root executor 实现，不开放任意命令执行。

## 3. 检查清单

- [x] 需求、范围和验收标准无关键歧义。
- [x] 复用现有 Agent 契约、Responses、事件总线和运维架构。
- [x] 数据库变更为旁路新增，可回滚。
- [x] 前端用户端与管理端均有明确交互与测试范围。
- [x] 生产已有密钥、路径、备份脚本和回滚能力。
- [x] 发布前必须处理生产版本不一致与磁盘 80% 风险。
