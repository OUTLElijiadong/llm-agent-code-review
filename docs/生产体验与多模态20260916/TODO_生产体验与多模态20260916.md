# 生产体验与多模态 2026-09-16 待办

- [x] 完整增量 Git bundle 发布 `fe66b9c` 并记录备份、迁移和回滚状态；生产备份及独立恢复校验通过。
- [x] 发布后核对 `/readyz` release 与本地完整 SHA 一致，执行 `/healthz`、HTTPS、关键页面和权限冒烟。
- [ ] 生产管理员使用真实可用凭据执行一次模型同步/连接测试；当前页面显示已保存凭据不可用，不能把列表展示当作调用成功。
- [ ] 生产多模态真实图片调用需使用非敏感样本，核对视觉模型、图片资产引用、AiCallLog 脱敏和同会话模型恢复。
- [ ] 未覆盖的全站按钮、所有组织组合、历史孤儿账本和项目 161 前后字段证据继续按既有 QA 矩阵补测；本轮不自动改写历史状态。
