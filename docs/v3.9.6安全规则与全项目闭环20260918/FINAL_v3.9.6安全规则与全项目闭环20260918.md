# FINAL_v3.9.6安全规则与全项目闭环20260918

## 交付结论

v3.9.6 已完成生产发布。分散的安全态势、审查规则和规则来源已收敛为一个入口，并修复了截断被误判、规则漂移、输入无界、治理脏值放行及 multipart 异常临时文件未关闭等完整性问题。生产同 SHA、备份恢复、迁移、健康检查和真实 DeepSeek 全项目回归均有证据；管理员后台二次浏览器登录受本机密码库解锁条件阻断，未将其冒充为已验收。

## 已实现

1. `/security` 统一“安全态势、审查规则、规则基座”，旧 `/rules` 保留兼容跳转。
2. 报告模板收敛到 `/report/templates`，管理员旧地址只做兼容重定向。
3. 新增有来源、执行器、状态和权限裁剪的统一规则目录；CodeQL 只表示 Prism 仓库 CI 能力，不冒充平台上传项目执行。
4. 审查任务冻结完整规则快照；逐文件/分片记录成功与失败，截断或解析失败不会再被记为完整成功，恢复只复用已完成文件证据。
5. DeepSeek 上下文与输出预算按模型配置钳制；1M 上下文不是“越宽松越好”，完整运行依赖分片、预算、续跑和覆盖账本共同保证。
6. 增加字段长度和枚举约束、Jinja 沙箱、上传前置总量限制、目录逐文件流式限制及治理 fail-closed。
7. 不做破坏业务的全局特殊字符删除；数据库参数化、输出上下文净化和字段语义校验各负其责。新增普通 JSON/API 4MiB 网关边界，以及账号、项目、规则、反馈、聊天字段的控制字符和语义校验。

## 架构判断

- CNVD/CNNVD 是漏洞情报、编号和中文分类来源，不是可直接执行的源码审查引擎。
- GitHub CodeQL 是本仓库 CI 的权威可执行基座；用户上传项目的 CLI 沙箱与 SARIF 合并尚未实现，不能标成已完成。
- “用户端与管理端标题相似”不足以直接合并。报告模板五项语义一致，已合并；仪表盘、Agent 治理、知识空间和监控告警的数据域、写权限或审计职责不同，本轮保留但通过清晰命名与统一入口降低冲突。

## 生产结论

- 版本：`3.9.6`；最终发布要求生产 Git HEAD、前后端镜像 release、`/healthz` 与 `/readyz` 返回的 release 完全一致，精确 SHA 以部署生成的状态文件及知识库闭环记录为准。
- 发布脚本 exit 0；独立恢复验证 97 张表；Alembic head=`053_role_pentest_authorization`；同源 HTTPS `health/ready` 冒烟通过。
- 角色：活动集合为 `user/reviewer/admin/super_admin`，`auditor` disabled，活动最高管理员 1；评审员保留 Agent 自定义与渗透授权。
- 数据治理：两条已确认验收账号软停用并使 token 失效，关联项目/任务/角色均保留。
- 真实 DeepSeek：任务 #178 以 `full` 完成 5/5 文件、33 条发现；截断首轮失败 8 次均在预算提升后成功，覆盖账本完整。
- GitHub CodeQL：运行 `35320485649` 的 Python 与 JavaScript/TypeScript 矩阵均成功，Code Scanning API 已记录两个对应分析。
- 浏览器：公开登录页显示 `v3.9.6`；后台页面重新登录需要 Safari 解锁 macOS 密码库，后台截图验收保留为待补证据。
