# APPROVAL_v3.9.6安全规则与全项目闭环20260918

## 审批检查清单

- [x] 用户已明确要求继续完整完成，并授权查看知识库和执行必要生产验收。
- [x] 版本选择为兼容升级 `v3.9.6`，不回滚 v3.9.5 已验收功能。
- [x] 页面合并保留旧路由兼容，避免书签和历史链接失效。
- [x] 规则合并采用统一只读投影，避免三类执行器双写和职责混乱。
- [x] 权威规则基座选择 GitHub CodeQL；CNVD/CNNVD 限于情报和分类映射。
- [x] 特殊字符不做一刀切过滤，采用服务端校验、参数化和上下文输出编码。
- [x] 真实 DeepSeek 调用有预算门禁、可续跑账本和完整性判定。
- [x] 测试/演示数据只有证据充分才治理，动作可回滚且必须重算。
- [x] 所有自动化和生产验收结果都保留证据边界。

## 执行批准

按 `TASK` 依赖顺序进入 Automate。若出现以下情况中断破坏性步骤但继续可逆工作：生产备份/恢复失败、数据库迁移不一致、外部引擎输出无法验证、未知账号归属、预算门禁拒绝真实模型调用或发布候选包含用户自有文件。
