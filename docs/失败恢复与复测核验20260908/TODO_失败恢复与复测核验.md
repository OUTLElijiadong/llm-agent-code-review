# 剩余待办

1. **QA账号停用待明确批准**：本轮107、108、109、110仍为启用。自动审批拒绝执行，要求用户批准这四账号的停用与会话撤销；确认问题已提出。批准后运行服务器专用目录中已冻结的finalize_qa_20260908.py --execute，成功前不得重复执行，连接中断先检查唯一结果文件。保留项目164/165、文件1894/1895与审计；当前只读核实0模型/0审查。
2. **全控件逐角色实点仍未完成**：104 Vue/616交互节点、3080角色格清单已建立，四账号新增36条浏览器操作与观察；新版台账14格通过、9格部分覆盖、11格仅观察、2430格未测、616格管理员登录阻断，并非616控件全通过。管理员须用户正常登录；包含真实模型/运维操作、缺少任务/报告夹具和未执行下载的控件仍应逐项补测。API矩阵不能抵消UI未测，动态单实例不能算所有状态覆盖。
3. **动效待库和位置确认，随后单独实施**：已提出Vue Transition+CSS用于小菱真实步骤、工具时间线、结果展开及代码定位，并遵循reduced-motion。尚未收到明确方案选择，本轮未部署生产动效。
4. **安全架构建议项独立排期**：CSP从Report-Only转强制需先采集真实资源兼容性；JWT迁HttpOnly Cookie需同时设计CSRF与流式认证；弱密码拒绝需统一注册/改密服务端合同与历史兼容。本轮未把建议项虚报成已修复缺陷。
5. **未发请求的88个路由角色格保留范围说明**：完整313路由的两类计划共626格，538格已实测401/403；其余88格包含公开入口、无统一权限门禁和依赖未能证明安全的入口。不得把它们写成403通过，也不能未经逐项核对就发送潜在业务写请求。下一轮按实际鉴权合同区分不适用、可安全验证和需要专用业务夹具的项目。
6. **验收工具的生产路由发现兼容性**：现有build_plan直接遍历app.routes，在FastAPI0.139的嵌套路由结构下会只发现3条。此次使用已冻结且49源码指纹一致的313路由计划完成当前验收；没有修改生产工具的自动发现实现。下次动态生成计划前应补新结构支持及完整性门禁，不能将3条小清单的passed当作全量路由通过。

历史task11/team5、task35/team12及孤儿账本只读复核已完成，状态保留是用户要求，不是需要自动“修正”的待办。项目161完整八字段证据已闭合。应用代码已通过完整Git bundle发布3.8.7；Git远端未推送，符合既定发布方式。
