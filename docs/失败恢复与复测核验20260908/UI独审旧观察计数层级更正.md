# UI独审旧观察计数层级更正

最终UI台账独立复核11组全部通过，原始结果为`证据/生产3.8.7-UI台账独立复核.json`。3080格准确分为2430未测、616管理员阻塞、14已列明场景通过、9部分实测、11显隐/禁用观察。与原四份浏览器观察和精确动作补充逐条对照，没有把HTTP、本地测试或旧版观察算成本轮按钮通过。

首次审阅器将`previous_release_observations`的34个历史分组计数，与分组内66条`observations`误作同一层级，产生了一项错误失败。两版历史分组逐字段已确认完全相等；正确重数应遍历各组内的`observations`。本次只修正独立审阅器，没有修改台账、原观察、初始化文件或应用代码。

首次输出保留为`证据/生产3.8.7-UI独审首次旧观察层级误计.json`。修正后重新读取全部JSON和CSV逐字段核验，并重算104份源码SHA，11组均通过。旧34格66条观察和24条额外记录完整保留；本轮36条原观察中29条形成36次映射，落在34格，另外7条按页面场景/不可唯一定位保留为补充。
