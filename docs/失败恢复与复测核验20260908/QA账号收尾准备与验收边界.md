# QA账号收尾准备与验收边界

本轮收尾工具和44项本地隔离测试已完成，另一位子代理完整审阅并独立重跑44项通过。当前状态为**待主代理明确开始生产收尾，尚未执行生产**。准备脚本不改应用源码、迁移或已发布镜像，不代表全部按钮完成验收。

## 固定执行范围

| 对象 | 本轮精确范围 |
| --- | --- |
| 标记 | `20260908` |
| 四个专用账号 | `owner_a=107`、`member_a=108`、`owner_b=109`、`no_permission=110` |
| 专用角色 | `qa_permission_20260908`，ID为7，权限恰好20项 |
| 角色分配 | 107、108、109仅此角色；110没有角色；角色7不得分配给其他账号 |
| 保留项目 | 164属于107；165属于109，标记名称准确且均active |
| 保留成员关系 | 164/107为owner；164/108为reviewer；165/109为owner |
| 保留文件 | 1894属于164；1895属于165，均active |
| 凭据位置 | 服务器`/root/prism-acceptance-20260908/credentials.json`，root私有目录、root拥有的0600普通文件 |
| 停用范围 | 四个QA用户正常登录时会话版本各加1；随后复用已发布`prepare_permission_acceptance.py --disable`禁用四用户并各再加1 |

QA项目、文件、成员关系、专用权限和角色均保留。快照还查询“属于这四个QA用户的其他项目/成员关系”和“属于这两个项目的其他文件”，出现额外范围即拒绝继续；不会自动删除额外记录以满足门禁。UI如已实际新增资源，应先审阅事实再显式调整本轮验收范围。

正常登录还会按既有认证合同更新这四个QA账号的最后登录时间/IP，并产生认证审计；停用也会触发用户行的更新时间。这里不把认证操作称为全库只读，也不要求这四个QA用户的所有字段保持不变。

## 执行与失败恢复

工具为`证据/finalize_qa_20260908.py`。无参数运行只输出计划，不读取凭据，不创建HTTP客户端，不运行容器命令。仅主代理确认QA验收结束后使用`--execute`。

开始执行时以`O_EXCL`创建root0600结果文件`/root/prism-acceptance-20260908/finalize-qa-result.json`。已存在即拒绝重放；不要删除结果后自动重试。工具按阶段落盘并fsync，登录请求和禁用命令都只发送一次。请求超时或命令结果不明时保留失败阶段，由主代理只读核验实际状态后决定下一步。

MySQL采集使用明确的`utf8mb4`客户端字符集，设置只读一致性事务并以ROLLBACK结束，避免上一轮中文标记按latin1解析导致误报。三份脱敏快照保留登录前、禁用前、禁用后的状态与哈希。项目比较八字段；文件比较身份、元数据、版本、状态、时间、内容摘要和二进制摘要，输出不含文件正文。三次快照均要求QA模型用量及QA审查任务为0。

HTTP仅允许正常密码登录和`/api/auth/me`，TLS验证开启，重定向和环境代理禁用。四次登录及四次禁用前me应均200。禁用后用四个内存令牌再读me，实际合同为403/40301（账号停用判断先于令牌版本），不能误写为401。密码、JWT和HTTP正文不会记录到结果或异常文字。

容器内凭据副本仅放root0700随机目录，0600独占创建；现有禁用脚本先核SHA256，再调用一次，普通退出路径清理临时副本。若容器被强制终止，临时目录可能留存，应由主代理按本次随机路径核验，不对整个`/tmp`执行删除。

## 本地验证与独立复核

`证据/test_finalize_qa_20260908.py`共44项，原始结果为`证据/QA收尾脚本本地隔离测试.xml`，44通过、0失败、0错误。覆盖默认不读凭据、旧身份拒绝、角色/权限/成员/项目/文件范围异常拒绝、SQL只读和UTF8、HTTP白名单、超时不重试、错误脱敏、结果文件防重放、登录及禁用期间文件/项目漂移中止。

集成用例在临时内存SQLite真实建表，实际调用当前FastAPI登录和me接口，再运行当前`prepare_permission_acceptance.py --disable`。确认四用户版本5→6→7、状态1→0，旧令牌四次403/40301；同时从数据库完整重读无关管理员、历史项目161、历史审查161及QA项目/文件，验证所有字段不变。该测试是隔离夹具，不能替代生产MySQL或真实浏览器证据。

`security_catalog`子代理完整读取脚本、测试和实际禁用/认证契约，独立重跑44项通过；其独审材料另存于本轮证据目录。没有生产登录、禁用、模型调用、下载或UI点击。

## 本轮UI台账边界

当前616个源码控件对应5角色共3080格。初始化台账2464格untested、616格管理员blocked。92项真实HTTPS夹具结果、权限矩阵、收尾API结果均不能自动转为按钮点击passed。新版UI仅可在主代理给出准确账号、动作、环境和观察后逐格另存回填，不覆盖初始化与上轮冻结证据。
