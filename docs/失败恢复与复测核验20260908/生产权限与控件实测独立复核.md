# 生产权限与控件实测独立复核

本次独立读取主代理实际 HTTPS 结果、实际执行计划、四份浏览器观察及补充精确动作，在本地生成独立台账。未发生产请求，未修改应用，未覆盖初始化台账。主代理为真实浏览器操作者，`usage_attribution` 为离线审阅与映射者。

HTTPS 两组独立 21 项核对通过：夹具组 92 请求、38 内容断言；完整矩阵组 550 请求、11 内容断言。完整矩阵实际为 299 次匿名 401、239 次无权限 403、12 次正常登录及自身 RBAC 读取 200，逐方法、路径、账号与计划完全对应；另 88 个公开或前置拒绝尚未证明的计划角色格没有发请求，仍保留原因。

实际完整计划含 313 路由、49 份源码 SHA，全部与当前已发布源码一致。runner 的 `plan_sha256` 是 `sha256(json.dumps(plan, sort_keys=True).encode())`，按该算法重算与结果完全相同。初读时将其误当文件原字节 SHA 的疑点已纠正，未把审阅口径错误当应用缺陷。首轮实际发现计划仅 3 路由、10 份源码，该批没有任何完整矩阵负向请求，因此 92 请求只按夹具验收统计。夹具包含授权创建、修改 QA 项目/文件/成员，不能将其写成生产零写入。

104 Vue 文件、616 源码交互节点、452 按钮或链接的指纹核对一致。36 条原始浏览器观察（17/7/6/6）中，29 条可唯一映射，另 7 条作为页面或未纳入清单的补充场景保留；映射产生 36 个“观察—控件”关联，涉及 34 个角色格。原始观察数、关联数与唯一控件数是不同口径。

| 角色 | 本轮账号 | 已列静态场景通过 | 部分实测 | 仅观察 | 未测 | 阻塞 |
| --- | --- | --- | --- | --- | --- | --- |
| 项目 A 负责人 | 107 | 5 | 6 | 1 | 604 | 0 |
| 项目 A 普通成员 | 108 | 2 | 2 | 8 | 604 | 0 |
| 项目 B 负责人 | 109 | 4 | 0 | 0 | 612 | 0 |
| 无权限账号 | 110 | 3 | 1 | 2 | 610 | 0 |
| 管理员 | 未登录 | 0 | 0 | 0 | 0 | 616 |
| 合计 | — | **14** | **9** | **11** | **2430** | **616** |

`passed` 仅表示台账列出的静态按钮场景已观察到结果；动态项目、文件、功能搜索结果只测 QA 实例，记部分实测。owner_a 刷新只捕获点击、编辑首次紧连点击未打开、编辑取消缺少紧随结果，以及 no_permission 退出未明确捕获最终登录页，均未升级为完整通过。成员保存按钮隐藏只说明可见性，未实际改编辑器文本，不能把 AX 的 settable 判断为只读验证。Python 文件行不显示二进制下载按钮，含糊的“下载可见”没有映射到它或两个同名下载源码入口；本轮没有生产下载验收。

所有初始化 3080 格及旧版 34 格/66 条观察保持；24 条旧补充场景与 1 条历史歧义映射记录仍在原证据中。API 通过没有计入 UI，管理员仍需正常登录，未测格没有用本地组件测试抵消。新产物为 `证据/生产3.8.7-全部控件逐角色验收台账.json`、同名 CSV 和 `生产3.8.7-全部控件逐角色汇总.json`。明确动作另存 `生产浏览器精确动作补充.json`，原始四份浏览器观察未修改。

验收结束只读快照确认 107–110 仍启用，0 条模型日志、0 审查任务。自动审批拒绝的停用未执行；正常浏览器退出不等于账号停用。本报告按该快照描述状态，不自行关闭账号。

可重跑离线工具为 `证据/build_current_production_ui_ledger.py` 与 `证据/independent_https_results_recheck.py`。精确请求核对见 `证据/生产HTTPS两组结果独立复核.json`。本轮任务错误专项 42 请求由另一独审报告负责，三个有效批次共 684 请求只是算术汇总，不混加到测试用例数量。
