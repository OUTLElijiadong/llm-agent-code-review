# 失败恢复与复测核验交付记录

## 发布结果

生产已运行3.8.7，代码提交4035d1902342ab88357b5bb6071545d1a0572dd1。完整Git bundle发布，未推送Git远端。274090027字节，SHA256为68f4066025523e5c1687ffa99a0bf8ad726269b468443c5afe43f7171404a300。低速上传改为差异压缩传输，服务器重建后的完整包大小、SHA和Git历史验证均通过；差异传输文件不是最终发布包。

正式脚本完成备份、隔离恢复、构建、迁移检查和HTTPS门禁，退出0。Alembic已经位于048，本轮没有升级数据库结构。备份405224409字节，独立恢复89表/048，未恢复生产业务库。前后端运行镜像、发布账本及默认Compose环境一致；270本版前端文件逐路径摘要匹配。默认环境曾产生旧标签警告，原日志保留，随后只校准四个版本字段且保留其他配置字节，复核运维门禁ok。

## 本轮具体修复

- 审查任务读取统一返回“审查任务不存在或当前账号无权访问”，附下一步操作。实际任务162存在，但父项目158已删除，故旧实现透传项目404；不能将其说成数据库完全不存在任务162。
- 审查详情区分无权/不存在与临时读取失败，清理失权源码及问题缓存、拒绝迟到响应；保留错误、请求编号、重试和返回列表。仅问题权限撤回也清理相关数据。
- 项目列表、详情、上传表单、Agent中心与报告详情补齐部分读取失败、保留用户输入、加载反馈、手动重试、重复提交锁和迟到响应处理。下载本地异常有反馈及资源清理；此项有组件测试，未冒充本轮生产下载实测。
- 登录限流改用毫秒TTL向上取秒，修复亚秒剩余时间误报60秒及意外重启窗口。仍保留5次/60秒准入合同；正确密码不能绕过未结束的受限窗口。配置Redis时故障返回明确503和有界重探测，不以冷进程空计数放行；生产Redis实测PING成功。
- 部署失败保留准确退出码、阶段、迁移尝试状态和恢复结果，修复子Shell重复回滚/解锁及末尾状态读取误判。13种隔离失败场景验证；没有在生产故意注入故障或触发回滚。

“所有失败都能回退”存在业务边界：临时读取可重试；认证/授权失败不能降级放行；已经提交的会话版本不逆向改写；数据库迁移不能依靠自动恢复旧库覆盖业务数据。本轮按这些边界提供明确反馈，没有用成功提示掩盖部分失败。

## 验证与证据

最终后端3975项通过（含5项真实本地Redis Lua），前端1033项通过；无失败/错误/跳过，后端1条既有收集警告。Ruff、ESLint、类型检查与生产构建通过。源码提交后只新增验收文档与辅助证据脚本，没有修改已发布应用。

真实HTTPS分三组：92个夹具请求（38内容断言），550个完整权限矩阵请求（299匿名401、239无权限403、12身份/RBAC读取），42个任务错误专项请求。三组有效验收合计684请求通过；不与单元测试数量混加。首个专项旧脚本13次尝试中的1次断言失败仍保留，它误把网关替换请求编号当成应用异常；修正后42全过，独立重建30条错误信封长度/SHA相同，未包含内部详情。

首次在生产FastAPI0.139.0中遍历app.routes仅得到3个APIRoute；实际嵌套路由为_IncludedRouter，不能把小清单当完整矩阵。已使用完整313路由、49源文件SHA绑定计划完成矩阵补测；首次92个请求只作为夹具验收。官方发布说明确认0.137起内部路由结构变化：[FastAPI发布说明](https://fastapi.tiangolo.com/release-notes/#01370)。

313路由的两类计划共626格，538格已发出安全的401/403负向请求，另88格保留公开、未知依赖等未请求原因；不能声称所有鉴权机制或全部业务正向流程均已覆盖。

四类账号均通过正常浏览器登录，保存36条操作/观察记录。普通成员可读项目与源码，编辑/删除/上传/保存等写入口按项目身份隐藏；独立账号只看到自己的项目，跨账号错误页可重试和返回；零权限账号直接访问审查页进入403，返回首页正常。不可见任务错误页重试后仍诚实报错，更新请求编号，并可返回审查列表。控件台账按真实点击、可见性和动态实例部分覆盖分别记录，仍有未测项，详见TODO。

42用量归因外键与048契约一致。项目161八字段SHA逐项一致，整行9e49ca15c51357ed0fc9c71703c8371ab9b67593e493b8385b4e753826131194。审查任务161历史存储总数16未改，报告摘要仍为4条未分级，报告SHA431b70ea13309a291f8483c3e8ab645d70d613305fa3206f1c62197c19d7a43a。历史task11/team5、task35/team12、7孤儿及3执行中记录保持；全局运维计数随时间变化，未用总量变化推断历史状态被改写。

## 收尾状态

新知识库preflight收据20260908-043653-5695123a36为ready；客观复测澄清通过record-correction写入待确认草稿，没有覆盖原报告。最终validate为485文件、0错误、0警告；postflight退出0并包含QA停用阻断与UI/动效未完成边界，详细回执另见证据。本地无网络Redis测试容器已清理。

本轮QA107–110只读快照确认0模型调用、0审查任务。有限收尾脚本固定这4账号/角色7，保留项目164/165与文件1894/1895，已44项测试与独立复核。自动审批拒绝停用，理由是需要用户明确批准具体生产访问变更范围；确认已提出，尚未停用，不能声称测试账号已收尾。

CSP、JWT存储、弱密码政策仍为独立建议项。本轮只核事实，不擅自迁移认证架构；动效库与位置待用户确认后单独实施。完整未完成项及操作入口在TODO文档中列明。
