# QA 收尾脚本独立复核

已完整读取本轮收尾脚本、44项测试、现有 `prepare_permission_acceptance.py --disable`、真实登录和鉴权依赖，并对照项目/代码文件完整模型字段。未发现阻止本轮限定收尾的实现问题。独立本地复跑44项全部通过，零失败、错误或跳过；该结论是准备就绪，不表示生产账号已停用。

固定 marker=20260908、账号107/108/109/110、专用角色7、严格20权限、项目164/165及文件1894/1895。查询包含QA账号的额外项目、成员和角色关联，以及项目的额外文件；出现未预期范围便停止。三阶段快照只在只读一致性事务内查询，UTF8显式指定，最终ROLLBACK。项目8字段及代码文件15字段均进入哈希，正文和二进制只使用SHA，不输出原内容。

四次正常登录先逐项验证真实身份与me200，然后核登录版本精确+1。实际停用复用固定SHA的已有CLI，对四个用户加锁并仅设status0、token_version再+1。停用后四个旧token应返回403/40301：真实依赖先检查status，再检查会话版本，不能误要求401。既有三条成员关系、角色与权限、QA项目和文件仍保留。

凭据限定root私有目录、root0600正规文件及NOFOLLOW；不从环境或任意URL取凭据。默认计划不读凭据、不构造HTTP客户端。执行时TLS正常验证、禁环境代理与重定向，只允许login/me两条路径。容器临时副本root执行，目录0700、文件0600，普通退出清理。命令输出只解析固定status/IDs；异常输出只含固定错误码，不记录JWT、密码或异常原文。O_EXCL结果文件和fsync执行意图防止自动重放；发生不确定结果后停止。

本地完整集成使用真实FastAPI登录/me、bcrypt/JWT、SQLAlchemy与现有prepare CLI，只将HTTP及DB边界替换为隔离TestClient/SQLite；没有真实网络、Docker或生产数据库请求。实测只停用四账号，历史admin、项目161、审查161的完整行保持，QA项目/文件完整行保持，模型账本0、新增审查0。生产SQL则按当前模型逐字段审阅，没有把本地SQLite当作MySQL实测。

硬杀或宿主失联可能遗留root-only临时目录和未完成收据；此时必须按已保存stage只读核查，不能重放停用。生产全库未改写的判断以固定CLI范围及发布后证据为准，不把本地历史夹具当作生产全库快照。

最终来源SHA、准确44项XML计数及执行范围在 `证据/QA收尾脚本独立复核.json`。脚本SHA为3232d5d6574e437ce02eb627c32a7f02b8b5584c9ba42e15f55bb70c94d8cdb6。本次没有修改应用或运行生产收尾；等待主代理明确通知执行。
