# 发布前数据与前端独立复核

本次独审只读已有生产 JSON 和采集器源码，在本地执行合成 API 的 Vue 挂载测试；没有再次连接生产、写库、下载业务文件或调用模型。

## 生产基线

`证据/发布前版本与历史账本独立复核.json` 全部断言通过。两次生产运行态 JSON 除采集时间外逐字段一致：版本3.8.6、发布SHA `0a14b5359a446ab7b447e10c061b1ebbe6d15e29`、048迁移、两容器身份/镜像/启动时间、账本SHA均未变。独立重数7表×6列为42条唯一、可空、BIGINT、SET NULL外键，目标表/列正确。

前端证据记录269个镜像文件、3709个服务目录文件，manifest和首页SHA与上轮一致。当前JSON只有总数和manifest，没有逐文件map；本次核对摘要及只读collector的逐文件比较逻辑，没有声称在本地重新计算当前269份原始文件。

历史task11/team5仍为queued/failed，task35/team12仍为waiting_dependency/failed。两任务、同团队6任务、23事件、7孤儿工具账本（125–129、2580–2581）与3条executing（472、634、2286；父run均failed）全部已输出字段及内容指纹与上轮一致。查询SHA与已审collector SQL匹配，首尾read_only=1且最终ROLLBACK。

全局动态计数不能写成不变：工具账本2576→2608（成功+28、失败+4）；completed根执行+1；ops成功42900→44831（+1931），ops失败43173→43178（+5）。这些只是快照差值，不用于推断调用者或原因。新工具总数2608与状态分组相加一致；无无效参数、终态缺结果或请求绑定不匹配。

## 项目161与审查任务161

`证据/发布前项目与审查161独立复核.json` 全部断言通过。项目161的8个字段逐项SHA及整行SHA与上轮及已审旧备份基线完全一致，collector嵌入基线字节也与真实归档文件一致。只读事务目标行唯一，未重新读取或恢复备份。

审查任务161仍success，旧数据库total=16、更新时间、报告原文SHA均不变；helper、collector、SQL指纹匹配。旧结构证据独立重数为4个发现标题、16个字段项目符号，0个显式严重度标签；当前摘要仍4条未分级。不能把16字段当16问题，也不能将4条报告发现说成已确诊漏洞或将无严重度说成零风险。未下载报告正文；当前报告/helper指纹与既有证据一致。

## 前端独立复核

完整复读7个应用文件的失败恢复改动、父子提交边界和调用函数；独立执行`FailureRecovery.test.ts`24项及`FailureRecoveryIndependent.test.ts`4项，28/28通过，API合成，真实Vue与ElementPlus挂载。证据为`前端失败恢复最终独立复跑.xml`与`前端失败恢复独立复核指纹.json`。父代理88项关联专项没有在本子任务重复执行。

独审打回的四个实际边界已关闭：报告本地点击异常释放Blob URL及锚点；403后迟到HTML不重开预览；卸载后迟到HTML不回写；拖拽收集等待期父提交锁生效后不补发分析。准确四反例来自Git HEAD报告基线与本轮ProjectForm，见`前端独立四反例-报告基线与当前表单.xml`（4失败），不冒充父代理84专项时的全部源码。早期空测试路径、mock路由缺导出均是本独审夹具准备错误，保留定位XML；候选报告已修后的首次有效运行是3通过/表单1失败，亦单独保留。

普通503保留旧筛选快照且提示条件不一致；401/403/404清缓存。创建/上传由父组件锁覆盖完整Promise，子组件验证锁不会因emit立即返回而提前解锁；写请求超时保留输入、不自动重发。Agent独立请求成功部分可用，失败不能弹全部同步成功。报告导出在返回后复核权限和卸载状态，失败保留反馈，资源在finally释放。本地锚点替身不等于真实浏览器已落盘下载。

## 控件台账

`证据/本轮控件台账独立初始化复核.json` 全部通过：104份Vue源码SHA正确，616个唯一交互节点、452按钮；五角色各616个唯一节点，共3080格。JSON/CSV每字段一致，2464未测、616管理员登录阻塞、0本轮通过；本轮账号ID均为空。旧QA只作为历史引用。

上轮35格67条观察中34格66条得到唯一映射，另外1格1条同名源码下载观察完整保存在未映射文件；24条补充观察保留。历史观察内容/证据与旧台账逐项相同，不提升为本轮通过。此处是初始化计划，不能替代后续真实身份和生产交互验收。

本次独审无未关闭的已复现阻断项。应用与生产发布结论由主任务最终全量测试和真实验收给出，本报告不代表全站每个按钮完成。
