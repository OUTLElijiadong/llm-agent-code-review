# 发布后数据与备份独立复核

2026-09-08，独立审阅人 `usage_attribution`。本次仅在本地读取已采集证据、采集器源码和正式发布日志，35 项检查通过；未再次连接生产，未重跑测试、模型、迁移、备份或恢复。应用目录相对正式发布提交 `4035d1902342ab88357b5bb6071545d1a0572dd1` 无差异。

| 核验对象 | 独立结果 |
| --- | --- |
| 运行版本与镜像 | 3.8.7；前后端均绑定正式提交，镜像 ID 与正式构建日志一致，健康检查正常。运行态与资源证据的容器 ID、启动时间及发布账本 SHA 一致。 |
| 数据库 | 048；7 表各 6 个，共 42 个唯一外键，完整定义与发布前相同，均为可空 BIGINT、删除父行 SET NULL。 |
| 指定历史数据 | 两项历史任务、6 个团队任务、23 条事件、7 条孤儿工具记录、3 条 executing 旧记录完整字段/摘要与发布前相同。task11/team5 仍 queued/failed；task35/team12 仍 waiting_dependency/failed。 |
| 工具账本 | 总数 2608，前后相同；关系及内容完整性聚合相同。仅全局 ops_execution 的 failed 增 1、success 增 63，不能据此推定调用者或原因。 |
| 项目 161 | 八字段及行 SHA 保持；行 SHA 为 `9e49ca15c51357ed0fc9c71703c8371ab9b67593e493b8385b4e753826131194`。 |
| 审查任务 161 | 存储旧值 16、success 状态、更新时间及报告 SHA 保持；同版解析器返回 4 条未分级报告发现。不能将其写成 4 个已确认漏洞或零风险。 |
| 前端资源 | 实际为 **270** 个镜像应有文件，与 served 对应路径逐一 SHA 相等；独立重算清单 SHA 为 `bbbe99af2f6212fcc497f06b9428832dacde3fb27a6c6b50b910c9ee738282ee`。公开目录共 3725 文件，包含保留资源。 |
| 备份 | `code_review_20260908T053024Z_4035d1902342.sql.gz`，405224409 bytes，SHA `e9672fb383e674c0394769c6a3ac609c2bbc2aa4dd727be4f563d8fa03c4dc9b`；白名单 meta 为本次提交、89 表、048，与独立恢复日志一致。 |

正式发布原日志已完整阅读并按原字节保存为 `证据/生产正式发布.log`：32334 bytes，SHA `4c39f9f2aabf7a28dddffc443ce592b95da53aab09260a3d7b362d6785fde00e`。日志中 05:34:39 的默认 `APP_RELEASE` 漂移警告如实保留。随后 `生产默认发布环境校准.json` 记录已备份并仅校准四个默认发布字段，05:41:30 的 `发布后生产运维门禁.json` 确认账本、运行镜像与默认环境一致，`status=ok`、`can_continue=true`，无阻断项。

备份 gzip 大小与 SHA 来自已审采集器对生产文件的实际读取。本地独立重算的是 270 路径的规范清单和 checksum 旁文件的规范字节 SHA；未重新下载或解压备份，metadata 原始字节未归档，不能声称本地重算其原字节 SHA。3725 个完整目录仅有采集摘要，本次未独立重算其全部路径。报告 Markdown 原文没有下载，本次核对前后原文 SHA、同版 helper 与完整输出摘要。

日志存在发布完成事件及 89 表/048 的隔离恢复通过事件，但没有备份文件名或进程退出码行：精确备份文件关联来自采集器与发布账本的核对；发布进程 exit 0 来自主代理工具执行结果，不能冒称从日志文本重新算得。默认环境原文未读取，其无关字段保留结论来自主代理校准记录；后续运维门禁提供独立的一致性检查结果。

离线复核命令为 `python3 docs/失败恢复与复测核验20260908/证据/independent_post_release_recheck.py`，退出 0。完整来源 SHA、逐项检查、历史摘要及范围在 `证据/3.8.7发布后数据与备份独立复核.json`。本结论覆盖发布与上述指定数据，不代表全部角色按钮已完成浏览器点击验收。
