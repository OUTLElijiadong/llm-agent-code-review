# 发布失败处理独立复核

复核时间：2026-09-08。范围是发布事务失败反馈、应用回退和维护锁；只在本地隔离工作区执行真实 `deploy.sh`、`rollback.sh` 和共享函数。Docker、curl、Git、备份及资源同步均为可控命令替身，没有生产连接、数据库迁移或模型请求。不能将本次结果称为生产故障演练。

## 准确旧反例

`证据/发布失败边界独立反例.json` 绑定旧部署脚本 SHA `8ffc22303c96298a7fbadf84b2471ed61491a307ef06c909e949cfa4c1cbd6fa` 和旧回退脚本 SHA `5c676c3585aa5cb4f5db4b4af51cb997bf0913b75591a39d1dab0f75a82ce99e`，独立复现三项：

- 失败处理与 EXIT 各释放一次锁，合计两次；在两次之间模拟下一任务取得同路径空目录锁，第二次删除了新持有者的锁。
- 应用已回退、冒烟和账本均完成后，最终 `compose ps` 返回 46，父发布却报告“自动回滚失败”。
- 资源同步返回 43，原 `deploy_fatal` 将发布退出码改成 1。

主代理已修复，独立审查未修改应用和部署源码。

## 修复复验

`证据/发布失败边界独立修复复核.json` 六个独立场景全部通过：

| 场景 | 退出码 | 独立断言 |
| --- | --- | --- |
| 备份失败后的锁交接 | 41 | 只释放一次；模拟下一持有者的锁仍在 |
| 回退完成后的容器列表读取失败 | 44 | 明确只读展示警告；回退成功；保留原发布失败码 |
| 资源同步失败 | 43 | 原错误码保留，应用回退完成 |
| 迁移预检显式 fatal | 1 | 显示阶段；应用未切换；原账本不变，pending 保留 |
| 迁移核验显式 fatal | 1 | 显示阶段及“迁移已尝试”；未假称数据库还原 |
| 发布账本提交后的容器列表读取失败 | 0 | 保留已验收发布；不回退；明确展示警告 |

复验执行前冻结源码字节，结束时重新计算指纹，三份源文件均无漂移。脚本为 `证据/independent_deploy_failure_boundaries.py`，通过 `--fixtures` 指向父矩阵生成的无网络替身目录，并以 `--source` 选择待审源码。

- `deploy/deploy.sh`：`0544c0a996817210b7385f2486dc2877d9af89adb5cbfdc5e4787537006a341e`
- `deploy/rollback.sh`：`bad177ec5a332d90134840bb7c4768a13b854d8917312348356ed65d2b517eb5`
- `deploy/lib/common.sh`：`949687d893c55e5cbc5bd5f3be7586f2163965dcd57b3fea4eb5b034def1f0f1`

父代理新增的 13 场景矩阵使用真实回退脚本，覆盖构建、切换、健康检查、冒烟、同步及回退自身失败；本独审六项补充其锁交接、显式退出和提交后展示边界。两组存在重叠，不直接相加宣称独立故障类型总数。

`ops-check.sh` 的新增 `-1` 分支已静态复核：文案改为采集失败，磁盘/内存门禁仍关闭。该集成注入由主代理执行，本次六项证据不包含其运行结果。

## 结论与边界

上述三项反例已关闭，六项独立边界通过，无新的阻断发现。应用回退只切换经过账本和镜像校验的前后端，不自动执行数据库 downgrade 或 restore；迁移失败可能有部分数据库变更，恢复仍须依据已验证备份和结构证据。命令替身验证失败控制流，不证明真实 Docker、数据库、网络、断电或外部进程强杀均可自动恢复。
