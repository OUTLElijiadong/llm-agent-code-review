# 审查详情失败恢复独立复核

## 首轮结论

独立读取四个变更文件、真实审查路由、项目资源访问服务、异常响应与前端 HTTP 合同，逐条重算作者的四个 SHA256 和五份 XML。均与作者冻结指纹一致，明细在 `证据/审查详情首轮独立指纹复核.json`。

后端两个 GET 只将父项目资源的 NotFoundError 收敛为审查任务统一 404；先执行真实用户/RBAC 依赖，owner 与 reviewer 保留资源交集，认证 401、权限 403、基础设施 503 未被错误转换。服务未增加写入。作者 14 项真实路由测试包含六类资源状态 × 两接口、合法成员、缺权限/禁用/匿名、503 传播和只读 SQL 监听；XML 14/14 通过、相关 118/118 通过，红测14项中2失败，重算一致。

前端的首次失败/重试/返回列表、请求编号、detail 401/403/404 清理缓存、序号丢弃迟到代码/问题、路由切换和卸载停止轮询实现与新增测试一致；52项作者回归全通过，红测其中6失败，重算一致。重试只读取原任务，不启动新审查。

## 打回问题

仍有一个必须补齐的访问撤回分支：用户保留 review:view，仅失去 issue:view 时，任务详情 GET 继续200，而问题 GET 返回403。`doLoadIssues` 的 catch 只更新 issuesError，旧 issues 与已选择问题仍留在页面，可继续点击；新的清理逻辑只在详情读取失败时触发，因此没有覆盖该情况。

独立临时挂载测试仅模拟本地 API 响应，先加载1条私有问题，再在3秒轮询中保持详情200、问题40303。新断言“旧问题标题和行消失”实际失败。`证据/审查问题权限撤回独立红测.xml` 为1项1失败，源码副本 `证据/审查问题权限撤回独立测试.ts`；没有修改作者业务源或正式测试。临时测试不在默认 src 测试集合中。

已交作者有界修正：问题读取的访问类失败清除问题/选中项/抽屉及相关迟到状态，保留仍获授权的任务摘要；网络/503失败继续明确标注上次成功内容。修后需同一独立反例转绿，再刷新最终文件指纹。当前首轮不能直接记为全部通过。

## 修复后复核结论

作者按打回范围补齐 `discardIssueSnapshot`：问题401/403/404清空问题条目、总数、选择、抽屉和仅由问题补充的文件；清除代码预览并递增代码序号，忽略迟到旧问题事件。详情仍200时保留其已授权摘要和任务文件，问题503继续保留明确标识的旧结果。错误提示仅在确有旧问题时描述缓存内容，不再声称空列表保存了上次数据。

独立重新读取最终 diff，并原样运行同一个独立反例，`审查问题权限撤回独立修复后.xml` 为1项1通过。作者新增三类访问失效与503的四项回归，`审查问题访问失效-准确红测.xml` 为56项中3失败/53通过，`审查问题访问失效-修复后.xml` 为56项全部通过；独立重新计算 testcase 数、失败数和 SHA。后端文件保持首轮指纹，两个前端文件的新 SHA 与作者最终冻结消息一致。数据在 `证据/审查详情最终独立复核.json`。

该打回项已经关闭，当前有界审查未发现剩余阻断。没有改动作者业务文件、正式测试、生产或历史数据。自建 `frontend/.audit-review-20260908` 的两个文件与空目录均已删除，独立测试与运行配置副本保存在文档证据目录，不进入前端全量/lint/build。独立复核没有再次运行作者全套52/56项，而是逐条审阅并重算其证据，再实际执行独立反例；主任务负责最终全量验证。
