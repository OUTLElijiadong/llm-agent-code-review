# 全量功能回归测试 · 待办

## 需要支持

1. 恢复应用内 Browser 路由能力，避免后续全量测试依赖 Safari Computer Use 兜底。
2. 如需覆盖 Agent 自进化最终审批/回滚、真实账号禁用/重置密码，请提供隔离账号或隔离库。

## 非阻塞优化

1. 迁移 Sass `@import` 和 legacy JS API。
2. 拆分 Monaco Editor 等大体积构建 chunk。
3. 增加 API 路由、导出器、讨论 WebSocket 集成测试，提高当前 `39%` 覆盖率。

## 2026-06-10 复测后的待办

1. 覆盖率已随测试增长到约 `40%`，仍低于核心目标；建议继续补充 API 配置、全量安全扫描、报告导出和 WebSocket 讨论链路的集成测试。
2. 如需验证真实“启动审查”按钮最终调用外部大模型，请提供可消耗的测试 API Key 或隔离额度；本轮 UI 只验证到项目/文件选择和重置，接口层验证使用受控分支。
3. 如需验证用户禁用、重置密码、Agent 自进化审批/回滚的最终提交，请提供隔离用户或隔离库，避免影响 `admin` 和真实业务数据。
