# 全量功能回归测试 · 验收记录

## 执行信息

- 日期：2026-06-03
- 环境：macOS + Docker MySQL + FastAPI + Vite + Safari Computer Use
- 数据库约束：使用 `cr_mysql`，禁止 SQLite
- 测试账号：本地管理员账号
- 测试数据：`qa_deepseek_full_click_20260602` 合成项目、`qa_security_fixture.py` 合成文件、QA 自定义规则和任务 19-25

## 自动化检查

| 检查项 | 状态 | 结果 |
| --- | --- | --- |
| Docker MySQL 健康检查 | 已完成 | `cr_mysql` 状态为 `healthy` |
| 后端 pytest | 已完成 | `112 passed`，覆盖率 `38%` |
| 后端 ruff | 已完成 | `All checks passed!` |
| 后端 compileall | 已完成 | 通过 |
| 前端构建 | 已完成 | `npm run build` 通过 |
| API DB 交叉核对 | 已完成 | `_qa_crosscheck.py` 全部通过 |
| FastAPI 日志 | 已完成 | 清理与回归请求均为预期 `2xx`，未发现未处理异常 |
| 清理后残留核对 | 已完成 | project/file/version/task/task_file/issue/report/ai_log/rule/audit_log 的 QA 残留均为 `0` |

## 真实点击覆盖

| 页面或功能 | 已点击内容 | 结果 |
| --- | --- | --- |
| 登录/注册/退出 | 登录、登出、注册表单跳转与返回 | 成功；未创建真实业务用户 |
| 仪表盘 | 时间范围、导出周报、新建审查、统计卡联动 | 成功 |
| 项目管理 | 表格/卡片、搜索/筛选/重置、新建、编辑、详情、删除确认 | 成功；QA 项目已真实点击删除 |
| 代码中心/编辑器 | 项目筛选、文件查看、编辑保存、版本查看、恢复确认、下载 | 成功；修复编辑器旧版本污染问题 |
| 审查启动 | quick/standard/security/performance/full/discuss 六种类型、项目/文件选择、重置、提交 | 成功；DeepSeek 在 QA 文件上真实调用 |
| 审查任务 | 状态筛选、排序、日期、详情、删除确认与删除 | 成功；QA 任务 19-25 已真实点击删除 |
| 圆桌讨论 | 启动讨论、消息流、停止分支、结果落库 | 成功；WebSocket 打开/关闭正常 |
| 问题中心 | 筛选、全选、行选择、详情抽屉、状态操作 | 成功 |
| 报告列表/详情 | 查看、Word/PDF 导出、复制、打印入口、删除确认 | 成功；DOCX/PDF 二进制有效 |
| 安全中心 | 安全扫描弹窗、参数开关、扫描提交、结果查看 | 成功；使用 QA 文件验证 |
| 审查规则 | 新建、编辑、筛选、启停、删除确认与删除 | 成功；新增搜索可定位大量规则中的自定义规则 |
| 个人中心 | 资料编辑、密码表单校验 | 成功；未提交真实密码变更 |
| 用户管理 | 搜索、状态筛选、角色筛选、重置密码/状态确认框 | 成功；未改动真实账号 |
| Agent 中心/日志 | Agent 抽屉、运行态、日志筛选、详情查看 | 成功 |
| 系统审计 | 搜索、筛选、分页、详情 | 成功 |
| Agent 自进化 | 提案列表、筛选、详情、审批/回滚确认路径 | 成功；未执行会改变规则库的进化审批 |

## 缺陷与修复

| 缺陷 | 修复结果 |
| --- | --- |
| Axios 统一 JSON 拦截器把 Blob 导出响应误判为失败 | Blob 响应直接放行，下载 helper 返回 `response.data` |
| AI 修复提示词弹窗打开即自动调用外部模型 | 改为显式生成；LLM 润色增加二次确认 |
| AI 提示词可能携带二进制 Base64 或过长文本 | 跳过二进制上下文；脱敏后限制 `4000` 字符 |
| passlib 与 bcrypt 4.1.x 兼容告警 | 固定 `bcrypt>=4.0.0,<4.1.0` |
| 本地核对脚本受代理环境变量影响 | `httpx.Client(trust_env=False)` |
| 编辑器保存后可能保留旧版本内容 | 修复 Monaco 编辑器与当前文件版本同步 |
| 路由切换导致圆桌讨论状态丢失 | 改为不触发重挂载的 URL 清理方式 |
| 审查任务与文件关系未持久化，报告/讨论回查不稳定 | 新增 `review_task_file` 关联表和服务层写入 |
| Word/PDF 报告导出缺少多文件/问题上下文 | 导出器补充任务文件和问题统计 |
| Monaco worker 警告 | 增加 worker 配置 |
| Element Plus tag 类型警告 | 补齐显式 tag type |
| 规则列表大量数据下自定义规则不易定位 | 新增规则页搜索，并兼容下划线/短横线被输入法丢失的场景 |

## 清理结果

| 对象 | 结果 |
| --- | --- |
| QA 项目 `id=3` | UI 删除后 SQL 精确清理，残留 `0` |
| QA 文件 `id=496` 与版本 | SQL 精确清理，残留 `0` |
| QA 审查任务 `19-25` | UI 逐条删除后 SQL 精确清理，残留 `0` |
| QA 问题、任务文件关联、报告、AI 调用日志、审计日志 | SQL 精确清理，残留 `0` |
| QA 规则 `id=241` | UI 搜索定位后删除，残留 `0` |

## 验收结论

本轮全量真实点击、日志监控、DeepSeek QA 调用、缺陷修复、测试验证和合成数据清理均已完成。应用内 Browser 在后半程仍不可用，已用 Safari Computer Use 完成剩余真实点击验证。
