# Todo：通用 AI 接口配置与双栏滚动隔离

## 当前无生产阻断

通用配置已经在生产拉取模型并测试连接成功；系统默认回退、错误重试、停用覆盖清理和双栏滚动隔离均已交付。没有缺失 API Key、数据库迁移或容器配置。

## 需要外部条件的后续动作

1. **管理员登录态浏览器复核**：在正常管理员账号已登录的浏览器中打开 `https://lijiadong.cn/admin/llm`，从任一长管理页滚动到底部，点击“大模型配置”，确认右侧立即回到顶部；分别滚动侧栏和内容区，确认另一侧位置不变。该动作只读，不需要再次修改配置。
2. **远端分支同步（已完成）**：本地分支 `codex/production-resilience-20260901` 与 GitHub 同名远端分支已同步。生产运行代码仍是已验收的 `4b1711a`（`v3.8.3`）；后续文档提交不改变生产运行版本。

当前生产磁盘为 `85%`，`ops-check` 的 `disk_cleanup_review` 是非阻断人工审阅项；受控清理已执行并保留保护清单，不需要绕过脚本手工删除资源。

## 长期改进（不阻塞本次交付）

- 继续扩充跨文件、动态分派、装饰器、反射和 sanitizer 真值集，并按模型/提示词/分片器版本观测最差召回率与不稳定率。
- 若需要多副本重启无感追问，再将现有 Clarify 状态迁移到带 TTL、用户绑定和一次性消费语义的 Redis。
- 如需灾备级恢复，再配置独立于本机的异地备份副本和告警；当前本机备份、校验和隔离恢复已可用。
