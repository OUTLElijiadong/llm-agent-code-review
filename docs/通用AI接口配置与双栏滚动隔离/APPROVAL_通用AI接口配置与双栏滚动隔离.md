# 通用 AI 接口配置与双栏滚动隔离：执行审批

## 审批依据

- 用户已明确要求完整完成后续待办、优化通用 AI 接口配置、支持拉取模型，并修复管理后台双栏滚动问题。
- 用户已授权读取生产状态和完整执行权限，随后要求继续并完成本项工作。
- 本次实现不恢复已退役的 FlyTrap，不删除历史证据，不输出或提交 API Key 明文。

## 执行检查清单

| 门控 | 结论 | 依据 |
| --- | --- | --- |
| 完整性 | 通过 | 模型拉取、连接测试、配置保存、运行时传递、错误回退、滚动隔离、备份验真均已纳入原子任务 |
| 一致性 | 通过 | 复用现有 FastAPI、SQLAlchemy、Fernet、Vue、Element Plus 和部署脚本结构 |
| 可行性 | 通过 | 不新增数据库迁移；新增字段对旧 JSON 和旧客户端均有默认值 |
| 可控性 | 通过 | 上游请求受 SSRF、总超时和有限重试约束；备份恢复目标有生产容器硬拒绝 |
| 可测性 | 通过 | 后端、前端、构建、部署脚本和生产浏览器均有独立验收入口 |

## 执行边界

1. 允许提交并部署本任务代码至当前生产分支。
2. 允许在生产页面执行只读的模型拉取和最小连接测试。
3. 允许备份验证脚本在专用 `cr_testdb` 容器内创建并清理随机临时数据库。
4. 禁止在 `cr_mysql` 生产数据库中恢复备份，禁止记录或回显密钥，禁止恢复 FlyTrap。

## 审批结论

需求、边界、架构、任务依赖和验收标准均已闭合，进入自动化实现、测试与生产验收阶段。
