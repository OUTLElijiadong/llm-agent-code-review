# 服务器错误修复与数据库迁移 - TODO

## 当前待办

- 无阻塞待办。

## 后续建议

- 把数据库迁移机制固化为 Alembic 或可重复执行的部署迁移脚本，避免服务器初始化 SQL 再次落后于 ORM 模型。
- 定期把 `/opt/code-review/backups/` 下的数据库备份同步到对象存储或本地归档。
- 如需完整浏览器级回归，可补装 Playwright 或使用真实浏览器录制登录后主流程。
