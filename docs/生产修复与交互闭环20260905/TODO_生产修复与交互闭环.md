# 生产修复与交互闭环：待办

## 阻塞项

| 待办 | 当前状态 | 需要的支持 |
| --- | --- | --- |
| 稳定提交和发布包 | 2026-09-06 已明确授权，待执行 | 仅在既有分支创建本地 Git commit；不创建分支、不推送；排除旧只读审计中的数据库副本和用户文件 |
| 生产迁移 047 | 已获发布授权，待执行 | 稳定提交后先生成当次备份并验证恢复，再正常发布；最终结果另存实际发布验收记录 |
| 生产精确隔离 | 仅 dry-run | 核对确证来源、精确目标、当次备份与可逆清单；不新增人为审批编号要求，不处理来源不明的业务项目 |
| 真实账号验收 | 阻塞 | 管理员、普通成员和无权限账号的已登录浏览器会话 |
| 真实模型闭环 | 未执行 | 取得真实会话后核对已有模型可用性，以受控小任务验收；不猜密钥、不把接口桩算成真实模型 |
| 发布链版本防护 | 本地 19 项反例和完整 Shell 回归通过；生产未应用 | 实际发布前后以已验证账本校准 `.env` 中 APP_RELEASE、APP_VERSION、BACKEND_RELEASE、FRONTEND_RELEASE；不改其它字段；ops-check 的 release 项必须为 ok |

## 已知限制

- 生产历史快照和 AI 用量中的缺失值没有可验证来源，继续显示为 unknown。
- `system` 指标依赖运行环境的 `psutil` 能力；不可用时页面显示不可用，不填充假数值。
- 独立复核发现 1 个非阻断 `PytestCollectionWarning`，不影响通过，但后续可清理测试类命名。
- 本次前端浏览器检查覆盖本地隔离环境和响应式 DOM 尺寸，生产真实数据页面仍需认证后复验。
- 扩大 Ruff 检查到整个 `backend/` 时，5 个未修改的历史辅助脚本报 149 项问题；运行时应用、047 迁移和本轮隔离脚本的定向检查通过。不得表述为整个仓库 Ruff 零问题，也不在此次发布修复中批量改写旧种子数据脚本。
- ShellCheck 保留 1 项原有 `SC2034` 未使用局部变量警告；本轮没有新增 ShellCheck 警告。Shell 语法与完整事务回归通过，不等于 ShellCheck 零警告。

## 执行指引

获得上述支持后，按 `FINAL_生产修复与交互闭环.md` 的发布门禁顺序执行；每一步保存命令输出、版本 SHA、数据库版本、HTTP 响应和浏览器截图，任何一步失败立即停止，不把部分成功记为发布成功。

正式巡检必须同时核对 JSON 顶层 `status="ok"` 和 release 一致性，不能只凭退出码 0 判定完全健康：`degraded` 也可能返回 0。默认配置仍漂移时，先保留恢复材料并完成校准，不直接使用裸 Compose 重建。
