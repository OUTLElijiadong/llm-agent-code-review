# 生产修复与交互闭环：需求对齐

## 原始需求与授权

用户要求生产全量核验、数据库真实数据、接口和输入输出闭环、扫描内容非空、规范化数据库与代码，以及清晰的人机交互和每步反馈。2026-09-05 用户回复“继续，完全允许”，本阶段从只读审计转入实现、测试及有备份的生产验收。

前置只读审计位于 `../生产只读全量核验20260905/`，其证据保持不变。知识库前置收据为 `20260905-105929-703efae48d`，硬门为 `ready`。

## 生产发布续接

本轮原始需求为“帮我完整修复后上传生产，确保生产不会复现错误”。本地核验日期为 2026-09-06（UTC+08:00），对应 UTC 2026-09-05；本轮知识库前置收据为 `20260905-165215-7db64dba4a`，硬门为 `ready`。不把 UTC 文件名日期误读为本地验收日期。

沿用既有生产操作授权，不重复要求另行指定数据库管理员、审批编号或人为新增维护窗口。本轮仍须满足明确 Git commit 授权、稳定版本、备份恢复验证以及真实账号会话的实际门禁；没有绕过提交要求发布脏工作区。

新增已复现范围：总览在数据缺失或失败时误报正常、权限拒绝后的自动刷新、发布清单与仅允许 all 的脚本不一致，以及生产默认 Compose 版本漂移、回滚版本标识和恢复前迁移镜像绑定风险。只修复与本次可安全发布直接相关的问题。

生产验收必须使用同一制品重新运行原缺陷反例和真实业务回显。有限测试不能证明任意未来输入或外部依赖永不出错；不能将本地隔离数据库、接口桩或未登录页面当成生产签收。

## 执行边界

- 以已复现问题为修复依据，不承诺有限测试能证明系统绝无缺陷。
- 保留合法空源码标记文件，但禁止有效内容为空的扫描输入；不删除真实代码以满足非空。
- 所有必需分析失败或覆盖不完整不得显示成功和满分；保留已产生的问题及失败原因。
- 测试数据只按确证来源及显式清单可逆隔离，不按名称模糊匹配删除。
- 不添加无依据的全库级联外键，不回写无法证明的历史扫描版本或上游用量。
- 生产发布沿用备份、恢复验证、精确版本及回滚门禁。脚本要求干净提交；已向用户单独询问本地提交授权，不绕过门禁。
- 四个设计参考已识别标题，未全片观看；优化层级、真实反馈、错误恢复和适度动效，不虚构数据或进度。
