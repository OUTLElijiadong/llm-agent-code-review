# 独立只读后端验收报告

## 1. 验收范围

- 仓库：`/Users/li/Documents/代码程序/基于大模型智能体的代码审查平台`
- 验收日期：2026-09-05
- 模式：独立只读；不修改源码、不访问生产数据库、不调用真实模型、不读取真实凭据或 `.env`，不提交 Git。
- 重点阅读：`backend/app/services/review_input_service.py:13`、`backend/app/services/review_service.py:116`、`backend/app/services/review_service.py:218`、`backend/app/services/review_service.py:314`、`backend/app/services/review_service.py:566`、`backend/app/services/review_service.py:1535`、`backend/app/services/review_service.py:1727`、`backend/app/services/project_member_service.py:22`、`backend/app/services/project_service.py:311`、`backend/app/agents/security_sentinel_agent.py:418`、`backend/app/agents/security_sentinel_agent.py:607`、`backend/app/agents/security_sentinel_agent.py:1444`、`backend/app/agents/ai_prompt_agent.py:154`、`backend/alembic/versions/047_review_input_snapshot.py:12`。
- 重点测试：`backend/tests/test_review_recovery_integrity.py`、`backend/tests/test_review_input_lifecycle.py`、`backend/tests/test_project_lifecycle_guards.py`、`backend/tests/unit/agents/test_agent_project_quarantine.py`、`backend/tests/unit/agents/test_scan_entry_input_closure.py`、`backend/tests/unit/services/test_scheduler_configuration_closure.py`、`backend/tests/unit/test_migration_047_review_input_snapshot.py`。

## 2. 测试结论

### 全套后端

- 用户指定的 `independent-backend-final3`：退出码 `0`，`2782 passed`，`0 failed`，`0 errors`，`0 skipped`，耗时约 `86.99s`。
- 同一安全执行器下完成带源码 SHA 前后快照的最终复跑 `independent-backend-final3-hashcheck2`：退出码 `0`，JUnit `2782/2782` 通过，`0 failed`、`0 errors`、`0 skipped`，耗时约 `101.20s`。
- JUnit：`/tmp/prism-readonly-20260905-tests/independent-backend-final3-hashcheck2.xml`。
- 执行日志：`/tmp/prism-readonly-20260905-tests/independent-backend-final3-hashcheck2.log`。

### 目标闭环测试

- 当前源码下重新独立执行 `backend/tests/test_review_recovery_integrity.py`：`13 passed`，`0 failed`，`0 errors`，`0 skipped`。
- 目标反例集合在最终全套 JUnit 中共 `167` 个参数化实例，全部通过：
  - `test_review_recovery_integrity`：13
  - `test_review_input_lifecycle`：13
  - `test_project_lifecycle_guards`：12
  - `test_agent_project_quarantine`：18
  - `test_scan_entry_input_closure`：73
  - `test_scheduler_configuration_closure`：37
  - `test_migration_047_review_input_snapshot`：1
- `test_review_recovery_integrity.py` 当前仍保留恢复失败终态、历史快照拒绝替换、部分模型输出不得成功、损坏版本不得标记已验证、原子快照提交、跨连接冻结内容、取消/删除竞态、并行部分证据、自定义代理执行等断言；未发现断言被删除或弱化为仅检查表面成功。

## 3. 关键业务结论

### 扫描非空输入

- 当前未发现确定的新问题。
- `review_input_service.validate_review_input()` 在 `backend/app/services/review_input_service.py:13` 拒绝二进制及全空白文本；`review_service.start()` 在 `backend/app/services/review_service.py:141`、`backend/app/services/review_service.py:154`、`backend/app/services/review_service.py:156` 完成数量、项目归属、活动状态和非空校验后才原子冻结输入。
- `SecuritySentinelAgent.scan_file()` 在 `backend/app/agents/security_sentinel_agent.py:434` 于正则、静态规则和模型调用前拒绝无效输入；`scan_project()` 在 `backend/app/agents/security_sentinel_agent.py:698`、`backend/app/agents/security_sentinel_agent.py:702` 只把有效非空文本纳入项目审计，空项目返回 `success=False` 和 `failure_kind=empty_scan_input`。
- 当前全套及 `test_scan_entry_input_closure` 对空串、Unicode 空白、二进制、空可见范围和合法零发现分别通过。

### 真实数据与不可虚报成功

- 当前未发现确定的新问题。
- 普通审查在 `backend/app/services/review_service.py:588`、`backend/app/services/review_service.py:591` 先校验输入；部分覆盖失败通过 `ReviewCoverageError` 保留已产生证据并落为失败，成功路径位于 `backend/app/services/review_service.py:642`，不是“有结果就成功”。
- 项目安全审计在 `backend/app/agents/security_sentinel_agent.py:1290` 至 `backend/app/agents/security_sentinel_agent.py:1346` 对语义失败、预算耗尽、输出截断、无效契约和账目不一致返回失败/阻断；全量项目扫描在 `backend/app/agents/security_sentinel_agent.py:1687` 至 `backend/app/agents/security_sentinel_agent.py:1774` 只在无失败项目时返回成功。
- 当前测试覆盖合法零发现成功、全模型失败不成功、部分模型失败不成功、部分证据保留、损坏快照不标记已验证，均通过。

### 取消、删除与旧 Worker

- 当前未发现确定的新问题。
- 取消/删除在 `backend/app/services/review_service.py:1745` 至 `backend/app/services/review_service.py:1783` 使用行锁刷新任务状态、撤销 `execution_token`、记录终态时间和覆盖阶段；后台在 `backend/app/services/review_service.py:1820` 至 `backend/app/services/review_service.py:1853` 检查状态及租约，旧 Worker 不得覆盖终态任务。
- 项目删除在 `backend/app/services/project_service.py:323` 至 `backend/app/services/project_service.py:343` 拒绝仍有 `pending/running` 任务的项目，符合“先取消任务后删除”的闭环。
- 当前 13 个恢复/竞态反例和项目生命周期测试全部通过，包括跨连接取消、删除、并发派发失败、恢复清理失败、快照提交失败及新文件进度隔离。

## 4. 历史独立证据的冲突处理

- 历史文件 `/tmp/prism-readonly-20260905-tests/independent-review-recheck.json` 记录过 `33 pass / 7 fail / 0 error`、归并 `6` 个独立失败；该证据对应 2026-09-05 20:20 左右的旧源码快照。
- 当前实际源码下重新执行同一类 `test_review_recovery_integrity.py` 得到 `13 passed`；最终全套 JUnit 也显示该文件 `13/13` 通过。
- 历史失败不能被删除或改写，但不能继续当作当前缺陷结论。当前工作树与历史独立复核之间至少有 `review_service.py`、`review_input_service.py`、`test_review_input_lifecycle.py` 等源码/测试文件 SHA 差异，因此该历史结果属于待追溯历史证据，而非当前可复现问题。

## 5. 网络、凭据与源码完整性

- 最终全套执行使用 `/usr/bin/sandbox-exec` 和 `/tmp/prism-readonly-20260905-tests/readonly.sb`；策略拒绝网络，并拒绝仓库 `.env`、`deploy/.env`、前端环境文件及用户凭据目录读取。
- 执行器环境使用 `DB_HOST=sqlite`、隔离临时数据库和占位配置；未调用真实外部模型。`independent-backend-final3-network-audit.jsonl` 记录 `6` 次 `socket.getaddrinfo`，全部来自非法 LLM 配置参数校验路径 `backend/app/utils/api_resolver.py:229`，未记录 `socket.connect` 或 `socket.bind`，且测试退出码仍为 `0`。这是被审计到的 DNS 解析尝试，不是外网调用成功。
- SHA 前后核验文件：`/tmp/prism-readonly-20260905-tests/independent-final3-hashcheck2-before.sha256` 与 `/tmp/prism-readonly-20260905-tests/independent-final3-hashcheck2-after.sha256`。
- SHA 覆盖本次目标的 `9` 个源码/测试文件，前后完全相同，`sha_diff_lines=0`；最终复跑未修改这些文件。
- 工作树仍然是主线程的脏工作树；当前 `git status` 另有前端及其他主线程改动和用户原有未跟踪文件 `.coverage 2`。本只读任务没有修改仓库，也没有提交 Git。

## 6. 备份与 047 迁移证据边界

- 备份日志 `/tmp/prism-repair-backup-20260905.log` 显示 MySQL 健康、备份完成，备份大小 `402258176` 字节。
- `/tmp/prism-repair-backup-verify-20260905.log` 显示备份已在独立容器 `cr_testdb` 恢复验证，`89` 张表，恢复库 Alembic 版本为 `046_finding_aggregation`，备份 SHA 为 `c9e69911cbb2a046b2dc7412ae0061e150d4359a23468c5fa1f7384b9a71ada3`。
- `/tmp/prism-repair-ui-20260905/migration047-mysql-test.log` 显示独立 MySQL `8.0.46` 完成 047 upgrade、roundtrip 和 downgrade；遗留任务/输入各 `1` 条，`legacy_snapshot_unknown=1`，roundtrip `sha_len=64`，降级后任务/输入各 `1` 条。
- 以上仅证明备份可恢复和 047 迁移在独立 MySQL 测试中往返通过；恢复库仍停在 `046_finding_aggregation`，没有证明生产库已经应用 047，也没有证明生产应用已部署或生产数据已修复。

## 7. 未覆盖与非阻断注意项

- 未测试：真实 MySQL/InnoDB `REPEATABLE READ` 运行时、真实外部 LLM、HTTP 授权中间件全链路、前端 UI 及主线程当前进行中的导出/调度改动。
- 全套 JUnit 有 `1` 个非失败警告：`backend/app/agents/test_review_reporter_agent.py:96` 的 `PytestCollectionWarning`，原因是测试类带 `__init__`，来源为 `tests/unit/services/test_review_dynamic_roles.py`。
- 结论是“当前本地源码独立后端验收通过、未发现确定的新问题”，不是生产发布或生产修复签收。

## 最终判定

**PASS（当前源码独立只读后端验收）**：全套 `2782/2782` 通过，目标闭环 `167/167` 通过，恢复反例 `13/13` 通过，最终 SHA 前后稳定；扫描非空、真实结果/不可虚报成功、取消删除闭环未发现可确认的新问题。

**NOT PRODUCTION SIGN-OFF**：没有生产数据库、真实模型、生产 HTTP 或浏览器验收证据；不能宣称生产已经修复或已经发布。
