# 独立只读计数复核

## 结论

通过：指定计数与基线哈希未发现实质不一致。

复核时刻（UTC）：`2026-09-05T18:34:22.089075+00:00`。本次只重算已有证据，没有重新运行任何测试。

## 独立计数

| 核验项 | 直接重算结果 | 结论 |
| --- | --- | --- |
| 后端 JUnit | 2782 个 testcase；通过 2782；失败 0；错误 0；跳过 0 | 一致 |
| 前端 JSON | 77 个独立文件；575 个断言；通过 575；失败 0；pending 0；skipped 0；todo 0 | 一致 |
| SQLite 最近24小时登录 | success=7；failed=1 | 与页面文本及主代理计数JSON一致 |
| OpenAPI 基线双文件 | 字节一致=True | 一致 |

前端文件数来自 `testResults` 的77个唯一文件，不把 JSON 顶层的178个 suite 误当成178个文件。后端逐个计算 testcase 及 failure/error/skipped 节点，并与 suite 汇总交叉核对。

## 数据库窗口与只读性

- 数据库：`/tmp/prism-repair-ui-20260905/isolated-ui.db`。
- 窗口：`2026-09-04T18:34:22.089075+00:00` 至 `2026-09-05T18:34:22.089075+00:00`，UTC，含起止边界。
- 使用 `mode=ro&immutable=1`；读取前确认无 WAL/journal 文件，没有启动服务、写库或修改记录。
- 页面文本解析值：success=7、failed=1；`local-db-login-counts.json` 数值：success=7、failed=1。
- 数据库读取前后 SHA256 一致：`4eec75374525f352fbd5d28f15c7076768536220a65821999f24c14ca2531364`。
- 本次输入文件前后字节一致：`True`。

```sql
SELECT status, COUNT(id) AS row_count FROM audit_log WHERE action = 'login' AND create_time >= ? AND create_time <= ? GROUP BY status ORDER BY status
```

具体窗口参数与输入指纹见同名 JSON。

## 基线哈希

- 仓库文件：`/Users/li/Documents/代码程序/基于大模型智能体的代码审查平台/docs/generated/openapi-baseline.json`。
- 导出文件：`/tmp/prism-release-20260906/baseline-openapi.json`。
- 两文件 SHA256：`c5bcac0150b09cb827788dfcec3bc0d4e215979d0d37082fbf8e429c4b64967e`。
- 这里只确认本地源码契约文件一致；不据此声称当前生产版本、生产部署或运行状态已经验收。

## 截图证据边界

- 390：只查看既有390×844布局截图，安全徽章保持单行，画面内卡片未见明显错位；单帧布局检查不等于完整浏览器功能验收，也不用于证明最终7/1登录计数。
- 320：仅保留已有 DOM 数据的证据属性；`admin-overview-320.png` **不作为视觉证据，不发布、不据此作视觉结论**。
- 本次没有操作浏览器，没有更改或关闭主代理保留的生产登录页。

## 适用范围

全部结论仅限已有本地隔离测试产物、隔离 SQLite 数据与源码契约。**不是生产部署签收，也不是生产数据库、全账号矩阵或真实模型验收。**

未访问生产、未读取 `.env`、未重跑测试、未启动服务、未改项目代码、未写数据库、未 commit/push；仅写入本报告及同名 JSON。
