# 基于大模型智能体的代码审查平台

## 一、项目规划

### 1.1 项目概述
基于FastAPI+Vue3+DeepSeek大模型的智能代码审查平台，支持多语言代码自动审查、问题检测、报告导出，并已加入轻量多 Agent 审查编排能力。

### 1.2 技术栈
- **后端**: Python 3.11 + FastAPI + SQLAlchemy + Alembic + Pydantic
- **前端**: Vue3 + TypeScript + Element Plus + Pinia + ECharts + Monaco Editor
- **AI**: DeepSeek API (默认 `deepseek-v4-flash`) + 多 Agent 审查编排
- **数据库**: MySQL 8.0
- **部署**: Docker Compose (MySQL + Backend + Frontend/Nginx + ClamAV)，Nginx 挂载 Let's Encrypt 证书提供 HTTPS

### 1.3 小菱多智能体总调度体系（2026-08-10）

小菱是用户端和管理端共用的全局总调度入口。所有跨 Agent、跨会话通信都进入持久化 Agent Mesh，由数据库账本保存标准 JSON 消息、幂等键、trace、投递状态和不可变事件时间线；Redis 仅作通知加速，不作为唯一事实源。

- **发现**：`ListAgents` 返回当前账户可寻址的内置 runtime/service Agent、已发布 custom Agent 和 user/admin 会话。生产验收返回 `runtime=14、service=18、custom=2、session=5`，合计 39 个对象。
- **通信**：`SendMessage` 只接受严格版本化信封，地址必须来自发现结果；同账户会话隔离，Agent 协作遵循现有 contracts 白名单。状态按 `queued → delivered → processing → completed|failed|expired|dead_letter` 流转，支持 ACK、幂等、重试和 trace 恢复。
- **自动续跑**：会话心跳注册后轮询收件箱；消息以隐藏结构化协作上下文启动 Responses 回合，不伪造成用户发言。写操作、MCP、运维和高风险工具仍受 RBAC、审批、审计和受控执行器约束。
- **界面**：普通用户端 `AgentChatDrawer` 与管理端 `AdminCopilot` 均展示 Agent 调用链，子 Agent 消息默认折叠，展开后仅显示脱敏后的方向、主题、状态、trace 和摘要。
- **接口**：`POST /api/agent-mesh/conversations/heartbeat`、`GET /api/agent-mesh/agents`、`POST /api/agent-mesh/messages`、`GET /api/agent-mesh/inbox`、`POST /api/agent-mesh/messages/{message_id}/ack`、`GET /api/agent-mesh/traces/{trace_id}`。

详细的 6A 设计、任务拆分、生产证据与待办见 [`docs/小菱多智能体总调度体系/`](docs/小菱多智能体总调度体系/)。

### 1.4 项目结构
```
├── backend/                  # 后端服务
│   ├── app/
│   │   ├── agents/          # Agent注册中心与编排 (14个Agent)
│   │   ├── ai/              # AI智能体模块 (Prompt/解析/分片/评分)
│   │   ├── api/v1/          # 28个路由模块 (194条业务 HTTP + 1条 WebSocket)
│   │   ├── core/            # 核心配置/安全/数据库/异常/依赖
│   │   ├── exporters/       # Word/PDF报告导出
│   │   ├── models/          # 30个模型模块 (51张ORM表声明)
│   │   ├── schemas/         # Pydantic校验Schema (28个模块)
│   │   ├── services/        # 业务逻辑层 (45个模块)
│   │   └── utils/           # 工具函数
│   ├── alembic/             # 数据库迁移
│   └── requirements.txt     # Python依赖
├── frontend/                 # 前端SPA
│   └── src/
│       ├── api/             # API封装 (27个模块)
│       ├── components/      # 通用组件 (25个)
│       ├── views/           # 页面 (53个)
│       ├── router/          # 路由 + 守卫
│       ├── stores/          # Pinia状态管理
│       ├── types/           # TypeScript类型定义 (18个)
│       ├── utils/           # 工具函数
│       ├── constants/       # 常量定义
│       └── assets/styles/   # 全局样式
├── deploy/                   # 部署配置
│   ├── docker-compose.yml
│   ├── deploy.sh
│   ├── README.md
│   ├── .env.example
│   └── mysql/
│       ├── init.sql          # 22张基础表DDL；其余结构由Alembic补齐
│       └── seed.sql          # 管理员 + 内置规则
├── docs/                     # 设计文档
│   ├── 01-11 核心文档       # 11份基础设计文档
│   ├── v2.0/                # v2.0/v2.1功能设计文档 (10份)
│   ├── 多Agent优化/          # 多Agent优化6A工作流文档
│   ├── 圆桌讨论/             # v2.3 圆桌讨论审功能文档
│   ├── Agent自进化/          # v3.0 Agent自进化6A文档
│   └── 其他专项6A文档        # 全量回归、日志追溯、前端优化、态势刷新等
└── .env                      # 环境变量配置
```

## 二、实施方案

### 2.1 后端 API 路由（194 条业务 HTTP + 1 条 WebSocket）

> 统计日期：2026-07-10。口径为 FastAPI `/api/*` HTTP 操作数，同一路径的不同方法分别计数；`/healthz` 不计入业务接口。逐条最新契约以路由、Pydantic Schema 和 OpenAPI 为准。

| 路由前缀 | 说明 |
|---------|-----|
| `/api/auth`、`/api/users`、`/api/me` | 鉴权、用户管理、用户画像 |
| `/api/rbac`、`/api/admin/audit` | RBAC、菜单、数据范围、操作审计 |
| `/api/projects`、项目成员子路由 | 项目与协作成员管理 |
| `/api/code-files` | 文件上传、编辑、重命名、版本与回滚 |
| `/api/rules`、`/api/review`、`/api/issues` | 规则、审查任务、问题闭环 |
| `/api/reports`、`/api/dashboard` | 报告、模板、导出和仪表盘 |
| `/api/ai-logs`、`/api/api-config`、`/api/admin/llm` | AI 日志、个人/全局模型配置 |
| `/api/agents`、`/api/admin/*` | Agent 中心、Skill、治理、策略、任务、知识、指标与告警 |
| `/api/ai`、`/api/ai-prompt` | AI 助手调度与修复提示词 |
| `/api/security`、`/api/evolution` | 安全审计与 Agent 自进化 |
| `/api/discuss`、`/api/ws/discuss/{session_id}` | 圆桌讨论预检与 WebSocket |
| `/api/maintenance`、`/api/feedback`、`/api/forum` | 工单、反馈、开发者论坛 |
| `/api/knowledge` | 个人知识库 |

### 2.2 数据库（51 张 ORM 表声明）

| 领域 | 表 | 数量 |
| --- | --- | ---: |
| 用户与 RBAC | `user`、`role`、`permission`、`role_permission`、`user_role`、`menu`、`data_scope` | 7 |
| 项目与代码 | `project`、`project_member`、`code_file`、`code_version` | 4 |
| 审查与报告 | `review_rule`、`review_task`、`review_task_file`、`review_issue`、`review_report`、`report_template`、`malware_scan_log` | 7 |
| AI、配置与审计 | `ai_call_log`、`user_api_config`、`system_config`、`audit_log` | 4 |
| 自进化 | `eval_case`、`evolution_proposal`、`review_experience`、`agent_skill_record` | 4 |
| 社区与个人中心 | `maintenance_ticket`、`user_feedback`、`forum_post`、`forum_reply`、`user_profile`、`knowledge_doc`、`knowledge_chunk` | 7 |
| Agent 治理 | Profile、Skill Binding、Tool Permission、Policy、Approval、Memory、Knowledge、Job、Reflection、Reward、Artifact、Alert、Metric 等 18 张表 | 18 |
| **合计** |  | **51** |

`deploy/mysql/init.sql` 只创建 22 张基础表，首次部署和升级都必须执行 Alembic 001～009（或届时最新 head）。2026-07-10 生产库实际为 54 张表：51 张当前 ORM 表、`alembic_version` 和 2 张历史遗留表。

## 三、进度记录

### 2026-07-15 Agent 项目选择模糊匹配与自定义输入（已完成）
- [x] Align：确认后端已有项目名称模糊解析，但前端收到后端候选后只显示候选子集，项目较多时无法搜索完整项目库
- [x] Architect / Atomize / Approve：采用候选合并、远程关键词查询和“其他（自定义输入）”提交转换，不改变项目权限边界
- [x] Automate：实现推荐候选与项目列表合并、远程关键词搜索、自定义名称/ID 转换，并修复 Clarify 下拉被 Agent 抽屉遮挡的问题
- [x] Assess：前端 56 passed、后端 1041 passed，ESLint、Ruff、生产构建和 diff check 通过
- [x] Deploy：Frontend 切换至新镜像；真实聊天显示 13 个项目与“其他”，搜索“皮卡丘”仅返回两个匹配项目，自定义名称回填接口 200
- [x] Runtime：收尾巡检发现 Frontend 构建内存峰值曾使 `clamd` 被主机 OOM 终止；重启 ClamAV 后恢复 `healthy`，Backend 到 `clamav:3310` 的 PING/PONG 验证通过

### 2026-07-15 用户功能导航与接口接入（已完成）
- [x] Align：生产复现管理员进入代码中心、审查规则、个人知识库和个性化画像时被前端守卫送回工作台
- [x] 接口基线：项目、规则、论坛、知识库文档/统计、画像、维修 7 个生产首屏 API 均为 HTTP 200
- [x] Architect / Atomize / Approve：确定管理员可使用全部已认证主站页，普通用户/审查员仍禁止进入 `/admin/*`
- [x] Automate：统一管理员主站导航语义，修复侧边栏、路由守卫和全局搜索，并增加角色/路由/API 契约测试
- [x] Assess：前端 50 passed、后端 1040 passed，ESLint、Ruff、生产构建和 diff check 通过
- [x] Deploy：Frontend 切换至新镜像，容器重启 0、OOM=false，HTTP/HTTPS 200；六页面真实点击、搜索入口、页面数据和 7 个 API 全部通过

### 2026-07-15 小助手启动审查契约对齐（已完成）
- [x] Align：确认旧测试缺少 `file_ids`，并发现生产自动查询把字符串状态误写为 `CodeFile.status == 1`
- [x] Architect / Atomize / Approve：锁定显式文件 ID、自动 active 文件查询和 deleted 排除三条契约
- [x] Automate：修复 `status="active"` 查询并按 ID 排序；测试覆盖显式 ID、自动 active 查询、deleted 排除和查询异常
- [x] 线上第一次冒烟：发现 ChatPlanner 固定工具绕过 legacy handler，仍以空 `file_ids` 触发 ReviewStartIn 502；请求未创建任务
- [x] 统一修复：`file_ids` 工具参数改为可选，缺省文件解析下沉到 `Orchestrator.start_review`，ChatAssistant 删除重复查询
- [x] 生产边界收口：首轮任务 #58 暴露 Planner 生成 `$[0].files[].id` 动态引用并降级；新增失败测试后，仅对 `start_review` 该类引用改用服务端 active 文件解析
- [x] Assess：相关四文件 84 passed，后端全量 1039 passed，ChatPlanner 覆盖率 100%，Ruff、compileall、diff check 通过
- [x] Deploy：白名单同步并重建生产 Backend；真实对话以单步 `start_review` 创建任务 #59，任务名保留、处理 1/1 文件并成功，容器 healthy、重启 0、OOM=false

### 2026-07-15 圆桌讨论群聊语义完善（已完成）
- [x] 完成 Align：确认当前实现已支持共享可见、相互表态和主持总结，但缺少自主静音，`full` 未包含通用质量 Agent，用户插话未完整进入主持总结上下文
- [x] 完成生产只读基线检查：`81.70.251.90:/opt/code-review` 四个 Compose 容器运行，Backend 健康与就绪检查通过
- [x] 完成 Architect / Atomize / Approve：新增 `docs/圆桌讨论群聊语义完善/` 6A 文档，锁定五个审查子 Agent + 一个主持/总结 Agent 的范围和协议
- [x] Automate：测试先行实现五个审查子 Agent 全量参会、自主发言/静音、结构化立场、回应对象、完整用户上下文和前端状态展示
- [x] Assess：圆桌定向 36 passed，后端扩大回归 1034 passed（另有 1 项无关既有 ChatAssistant 契约测试失败），前端 49 passed，Ruff/compileall/ESLint/类型检查/生产构建通过
- [x] Deploy：完成源文件和 90 MB 数据库备份，以白名单同步生产；Backend/Frontend 新镜像构建并切换成功，容器重启次数 0、OOM=false，健康/就绪与 HTTP/HTTPS 均为 200
- [x] 线上圆桌验收：预检返回五个审查子 Agent 和合法 WebSocket 地址；真实浏览器完成 1280x720 与 390x844 检查，无横向溢出、五 Agent + 主持人 + 用户完整显示、控制台 0 错误/警告
- [x] 真实 LLM 全链路：2 轮共 10 个 Agent 决策（4 发言 / 6 静音），主持总结成功；报告 #57 以 `discuss/success` 落库，评分 29、9 条结构化问题、摘要长度 803

### 2026-05-26 开发完成
- [x] M0: 项目基础设施 (配置/入口/数据库/鉴权/前端骨架/Docker)
- [x] M1: 核心业务CRUD (项目/文件/版本/规则)
- [x] M2: AI审查智能体 (DeepSeek调用/分片/解析/评分/Prompt)
- [x] M3: 前端全页面 (仪表盘/审查/报告/管理)

### 2026-05-26 代码修复与验证
- [x] 修复API路径不一致 (http.ts baseURL统一为'/api')
- [x] 修复Element Plus CSS导入 (element-plus/dist/index.css)
- [x] 修复Element Plus中文语言包导入路径
- [x] 修复seed.sql管理员密码bcrypt哈希 (密码: admin123)
- [x] 修复Python 3.9兼容性 (X|None → Optional[X], 共16处)
- [x] 修复requirements.txt (补充bcrypt<5.0 + pydantic[email])
- [x] 修复codeFile.ts FormData上传Content-Type
- [x] 修复project.ts deleteProject动态导入
- [x] 修复ReviewTaskDetail.vue getCodeFileDetail导入
- [x] 修复ReviewStart.vue getCodeFiles导入
- [x] 修复CodeFileList.vue list函数参数
- [x] 修复VersionHistory.vue listVersions返回值
- [x] 修复ProjectDetail.vue upload函数参数
- [x] 修复ReportDetail.vue duration_ms类型
- [x] 修复UserManage.vue filterStatus比较
- [x] **后端验证**: 所有模块导入成功 + 47条API路由注册正确
- [x] **前端验证**: vue-tsc类型检查零错误通过

### 2026-05-26 多 Agent 优化与文档对齐
- [x] 新增多 Agent 编排模块：通用质量、安全、可靠性、性能、可维护性代理
- [x] 审查类型对齐：quick/standard 使用通用代理，security/performance/full 使用专项代理组合
- [x] 修复分片行号约定：模型返回相对行号，后端统一换算绝对行号
- [x] 修复前端审查任务跳转路径和任务状态枚举
- [x] 修复前端登录态恢复接口 GET/POST 不一致
- [x] 修复报告详情中审查类型硬编码为 full 的问题
- [x] 加强审查启动权限边界：项目必须归属当前用户或管理员，文件必须属于当前项目
- [x] 新增 11 个后端单元测试，验证多 Agent 映射、Prompt 注入、行号换算和摘要
- [x] 同步更新 README、核心 docs 和 `docs/多Agent优化/` 6A 文档
- [x] **后端验证**: 新增测试 11 passed + 本次触及文件 ruff 通过 + compileall 通过
- [x] **前端验证**: `npm run build` 通过

### 2026-05-27 人机交互加载动画优化
- [x] 新增 PrismLoading 统一加载动画组件，提供页面级、局部区域级加载提示
- [x] 接入路由切换加载遮罩，懒加载页面和守卫等待期间显示动画提示
- [x] 优化仪表盘、审查启动文件列表、报告详情的加载状态展示
- [x] 统一 Element Plus `v-loading` 遮罩视觉，补充动画和“正在加载数据...”提示

### 2026-05-27 人机交互缺陷修复
- [x] 修复管理员快速登录密码与种子数据不一致问题
- [x] 修复登录失败重复错误提示，统一由请求拦截器展示错误信息
- [x] 修复管理员项目列表权限范围，管理员可查看全部活跃项目
- [x] 修复仪表盘项目和文件统计口径，已删除项目不再计入活跃统计
- [x] 修复项目更新接口请求方法，前端改为调用后端 `PUT /projects/{id}`
- [x] 修复严重度中文/英文枚举混用导致的筛选、图表颜色和标签样式异常
- [x] 实装全局搜索入口和 `⌘K/Ctrl+K` 快捷键，支持快速跳转页面和打开 Agent 助手
- [x] 优化移动端主布局，侧边栏改为抽屉式导航并增加遮罩关闭
- [x] 优化项目创建表单，允许不选择文件夹时手动创建项目
- [x] 补充 Agent 抽屉关闭按钮可访问名称，修复无文本按钮的可访问性问题

### 2026-05-27 v2.0 完整交付 (M1 + M2 + M3 + M4 + M5 + AiPromptAgent)

#### M2 · Agent 调用事件总线 + SSE 反馈链
- [x] 后端 `AgentEventBus` 单例 + `AgentEvent` 数据类(dispatch/thinking/progress/complete/failed/clarify 六类事件)
- [x] `BaseAgent.call()` 自动 emit 事件,统一从 ctx.extra.trace_id 关联调用链
- [x] `Orchestrator.chat()` 生成 trace_id 并广播 DISPATCH,子 Agent 自动跟随
- [x] 新增 `GET /api/agents/events` SSE 端点,默认回放最近 20 条历史
- [x] 前端 `utils/agentEventStream.ts` 基于 fetch+ReadableStream 解析 SSE(支持 Bearer Token)
- [x] `AgentChatDrawer` 重写,每条 assistant 消息附带"Agent 调度链"折叠面板,实时展示派发→思考→完成
- [x] 单测: 4 项 EventBus 测试(包含异步订阅与历史上限)

#### M4 · Agent 主动提问 (Clarify Protocol)
- [x] 后端 `ClarifyStore` TTL=5min 内存存储,5 分钟未回填自动失效
- [x] `ChatAssistantAgent._maybe_clarify()` 在关键 intent 缺字段时返回 CLARIFY,不再基于猜测
- [x] 定义 6 个必填字段映射(delete_project/start_review/list_review_issues/list_code_files/create_project/review_code)
- [x] 4 种控件类型(text/select_project/select_task/code)前端自动渲染对应组件
- [x] 新增 `POST /api/agents/clarify` 回填路由,合并 payload 后继续走原 intent handler
- [x] `ChatResponse` Schema 扩展,加入 `clarify?: ClarifyOut`,v1.0 客户端无感知降级
- [x] 前端 AgentChatDrawer 自动渲染追问卡片(支持项目下拉/任务下拉/代码片段输入)
- [x] 单测: 5 项 Clarify 测试(TTL + 触发条件 + 完整 payload 不触发)

#### M1 · Agent 元数据 + Agent 中心同步
- [x] **v2.0 设计文档**: 编写 `docs/v2.0/` 7 份开发文档(总体规划、办公室设计、图标规范、事件总线、Clarify、AiPrompt、迁移)
- [x] **Agent 元数据下沉到 BaseAgent**: 12 个 Agent 全部填齐 icon/color/category/skills
- [x] **AgentRegistry v2.0**: 新增 `list_runtime()`、`get_metadata()`、`summary()`
- [x] **agent_service v2.0**: 新增 `get_runtime_agents()`、`get_runtime_summary()`、`get_situation()`
- [x] **新 API 路由**: `/api/agents/runtime`、`/api/agents/runtime/summary`、`/api/agents/situation`
- [x] **AiPromptAgent 新成员**: 把审查问题翻译成可粘贴 Cursor/Copilot/ChatGPT/Claude Code 的提示词,默认 LLM 润色,敏感字段脱敏
- [x] **新 API 路由**: `/api/ai-prompt/tools`、`/api/ai-prompt/issue`、`/api/ai-prompt/task`
- [x] **前端 Agent 中心重写**: 替换为"Agent 办公室"视图,顶部态势感知带 + 12 张工位卡 + category 分桶 + 详情抽屉
- [x] **AgentAvatar 组件**: 每个 Agent 一个独立 SVG 几何符号 + 6 种状态光环动画 + reduced-motion 兜底
- [x] **SituationPanel 组件**: 在岗/工作中/空闲/今日调用 + 60 分钟波形 + 热点 Agent 排行
- [x] **AgentDeskCard 组件**: 单工位卡,显示头像、状态徽标、调用统计、成功率
- [x] **AiPromptModal 组件**: Issue / Task 两种来源生成可粘贴提示词,支持工具切换、Markdown 下载
- [x] **IssueDetailDrawer + ReviewTaskDetail**: 新增"生成 AI 修复提示词""AI 修复包"按钮
- [x] **图表数据修复**:
  - 新建 `frontend/src/constants/dim.ts` 集中维度键归一化(支持 maintainability/bug 等同义词)
  - Dashboard 使用 `normalizeDimKey` 兼容多种 issue_type 写法
  - Dashboard 6 张统计卡空数据时不再显示"▲ 较上月"假 delta
  - ReportDetail 雷达图缺失维度用 NaN 显示真实缺口,不再用整体 score 兜底假满分
  - ReviewTaskDetail 用归一化 dimColor/dimLabel 兼容多种枚举写法
- [x] **测试**: v2.0 新增 21 项单测全部通过 + v1.0 17 项继续通过 = **38 passed**
- [x] **前端验证**: vue-tsc 类型检查零错误,npm run build 通过(含 SSE/Clarify 新组件)

#### v2.0 收尾 (A1+A2+A3 + B2 + C3)
- [x] **A1** ChatAgent 加入 `generate_ai_prompt` 意图,新增 handler 路由到 AiPromptAgent;Clarify 字段映射覆盖 scope/target_tool/issue_id/task_id/project_id
- [x] **A2** Agent 办公室订阅 SSE,工位卡按事件实时切换 status(thinking/working/error/blocked),态势带 working/idle 计数同步;错误状态 6 秒自动回 idle
- [x] **A3** SSE 客户端加入指数退避自动重连(5s→30s 上限),onStatus 回调暴露 connecting/connected/reconnecting/closed 四态
- [x] **B2 图表二次审计**:
  - ProjectList 评分量表删除 `id*7+file_count*3` 派生的**假数字**,改用后端真实评分,无审查记录显示 —
  - 后端 ProjectOut 新增可选 `score` 字段,project_service 列表 API 聚合最近一次成功审查评分
  - IssueHub 严重度/类型标签接入 `severityClass` + `dimLabel`,兼容中英文混存
- [x] **C3 项目级 AI 修复手册**: AiPromptAgent 新增 `execute_for_project` 按严重度优先取 top_n 问题;新增 `POST /api/ai-prompt/project`;ProjectDetail 顶部加 `AI 修复手册` 按钮;AiPromptModal 增加 `source='project'` 模式
- [x] **测试**: 38 项全部继续通过,无回归
- [x] **前端验证**: vue-tsc 零错误,npm run build 通过

### 2026-05-27 登录注册修复
- [x] **修复vite.config.ts代理端口错误**: 前端dev server代理目标从 `localhost:8001` 修正为 `localhost:8000`
  - 后端运行在8000端口，代理配置错误导致所有 /api 请求无法到达后端
  - 验证：通过前端代理 curl 测试 login/register 均返回200成功
  - 前端vue-tsc类型检查零错误通过

### 2026-05-28 v2.1.1 安全能力增强交付 (G1~G6)

#### SecuritySentinel 扫描规则扩充
- [x] **后端静态规则从 13 条扩充到 20 条**: 新增 SQL 字符串拼接(CWE-89)、XXE 外部实体注入(CWE-611)、开放重定向(CWE-601)、路径遍历(CWE-22)、日志注入(CWE-117)、Cookie 缺少 HttpOnly(CWE-1004)、Cookie 缺少 Secure(CWE-614) 共 7 条规则
- [x] Cookie 安全标志采用与 HTTP 响应头相同的"缺失检测"逻辑(require_presence_of → pattern 未命中 → 报警)
- [x] 敏感信息正则库: 20 类(原有 12 类 + v2.1.1 扩展 GitLab/Discord/Telegram/Heroku/SendGrid/Twilio/Mailgun/Square 共 8 类)
- [x] 后端验证: 20 条静态规则全部命中测试通过,81 条 API 路由注册正确,13 个 Agent 正常注册

#### 安全知识中心页面增强
- [x] **新增「我的项目安全概览」区域**: 调用 `/api/security/dashboard-summary` 展示当前用户项目的平均风险评分、严重度分布、OWASP 热点
- [x] 三种状态展示: 有数据(完整评分卡 + OWASP 热点) / 有项目未扫描(引导扫描) / 无项目(引导创建)
- [x] 路由 `/security` 已注册,侧边栏「安全中心」菜单项(Agent 中心与审查规则之间)可直达

#### 仪表盘安全态势卡增强
- [x] **新增 SVG 趋势火花图**: SecurityPostureCard 集成的 SecurityPostureCard 新增"严重+高危趋势"迷你折线图,数据来源于 `/api/security/dashboard-summary` 的 `trend` 字段
- [x] 增强数据显示: 总量统计(总安全发现数)、扫描覆盖率(已扫/总项目)
- [x] 前端控制面板 `SecurityPostureCard` 复用 `days` prop 响应 Dashboard 时间选择器切换

#### 自动化验证
- [x] **后端**: ruff 零错误 + compileall 通过 + 所有 81 条路由注册正确 + 13 Agent 注册正确
- [x] **前端**: vue-tsc 类型检查零错误, npm run build 通过

> v2.1 新增 SecuritySentinel Agent(第 13 个 Agent) + 6 条安全 API 路由;
> v2.1.1 在此基础上扩展到 20 条静态规则 + 20 类敏感信息正则 + 安全知识中心 + 仪表盘安全态势卡。

> v1.0 注册 47 条 `/api` 路由; v2.0 新增 Agent 中心(AI助手+Agent管理+提示词+审计) 扩展到 14 组共 70 条端点;
> v2.1 / v2.1.1 新增安全审计组(security)扩展到 15 组共 81 条端点,Agent 数 13。
> 当前主项目基线见本文件 2026-06-05「文档与项目全量对齐」记录:87 条 HTTP API + 1 条 WebSocket + 14 个 Agent。

### 2026-05-29 v2.3 M7 · 多 Agent 圆桌讨论审修复与验收

> 用户报告「圆桌讨论审无法成功启动讨论」,本次定位根因、重写聊天室、修复前后端缺陷并完成真实点击端到端测试。
> 详见 `docs/圆桌讨论/圆桌讨论功能文档.md`。

#### 核心缺陷修复
- [x] **致命:点击启动后讨论面板弹不出**。根因为布局 `<router-view>` 以 `fullPath` 为 key,`AgentCenter` 在 `initDiscussionFromRoute()` 里 `router.replace({query:{}})` 清理地址栏触发**整组件重挂载**,刚设置的讨论状态被重置。改用 `window.history.replaceState(window.history.state,'',route.path)` 仅清理 URL、不触发路由导航,避免重挂载
- [x] **后端核心:Agent 发言为空**。讨论发言与共识汇总误用 `response_format=json_object`,改为 `json_mode=False` 输出纯文本自然语言
- [x] **后端:推送中断**。`DiscussionBus.close_session` 向队列放入 dict 导致 `WebSocket.send_text` 抛错,`session_end` 统一序列化为 JSON 字符串
- [x] **后端:终止不收敛**。新增 `request_stop` 标记会话 concluded;`stop` 即使在暂停态也唤醒并走主持人小结

#### 聊天室前端重写
- [x] `AgentDiscussionPanel.vue` 重写为聊天室方框:逐条发言气泡、参会者条(主持人/各 Agent/你)、「正在发言」打字指示、用户气泡右对齐、Markdown 渲染、回到底部按钮
- [x] 发言式讨论:后发言 Agent 通过 `_build_history()` 可见前面**全部**发言(含其他 Agent 与用户),实现真正的圆桌交流
- [x] 用户插话经 WebSocket 广播为 `role:user` 发言,所有 Agent 可见并影响后续讨论与共识
- [x] 状态机区分 live/paused/concluded:暂停态仍可发言与操作;结束态全部控件禁用,占位符提示「讨论已结束,无法发言」

#### 真实点击端到端测试(localhost:5173,控制台零报错)
- [x] 启动讨论审 → 面板弹出 → URL 被 `history.replaceState` 清理
- [x] 多 Agent 逐条发言 + Markdown 渲染 + 打字指示
- [x] 暂停/继续(⏸↔▶)、终止(含二次确认,暂停态亦可终止)、关闭(进行中二次确认/已结束直接关)
- [x] 发送插话(出现「你」气泡 + 输入框清空 + **被纳入主持人共识小结**)、4 个快捷提问、滚动到底「↓ 最新」
- [x] 结束态禁用校验:暂停/终止/快捷/发送/输入框全部置灰

### 2026-05-29 v2.3.1 · 圆桌讨论沉淀报告 + 真实数据 + 反驳式讨论

> 在 M7 基础上,讨论结束自动生成可查询的审查报告,全过程调用沉淀为真实日志/事件,并让各 Agent 互相反驳。

#### 讨论沉淀为审查报告
- [x] `DiscussionOrchestrator` 贯通 `user_id/project_id/file_id/review_type`,讨论开始即经 `SessionLocal` 创建 `ReviewTask(review_type=discuss, running)`
- [x] 讨论结束(自然结束或终止)在 `finally` 收尾:LLM 抽取共识为结构化 `ReviewIssue` → `compute_score` 评分 → 回填 `ReviewTask(success)` + 共识小结作 summary
- [x] 报告天然出现在「报告列表 / 审查任务 / 仪表盘」(报告列表本质即 `status==success` 的任务);报告详情复用 `report_service`(评分/严重度/by_type/问题清单)
- [x] 前端面板讨论结束显示「📄 查看报告」按钮,直达 `/reports/{id}`

#### 真实数据(重点 Agent 中心 / 调用日志)
- [x] 讨论每次 LLM 调用经 `log_deferred()` 写入 `AiCallLog`(带 task_id/user_id/file_id + `/<code>-agent` 后缀归因),Agent 中心统计与「Agent 调用日志」反映真实讨论
- [x] 编排器经 `AgentEventBus` 广播 dispatch/thinking/complete,讨论时 Agent 办公室工位卡实时点亮
- [x] 全量审计前后端无 mock/random/硬编码假数据(动画/trace_id 的 Math.random 属正常用途)

#### 反驳式讨论 + 重连修复
- [x] 发言 prompt 显式要求对他人观点表态(同意/补充/质疑/反驳并给理由),形成真正交锋
- [x] `DiscussionBus.subscribe()` 对已结束会话补发 `round_start/done` 终态帧,重连显示「已结束」而非「进行中」;会话记 `report_task_id` 供重连显示报告入口

#### 真实点击端到端验证(控制台零报错)
- [x] 两次完整讨论:报告总数 6→7→8;报告 `review_type=discuss`,含 10/7 条结构化问题、评分(30/52)、严重度分布、真实共识小结
- [x] 「查看报告」跳转 `/reports/14` 详情正常渲染;讨论记录检出 14 处反驳表态语
- [x] 单次讨论新增 10 条 `AiCallLog`(task #N、真实 token/耗时);Agent 中心总调用 123→133;重连显示「已结束·第2/2轮」
- [x] 后端 `py_compile` 通过 + 前端 `vue-tsc` 零错误;数据库坚持 Docker MySQL(`cr_mysql` healthy)

### 2026-05-29 v2.3.2 · 全站数据准确性核对 + 按钮全测

> 用户要求"确保所有数据准确、每个按钮达到效果"。本轮做了全站 API↔数据库交叉核对与按钮普查。

#### 数据准确性:修复软删除任务导致的统计虚高(5 处)
- [x] 编写 `backend/_qa_crosscheck.py`:用后端 SessionLocal 直查 MySQL 真值,与各页面接口聚合逐项比对
- [x] **根因**:多处问题/任务统计未排除"软删除任务"(`status='deleted'`),删除报告后旧问题仍计入,导致数据虚高
- [x] `dashboard_service`:新增 `_valid_task_ids()` 助手;`get_summary` 的 total_issues/severe_issues、`get_risk_distribution`、`get_issue_type_statistics`、`get_review_frequency` 一律排除已删除任务(仪表盘累计问题 68→43、严重 9→7)
- [x] `security_service`:安全态势 `task_rows` 加 `status != 'deleted'` 过滤
- [x] `project_service`:项目详情 `recent_tasks` 排除已删除任务
- [x] **核对结论**:报告8/任务8/AI日志143/用户1/审计85/项目2/文件495/问题43/严重7 —— 接口聚合与数据库**精确相等**;报告详情(总数/严重度和/类型和)内部一致;Agent 调用统计与 AiCallLog 对账一致;安全态势(严重6/高10)与 DB 一致

#### 按钮普查:全站唯一空按钮已实现
- [x] 扫描全部前端找占位/"即将上线"按钮,**全站仅 1 个**:仪表盘「导出周报」
- [x] 将其实现为真实功能:用仪表盘实时数据生成可打印/存 PDF 的周报(概览+风险分布+问题类型+最近任务),`window.open`+`print`,实测生成 3KB 报告并正确提示
- [x] 真实点击验证:导航(全部页面)、时间选择器、新建审查、项目删除(确认框)、**规则启停(数据库翻转再恢复)**、报告 Word/PDF 导出(端点 200)、查看报告、周报导出
- [x] 安全起见未执行(均已确认接线):单管理员账号的禁用/重置密码/退出、安全扫描、各处删除(仅验证到确认弹窗)
- [x] 全站约 20 个页面逐一访问**零控制台报错**;`vue-tsc` 零错误(所有 @click 处理函数已定义、API 调用类型正确)

### 2026-06-01 全量功能回归测试与安全修复

> 本轮按 `docs/全量功能回归测试/` 执行自动化检查、真实点击、日志监控和缺陷修复。

#### 自动化验证
- [x] Docker MySQL `cr_mysql` 健康状态为 `healthy`
- [x] 后端 `ruff check app tests _qa_crosscheck.py` 与 `compileall` 通过
- [x] 后端完整测试 `110 passed`，覆盖率 `38%`
- [x] 前端 `npm run build` 通过
- [x] API 与 MySQL 交叉核对全部通过：报告、任务、AI 日志、用户、审计、项目、文件、问题、仪表盘、报告详情和 Agent 态势均一致
- [x] 运行时核对：`87` 条 API HTTP 路由、`1` 条讨论 WebSocket、`14` 个在线 Agent

#### 缺陷修复
- [x] 修复 `.env` 代理变量影响本地 `httpx` 核对脚本的问题，核对客户端显式使用 `trust_env=False`
- [x] 固定 `bcrypt>=4.0.0,<4.1.0`，规避 passlib 与 bcrypt 4.1.x 的兼容性问题
- [x] 清理 Ruff 超长行和 SQLAlchemy `IN(subquery)` 警告
- [x] AI 修复提示词改为显式生成；启用 LLM 润色时增加二次确认，避免弹窗打开即发送代码上下文
- [x] AI 提示词上下文跳过二进制 Base64 文件，文本上下文脱敏后限制为 `4000` 字符
- [x] 修复 Axios Blob 下载链路：Word/PDF 导出不再被统一 JSON 拦截器误判失败
- [x] Word 导出返回有效 DOCX，PDF 导出返回有效 PDF，后端日志均为 `200 OK`

#### 真实点击边界
- [x] 已覆盖登录、仪表盘、审查启动、项目、文件、版本、审查任务、问题中心、报告列表、报告详情和 AI 修复提示词的无破坏性按钮
- [x] 删除、状态修改、规则切换、重置密码、密码提交、AI 审查、安全扫描、圆桌讨论、Agent 对话和自进化写操作停在确认前或未执行
- [ ] 应用内 Browser 连接中断后未恢复；Safari 也未暴露可操作窗口，剩余管理员页面真实点击待浏览器连接恢复后补测

### 2026-06-03 全量真实点击补测、修复与清理

> 用户确认允许使用 DeepSeek，并允许创建/删除合成测试数据。本轮用 Safari Computer Use 兜底完成剩余真实点击，不干预正常业务数据。

#### 全量真实点击完成
- [x] 使用合成项目 `qa_deepseek_full_click_20260602`、合成文件 `qa_security_fixture.py` 和 QA 自定义规则执行端到端测试
- [x] 真实点击覆盖：登录/注册/退出、仪表盘、项目、代码中心、编辑器、版本、审查启动、审查任务、审查详情、问题中心、报告、AI 修复提示词、安全中心、审查规则、个人中心、用户管理、Agent 中心、Agent 调用日志、系统审计、Agent 自进化
- [x] DeepSeek 路径覆盖：quick/standard/security/performance/full/discuss 审查、安全扫描、圆桌讨论停止分支和 AI 日志落库
- [x] 删除闭环覆盖：QA 审查任务 19-25 逐条真实点击删除、QA 项目真实点击删除、QA 规则筛选定位后真实点击删除

#### 本轮新增修复
- [x] 修复 Monaco 编辑器切换文件/版本后的旧内容同步问题
- [x] 修复圆桌讨论 URL 清理触发路由重挂载导致状态丢失的问题
- [x] 新增 `review_task_file` 持久化任务-文件关联，稳定报告、讨论和多文件任务回查
- [x] 修复报告详情和 Word/PDF 导出对多文件任务、问题统计和二进制响应的处理
- [x] 配置 Monaco workers，补齐 Element Plus tag type，消除运行时警告
- [x] 审查规则页新增搜索栏，并兼容下划线/短横线被输入法丢失的编码搜索

#### 最终验证与清理
- [x] 后端完整测试 `112 passed`，`compileall` 与 `ruff check app tests` 通过
- [x] 前端 `npm run build` 通过；仅剩既有 Sass 弃用、Rollup pure 注释和 Monaco 大 chunk 警告
- [x] `_qa_crosscheck.py` 清理后通过：报告/任务/AI 日志/审计/项目/文件/仪表盘/报告详情/API 与 MySQL 精确一致
- [x] QA 数据残留为 `0`：project/file/version/task/task_file/issue/report/ai_call_log/rule/audit_log 均已清理
- [x] 后端日志监控未发现未处理异常；清理相关接口均为 `200 OK`

### 2026-06-03 日志与数据真实追溯补强

> 本轮按 `docs/日志数据追溯验证/` 执行真实网页查看、真实点击、日志监控和 API/数据库交叉核对。

#### 本轮修复
- [x] AI 调用日志 API 增加项目、任务、文件、用户、分片、Token、Prompt、Response 的完整追溯字段
- [x] AI 调用日志页面新增项目/任务/文件/用户/分片列，详情弹窗展示完整追溯链并支持跳转
- [x] 审查任务详情 API 增加 `project_id/project_name/files`，页面文件列表不再依赖当前问题分页
- [x] 审查任务详情页新增任务、项目、报告追溯条；文件列表新增“打开”跳转到真实代码文件
- [x] 系统审计页新增日志 ID、操作者 ID 和“查看”追溯入口

#### 真实网页验证
- [x] 使用内置 Browser 打开 `http://localhost:5173`，真实登录 `admin/admin123`
- [x] 真实点击 `Agent 调用日志`：日志 #226 显示项目、任务、文件、用户、分片、Token；详情弹窗显示 Prompt/Response
- [x] 真实点击 AI 日志详情中的任务链接：进入 `/reviews/18`，看到任务 #18、项目、报告入口、文件和问题列表
- [x] 真实点击任务详情文件“打开”：进入 `/code/1/file/13`，看到 `report_record.py` 真实代码
- [x] 真实点击任务详情报告入口：进入 `/reports/18`，报告统计、文件统计和问题统计正常展示
- [x] 真实点击系统审计最新记录“查看”：进入 `/admin/users`，定位到 admin 用户管理页
- [x] 截图证据保存在 `docs/日志数据追溯验证/screenshots/`

#### 最终验证
- [x] 后端完整测试 `112 passed`
- [x] `ruff check app tests` 通过
- [x] 前端 `npm run build` 通过；仅剩既有 Sass 弃用、Rollup pure 注释和 Monaco 大 chunk 警告
- [x] `_qa_crosscheck.py` 通过：AI 日志 `205` 条、审计日志 `122` 条均与 MySQL 精确一致
- [x] 后端真实页面操作日志均为 `200 OK`
- [x] 浏览器控制台错误为 `[]`

### 2026-06-05 前端高级感优化
- [x] 新增 `docs/前端高级感优化/` 6A 工作流文档，记录对齐、共识、设计、任务、验收、总结和待办
- [x] 扩展 Prism 视觉令牌，增加后台工作台 surface、focus、panel shadow、hairline 等变量
- [x] 新增全局页面壳与 surface 基础类，支撑后续页面统一
- [x] 优化 AppLayout 与 AppHeader，统一后台冷灰工作台背景和半透明工具栏
- [x] 优化登录页移动端，保留轻量 Prism 品牌标识并改进表单密度
- [x] 优化 Dashboard 统计卡和活动流，使用 Element Plus 图标替代字符符号
- [x] 优化 Agent 态势面板与安全态势卡，收敛过强光效和 emoji 表达
- [x] **前端验证**: `npm run build` 通过；浏览器验证登录页和仪表盘桌面/移动端无横向溢出

### 2026-06-05 态势感知实时准确刷新
- [x] 新增 `docs/态势感知实时准确刷新/` 6A 工作流文档，记录对齐、共识、设计、任务、验收、总结和待办
- [x] 后端态势感知改为按每个 Agent 的最新生命周期事件推导状态，完成/失败事件会覆盖旧的进行中事件
- [x] `/api/agents/runtime` 回填 EventBus 最新状态，页面首次加载可恢复真实 Agent 运行态
- [x] 前端 Agent 中心收到 SSE 后即时同步卡片状态和工作中/空闲计数，统计刷新防抖缩短到 800ms，兜底刷新缩短到 15s
- [x] Agent 卡片按 `working/thinking/blocked/error/idle/offline` 状态优先展示，正在执行工作的 Agent 自动前置
- [x] **后端验证**: 13 项 Agent 运行态/服务测试通过，ruff 通过，compileall 通过
- [x] **前端验证**: `npm run build` 通过；Vite 登录页浏览器验证正常且控制台无 error
- [ ] **待复验**: 后端真实运行且数据库可用时，登录后进入 `/agents` 做一次端到端浏览器复验

### 2026-06-05 文档与项目全量对齐
- [x] 重新核对当前项目事实：`87` 条 HTTP API、`1` 条讨论 WebSocket、`14` 个运行时 Agent、`14` 张业务表
- [x] 同步 README、说明文档、需求、系统设计、API、后端、前端、部署、测试和开发计划文档中的当前数量、模型、上传限制、接口状态和部署路径
- [x] 明确文件上传当前实现：默认最大 `20MB`，扩展名不限制但阻止系统/构建目录，安全扫描与审查入口通过语言/二进制过滤兜底
- [x] 当前验证：后端 `115 passed`，覆盖率 `39%`；`ruff`、`compileall` 通过；前端 `npm run build` 通过且本次未输出构建警告

### 2026-06-05 前端构建警告清理
- [x] 新增 `docs/前端构建警告清理/` 6A 工作流文档，记录对齐、共识、设计、任务、验收、总结和待办
- [x] 全局 Sass 入口从 `@import` 迁移到 `@use`，Google Fonts 改为 HTML `<link>` 加载
- [x] Vite SCSS 预处理启用 `modern-compiler`，消除 Sass legacy JS API deprecation
- [x] Vite Rollup 配置精确过滤 Element Plus 依赖内 `@vueuse/core` 的已知 pure annotation warning
- [x] 配置生产构建 `manualChunks`，拆分 Monaco、ECharts、Element Plus、Vue 和常用工具依赖
- [x] 同步清理前端负 `letter-spacing` 样式，避免后续视觉实现留下规范冲突
- [x] **前端验证**: `npm run build` 通过，构建日志扫描无 warning/deprecation/Rollup/chunk-size 提示
- [x] **浏览器验证**: 登录、仪表盘、安全态势卡、Monaco 编辑器加载与保存均正常，控制台 warn/error 为空

### 2026-06-07 前后端接口适配修复
- [x] 修复生产 Nginx 对 `/api/ws/` 圆桌讨论 WebSocket 的反代配置，补齐 `Upgrade` / `Connection: upgrade` 协议升级头
- [x] 修复前端 `updateProject` 返回类型，与后端 `PUT /api/projects/{project_id}` 的 `Resp[None]` 契约对齐
- [x] 修复前端统一响应类型 `Resp<T>`，允许后端空响应 `data: null`
- [x] 修复前端 `EvolutionProposal` 的 `payload/evidence/eval_score` 类型，兼容后端对象或数组输出
- [x] 修复 Agent 中心“发起审查”跳转路径，从 `/review/start` 改为实际路由 `/reviews/start`
- [x] **前端验证**: `npm run build` 通过
- [x] **后端验证**: `app.openapi()` 生成通过，确认 `/api` 路径正常注册

### 2026-06-07 真实点击测试@电脑
- [x] **静态接口复验**: 从后端 `app.openapi()` 读取 87 个 `/api` HTTP 端点；扫描前端实际发起的 86 个 HTTP 端点，匹配 86、缺失 0
- [x] **边界说明**: 后端额外 `GET /api/ai/agents` 未被本轮前端扫描引用；该项为后端补充查询端点，不构成前端接口缺失
- [x] **实时通道复验**: 前端 `GET /api/agents/events` SSE 已纳入 HTTP 扫描；圆桌讨论 `ws(s)://.../api/ws/discuss/{sessionId}` 与后端 `/api/ws/discuss/{session_id}` WebSocket 路由及代理配置一致
- [x] 使用 Safari 真实登录 `admin/admin123`，验证工作台、项目管理、代码中心、审查任务、问题追踪、审查报告主流程页面加载正常
- [x] 实点审查详情与报告详情，确认任务详情、问题列表、报告指标和文件表格均从后端接口正常渲染
- [x] 实点 Agent 中心、安全中心、审查规则、个人中心、用户管理、Agent 调用日志、系统操作审计、Agent 自进化页面，接口数据均正常展示
- [x] 实点 Agent 自进化的经验记忆、黄金回归集、反馈明细标签页，确认对象/数组型数据结构渲染正常
- [x] 实点“审查任务 → 启动审查”，确认跳转到 `/reviews/start`；选择项目后成功加载 72 个文件，未实际启动 AI 审查任务
- [x] 打开顶部 Agent 助手抽屉，确认初始消息和输入区正常；未发送消息，避免触发外部模型调用
- [x] **日志验证**: 后端点击过程业务接口均为 `200 OK`，本轮复验未见业务接口 `404/500`；此前登录态探测 `/api/auth/me` 的一次 `401 Unauthorized` 属预期
- [x] **服务验证**: `GET /healthz` 返回 `{"status":"ok"}`，前端首页 HTTP 状态 `200`
- [x] **构建验证**: `frontend` 执行 `npm run build` 通过

### 2026-06-10 全量项目安全扫描
- [x] 新增 `POST /api/security/scan-all-projects`，支持扫描当前用户可见的全部活跃项目；普通用户仅扫描自己的项目，管理员扫描全平台活跃项目
- [x] `SecuritySentinelAgent` 新增全量聚合能力，复用单项目 `scan_project`，聚合 findings、风险评分、扫描文件数、接口清单、代码联动关系、跨文件攻击路径和项目跳过信息
- [x] 全量/单项目威胁模型新增 `api_endpoints`、`code_links`；全量扫描新增 `discussion` 多 Agent 讨论摘要，包含安全、可靠性、性能、可维护性和主持 Agent 共识
- [x] 前端 `SecurityScanModal` 新增 `all-projects` 模式，展示接口扫描、代码联动关系、多 Agent 讨论结论；安全中心提供「全量扫描」入口，同时保留「单项目扫描」跳转和项目详情页单项目扫描按钮
- [x] 同步新增 `docs/全量项目安全扫描/` 6A 文档与 API 文档条目
- [x] **后端验证**: `test_security_sentinel_agent.py` 21 passed，变更文件 `compileall` 与 `ruff check` 通过，OpenAPI 确认 `/api/security/scan-all-projects` 已注册，响应 schema 包含 `discussion`、`api_endpoints`、`code_links`
- [x] **前端验证**: `npm run build` 通过，包含 `vue-tsc` 类型检查；Vite `/security` HTTP 200，当前环境未安装 Playwright，未做登录后交互检查

### 2026-06-10 API 与真实按钮全量复测
- [x] Docker MySQL `cr_mysql` 健康检查通过，后端 `/healthz` 返回 `{"status":"ok"}`，前端 Vite 首页 HTTP `200`
- [x] 修复 `user_api_config.user_id` 与 `user.id` 在 MySQL 下外键类型不一致导致的迁移失败；`alembic upgrade head` 复测通过
- [x] 后端全量验证：`pytest` 结果 `141 passed`、覆盖率约 `40%`，`ruff check app tests alembic/versions/001_add_user_api_config.py` 通过，`compileall` 通过
- [x] 前端验证：`npm run build` 通过
- [x] 业务路由枚举：排除 Swagger/OpenAPI/Redoc 后当前为 `93` 条 HTTP + `1` 条 WebSocket
- [x] API 冒烟：真实调用 `93` 条业务 HTTP 接口，结果 `API_SMOKE_TOTAL=93 PASSED=93 FAILED=0`
- [x] 真实按钮首轮覆盖：登录、全局搜索、项目管理、代码中心、问题追踪、报告、Agent 中心、安全中心、个人中心、用户管理、AI 日志等 `29` 项通过；首轮脚本失败项均定位为选择器/文案误判
- [x] 真实按钮定向复测：工作台、Agent 抽屉、仪表盘新建审查、项目新建/编辑/详情、代码查看/保存/版本、审查启动页控件、规则弹窗、系统审计 `20` 项全部通过，API 4xx/5xx 为 `0`，console error 为 `0`
- [x] QA 数据清理：测试项目 `id=6`、`id=7` 已清理；真实用户密码、用户状态、生产规则和外部大模型调用均未执行破坏性提交
- [x] 同步更新 `docs/全量功能回归测试/` 验收、总结和 TODO 文档

## 四、环境要求与启动流程

### 4.1 必需软件
- Docker Desktop (提供 MySQL 8.0 容器，**严禁使用 SQLite**)
- Python 3.9+（项目已在 `backend/.venv` 中创建虚拟环境）
- Node.js 18+（项目已在 `frontend/node_modules` 中安装依赖）

### 4.2 完整启动流程（严格按顺序执行）

> **每次启动前必须确认**：`.env` 中 `DB_HOST=127.0.0.1`、`DB_PORT=3307`，严禁改为 `sqlite`。

#### 步骤 1：启动 Docker Desktop

```bash
open -a Docker
```
等待 Docker 就绪后继续（菜单栏 Docker 图标不再转圈）。

#### 步骤 2：启动 Docker MySQL 容器

```bash
docker compose --env-file .env -f deploy/docker-compose.yml up -d mysql
```

等待 MySQL 健康就绪（约 15 秒），验证：
```bash
docker inspect cr_mysql --format '{{.State.Health.Status}}'
# 输出 healthy 即为就绪
```

> MySQL 映射到宿主机 **3307 端口**，数据持久化在 Docker 卷 `mysql_data` 中，重启不会丢失。

#### 步骤 3：运行数据库迁移

```bash
cd backend && source .venv/bin/activate
alembic upgrade head
```

> 如果数据库已是最新版本，此步骤会提示 "No migrations to apply"，正常跳过即可。

#### 步骤 4：启动后端

```bash
cd backend && source .venv/bin/activate
uvicorn app.main:app --reload --port 8000 --host 0.0.0.0
```

验证：`curl http://localhost:8000/healthz` 返回 `{"status":"ok"}`。

#### 步骤 5：启动前端

```bash
cd frontend
npm run dev
```

#### 步骤 6：访问

| 服务 | 地址 |
|------|------|
| 前端页面 | [http://localhost:5173](http://localhost:5173) |
| API 文档 (Swagger) | [http://localhost:8000/docs](http://localhost:8000/docs) |
| 管理员账号 | `admin` / `admin123` |

### 4.3 停止服务

```bash
# 停止 MySQL 容器（数据不会丢失）
docker stop cr_mysql

# 按 Ctrl+C 停止后端和前端
```

### 4.4 一键快速启动（已安装依赖后）

```bash
# 终端 1：Docker MySQL
open -a Docker && sleep 15 && docker compose --env-file .env -f deploy/docker-compose.yml up -d mysql

# 终端 2：后端
cd backend && source .venv/bin/activate && alembic upgrade head && uvicorn app.main:app --reload --port 8000 --host 0.0.0.0

# 终端 3：前端
cd frontend && npm run dev
```

## 五、注意事项

1. **数据库硬约束**：`DB_HOST` 必须为 `127.0.0.1`，**绝不允许改为 `sqlite`**。生产环境 Docker Compose 内部使用 `DB_HOST=mysql`。
2. `.env` 中的 `DEEPSEEK_API_KEY=sk-xxxxx` 需替换为真实密钥才能使用 AI 审查功能。
3. Docker MySQL 端口映射：宿主机 `3307` → 容器 `3306`，避免与宿主机本地 MySQL 冲突。
4. `deploy/mysql/seed.sql` 中的管理员密码 `admin123` 的 bcrypt 哈希已实际生成。
5. 生产环境当前拓扑为 MySQL + Backend + Frontend/Nginx + ClamAV 共 4 个容器。现有 `deploy.sh` 尚未自动执行 Alembic、目标 SHA 断言、备份和失败回滚；整改完成前必须按 `deploy/README.md` 先备份，再显式核对目标版本、执行 `alembic upgrade head`、检查迁移头与健康状态。
6. API 密钥存放在 `.env` 文件中，已加入 `.gitignore`。

### 2026-06-12 腾讯云服务器部署
- [x] 读取项目部署文档与现有 Docker Compose 配置，确认生产部署入口为 `deploy/docker-compose.yml`
- [x] 服务器 `81.70.251.90` 环境检查完成：OpenCloudOS 9.4，端口 `80/8000/3307` 初始未占用
- [x] 安装 Docker 28.0.1 与 Docker Compose 2.32.1
- [x] 新增 `docs/腾讯云服务器部署/` 6A 部署文档
- [x] 同步项目到 `/opt/code-review` 并启动 Compose 服务
- [x] 修复服务器构建问题：Dockerfile 切换腾讯云/PyPI/npm 镜像源，前端构建设置 Node 堆上限并新增 2GB swap
- [x] 验证公网前端、Swagger 文档、OpenAPI、API 代理、管理员登录、容器状态和后端日志

### 2026-06-12 域名 HTTPS 配置
- [x] 配置 `lijiadong.cn` 指向腾讯云服务器 `81.70.251.90`
- [x] 前端容器切换为 Caddy，新增 `443` 端口和证书持久化卷
- [x] Caddy 自动签发 Let’s Encrypt 证书
- [x] 验证 `https://lijiadong.cn/`、`https://lijiadong.cn/docs`、HTTPS API 代理和登录接口

### 2026-06-12 服务器错误修复与数据库迁移
- [x] 新增 `docs/服务器错误修复与数据库迁移/` 6A 工作流文档
- [x] 复现线上 500：`/api/rules` 因 `review_rule.language` 字段缺失失败，`/api/api-config` 因 `user_api_config` 表缺失失败
- [x] 备份服务器原库到 `/opt/code-review/backups/code_review_before_local_migration_20260612_005828_valid.sql.gz`
- [x] 从本机 `cr_mysql` 导出当前数据库并上传到服务器 `/opt/code-review/backups/code_review_local_current_20260612_005828_valid.sql.gz`
- [x] 停止线上后端写入后导入数据库，再重启后端；线上核心数据已恢复：`project=8`、`code_file=500`、`review_task=24`、`review_issue=138`、`review_rule=116`
- [x] 修复 PDF 报告导出字体注册问题，缺少系统中文字体时回退到 ReportLab 内置 `STSong-Light` CID 字体
- [x] 新增 PDF 导出回归测试，`python3 -m pytest -o addopts='' tests/unit/utils/test_pdf_exporter.py -q` 通过
- [x] 线上验证：HTTPS 首页、HTTP 跳转、Swagger、登录、44 个只读 API/导出接口全部通过，Word/PDF 导出均为 `200`
- [x] 补充验证：42 个写入/危险路由受控接线验证失败数 `0`；圆桌讨论预检 `200`；Agent SSE `200` 且收到事件数据

### 2026-06-12 角色分流与注册页优化
- [x] 新增 `docs/角色分流与注册页优化/` 6A 工作流文档
- [x] 新增前端角色首页工具：`admin -> /admin/users`、`reviewer -> /reviews`、`user -> /projects`
- [x] 修复登录后固定进入 `/dashboard` 的问题，根路径和已登录访问登录/注册页也按角色分流
- [x] 侧栏导航与全局搜索入口按角色裁剪，减少不同角色共用同一套入口的问题
- [x] 注册页重做为 Prism 棱镜品牌视觉，与登录页和项目整体风格一致
- [x] 前端验证：`npm run build` 通过

### 2026-06-12 响应式布局与完整测试
- [x] 新增 `docs/响应式布局与完整测试/` 6A 工作流文档
- [x] 新增全局响应式契约：页头、操作区、筛选条、分页、表格容器和弹窗在窄屏下自动适配
- [x] 优化应用壳层与顶部栏：动态视口高度、稳定主滚动区、平板/手机按钮压缩显示
- [x] 优化仪表盘：统计卡、图表卡、活动流在小屏下重排
- [x] 优化项目列表：筛选区换行、表格局部横向滚动、卡片网格自适应、手机分页收敛
- [x] 前端验证：`npm run build` 通过
- [x] 后端验证：`cd backend && ./.venv/bin/python -m pytest -q` 通过，`142 passed`
- [x] 浏览器验收：`/admin/users`、`/dashboard`、`/projects` 在桌面/平板/手机视口下无顶层横向溢出

### 2026-06-12 腾讯云服务器增量上线
- [x] 本机最新提交 `539f63d feat: enhance security scanning and platform UX` 已通过 `rsync` 增量同步到服务器 `/opt/code-review`
- [x] 执行服务器 `deploy/deploy.sh`，后端镜像缓存构建成功，前端生产包重新构建成功，`cr_backend`、`cr_frontend`、`cr_mysql` 已重启并运行
- [x] 线上数据库结构验证通过：`user_api_config` 表存在，当前业务表数量 `18`
- [x] 线上接口验证通过：`/healthz`、`/openapi.json`、`/docs` 返回 `200`，OpenAPI 已包含 `/api/security/scan-all-projects`、`/api/api-config`、`/api/api-config/test`
- [x] 公网 HTTP 验证通过：`http://81.70.251.90/` 返回 `200`，`http://lijiadong.cn/` 正确 `301` 跳转到 HTTPS
- [x] 服务器侧 HTTPS 验证通过：`https://lijiadong.cn/` 和 `https://lijiadong.cn/docs` 返回 `200`，证书为 Let's Encrypt `lijiadong.cn`
- [ ] 本机直连 `https://lijiadong.cn/` 在 TLS 握手阶段出现 `Connection reset by peer`，服务器配置与容器日志未发现应用侧错误，需继续观察公网链路或腾讯云边界策略

### 2026-06-12 圆桌讨论 WebSocket 线上修复
- [x] 新增 `docs/圆桌讨论WebSocket线上修复/` 6A 工作流文档
- [x] 前端圆桌讨论连接改为使用 `/api/discuss/start` 返回的 `ws_url`，通过 `discuss_ws` 传递到 Agent 中心和讨论面板
- [x] WebSocket 鉴权改为优先使用 `Sec-WebSocket-Protocol: prism-auth`，后端兼容旧 query token，减少 token 出现在访问日志 URL 的风险
- [x] Caddy 显式代理 `/api/ws/*`、`/api/agents/events*`、`/healthz`、`/docs`、`/redoc`、`/openapi.json`
- [x] 部署到腾讯云：`cr_backend`、`cr_frontend`、`cr_mysql` 均 Running，MySQL healthy
- [x] 线上验证：圆桌讨论预检 `/api/discuss/start` 返回合法 `session_id` 与 `ws_url`；核心 HTTP API 19 条全部 `200`；SSE `/api/agents/events?replay=0` 返回 `200` 且首帧 `:connected`；HTTP/IP 与服务器侧 WSS 域名 WebSocket 均返回 `101 Switching Protocols`
- [x] 端口验证：公网 `80/443` 连通，后端 `8000` 服务器本机健康检查 `200`，业务 API 统一经 Caddy 网关接入
- [x] 服务器侧 HTTPS 验证：`https://lijiadong.cn/healthz` 与 `https://lijiadong.cn/docs` 均返回 `200`

### 2026-06-12 HTTPS 强制入口与公网链路复核
- [x] 服务器侧复核：`lijiadong.cn` 证书已切换为 Let’s Encrypt RSA 证书，服务器本机访问 `https://lijiadong.cn/`、`/healthz`、`/docs`、`/api/auth/login` 返回 `200`
- [x] 本地配置加固：Caddy 仅启用 HTTP/1.1 + HTTP/2，HTTPS 站点强制 RSA 证书策略，开启 HSTS，所有 HTTP Host 统一 301 到 `https://lijiadong.cn{uri}`
- [x] 部署入口加固：Compose 将后端 `8000` 与 MySQL `3307` 改为 `127.0.0.1` 绑定，公网 API 只能通过 `https://lijiadong.cn/api/*` 进入 Caddy 网关
- [x] 运维文档加固：`deploy.sh`、`deploy/README.md`、`deploy/.env.example` 不再引导生产访问明文 HTTP/IP 入口
- [x] 本地验证：`bash -n deploy/deploy.sh` 通过，`cd deploy && docker compose config --quiet` 通过
- [x] 服务器同步阻塞已解除：已同步 `frontend/Caddyfile` 与 `deploy/docker-compose.yml` 并重启 Compose
- [ ] 公网访问阻塞：本机与多个外部 HTTP 节点访问 `https://lijiadong.cn/` 出现 TLS reset/timeout；最新复核显示 HTTP Host 返回 DNSPod `webblock.html?d=lijiadong.cn`，需要在腾讯云控制台检查 ICP 备案、接入备案或域名管控状态

### 2026-06-12 HTTPS-only 恢复与 DNSPod 拦截定位
- [x] 按“只要 HTTPS”目标撤回 HTTP 兜底方案，恢复 Caddy 为 `lijiadong.cn` HTTPS 站点和 HTTP 全量跳转
- [x] 同步前端源码和 `frontend/Caddyfile` 到服务器 `/opt/code-review/frontend`，重建并启动 `cr_frontend`
- [x] 同步 `deploy/docker-compose.yml` 到服务器，公网入口收敛为 Caddy `80/443`；后端 `8000` 与 MySQL `3307` 均改为 `127.0.0.1` 绑定
- [x] 服务器侧验证：`https://lijiadong.cn/healthz` 返回 `200`，`https://lijiadong.cn/api/auth/login` 返回 `200` 且业务 `code=0`
- [x] 公网 IP 明文访问验证：`http://81.70.251.90/...` 由 Caddy `301` 到 `https://lijiadong.cn/...`
- [ ] 公网域名阻塞：强制解析 `lijiadong.cn:80 -> 81.70.251.90` 仍返回 DNSPod `webblock.html?d=lijiadong.cn`；强制解析 HTTPS 时 TLS 握手 `SSL_ERROR_SYSCALL`；服务器抓包显示 HTTP Host 和 HTTPS ClientHello 均到达容器后收到对端方向 `RST`，需在腾讯云控制台处理 ICP 备案/接入备案/域名管控状态
- [ ] 控制台操作条件不足：本机无腾讯云 CLI 凭证，Chrome 未运行且未安装 Codex Chrome Extension；需用户自行在腾讯云控制台处理，或启动 Chrome 并安装/启用 Codex Chrome Extension 后再授权我继续检查

### 2026-06-20 逻辑漏洞与全量优化
- [x] 新增 `docs/逻辑漏洞与全量优化/` 6A 工作流文档，记录对齐、共识、设计、任务、验收、总结和 TODO
- [x] 修复讨论审预检越权：`/api/discuss/start` 与普通审查一致校验项目 owner/admin，不能用猜测 ID 启动他人项目代码讨论
- [x] 修复讨论 WebSocket 会话越权：`DiscussionSession` 绑定 `owner_user_id`，连接时加载数据库用户并要求 owner/admin
- [x] 修复任务问题列表和问题详情对象级授权：普通用户不能按 `task_id` 或 `issue_id` 枚举他人审查问题
- [x] 修复审查规则写操作权限：普通用户不能切换内置规则、修改他人自定义规则或通过 Agent 旁路切换规则
- [x] 修复 Clarify 回填跨用户风险：追问会话记录 `user_id`，回填接口拒绝其他用户继续执行
- [x] 新增用户自定义 AI API SSRF 防护：默认阻止 localhost、单标签内网主机名、`.local/.internal/.lan` 等私有域、内网/链路本地/保留 IP
- [x] 新增 `ALLOW_PRIVATE_AI_BASE_URL` 与 `ENFORCE_AI_BASE_URL_DNS_CHECK` 环境开关，默认关闭内网地址和 DNS 强校验，兼容当前 fake-IP/代理 DNS 环境
- [x] 恢复 `frontend/Caddyfile` HTTPS-only：HTTP 只做 301 到 `https://lijiadong.cn{uri}`，HTTPS 开启 HSTS 和基础安全响应头
- [x] 前端补充 redirect 和外链安全细节：拒绝 `//` 协议相对跳转，外链补齐 `noopener noreferrer`
- [x] **后端验证**：`160 passed`，`ruff check app tests` 通过，`compileall app tests` 通过
- [x] **前端验证**：`npm run build` 通过
- [x] **部署配置验证**：`cd deploy && docker compose config --quiet` 通过
- [x] **历史待办已关闭（方案替代）**：后续生产网关已从 Caddy 迁移到 Nginx；本轮已通过 Nginx 容器 `nginx -t`、HTTP 308 与 HTTPS 冒烟，不再补跑已弃用 Caddy 配置的容器校验

### 2026-06-25 Agent 治理平台
- [x] 根据用户选择锁定任务名称 `agent-governance-platform`，目标规格为 D3/AP3/AG2/K3/S3/UI2/M3/O2/T3/SEC3 + N1/A3/H1/E4/C2/K2/I3/R3/P3/X2/G3/V3/L4
- [x] 读取现有项目说明文档、README、后端 Agent/自进化/知识库/权限服务、前端路由与管理端页面，确认项目已有多 Agent、自进化、知识库、审计与管理页面基础
- [x] 新增 `docs/agent-governance-platform/ALIGNMENT_agent-governance-platform.md`，沉淀原始需求、项目上下文、范围边界、初步技术方向与关键风险
- [x] 新增 `docs/agent-governance-platform/QUESTION_OPTIONS_agent-governance-platform.md`，将实现前必须确认的高风险自治边界整理为可选项
- [x] 新增 `docs/agent-governance-platform/BOUNDARY_FORM_agent-governance-platform.md`，提供可直接填写的边界确认模板
- [x] 用户确认边界：Q1-A、Q2-B、Q3-A、Q4-A、Q5-B、Q6-B、Q7-B、Q8-B、Q9-B、Q10-B、Q11-C、Q12-C、Q13-A、Q14-A、Q15-C
- [x] 新增 `docs/agent-governance-platform/CONSENSUS_agent-governance-platform.md`，锁定需求描述、技术约束、验收标准、安全边界与进入 Architect 阶段条件
- [x] 新增 `docs/agent-governance-platform/DESIGN_agent-governance-platform.md`、`TASK_agent-governance-platform.md`、`APPROVAL_agent-governance-platform.md`，完成 Architect、Atomize、Approve 阶段
- [x] 新增 Agent 治理数据模型与 Alembic `002_agent_governance_platform.py`，覆盖 Agent 画像、skill、工具权限、策略、审批、工具日志、记忆、知识、调度、反思、奖惩、版本、告警和指标
- [x] 新增策略引擎、审批服务、工具网关、Agent 画像、独立记忆、独立知识库、调度、监控、奖惩、回滚服务；所有 Agent 自动绑定 `selfimprovingagent` 与 `reflection`
- [x] 接入 APScheduler 后台调度运行时，默认注册每日知识抓取、每日反思、每日自进化 3 个任务，并保留管理端手动运行；知识抓取支持项目代码、官方 URL、指定 URL、GitHub issue/PR 白名单来源
- [x] 新增 `/api/admin/governance`、`/api/admin/approvals`、`/api/admin/policies`、`/api/admin/tools`、`/api/admin/jobs`、`/api/admin/observability`、`/api/admin/rollback`、`/api/admin/rewards` 管理端 API
- [x] 新增独立 `AdminLayout`，管理员默认进入 `/admin/overview`；管理端只展示后台菜单，用户端继续使用原 `AppLayout`
- [x] 新增管理端治理工作台页面：总览大屏、Agent 管理、审批中心、策略中心、工具权限、知识与记忆、任务调度、监控告警、奖惩趋势、回滚中心；支持策略编辑、工具权限、知识来源、手动抓取、任务配置、告警关闭、奖惩记录和版本回滚
- [x] 新增 `ACCEPTANCE_agent-governance-platform.md`、`FINAL_agent-governance-platform.md`、`TODO_agent-governance-platform.md`，完成 Assess 阶段文档闭环
- [x] **后端验证**：全量测试 `184 passed`，`ruff check app tests` 通过，`compileall app tests` 通过，`alembic current` 为 `002 (head)`，`/api/admin` 总计 38 个路由且 Agent 治理 34 个路由关键端点无缺失
- [x] **接口闭环验证**：新增管理端 API 集成测试，覆盖 Agent、记忆、知识源、抓取、审批、策略、工具权限、任务、告警、奖惩、回滚真实 FastAPI 端点；自动校验 `frontend/src/api/adminGovernance.ts` 中 23 条管理端 API 调用全部匹配后端 `/api/admin` 注册路由；全前端 API 契约测试确认 150 条 HTTP 调用缺失 0，SSE `/agents/events` 与 WebSocket `/api/ws/discuss/{session_id}` 均匹配后端真实端点
- [x] **前端验证**：`npm run build` 通过；保留 Sass legacy JS API、Element Plus/@vueuse pure 注释、chunk 体积 3 类非阻塞警告，当前不影响业务运行，后续影响主要是依赖升级兼容和首屏加载性能优化
- [ ] **待补配置**：生产知识源白名单、Webhook、生产调度时间、GitHub token 可选配置、多副本调度锁、管理端浏览器端到端测试，详见 `docs/agent-governance-platform/TODO_agent-governance-platform.md`

### 2026-06-25 权限隔离与圆桌修复与MetaGPT编排

> 本次任务覆盖用户 5 项诉求：审查员界面数据隔离、服务器圆桌讨论 WebSocket 修复、管理员/普通账号数据隔离、MetaGPT 多 Agent 宏观调控、本地与服务器同步对齐。详见 `docs/权限隔离与圆桌修复与MetaGPT编排/`。

#### 数据隔离(project_member)
- [x] 新增 `project_member` 表 + ORM + Alembic 004 迁移(含 owner 回填),服务器 DESCRIBE 验证 6 字段 9 条记录
- [x] 新增 `project_member_service`,8 个通用函数(get_visible_project_ids/is_project_member/require_project_access/add_member/remove_member/update_member_role/list_members/ensure_owner_member)
- [x] 改造 5 个 service 按可见项目过滤：project/review/issue/dashboard/security
- [x] 管理员视角全局可见,普通用户视角 owner∪member,写权限保持 owner∪admin
- [x] 新增 `/api/projects/{id}/members` 成员管理 CRUD API

#### SSE 事件隔离
- [x] AgentEvent 增加 user_id 字段,emit_event 支持 user_id 参数
- [x] `/api/agents/events` SSE 端点 _should_deliver 过滤：admin 全见,普通用户仅自己+系统事件

#### MetaGPT 编排层(新增上层不破坏现有)
- [x] 新增 `backend/app/agents/metagpt/` 模块：Message/Role/RoleAdapter/Environment/factory
- [x] Environment 持有消息队列,Role 通过 _watch 订阅动作,RoleAdapter 包装现有 BaseAgent 零修改
- [x] 工厂函数 build_review_environment/build_discussion_environment 从 AgentRegistry 构建 Environment
- [x] 新增 `/api/agents/metagpt/info` 和 `/api/agents/metagpt/preview` 端点
- [x] 修复 Role._handle 订阅过滤 bug(广播消息绕过 watch 过滤)

#### 圆桌讨论 WebSocket 修复
- [x] 服务器 WS 路由 `/api/ws/discuss/{session_id}` 正常,鉴权正确(无效 token 403,有效 token 101)
- [x] Caddy/nginx 已配置 `/api/ws/` 反代含 Upgrade/Connection 头
- [x] WS 握手返回 101 Switching Protocols,讨论流程 R1/R2 发言正常,SSE :connected 正常

#### 测试与同步
- [x] 新增 44 项单元测试(20 MetaGPT + 24 project_member_service),全量 325 passed(2 项预存在失败与本次无关)
- [x] GitHub push 2 个 commit(7c6ef5d v2.5 + 17bdb50 v2.4)
- [x] 服务器 git pull + docker build + up + alembic 001→005(head) + WS 验证全部通过
- [x] **本地验证**：325 passed,ruff/compileall 通过
- [x] **服务器验证**：3 容器 Running,healthz 200,alembic 005 head,14 Agent 注册,治理调度 3 daily jobs,WS 101,SSE connected
- [ ] **待补**：前端成员管理 UI(F1)、MetaGPT 编排面板(F2)、审查员视角真实点击验证(F3)、MetaGPT 实战接入圆桌讨论(B1),详见 `docs/权限隔离与圆桌修复与MetaGPT编排/TODO_权限隔离与圆桌修复与MetaGPT编排.md`

### 2026-06-25 v3.0 AgentSkill 自进化与总调度升级（6A 全流程）

> 任务文档：`docs/AgentSkill自进化与总调度升级/`（ALIGNMENT/CONSENSUS/DESIGN/TASK/ACCEPTANCE/FINAL/TODO 共 7 份）
> 服务器：81.70.251.90（/opt/code-review）

#### 用户三大诉求
- 给每个 Agent 添加自我进化方式的 skill（per-Agent 专属进化）
- 安装 proactive-agent 和 self-improvement 这类 skill
- 聊天 Agent 变成总调度 Agent，可以操控所有 Agent

#### P0-P3：Skill 抽象层 + 数据库迁移（已完成）
- [x] 新增 `backend/app/agents/skills/` 模块：BaseSkill/SelfImprovementSkill/ProactiveSkill 三层抽象 + SkillRegistry 单例（线程安全 + 同名去重）
- [x] 14 个 Agent 专属 Skill 子类（28 个）：language_detector/project_analyzer/code_reviewer/project_manager/review_orchestrator/code_file_manager/dashboard/rule_manager/reporter/ai_prompt/security_sentinel/evolution/chat_assistant/orchestrator 各自挂载 SelfImprovement + Proactive
- [x] EvolutionAgent 下沉为 Skill 基座，保留七步闭环
- [x] Alembic 005 迁移：agent_skill_record 表 + ai_call_log.agent_label 列
- [x] BaseAgent 扩展 attach_skill/_init_skills，Orchestrator 新增 invoke_tool/invoke_skill/list_agent_skills/trigger_evolution

#### P4：双层调度（已完成）
- [x] 新增 `backend/app/agents/chat_planner.py`：ChatPlanner 类 + ToolCall dataclass，LLM function calling 动态规划调用链
- [x] ChatAssistantAgent 升级双层调度：第一层意图分类 + 第二层 LLM 规划 + _execute_plan 顺序执行
- [x] 降级路径：CHAT_DOUBLE_LAYER_ENABLED 开关 + planner 异常自动降级单层 handler
- [x] plan_steps 透传到 ChatResponse（PlanStepOut schema）

#### P5：三种触发机制（已完成）
- [x] 手动 API：POST /agents/{name}/skills/{skill}/invoke + POST /evolution/trigger + GET /agents/{name}/skills + GET /agents/skill-records
- [x] 定时触发：scheduler_service per-Agent cron 任务（31 jobs）
- [x] 事件触发：event_bus 订阅 5 个新事件类型（agent.skill.invoked/agent.skill.failed/agent.evolved/agent.feedback.recorded/agent.proactive.triggered）
- [x] 防翻车双门槛：min_samples≥20 + min_distinct_tasks≥2 + 人工闸门 + 可回滚

#### P6：前端可视化（已完成）
- [x] T18: AgentCenter.vue 抽屉展示 per-Agent skills 元数据 + admin 触发自进化按钮
- [x] T19: EvolutionCenter.vue 添加 per-Agent 自进化控制台（Agent 选择器 + 触发按钮 + 调用记录表格）
- [x] T20: AgentChatDrawer.vue 展示双层调度 step tree（后端 ai_chat.py 透传 plan_steps）
- [x] T21: 新增 SkillManager.vue 页面 + 路由 /admin/skills + 侧边栏入口

#### P7：服务器同步与部署（已完成）
- [x] 9 个前端文件 + 1 个后端 ai_chat.py 同步到服务器 81.70.251.90:/opt/code-review（rsync + expect）
- [x] backend 容器重建：docker compose build/up backend 成功
- [x] frontend 容器重建：docker compose build/up frontend 成功
- [x] 健康检查：/ /admin/skills /admin/evolution /docs 全部 HTTP 200，nginx -t 通过
- [x] 28 个 Skill 全部注册成功（14 Agent × 2 Skill）
- [x] 14 个 Agent 全部注册成功
- [x] Agent 治理调度器启动：31 个 jobs
- [x] Skill 事件触发订阅器已启动
- [x] alembic current = 006 (head)
- [x] 3 个交付文档（ACCEPTANCE/FINAL/TODO）同步到服务器

#### 已知限制（非阻塞，详见 TODO 文档）
- [ ] ai_call_log.agent_label NULL 历史数据回填（P1）
- [ ] clamav 容器 unhealthy 修复（P2，与本次无关）
- [ ] frontend SSL 证书启动优化（P3，docker restart 已自动恢复）
- [ ] 前端 TypeScript 严格类型检查（P4）
- [ ] Skill 调用记录分页查询优化（P5）

### 2026-07-02 开题报告论文制作
- [x] 读取并分析开题报告模板、无批注模板和王忆涵样例开题报告。
- [x] 基于当前项目真实功能生成《基于python的代码安全审计系统的设计与实现-开题报告.docx》。
- [x] 生成并嵌入系统功能结构图，按用户示例采用横向树状模块图样式。
- [x] 联网通过知网检索页、期刊官网、万方/维普或期刊 PDF 核验 15 条中文真实文献，避免虚构参考文献。
- [x] 使用 Word 渲染流程输出 5 页 PNG 并人工检查主要页面，无明显裁剪、重叠和图片缺失。
- [x] 按王忆涵开题报告样例再次对比并修订：正文和图统一为“基于python的代码安全审计系统的设计与实现”，删除 Word 正文与参考文献中的网址，正文引用编号改为上标，主要内容与功能结构图同步为 6 个模块、24 个子功能。
- [x] 2026-07-04 按《参考文献及说明.docx》重新优化参考文献：15 条均限定为 2024-2026 年中文期刊论文，作者超过三位写“等”，Word 正文与参考文献继续保持无网址；重新渲染为 4 页并逐页检查通过。
- [x] 2026-07-04 按《开题报告模板27版-带批注.docx》阶段性调整：参考文献更新为 10 条近年真实文献，满足英文至少 1 篇、2021 年以后、类型不少于三种；完整复查基本信息栏、正文、功能图、图题、进度安排、参考文献和意见栏的字体、行距、段前段后及缩进，当次渲染为 7 页并逐页检查通过。
- [x] 2026-07-04 按 27 版模板正文示例和王忆涵样例重写开题报告正文：研究背景与意义改为正式开题报告叙述，主要内容曾改为“模块名—子功能名—功能说明”分段结构，并完成渲染检查。
- [x] 2026-07-04 再次按王忆涵样例修正“主要内容”栏：由分段展开式改为一段式“1. 子功能：功能说明”，模块名和子功能均首行缩进 2 字符；主要内容为 34 段，当次渲染为 7 页并检查通过。
- [x] 2026-07-04 根据用户最新反馈完成最终版式修复：背景部分删除人为插入空段，保留 `一、研究背景与意义：`、`（一） 研究背景`、`（二） 研究意义` 编号层级；主要内容按 27 版模板改为“模块名 + 子功能标题 + 功能说明段”结构，并将主要内容、工作方案、参考文献显式编号为 `二、三、四`；重新清理批注/修订并渲染 8 页逐页检查通过。
- [x] 2026-07-04 根据用户反馈加厚主要内容描述：24 个子功能说明调整为中等长度，页面不再显得过空；重新清理批注/修订并渲染 9 页逐页检查通过，无单独空白尾页。
- [x] 2026-07-04 根据用户反馈补齐“主要内容”模块编号：正文 6 个模块标题已改为 `（一）` 至 `（六）` 形式，功能结构图保持简洁模块名；最终 Word 已重新渲染 9 页并抽查关键页面通过。
- [x] 2026-07-04 根据用户最新反馈调整主要内容观感：24 个子功能说明平均约 97 字，保持“标题 + 说明约五行左右”；不为压缩页数调整字号、行距、段前段后，最终按模板格式自然渲染为 10 页并完成视觉检查。

### 2026-07-09 真实数据源替换
- [x] 新增 `docs/真实数据源替换/` 6A 工作流文档，记录运行时代码中演示/示例/dummy 数据源排查、边界和验收。
- [x] 移除大模型配置页“填入 gpt-5.5 示例”按钮，配置只来自系统默认、已保存全局配置或用户手动输入的真实端点。
- [x] 仪表盘最近 Agent 活动无数据时不再伪造“正在等待 Agent 审查任务上线”，改为真实空态。
- [x] 仪表盘最近任务接口补齐真实 `task_name/project_name/status/project_id` 字段，前端活动流和周报展示数据库真实任务与项目名称。
- [x] `Orchestrator.inject_db()` 禁止缺失用户时回退为 dummy admin，避免误用伪身份执行请求级 Agent 操作。
- [x] 新增真实数据源回归测试，覆盖最近任务真实字段和禁止 dummy user 注入。
- [x] 验证通过：后端完整测试 `793 passed`，`ruff check app tests/unit/services/test_real_data_sources.py` 通过，`compileall` 通过，前端 `npm run build` 通过；运行时代码残留搜索未再命中伪活动、示例端点、示例模型或 dummy user helper。

### 2026-07-10 测试覆盖率审查与补全（已完成）
- [x] 读取项目生命周期说明、测试配置和当前未提交改动，确认不回退既有工作。
- [x] 复现后端测试基线：`793 passed in 9.92s`，行覆盖率 `61.17%`（8,926/14,593）。
- [x] 确认前端当前无测试框架、测试脚本和测试文件。
- [x] 新建 `docs/测试覆盖率审查与补全/` 的 Align、Consensus、Design、Task、Acceptance 文档。
- [x] 补齐后端鉴权、用户、项目、问题核心服务测试（10 passed），并修复用户状态非数字过滤的 SQLAlchemy 条件表达式缺陷。
- [x] 补齐论坛、工单、用户反馈、画像、个人知识库和个性化服务测试（14 passed），目标 6 个服务联合行覆盖率达到 100%。
- [x] 补齐代码分片、编码转换和 Word 导出测试（9 passed），目标 3 个模块行覆盖率达到 100%；修复函数边界正则跨行吞空白导致行号提前的问题。
- [x] 补齐后端讨论编排隔离测试（17 passed，目标模块 282/290 行、97%），覆盖完整生命周期、控制状态机、环境/事件发布、问题抽取与报告收尾；修复讨论事件缺少 `AgentEvent.user_id` 导致 SSE 跨用户广播的隔离缺陷。
- [x] 建立前端 Vitest 3.2.4 + V8/jsdom 测试基础设施，新增 4 个测试文件、18 项测试；`src/utils` 语句/函数/行覆盖率均为 100%，分支覆盖率 93.52%。
- [x] 修复 `detectLanguage()` 无法识别标准 `Dockerfile` 的不可达映射缺陷，并覆盖大小写、路径、未知扩展名和空扩展名场景。
- [x] 后端全量回归 843 passed，最终行覆盖率 68.37%（9,978/14,594），较 61.17% 基线提升 7.20 个百分点并超过 65% 目标。
- [x] 第一轮质量门控：生产代码及本任务 Python 文件 Ruff 通过，compileall 通过，前端 18 项测试与 `vue-tsc + vite build` 通过；初次全仓检查发现 54 项既有测试 Ruff 问题。
- [x] 生成 FINAL/TODO，记录覆盖率差异、剩余热点、初始 Ruff 债务和后续操作指引。
- [x] P1 清理全部 54 项既有测试 Ruff 问题：I001 16、F401 8、F541 12、F841 18；保留漏洞扫描样例语义。
- [x] P1 复验通过：`ruff check app tests` 零问题，受影响测试 160 passed，后端全量 843 passed，compileall 通过。
- [x] P2 第二轮后端覆盖率提升完成：新增 EventBus 9 项、WebSocket 讨论 8 项、ChatAgent 23 项，共 40 项隔离测试；定向覆盖率分别达到 94%、99%、95%。
- [x] P2 测试发现并修复 EventBus 使用无效 `AgentContext(trace_id=...)`、导致事件触发 Skill 失效的问题，trace/event 元数据改由 `ctx.extra` 传递。
- [x] P2 最终门控：后端 883 passed，行覆盖率 73.15%（10,675/14,594），较 68.37% 新增覆盖 697 行并超过 72% 工作目标；全仓 Ruff、compileall、前端 18 项覆盖率测试与生产构建通过。
- [x] 按共识未启动 `review_service.py` 条件切片：前三个优先模块已达到目标，剩余热点转入 TODO。

### 2026-07-10 开题答辩 PPT 制作（已完成）
- [x] 阅读项目说明文档、开题报告与代码结构，确认六大模块、三十个子功能及技术路线。
- [x] 解析参考《手作工坊管理系统开题答辩.pptx》的 9 页叙事结构。
- [x] 确定采用“棱镜 Prism”紫蓝科技风，使用真实系统截图证明前期预研可行性。
- [x] 生成 10 页、16:9、可编辑的《基于大模型智能体的代码审查平台的设计与实现-开题答辩.pptx》，并为每页写入演讲者备注。
- [x] 使用 LibreOffice 渲染为 10 页 PDF 和逐页 PNG；完成 PPTX 完整性、页面标题、元素边界、参考文字残留及敏感信息检查。
- [x] 输出《开题答辩讲稿与常见问答.md》、FINAL、ACCEPTANCE 与 TODO 文档；正式答辩前仅待补充姓名、学号、班级和指导教师。

### 2026-07-10 项目工作方式与部署对齐（已完成）
- [x] 阅读根说明、README、核心 `docs/01-11`、部署文件与当前未提交工作区，梳理 6A 工作方式、前后端分层和权威事实来源。
- [x] 统一当前规模口径：194 条业务 HTTP、1 条 WebSocket、14 个 Agent、51 张 ORM 表声明、53 个 Vue 页面；历史旧数字保留为阶段快照。
- [x] 对生产环境完成只读核对：确认实际为 Nginx + 4 容器、Alembic `009 (head)`，并识别备份、发布闭环、HTTP/OpenAPI、ClamAV/YARA、API Key 与 Agent 参数契约偏差；未修改线上配置、容器、数据库、计划任务或网络策略。
- [x] 同步 README、API、部署、测试、开发计划和专项 6A 文档，补齐 `docs/项目工作方式与部署对齐/` 的 Align、Consensus、Design、Task、Acceptance、Final、TODO。
- [x] 最终验证（后续覆盖率 P3 已刷新）：后端 1025 passed、行覆盖率 77.871019%（11,785/15,134），全仓 Ruff 与 compileall 通过；前端 8 个 Vitest 文件/49 项测试通过，纳入范围行覆盖率 99.79%、分支 93.39%、函数 100%，生产构建通过；`bash -n deploy.sh` 与 Compose 配置解析通过。
- [x] 所有生产整改均保留在 TODO，优先顺序为凭据轮换、备份/恢复、Git + Alembic 发布闭环、HTTPS/OpenAPI、ClamAV/YARA 产品决策。
- [x] 最终一致性收尾（覆盖率 P3 后刷新）：项目规模与 Alembic head 口径不变；后端 1025 passed / 77.87%、全仓 Ruff、compileall、前端 49 tests / coverage / build、部署脚本与 Compose 解析均通过。

### 2026-07-10 生产 P0 加固与工程治理（本地验收完成；生产发布阻塞）
- [x] 继承 `docs/项目工作方式与部署对齐/TODO_项目工作方式与部署对齐.md` 的 P0/P1/P2 全部整改范围。
- [x] 新增 `docs/生产P0加固与工程治理/` 的 ALIGNMENT、CONSENSUS、DESIGN、TASK、ACCEPTANCE、FINAL、TODO 文档。
- [x] 明确当前 `main` 与 `origin/main` 均为 `09acea0f1a12c39b408fa0c52a3cc22c22d08af9`，但工作区存在大量并行修改；不执行 pull/rebase/reset，不将脏工作区整体部署到生产。
- [x] 明确默认方案：精确 SHA 发布、发布前备份、Alembic/健康/回滚闭环、ClamAV 保留并 fail-closed、生产关闭 OpenAPI、HTTP 跳转 HTTPS。
- [x] T02～T11：本地实现、定向验证、CI/契约与运维治理已完成。
- [x] T12：本地生产级全量门禁完成；全量测试、镜像、Alembic、Backend、ClamAV/YARA、Nginx/HTTPS 和真实隔离 MySQL 备份恢复通过。
- [ ] T13：生产分批变更阻塞/未执行；当前工作区不干净，尚无承载本轮实现的精确审查 SHA，禁止直接部署。
- [x] T14：完成最终评估与文档收口；明确“本地生产级门禁通过，不等于当前工作区获准生产发布”。
- [ ] 外部安全事项：高权限凭据轮换、异地备份目标、未知公网端口用途、GitHub 分支保护权限与云告警渠道。

- 2026-07-10 P0 实施进展：完成发布/回滚脚本、Compose 私网与资源/日志门禁、Nginx HTTPS 模板、请求 ID/健康/指标、ClamAV/YARA 与生产 fail-closed；定向后端 88 测试、Ruff、Shell/Compose 静态测试通过。
- 2026-07-10 P0 发布小修：frontend-only HTTPS 冒烟改为校验实际 `desired_backend` release，避免仅更新前端时误判旧 Backend，Shell/Compose 静态回归通过。
- 2026-07-10 T12 全量门禁：后端 1029 passed / 77.976741%，前端 8 文件 / 49 tests；Ruff、compileall、依赖审计、事实/OpenAPI 与部署门禁通过。
- 2026-07-10 T12 镜像与容器：Backend 改为双阶段构建，体积 `964 MB → 494 MB`；隔离 MySQL 完成 Alembic `001 → 009` 和 52 张表；Backend healthz/readyz/metrics、生产文档关闭、ClamAV/YARA/fail-closed 实测通过。
- 2026-07-10 T12 网关与恢复：Nginx `-t`、HTTP 308、HTTPS 首页和公网敏感路径拒绝通过；真实隔离 MySQL 备份的 gzip/SHA-256/元数据/恢复/清理通过；隔离容器、网络和卷已清理。
- 2026-07-10 T13/T14 收口（阶段快照，后续由 2026-07-11 Backend-only 发布记录更新）：当时未连接或修改生产服务器；T13 等待干净审查 SHA、远端 CI、凭据轮换与生产备份恢复，T14 已生成 ACCEPTANCE、FINAL 和 TODO。

- 2026-07-10 P1 T07 完成：API Key 改为独立动态密钥环（当前/previous/legacy JWT），用户与全局配置支持机会式重加密；坏密文自动失效，提交失败安全回退且日志不含密钥；47 项定向测试与 Ruff 通过。
- 2026-07-10 P1 T08 完成：22 个 Orchestrator 固定工具统一使用 Pydantic v2 严格参数契约，Planner JSON Schema 与执行前校验同源；兼容历史查询别名，拒绝缺失、extra、错类型及模型伪造 `ctx/user`，移除 TypeError 二次调用风险并统一结果包装；57 项定向测试、152 项 Agent 回归与 Ruff 通过。
- 2026-07-10 P1 T09 完成：后端生产/开发依赖使用 Python 3.11 哈希锁，生产 Python 与前端 npm 依赖审计均为 0 已知漏洞；ESLint、49 项前端测试、覆盖率与构建通过；GitHub Actions 补齐后端、MySQL 8 迁移、前端、事实/OpenAPI 和部署五类门禁。
- 2026-07-10 P1 T10 完成：自动生成 194 HTTP operations、1 WebSocket、51 ORM 表、14 Agents、53 Vue views、Alembic 009 等项目事实和 OpenAPI；破坏性检查阻止路径/方法删除及新增必填请求输入，4 项脚本单测与两项 CLI 门禁通过。
- 2026-07-10 P1 T11 完成：新增 JSON 运维巡检、默认 dry-run 的受控清理、每日备份/每周恢复验证/每 5 分钟巡检 systemd timers 和发布清单；Bash 3.2、fake Docker/curl、锁与保护策略、模板渲染测试全部通过。

### 2026-07-10 测试覆盖率 P3 全量补齐（已完成）
- [x] 新增 ReviewService、ChatPlanner、Orchestrator、Agent API、安全态势、Skill、配置安全、可观测性等高风险隔离测试；API 契约扫描明确排除 `*.test.ts`。
- [x] 后端覆盖率专项冻结快照提升至 1025 passed，行覆盖率 77.871019%（11,785/15,134，缺失 3,349），高于 CI 阈值 72%；白名单冻结后当前工作区另含 4 项并行脚本测试，自动发现全量为 1029 passed / 77.976741%（11,801/15,134），Ruff 与 compileall 全部通过。
- [x] 前端补齐 Pinia Store、Axios HTTP、路由守卫和安全态势关键组件测试；最终 8 文件/49 tests，纳入范围行覆盖率 99.79%、分支 93.39%、函数 100%，`vue-tsc + vite build` 通过。
- [x] 新增并升级 `.github/workflows/quality.yml`：Python 3.11 哈希锁、Ruff/pytest/coverage/compileall/pip-audit，MySQL 8 Alembic，Node 20.x ESLint/coverage/build/npm audit，以及事实/OpenAPI、Compose/Shell/systemd 门禁。
- [x] 服务器以 87 文件白名单同步并逐文件校验 SHA-256；临时 Python 3.9 容器复验 1025 passed / 77.890842%，前端 49 tests / coverage / build 全通过；冻结后并行新增的 4 项脚本测试另在 `/tmp` 隔离副本复验 4 passed，未写入 `/opt/code-review`。
- [x] 服务器前端首次构建受 Node 默认堆限制 OOM，改在隔离 `/tmp` 副本中使用 `NODE_OPTIONS=--max-old-space-size=1536` 后成功；该隔离测试阶段未重启生产容器，后端 `/healthz` 与前端 HTTP smoke 均通过。
- [x] 安全收尾：清理主工作区 3 份、服务器 2 份历史任务文档中的明文服务器凭据，服务器清理前文件已单独备份；代码白名单哈希仍 87/87 一致。
- [x] 修复 Skill 显式 action 优先级与安全态势“最新评分取成最早评分”两个真实缺陷；最终文档已同步更新。
- [ ] 外部安全待办：立即轮换曾在沟通渠道暴露的服务器高权限凭据，并审计近期登录记录。

### 2026-07-11 Backend `requirements.lock` 生产重建（已完成）
- [x] 经用户明确授权执行 Backend-only 生产发布；Frontend、MySQL、ClamAV 均未重建、未重启，数据库卷、ClamAV 卷和证书未操作。
- [x] 在服务器隔离构建/测试环境中严格按 `backend/requirements.lock` 安装 Python 3.11 依赖并执行完整门禁：`1029 passed`、`3 warnings`、11,809/15,139 行、精确覆盖率 `78.003831%`；Ruff、compileall、`pip check` 全部通过，测试阶段使用 `--network none` 与 1200 MiB 内存限制。
- [x] 最终镜像为 `sha256:9cddf0b8e2e400c18037dc6477a61a3555ce0b127d8ec388dd42f43016ccd8aa`（314,765,431 bytes）；`requirements.lock` SHA-256 为 `00570e12591d4954d000ada3325e697dc7296d09d76399f21e738e4ec2514778`，源码清单 SHA-256 为 `82a1d8144c1bda5b1c57a24f8428a5a46b8564629d8f29b6cb4f4d3dfc28206a`。
- [x] 镜像级验证通过：Python 3.11.15、默认 `uvicorn` 入口、`/opt/venv/bin` PATH、FastAPI 0.139.0、158 个 OpenAPI paths / 194 operations；运行镜像不含测试、coverage 或编译器。
- [x] 首次候选启动的 `uvicorn` PATH 问题确认来自候选脚本误复制旧容器系统变量，而非镜像缺陷；修正候选 env 白名单后使用镜像默认入口成功启动，没有热覆盖旧镜像或向旧镜像临时安装生产依赖。
- [x] 生产 `.env` 以服务器端随机值补齐独立 `API_KEY_ENCRYPTION_KEYS`（不记录密钥内容），同步只读 YARA 规则并新增 Backend-only Compose override；原配置均已备份且权限受控。
- [x] 候选端口 `127.0.0.1:18001` 验证 healthz/readyz/metrics、OpenAPI 关闭、Alembic `009=current=head`、ClamAV/YARA clean/infected 双引擎、只读规则挂载及生产配置后，删除候选容器并关闭临时端口。
- [x] 生产切换仅执行 `docker compose up -d --no-deps --no-build --force-recreate backend`；当前 `cr_backend` 为 running/healthy、restarts=0、OOM=false，release 为 `backend-20260710T155218Z`。
- [x] `deploy-backend:latest`、`deploy-backend:candidate-20260710T155218Z` 与 `deploy-backend:release-20260710T155218Z` 均指向同一最终镜像；上线前 Backend 镜像保留动态回滚标签，未执行 Alembic downgrade 或数据库恢复。
- [x] 生产冒烟与 5 分钟观察通过：healthz/readyz/metrics 200、OpenAPI 404、Alembic 009、ClamAV/YARA 双引擎、HTTP/HTTPS 首页 200、公网 Nginx → Backend 代理正常；6/6 样本健康、0 次重启、无 OOM、无 Backend 5xx、无 fatal/generic error 日志。
- [x] 发布证据与回滚资料位于 `/root/code-review-backups/backend-rebuild-20260710T152819Z`，最终审计目录为其下 `deploy-final-20260710T160352Z`。
- [ ] 外部安全待办仍为最高优先级：立即轮换曾在沟通渠道暴露的高权限凭据，并审计近期 SSH 登录、失败登录、SSH key、sudo 与异常进程。

### 2026-07-14 开题报告教师意见修改（已完成）
- [x] 读取指定开题报告、压缩包内 27 版带批注模板和参考文献说明。
- [x] 明确修改范围：正文 10 条引用分别引用并按顺序出现、功能结构图顶层标题缩短到“基于python的代码安全审计系统”、进度安排采用模板 14 个时间段。
- [x] 完成 Align、Consensus、Design、Task 与审批检查文档。
- [x] 生成开题报告 Word 修订版及 PDF 预览版，原始文件未覆盖。
- [x] 自动验收通过：正文引用 `[1]` 至 `[10]` 分别标注且均为上标，进度安排 14 段，参考文献仍为 10 条，结构图尺寸保持 1122×386。
- [x] 使用 LibreOffice 渲染为 A4 共 8 页并逐页检查，未发现文字裁切、图片缺失、内容重叠或异常空白页。
- [x] 完成 `docs/开题报告教师意见修改/` 的 Acceptance、Final 和 TODO 文档闭环。

### 2026-07-14 开题答辩 PPT 初期阶段优化（已完成）
- [x] 以 2026-07-10 版 PPT 为基线，保留原版备份和 10 页 Prism 视觉风格。
- [x] 封面填入汇报人李嘉栋、学号 2023020112、指导教师彭天彬。
- [x] 重写行业现状与核心痛点，突出专家依赖、规则工具语义不足和大模型结果治理问题。
- [x] 将项目功能页明确改为六大“拟实现”模块，突出代码进入、智能审查、人工复核与闭环追踪。
- [x] 删除原型截图和超前进度表述，将当前状态统一为“已完成选题与初步资料收集，正在需求分析，尚未进入功能开发与测试”。
- [x] 同步更新演讲者备注、答辩讲稿与常见问答。
- [x] 验收通过：PPTX 压缩结构完整，10 页备注齐全，LibreOffice 成功渲染 10 页 PDF 与逐页 PNG，无非设计越界、明显文本裁切或遮挡。

### 2026-07-14 开题答辩 PPT 更名与 3 分钟演讲稿（已完成）
- [x] 课题名称改为“基于python的代码审计系统的设计与实现”。
- [x] 封面、内容页页脚、英文副标题、研究问题、预期成果和 PPT 元数据已统一更名。
- [x] 生成新 PPTX《基于python的代码审计系统的设计与实现-开题答辩.pptx》，上一版未覆盖。
- [x] 新建 3 分钟逐页演讲稿，10 页共分配 175～180 秒，并增加 1 分钟、2 分钟和 2 分 40 秒控时节点。
- [x] PPT 内 10 页演讲者备注同步改为 3 分钟版。
- [x] 验收通过：10 页 PPTX 结构完整、备注齐全，PDF 与逐页 PNG 渲染正常，新封面无裁切、遮挡或异常换行。
