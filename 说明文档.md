# 基于大模型智能体的代码审查平台

## 一、项目规划

### 1.1 项目概述
基于FastAPI+Vue3+DeepSeek大模型的智能代码审查平台，支持多语言代码自动审查、问题检测、报告导出，并已加入轻量多 Agent 审查编排能力。

### 1.2 技术栈
- **后端**: Python 3.9 + FastAPI + SQLAlchemy + Alembic + Pydantic
- **前端**: Vue3 + TypeScript + Element Plus + Pinia + ECharts + Monaco Editor
- **AI**: DeepSeek API (deepseek-chat) + 多 Agent 审查编排
- **数据库**: MySQL 8.0
- **部署**: Docker Compose (MySQL + Backend + Frontend + Nginx)

### 1.3 项目结构
```
├── backend/                  # 后端服务
│   ├── app/
│   │   ├── agents/          # Agent注册中心与编排 (14个Agent)
│   │   ├── ai/              # AI智能体模块 (Prompt/解析/分片/评分)
│   │   ├── api/v1/          # RESTful API路由 (87条 HTTP + 1条 WebSocket)
│   │   ├── core/            # 核心配置/安全/数据库/异常/依赖
│   │   ├── exporters/       # Word/PDF报告导出
│   │   ├── models/          # 数据库ORM模型 (14张业务表)
│   │   ├── schemas/         # Pydantic校验Schema (14个模块)
│   │   ├── services/        # 业务逻辑层 (13个服务)
│   │   └── utils/           # 工具函数
│   ├── alembic/             # 数据库迁移
│   └── requirements.txt     # Python依赖
├── frontend/                 # 前端SPA
│   └── src/
│       ├── api/             # API封装 (13个模块)
│       ├── components/      # 通用组件 (22个)
│       ├── views/           # 页面 (25个)
│       ├── router/          # 路由 + 守卫
│       ├── stores/          # Pinia状态管理
│       ├── types/           # TypeScript类型定义 (13个)
│       ├── utils/           # 工具函数
│       ├── constants/       # 常量定义
│       └── assets/styles/   # 全局样式
├── deploy/                   # 部署配置
│   ├── docker-compose.yml
│   ├── nginx.conf
│   └── mysql/
│       ├── init.sql          # 核心表DDL
│       └── seed.sql          # 管理员 + 内置规则
├── docs/                     # 设计文档
│   ├── 01-11 核心文档       # 11份基础设计文档
│   ├── v2.0/                # v2.0新增功能设计文档 (7份)
│   ├── 多Agent优化/          # 多Agent优化6A工作流文档
│   └── 圆桌讨论/             # v2.3 圆桌讨论审功能文档
└── .env                      # 环境变量配置
```

## 二、实施方案

### 2.1 后端API路由 (87条 HTTP + 1条 WebSocket)
| 路由前缀 | 说明 |
|---------|-----|
| /api/auth | 鉴权 (注册/登录/个人信息/密码) |
| /api/users | 用户管理 (列表/重置密码/角色/状态) |
| /api/projects | 项目管理 (CRUD) |
| /api/code-files | 代码文件 (上传/编辑/重命名/版本/回滚) |
| /api/rules | 审查规则 (CRUD/启用开关) |
| /api/review | 代码审查 (启动/任务列表/详情/问题/进度) |
| /api/issues | 问题管理 (查看/状态更新/批量) |
| /api/reports | 报告 (列表/详情/Word导出/PDF导出) |
| /api/dashboard | 仪表盘 (汇总/风险/趋势/频次) |
| /api/ai-logs | AI日志 (管理员查看) |
| /api/agents | Agent中心 (运行时/态势/事件SSE/Clarify回填) |
| /api/security | 安全审计 (扫描/复审/威胁建模/态势汇总/规则清单) |
| /api/ai | AI助手对话 (Orchestrator编排) |
| /api/ai-prompt | AI提示词生成 (问题/任务/项目级修复手册) |
| /api/evolution | Agent 自进化 (经验/提案/评估/审批/回滚) |
| /api/admin/audit | 操作审计 (管理员查看) |
| /api/discuss | 圆桌讨论审预检 |
| /api/ws/discuss/{session_id} | 圆桌讨论审 WebSocket |

### 2.2 数据库表 (14张业务表)
user, project, code_file, code_version, review_rule, review_task, review_task_file, review_issue, ai_call_log, review_report, audit_log, eval_case, evolution_proposal, review_experience

## 三、进度记录

### 2026-05-26 开发完成
- [x] M0: 项目基础设施 (配置/入口/数据库/鉴权/前端骨架/Docker)
- [x] M1: 核心业务CRUD (项目/文件/版本/规则)
- [x] M2: AI审查智能体 (DeepSeek调用/分片/解析/评分/Prompt)
- [x] M3: 前端全页面 (仪表盘/审查/报告/管理)

### 2026-05-26 代码修复与验证
- [x] 修复API路径不一致 (http.ts baseURL统一为'/api')
- [x] 修复Element Plus CSS导入 (element-plus/dist/index.css)
- [x] 修复Element Plus中文语言包导入路径
- [x] 修复seed.sql管理员密码bcrypt哈希 (密码: admin123)
- [x] 修复Python 3.9兼容性 (X|None → Optional[X], 共16处)
- [x] 修复requirements.txt (补充bcrypt<5.0 + pydantic[email])
- [x] 修复codeFile.ts FormData上传Content-Type
- [x] 修复project.ts deleteProject动态导入
- [x] 修复ReviewTaskDetail.vue getCodeFileDetail导入
- [x] 修复ReviewStart.vue getCodeFiles导入
- [x] 修复CodeFileList.vue list函数参数
- [x] 修复VersionHistory.vue listVersions返回值
- [x] 修复ProjectDetail.vue upload函数参数
- [x] 修复ReportDetail.vue duration_ms类型
- [x] 修复UserManage.vue filterStatus比较
- [x] **后端验证**: 所有模块导入成功 + 47条API路由注册正确
- [x] **前端验证**: vue-tsc类型检查零错误通过

### 2026-05-26 多 Agent 优化与文档对齐
- [x] 新增多 Agent 编排模块：通用质量、安全、可靠性、性能、可维护性代理
- [x] 审查类型对齐：quick/standard 使用通用代理，security/performance/full 使用专项代理组合
- [x] 修复分片行号约定：模型返回相对行号，后端统一换算绝对行号
- [x] 修复前端审查任务跳转路径和任务状态枚举
- [x] 修复前端登录态恢复接口 GET/POST 不一致
- [x] 修复报告详情中审查类型硬编码为 full 的问题
- [x] 加强审查启动权限边界：项目必须归属当前用户或管理员，文件必须属于当前项目
- [x] 新增 11 个后端单元测试，验证多 Agent 映射、Prompt 注入、行号换算和摘要
- [x] 同步更新 README、核心 docs 和 `docs/多Agent优化/` 6A 文档
- [x] **后端验证**: 新增测试 11 passed + 本次触及文件 ruff 通过 + compileall 通过
- [x] **前端验证**: `npm run build` 通过

### 2026-05-27 人机交互加载动画优化
- [x] 新增 PrismLoading 统一加载动画组件，提供页面级、局部区域级加载提示
- [x] 接入路由切换加载遮罩，懒加载页面和守卫等待期间显示动画提示
- [x] 优化仪表盘、审查启动文件列表、报告详情的加载状态展示
- [x] 统一 Element Plus `v-loading` 遮罩视觉，补充动画和“正在加载数据...”提示

### 2026-05-27 人机交互缺陷修复
- [x] 修复管理员快速登录密码与种子数据不一致问题
- [x] 修复登录失败重复错误提示，统一由请求拦截器展示错误信息
- [x] 修复管理员项目列表权限范围，管理员可查看全部活跃项目
- [x] 修复仪表盘项目和文件统计口径，已删除项目不再计入活跃统计
- [x] 修复项目更新接口请求方法，前端改为调用后端 `PUT /projects/{id}`
- [x] 修复严重度中文/英文枚举混用导致的筛选、图表颜色和标签样式异常
- [x] 实装全局搜索入口和 `⌘K/Ctrl+K` 快捷键，支持快速跳转页面和打开 Agent 助手
- [x] 优化移动端主布局，侧边栏改为抽屉式导航并增加遮罩关闭
- [x] 优化项目创建表单，允许不选择文件夹时手动创建项目
- [x] 补充 Agent 抽屉关闭按钮可访问名称，修复无文本按钮的可访问性问题

### 2026-05-27 v2.0 完整交付 (M1 + M2 + M3 + M4 + M5 + AiPromptAgent)

#### M2 · Agent 调用事件总线 + SSE 反馈链
- [x] 后端 `AgentEventBus` 单例 + `AgentEvent` 数据类(dispatch/thinking/progress/complete/failed/clarify 六类事件)
- [x] `BaseAgent.call()` 自动 emit 事件,统一从 ctx.extra.trace_id 关联调用链
- [x] `Orchestrator.chat()` 生成 trace_id 并广播 DISPATCH,子 Agent 自动跟随
- [x] 新增 `GET /api/agents/events` SSE 端点,默认回放最近 20 条历史
- [x] 前端 `utils/agentEventStream.ts` 基于 fetch+ReadableStream 解析 SSE(支持 Bearer Token)
- [x] `AgentChatDrawer` 重写,每条 assistant 消息附带"Agent 调度链"折叠面板,实时展示派发→思考→完成
- [x] 单测: 4 项 EventBus 测试(包含异步订阅与历史上限)

#### M4 · Agent 主动提问 (Clarify Protocol)
- [x] 后端 `ClarifyStore` TTL=5min 内存存储,5 分钟未回填自动失效
- [x] `ChatAssistantAgent._maybe_clarify()` 在关键 intent 缺字段时返回 CLARIFY,不再基于猜测
- [x] 定义 6 个必填字段映射(delete_project/start_review/list_review_issues/list_code_files/create_project/review_code)
- [x] 4 种控件类型(text/select_project/select_task/code)前端自动渲染对应组件
- [x] 新增 `POST /api/agents/clarify` 回填路由,合并 payload 后继续走原 intent handler
- [x] `ChatResponse` Schema 扩展,加入 `clarify?: ClarifyOut`,v1.0 客户端无感知降级
- [x] 前端 AgentChatDrawer 自动渲染追问卡片(支持项目下拉/任务下拉/代码片段输入)
- [x] 单测: 5 项 Clarify 测试(TTL + 触发条件 + 完整 payload 不触发)

#### M1 · Agent 元数据 + Agent 中心同步
- [x] **v2.0 设计文档**: 编写 `docs/v2.0/` 7 份开发文档(总体规划、办公室设计、图标规范、事件总线、Clarify、AiPrompt、迁移)
- [x] **Agent 元数据下沉到 BaseAgent**: 12 个 Agent 全部填齐 icon/color/category/skills
- [x] **AgentRegistry v2.0**: 新增 `list_runtime()`、`get_metadata()`、`summary()`
- [x] **agent_service v2.0**: 新增 `get_runtime_agents()`、`get_runtime_summary()`、`get_situation()`
- [x] **新 API 路由**: `/api/agents/runtime`、`/api/agents/runtime/summary`、`/api/agents/situation`
- [x] **AiPromptAgent 新成员**: 把审查问题翻译成可粘贴 Cursor/Copilot/ChatGPT/Claude Code 的提示词,默认 LLM 润色,敏感字段脱敏
- [x] **新 API 路由**: `/api/ai-prompt/tools`、`/api/ai-prompt/issue`、`/api/ai-prompt/task`
- [x] **前端 Agent 中心重写**: 替换为"Agent 办公室"视图,顶部态势感知带 + 12 张工位卡 + category 分桶 + 详情抽屉
- [x] **AgentAvatar 组件**: 每个 Agent 一个独立 SVG 几何符号 + 6 种状态光环动画 + reduced-motion 兜底
- [x] **SituationPanel 组件**: 在岗/工作中/空闲/今日调用 + 60 分钟波形 + 热点 Agent 排行
- [x] **AgentDeskCard 组件**: 单工位卡,显示头像、状态徽标、调用统计、成功率
- [x] **AiPromptModal 组件**: Issue / Task 两种来源生成可粘贴提示词,支持工具切换、Markdown 下载
- [x] **IssueDetailDrawer + ReviewTaskDetail**: 新增"生成 AI 修复提示词""AI 修复包"按钮
- [x] **图表数据修复**:
  - 新建 `frontend/src/constants/dim.ts` 集中维度键归一化(支持 maintainability/bug 等同义词)
  - Dashboard 使用 `normalizeDimKey` 兼容多种 issue_type 写法
  - Dashboard 6 张统计卡空数据时不再显示"▲ 较上月"假 delta
  - ReportDetail 雷达图缺失维度用 NaN 显示真实缺口,不再用整体 score 兜底假满分
  - ReviewTaskDetail 用归一化 dimColor/dimLabel 兼容多种枚举写法
- [x] **测试**: v2.0 新增 21 项单测全部通过 + v1.0 17 项继续通过 = **38 passed**
- [x] **前端验证**: vue-tsc 类型检查零错误,npm run build 通过(含 SSE/Clarify 新组件)

#### v2.0 收尾 (A1+A2+A3 + B2 + C3)
- [x] **A1** ChatAgent 加入 `generate_ai_prompt` 意图,新增 handler 路由到 AiPromptAgent;Clarify 字段映射覆盖 scope/target_tool/issue_id/task_id/project_id
- [x] **A2** Agent 办公室订阅 SSE,工位卡按事件实时切换 status(thinking/working/error/blocked),态势带 working/idle 计数同步;错误状态 6 秒自动回 idle
- [x] **A3** SSE 客户端加入指数退避自动重连(5s→30s 上限),onStatus 回调暴露 connecting/connected/reconnecting/closed 四态
- [x] **B2 图表二次审计**:
  - ProjectList 评分量表删除 `id*7+file_count*3` 派生的**假数字**,改用后端真实评分,无审查记录显示 —
  - 后端 ProjectOut 新增可选 `score` 字段,project_service 列表 API 聚合最近一次成功审查评分
  - IssueHub 严重度/类型标签接入 `severityClass` + `dimLabel`,兼容中英文混存
- [x] **C3 项目级 AI 修复手册**: AiPromptAgent 新增 `execute_for_project` 按严重度优先取 top_n 问题;新增 `POST /api/ai-prompt/project`;ProjectDetail 顶部加 `AI 修复手册` 按钮;AiPromptModal 增加 `source='project'` 模式
- [x] **测试**: 38 项全部继续通过,无回归
- [x] **前端验证**: vue-tsc 零错误,npm run build 通过

### 2026-05-27 登录注册修复
- [x] **修复vite.config.ts代理端口错误**: 前端dev server代理目标从 `localhost:8001` 修正为 `localhost:8000`
  - 后端运行在8000端口，代理配置错误导致所有 /api 请求无法到达后端
  - 验证：通过前端代理 curl 测试 login/register 均返回200成功
  - 前端vue-tsc类型检查零错误通过

### 2026-05-28 v2.1.1 安全能力增强交付 (G1~G6)

#### SecuritySentinel 扫描规则扩充
- [x] **后端静态规则从 13 条扩充到 20 条**: 新增 SQL 字符串拼接(CWE-89)、XXE 外部实体注入(CWE-611)、开放重定向(CWE-601)、路径遍历(CWE-22)、日志注入(CWE-117)、Cookie 缺少 HttpOnly(CWE-1004)、Cookie 缺少 Secure(CWE-614) 共 7 条规则
- [x] Cookie 安全标志采用与 HTTP 响应头相同的"缺失检测"逻辑(require_presence_of → pattern 未命中 → 报警)
- [x] 敏感信息正则库: 20 类(原有 12 类 + v2.1.1 扩展 GitLab/Discord/Telegram/Heroku/SendGrid/Twilio/Mailgun/Square 共 8 类)
- [x] 后端验证: 20 条静态规则全部命中测试通过,81 条 API 路由注册正确,13 个 Agent 正常注册

#### 安全知识中心页面增强
- [x] **新增「我的项目安全概览」区域**: 调用 `/api/security/dashboard-summary` 展示当前用户项目的平均风险评分、严重度分布、OWASP 热点
- [x] 三种状态展示: 有数据(完整评分卡 + OWASP 热点) / 有项目未扫描(引导扫描) / 无项目(引导创建)
- [x] 路由 `/security` 已注册,侧边栏「安全中心」菜单项(Agent 中心与审查规则之间)可直达

#### 仪表盘安全态势卡增强
- [x] **新增 SVG 趋势火花图**: SecurityPostureCard 集成的 SecurityPostureCard 新增"严重+高危趋势"迷你折线图,数据来源于 `/api/security/dashboard-summary` 的 `trend` 字段
- [x] 增强数据显示: 总量统计(总安全发现数)、扫描覆盖率(已扫/总项目)
- [x] 前端控制面板 `SecurityPostureCard` 复用 `days` prop 响应 Dashboard 时间选择器切换

#### 自动化验证
- [x] **后端**: ruff 零错误 + compileall 通过 + 所有 81 条路由注册正确 + 13 Agent 注册正确
- [x] **前端**: vue-tsc 类型检查零错误, npm run build 通过

> v2.1 新增 SecuritySentinel Agent(第 13 个 Agent) + 6 条安全 API 路由;
> v2.1.1 在此基础上扩展到 20 条静态规则 + 20 类敏感信息正则 + 安全知识中心 + 仪表盘安全态势卡。

> v1.0 注册 47 条 `/api` 路由; v2.0 新增 Agent 中心(AI助手+Agent管理+提示词+审计) 扩展到 14 组共 70 条端点;
> v2.1 / v2.1.1 新增安全审计组(security)扩展到 15 组共 81 条端点,Agent 数 13。

### 2026-05-29 v2.3 M7 · 多 Agent 圆桌讨论审修复与验收

> 用户报告「圆桌讨论审无法成功启动讨论」,本次定位根因、重写聊天室、修复前后端缺陷并完成真实点击端到端测试。
> 详见 `docs/圆桌讨论/圆桌讨论功能文档.md`。

#### 核心缺陷修复
- [x] **致命:点击启动后讨论面板弹不出**。根因为布局 `<router-view>` 以 `fullPath` 为 key,`AgentCenter` 在 `initDiscussionFromRoute()` 里 `router.replace({query:{}})` 清理地址栏触发**整组件重挂载**,刚设置的讨论状态被重置。改用 `window.history.replaceState(window.history.state,'',route.path)` 仅清理 URL、不触发路由导航,避免重挂载
- [x] **后端核心:Agent 发言为空**。讨论发言与共识汇总误用 `response_format=json_object`,改为 `json_mode=False` 输出纯文本自然语言
- [x] **后端:推送中断**。`DiscussionBus.close_session` 向队列放入 dict 导致 `WebSocket.send_text` 抛错,`session_end` 统一序列化为 JSON 字符串
- [x] **后端:终止不收敛**。新增 `request_stop` 标记会话 concluded;`stop` 即使在暂停态也唤醒并走主持人小结

#### 聊天室前端重写
- [x] `AgentDiscussionPanel.vue` 重写为聊天室方框:逐条发言气泡、参会者条(主持人/各 Agent/你)、「正在发言」打字指示、用户气泡右对齐、Markdown 渲染、回到底部按钮
- [x] 发言式讨论:后发言 Agent 通过 `_build_history()` 可见前面**全部**发言(含其他 Agent 与用户),实现真正的圆桌交流
- [x] 用户插话经 WebSocket 广播为 `role:user` 发言,所有 Agent 可见并影响后续讨论与共识
- [x] 状态机区分 live/paused/concluded:暂停态仍可发言与操作;结束态全部控件禁用,占位符提示「讨论已结束,无法发言」

#### 真实点击端到端测试(localhost:5173,控制台零报错)
- [x] 启动讨论审 → 面板弹出 → URL 被 `history.replaceState` 清理
- [x] 多 Agent 逐条发言 + Markdown 渲染 + 打字指示
- [x] 暂停/继续(⏸↔▶)、终止(含二次确认,暂停态亦可终止)、关闭(进行中二次确认/已结束直接关)
- [x] 发送插话(出现「你」气泡 + 输入框清空 + **被纳入主持人共识小结**)、4 个快捷提问、滚动到底「↓ 最新」
- [x] 结束态禁用校验:暂停/终止/快捷/发送/输入框全部置灰

### 2026-05-29 v2.3.1 · 圆桌讨论沉淀报告 + 真实数据 + 反驳式讨论

> 在 M7 基础上,讨论结束自动生成可查询的审查报告,全过程调用沉淀为真实日志/事件,并让各 Agent 互相反驳。

#### 讨论沉淀为审查报告
- [x] `DiscussionOrchestrator` 贯通 `user_id/project_id/file_id/review_type`,讨论开始即经 `SessionLocal` 创建 `ReviewTask(review_type=discuss, running)`
- [x] 讨论结束(自然结束或终止)在 `finally` 收尾:LLM 抽取共识为结构化 `ReviewIssue` → `compute_score` 评分 → 回填 `ReviewTask(success)` + 共识小结作 summary
- [x] 报告天然出现在「报告列表 / 审查任务 / 仪表盘」(报告列表本质即 `status==success` 的任务);报告详情复用 `report_service`(评分/严重度/by_type/问题清单)
- [x] 前端面板讨论结束显示「📄 查看报告」按钮,直达 `/reports/{id}`

#### 真实数据(重点 Agent 中心 / 调用日志)
- [x] 讨论每次 LLM 调用经 `log_deferred()` 写入 `AiCallLog`(带 task_id/user_id/file_id + `/<code>-agent` 后缀归因),Agent 中心统计与「Agent 调用日志」反映真实讨论
- [x] 编排器经 `AgentEventBus` 广播 dispatch/thinking/complete,讨论时 Agent 办公室工位卡实时点亮
- [x] 全量审计前后端无 mock/random/硬编码假数据(动画/trace_id 的 Math.random 属正常用途)

#### 反驳式讨论 + 重连修复
- [x] 发言 prompt 显式要求对他人观点表态(同意/补充/质疑/反驳并给理由),形成真正交锋
- [x] `DiscussionBus.subscribe()` 对已结束会话补发 `round_start/done` 终态帧,重连显示「已结束」而非「进行中」;会话记 `report_task_id` 供重连显示报告入口

#### 真实点击端到端验证(控制台零报错)
- [x] 两次完整讨论:报告总数 6→7→8;报告 `review_type=discuss`,含 10/7 条结构化问题、评分(30/52)、严重度分布、真实共识小结
- [x] 「查看报告」跳转 `/reports/14` 详情正常渲染;讨论记录检出 14 处反驳表态语
- [x] 单次讨论新增 10 条 `AiCallLog`(task #N、真实 token/耗时);Agent 中心总调用 123→133;重连显示「已结束·第2/2轮」
- [x] 后端 `py_compile` 通过 + 前端 `vue-tsc` 零错误;数据库坚持 Docker MySQL(`cr_mysql` healthy)

### 2026-05-29 v2.3.2 · 全站数据准确性核对 + 按钮全测

> 用户要求"确保所有数据准确、每个按钮达到效果"。本轮做了全站 API↔数据库交叉核对与按钮普查。

#### 数据准确性:修复软删除任务导致的统计虚高(5 处)
- [x] 编写 `backend/_qa_crosscheck.py`:用后端 SessionLocal 直查 MySQL 真值,与各页面接口聚合逐项比对
- [x] **根因**:多处问题/任务统计未排除"软删除任务"(`status='deleted'`),删除报告后旧问题仍计入,导致数据虚高
- [x] `dashboard_service`:新增 `_valid_task_ids()` 助手;`get_summary` 的 total_issues/severe_issues、`get_risk_distribution`、`get_issue_type_statistics`、`get_review_frequency` 一律排除已删除任务(仪表盘累计问题 68→43、严重 9→7)
- [x] `security_service`:安全态势 `task_rows` 加 `status != 'deleted'` 过滤
- [x] `project_service`:项目详情 `recent_tasks` 排除已删除任务
- [x] **核对结论**:报告8/任务8/AI日志143/用户1/审计85/项目2/文件495/问题43/严重7 —— 接口聚合与数据库**精确相等**;报告详情(总数/严重度和/类型和)内部一致;Agent 调用统计与 AiCallLog 对账一致;安全态势(严重6/高10)与 DB 一致

#### 按钮普查:全站唯一空按钮已实现
- [x] 扫描全部前端找占位/"即将上线"按钮,**全站仅 1 个**:仪表盘「导出周报」
- [x] 将其实现为真实功能:用仪表盘实时数据生成可打印/存 PDF 的周报(概览+风险分布+问题类型+最近任务),`window.open`+`print`,实测生成 3KB 报告并正确提示
- [x] 真实点击验证:导航(全部页面)、时间选择器、新建审查、项目删除(确认框)、**规则启停(数据库翻转再恢复)**、报告 Word/PDF 导出(端点 200)、查看报告、周报导出
- [x] 安全起见未执行(均已确认接线):单管理员账号的禁用/重置密码/退出、安全扫描、各处删除(仅验证到确认弹窗)
- [x] 全站约 20 个页面逐一访问**零控制台报错**;`vue-tsc` 零错误(所有 @click 处理函数已定义、API 调用类型正确)

### 2026-06-01 全量功能回归测试与安全修复

> 本轮按 `docs/全量功能回归测试/` 执行自动化检查、真实点击、日志监控和缺陷修复。

#### 自动化验证
- [x] Docker MySQL `cr_mysql` 健康状态为 `healthy`
- [x] 后端 `ruff check app tests _qa_crosscheck.py` 与 `compileall` 通过
- [x] 后端完整测试 `110 passed`，覆盖率 `38%`
- [x] 前端 `npm run build` 通过
- [x] API 与 MySQL 交叉核对全部通过：报告、任务、AI 日志、用户、审计、项目、文件、问题、仪表盘、报告详情和 Agent 态势均一致
- [x] 运行时核对：`87` 条 API HTTP 路由、`1` 条讨论 WebSocket、`14` 个在线 Agent

#### 缺陷修复
- [x] 修复 `.env` 代理变量影响本地 `httpx` 核对脚本的问题，核对客户端显式使用 `trust_env=False`
- [x] 固定 `bcrypt>=4.0.0,<4.1.0`，规避 passlib 与 bcrypt 4.1.x 的兼容性问题
- [x] 清理 Ruff 超长行和 SQLAlchemy `IN(subquery)` 警告
- [x] AI 修复提示词改为显式生成；启用 LLM 润色时增加二次确认，避免弹窗打开即发送代码上下文
- [x] AI 提示词上下文跳过二进制 Base64 文件，文本上下文脱敏后限制为 `4000` 字符
- [x] 修复 Axios Blob 下载链路：Word/PDF 导出不再被统一 JSON 拦截器误判失败
- [x] Word 导出返回有效 DOCX，PDF 导出返回有效 PDF，后端日志均为 `200 OK`

#### 真实点击边界
- [x] 已覆盖登录、仪表盘、审查启动、项目、文件、版本、审查任务、问题中心、报告列表、报告详情和 AI 修复提示词的无破坏性按钮
- [x] 删除、状态修改、规则切换、重置密码、密码提交、AI 审查、安全扫描、圆桌讨论、Agent 对话和自进化写操作停在确认前或未执行
- [ ] 应用内 Browser 连接中断后未恢复；Safari 也未暴露可操作窗口，剩余管理员页面真实点击待浏览器连接恢复后补测

### 2026-06-03 全量真实点击补测、修复与清理

> 用户确认允许使用 DeepSeek，并允许创建/删除合成测试数据。本轮用 Safari Computer Use 兜底完成剩余真实点击，不干预正常业务数据。

#### 全量真实点击完成
- [x] 使用合成项目 `qa_deepseek_full_click_20260602`、合成文件 `qa_security_fixture.py` 和 QA 自定义规则执行端到端测试
- [x] 真实点击覆盖：登录/注册/退出、仪表盘、项目、代码中心、编辑器、版本、审查启动、审查任务、审查详情、问题中心、报告、AI 修复提示词、安全中心、审查规则、个人中心、用户管理、Agent 中心、Agent 调用日志、系统审计、Agent 自进化
- [x] DeepSeek 路径覆盖：quick/standard/security/performance/full/discuss 审查、安全扫描、圆桌讨论停止分支和 AI 日志落库
- [x] 删除闭环覆盖：QA 审查任务 19-25 逐条真实点击删除、QA 项目真实点击删除、QA 规则筛选定位后真实点击删除

#### 本轮新增修复
- [x] 修复 Monaco 编辑器切换文件/版本后的旧内容同步问题
- [x] 修复圆桌讨论 URL 清理触发路由重挂载导致状态丢失的问题
- [x] 新增 `review_task_file` 持久化任务-文件关联，稳定报告、讨论和多文件任务回查
- [x] 修复报告详情和 Word/PDF 导出对多文件任务、问题统计和二进制响应的处理
- [x] 配置 Monaco workers，补齐 Element Plus tag type，消除运行时警告
- [x] 审查规则页新增搜索栏，并兼容下划线/短横线被输入法丢失的编码搜索

#### 最终验证与清理
- [x] 后端完整测试 `112 passed`，`compileall` 与 `ruff check app tests` 通过
- [x] 前端 `npm run build` 通过；仅剩既有 Sass 弃用、Rollup pure 注释和 Monaco 大 chunk 警告
- [x] `_qa_crosscheck.py` 清理后通过：报告/任务/AI 日志/审计/项目/文件/仪表盘/报告详情/API 与 MySQL 精确一致
- [x] QA 数据残留为 `0`：project/file/version/task/task_file/issue/report/ai_call_log/rule/audit_log 均已清理
- [x] 后端日志监控未发现未处理异常；清理相关接口均为 `200 OK`

### 2026-06-03 日志与数据真实追溯补强

> 本轮按 `docs/日志数据追溯验证/` 执行真实网页查看、真实点击、日志监控和 API/数据库交叉核对。

#### 本轮修复
- [x] AI 调用日志 API 增加项目、任务、文件、用户、分片、Token、Prompt、Response 的完整追溯字段
- [x] AI 调用日志页面新增项目/任务/文件/用户/分片列，详情弹窗展示完整追溯链并支持跳转
- [x] 审查任务详情 API 增加 `project_id/project_name/files`，页面文件列表不再依赖当前问题分页
- [x] 审查任务详情页新增任务、项目、报告追溯条；文件列表新增“打开”跳转到真实代码文件
- [x] 系统审计页新增日志 ID、操作者 ID 和“查看”追溯入口

#### 真实网页验证
- [x] 使用内置 Browser 打开 `http://localhost:5173`，真实登录 `admin/admin123`
- [x] 真实点击 `Agent 调用日志`：日志 #226 显示项目、任务、文件、用户、分片、Token；详情弹窗显示 Prompt/Response
- [x] 真实点击 AI 日志详情中的任务链接：进入 `/reviews/18`，看到任务 #18、项目、报告入口、文件和问题列表
- [x] 真实点击任务详情文件“打开”：进入 `/code/1/file/13`，看到 `report_record.py` 真实代码
- [x] 真实点击任务详情报告入口：进入 `/reports/18`，报告统计、文件统计和问题统计正常展示
- [x] 真实点击系统审计最新记录“查看”：进入 `/admin/users`，定位到 admin 用户管理页
- [x] 截图证据保存在 `docs/日志数据追溯验证/screenshots/`

#### 最终验证
- [x] 后端完整测试 `112 passed`
- [x] `ruff check app tests` 通过
- [x] 前端 `npm run build` 通过；仅剩既有 Sass 弃用、Rollup pure 注释和 Monaco 大 chunk 警告
- [x] `_qa_crosscheck.py` 通过：AI 日志 `205` 条、审计日志 `122` 条均与 MySQL 精确一致
- [x] 后端真实页面操作日志均为 `200 OK`
- [x] 浏览器控制台错误为 `[]`

## 四、环境要求与启动流程

### 4.1 必需软件
- Docker Desktop (提供 MySQL 8.0 容器，**严禁使用 SQLite**)
- Python 3.9+（项目已在 `backend/.venv` 中创建虚拟环境）
- Node.js 18+（项目已在 `frontend/node_modules` 中安装依赖）

### 4.2 完整启动流程（严格按顺序执行）

> **每次启动前必须确认**：`.env` 中 `DB_HOST=127.0.0.1`、`DB_PORT=3307`，严禁改为 `sqlite`。

#### 步骤 1：启动 Docker Desktop

```bash
open -a Docker
```
等待 Docker 就绪后继续（菜单栏 Docker 图标不再转圈）。

#### 步骤 2：启动 Docker MySQL 容器

```bash
docker compose --env-file .env -f deploy/docker-compose.yml up -d mysql
```

等待 MySQL 健康就绪（约 15 秒），验证：
```bash
docker inspect cr_mysql --format '{{.State.Health.Status}}'
# 输出 healthy 即为就绪
```

> MySQL 映射到宿主机 **3307 端口**，数据持久化在 Docker 卷 `mysql_data` 中，重启不会丢失。

#### 步骤 3：运行数据库迁移

```bash
cd backend && source .venv/bin/activate
alembic upgrade head
```

> 如果数据库已是最新版本，此步骤会提示 "No migrations to apply"，正常跳过即可。

#### 步骤 4：启动后端

```bash
cd backend && source .venv/bin/activate
uvicorn app.main:app --reload --port 8000 --host 0.0.0.0
```

验证：`curl http://localhost:8000/healthz` 返回 `{"status":"ok"}`。

#### 步骤 5：启动前端

```bash
cd frontend
npm run dev
```

#### 步骤 6：访问

| 服务 | 地址 |
|------|------|
| 前端页面 | [http://localhost:5173](http://localhost:5173) |
| API 文档 (Swagger) | [http://localhost:8000/docs](http://localhost:8000/docs) |
| 管理员账号 | `admin` / `admin123` |

### 4.3 停止服务

```bash
# 停止 MySQL 容器（数据不会丢失）
docker stop cr_mysql

# 按 Ctrl+C 停止后端和前端
```

### 4.4 一键快速启动（已安装依赖后）

```bash
# 终端 1：Docker MySQL
open -a Docker && sleep 15 && docker compose --env-file .env -f deploy/docker-compose.yml up -d mysql

# 终端 2：后端
cd backend && source .venv/bin/activate && alembic upgrade head && uvicorn app.main:app --reload --port 8000 --host 0.0.0.0

# 终端 3：前端
cd frontend && npm run dev
```

## 五、注意事项

1. **数据库硬约束**：`DB_HOST` 必须为 `127.0.0.1`，**绝不允许改为 `sqlite`**。生产环境 Docker Compose 内部使用 `DB_HOST=mysql`。
2. `.env` 中的 `DEEPSEEK_API_KEY=sk-xxxxx` 需替换为真实密钥才能使用 AI 审查功能。
3. Docker MySQL 端口映射：宿主机 `3307` → 容器 `3306`，避免与宿主机本地 MySQL 冲突。
4. `deploy/mysql/seed.sql` 中的管理员密码 `admin123` 的 bcrypt 哈希已实际生成。
5. 生产环境全栈部署：`docker compose --env-file .env -f deploy/docker-compose.yml up -d`（MySQL + Backend + Frontend + Nginx）。
6. API 密钥存放在 `.env` 文件中，已加入 `.gitignore`。
